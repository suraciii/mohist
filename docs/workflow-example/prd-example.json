{
  "version": 1,
  "tasks": [
    {
      "id": "T-001",
      "order": 1,
      "title": "Implement user model and database schema",
      "description": "Create User model with email, password_hash fields. Add migration.",
      "acceptanceCriteria": [
        "User model has email and password_hash fields",
        "Migration creates users table",
        "Unit tests for model validation"
      ],
      "spec": "specs/auth-backend/spec.md",
      "passes": true,
      "attempts": 1
    },
    {
      "id": "T-002",
      "order": 2,
      "title": "Implement registration endpoint",
      "description": "POST /auth/register endpoint with email validation and password hashing.",
      "acceptanceCriteria": [
        "POST /auth/register accepts email and password",
        "Validates email format",
        "Hashes password before storage",
        "Returns user without password_hash"
      ],
      "dependsOn": ["T-001"],
      "spec": "specs/auth-backend/spec.md",
      "passes": true,
      "attempts": 1
    },
    {
      "id": "T-003",
      "order": 3,
      "title": "Implement login endpoint",
      "description": "POST /auth/login endpoint with JWT token generation.",
      "acceptanceCriteria": [
        "POST /auth/login accepts email and password",
        "Returns JWT on valid credentials",
        "Returns 401 on invalid credentials"
      ],
      "dependsOn": ["T-002"],
      "spec": "specs/auth-backend/spec.md",
      "passes": true,
      "attempts": 1
    },
    {
      "id": "T-004",
      "order": 4,
      "title": "Implement login form UI",
      "description": "Create login form component with email and password fields.",
      "acceptanceCriteria": [
        "Form has email and password fields",
        "Shows validation errors",
        "Calls /auth/login on submit",
        "Stores JWT in localStorage on success"
      ],
      "dependsOn": ["T-003"],
      "spec": "specs/auth-frontend/spec.md",
      "passes": false,
      "attempts": 0
    }
  ]
}