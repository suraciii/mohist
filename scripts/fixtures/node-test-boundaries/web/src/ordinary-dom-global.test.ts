const activeElement = document.activeElement
const userAgent = globalThis.navigator.userAgent
const globalDocument = global.document
const click = new MouseEvent('click')
const model = { window: 'domain' }

void activeElement
void userAgent
void globalDocument
void click
void model.window
