2026-08-16T14:23:22Z

- Read the issue-620 artifacts and self-review; self-review.md is intentionally unchanged.
- Addressed MF-001 in design, proposal, retry spec, and T-002: root Retry now has an explicit retry-specific launcher key and fresh Session/input/turn identity contract; threaded Retry uses the existing Session follow-up boundary.
- Addressed MF-002 in design, both capability specs, proposal, and T-001/T-002/T-003: committed pending Retry and selection operations are resumed by a fixed-key durable action-recovery reminder; interaction replay re-enters pending operations and provider-inbox ordering is explicit.
- Addressed MF-003 in design, retry spec, proposal, and T-002: retryability is an exact authoritative category allowlist with text-only behavior for unknown, missing, legacy, and non-allowlisted failures; the category-to-control matrix is required in tests.
- First artifact set committed as 5efd67194 after JSON and diff validation.
- Tightened MF-001 after that commit with the explicit `IAgentLauncher.LaunchConnectionRetryAsync` contract, exact `slack-retry:{projectId}:{actionKey}` key, and deterministic root identity derivation/adoption.
- Final audit passed: `tasks.json` parses, `git diff --check` is clean, the allowlist matches across design/spec/tasks, T-003 depends on the shared recovery owner T-002, and both commits leave `self-review.md` unchanged.
- Addressed MF-004 in `design.md`, the Retry spec, and T-002: threaded Retry uses an atomic Retry-only force-new-turn admission with pre-minted input/turn IDs, persists the follow-up operation ID, and resumes through an operation-targeted dispatcher instead of the first queued turn; tests cover an unrelated queued follow-up and recovery.
- Addressed MF-005 in `design.md`, `proposal.md`, both capability specs, and T-001/T-002: Retry authorization re-evaluates the current Connection policy and live Slack member/channel boundary through `SlackConnectionAccessDecider` and the validated adapter `SlackLeaseContext`; signed direct-message mode is persisted, and policy, allowlist, owner, and live-member changes are test cases.
- 2026-08-17T02:30:55Z: Addressed MF-006 without editing `self-review.md`: selection now has an explicit `SlackInteractionLeaseContext` with prompt-owner A's receiving lease plus a Server-side `ResolveCurrentTarget` callback. `SlackAdapterLeaseService.ResolveCurrentRuntimeLeaseContextAsync` validates the current B runtime lease for the same authenticated operator and adapter identity and returns a B-bound `SlackLeaseContext` for selected-Connection authorization. The artifacts require a valid non-owner `allowlist`/`anyone` path, B owner-policy/allowlist/owner-transfer/live-member/channel and lease denials with no winner or dispatch, and pre- versus post-commit recovery behavior. Committed the design/proposal boundary as `239bd9661`, the capability/task updates as `e18a70c2e`, and the exact outcome-wording cleanup as `ff3cce00f`.
- 2026-08-17T03:23:01Z: Implemented T-001's shared `SlackActionCodec` for v1 Stop, Retry, and selection payloads. Canonicalization includes the receiving workspace, Slack context, actor, direct-message mode, expiry, nonce, and shape-specific target fields; signatures use the verified Bot credential and constant-time comparison, while serialized values contain no credential. `SlackInteractionRoutes` now rejects non-`block_actions`, constructs a receiving lease plus Server-only current-target resolver, forwards the handler's stable result reference to the outbox, and enqueues replay presentations instead of suppressing them. `SlackAdapterLeaseService.ResolveCurrentRuntimeLeaseContextAsync` returns a target-fenced context for the same operator and adapter. The adapter remains acknowledgement-first and opaque, with exactly-one-action normalization and result-reference typing. Added Server codec/resolver tests and adapter forwarding/type tests.
- 2026-08-17: Completed the T-002 retry state-machine hardening. Retry operations now use an atomic interaction/recovery dispatch claim with lease takeover and stable-key replay; blocked threaded retries remain pending and target their persisted follow-up operation, while missing sources/Agents become durable unavailable or stale outcomes. Recovery updates the existing action presentation through an idempotent required-delivery upsert, including signed Stop controls for accepted attempts. Failed terminal deliveries now always render sanitized readable failure evidence, with Retry only for the exact authoritative allowlist and text-only legacy/non-retryable failures. Force-new-turn admission is restricted to `slack-retry` with pre-minted input/turn/operation IDs. Added migration claim, terminal presentation, and unrelated queued-follow-up coverage; the focused Server run passed with 3,877 tests, and the adapter suite passed with 72 tests. The broader run once timed out because Microsoft Testing Platform ignored the supplied filter and ran the full integration suite; a subsequent serial focused run passed.
