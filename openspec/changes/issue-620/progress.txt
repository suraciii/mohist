2026-08-16T14:23:22Z

- Read the issue-620 artifacts and self-review; self-review.md is intentionally unchanged.
- Addressed MF-001 in design, proposal, retry spec, and T-002: root Retry now has an explicit retry-specific launcher key and fresh Session/input/turn identity contract; threaded Retry uses the existing Session follow-up boundary.
- Addressed MF-002 in design, both capability specs, proposal, and T-001/T-002/T-003: committed pending Retry and selection operations are resumed by a fixed-key durable action-recovery reminder; interaction replay re-enters pending operations and provider-inbox ordering is explicit.
- Addressed MF-003 in design, retry spec, proposal, and T-002: retryability is an exact authoritative category allowlist with text-only behavior for unknown, missing, legacy, and non-allowlisted failures; the category-to-control matrix is required in tests.
- First artifact set committed as 5efd67194 after JSON and diff validation.
- Tightened MF-001 after that commit with the explicit `IAgentLauncher.LaunchConnectionRetryAsync` contract, exact `slack-retry:{projectId}:{actionKey}` key, and deterministic root identity derivation/adoption.
- Final audit passed: `tasks.json` parses, `git diff --check` is clean, the allowlist matches across design/spec/tasks, T-003 depends on the shared recovery owner T-002, and both commits leave `self-review.md` unchanged.
- Addressed MF-004 in `design.md`, the Retry spec, and T-002: threaded Retry uses an atomic Retry-only force-new-turn admission with pre-minted input/turn IDs, persists the follow-up operation ID, and resumes through an operation-targeted dispatcher instead of the first queued turn; tests cover an unrelated queued follow-up and recovery.
- Addressed MF-005 in `design.md`, `proposal.md`, both capability specs, and T-001/T-002: Retry authorization re-evaluates the current Connection policy and live Slack member/channel boundary through `SlackConnectionAccessDecider` and the validated adapter `SlackLeaseContext`; signed direct-message mode is persisted, and policy, allowlist, owner, and live-member changes are test cases.
