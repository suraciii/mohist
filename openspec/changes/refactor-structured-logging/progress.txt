# Ralph Progress Log
Started: 2026-04-14T10:47:40.570Z

---


## 2026-04-14T10:56 - T-001 创建 Log 模块核心实现

### What was done
- Created `packages/cli/src/util/log.ts` (~210 lines) ported from opencode's `util/log.ts`
- Removed zod dependency (Level is plain TypeScript union type)
- Removed Glob dependency (cleanup uses fs.readdirSync + regex pattern matching)
- Log directory changed to `~/.mohist/logs/` via `os.homedir()`
- Refactored `clone()` to bypass service cache by extracting `createLogger()` internal function
- Fixed `init({ print: true })` to reset write function (original opencode code didn't need this since init is called once)
- Wrote comprehensive tests (10 test cases) covering all acceptance criteria

### Learnings
- `clone()` on a named logger must bypass the service cache, otherwise `Log.create({ ...tags })` returns the cached instance instead of creating a new independent copy
- `init({ print: true })` must explicitly reset the `write` function to stderr default, otherwise a prior `init({ print: false })` call leaves a file stream writer in place

### Next steps
- T-002: Server daemon logging migration and init() integration

## 2026-04-14 T-002 Server 端日志迁移与 daemon 改造

### Done
- server/index.ts: Added Log.init() after ensureDataDir(), pre/post unhandledRejection handlers, replaced all console.* with Log.create({ service: 'server' })
- http-server.ts: Replaced console.log with log.info, added request duration logging in middleware with method/path/status/duration fields
- cli/commands/server.ts: Changed daemon stdio from ['ignore', logStream, logStream] to ['ignore', 'ignore', stderrStream]; added getLatestLogFile() helper; serverLogs() and serverStatus() now show latest timestamp log file path
- api/issues.ts: Replaced console.error in rollback catch with log.error
- api/explore.ts: Replaced console.error stream error with log.error
- api/questions.ts: Replaced console.warn/log with log.warn/log.info
- tests/start-handler-resilience.test.ts: Updated spies from console.error to process.stderr.write to match new Log output

### Learnings
- Log module defaults to process.stderr.write when not initialized (tests don't call Log.init), so test spies need to target process.stderr.write
- Pre-existing test failures in resolve-model.test.ts and api-routes.test.ts are unrelated to this change (LLM config issues)

### Next Steps
- T-003: Agent Runner 日志迁移

## 2026-04-14 T-003 Agent Runner 日志迁移

### Done
- agent-runner-service.ts: Imported Log module, created `log = Log.create({ service: 'agent-runner' })` at module level
- Replaced all 7 console.log/error calls with structured log.info/log.error
- Constructor: log.info for init and recoverable issues detection
- handleProvidersChanged: log.info for reload events, log.error for failures
- executeAgent: Added log.info at start (with issueNumber, projectId) and completion (with issueNumber, duration via startTime tracking)
- executeAgent catch: log.error with issueNumber, stage (fetched from currentIssue), and error message
- executeAgent status update failure: log.error with issueNumber and error
- resume: Added log.info at resume start, duration tracking, and same error/completion logging as executeAgent
- All tests pass, typecheck passes

### Learnings
- Both executeAgent and resume have nearly identical agent execution loops — error logging pattern is duplicated
- Stage info for error logging is fetched from issueRepo at error time to get the current stage, not the initial stage

### Next Steps
- T-004: Workflow 和 ACP Session 日志迁移

## 2026-04-14 T-004 Workflow 和 ACP Session 日志迁移

### Done
- workflow-controller.ts: Imported Log module, created `log = Log.create({ service: 'workflow' })`; replaced 4 console.log/warn calls with log.info/log.warn including structured fields (issueNumber, taskId, taskTitle, attempt, error)
- workflow-loader.ts: Imported Log module, replaced console.error with log.warn for workflow file parse errors
- acp-session.ts: Imported Log module, created `log = Log.create({ service: 'session' })`; replaced 2 console.error calls with log.error for cleanup failures and sessionUpdate errors
- issue-service.ts: Imported Log module, created `log = Log.create({ service: 'workflow' })`; added log.info in transitionToStage, transitionToStageByNumber, and setStatus with fromStage/toStage/fromStatus/toStatus fields and issueNumber
- Typecheck passes, all relevant tests pass (workflow-controller, workflow-loader, issue-service)

### Learnings
- Stage/status transitions live in issue-service.ts (via issueRepo), not in workflow-controller.ts; both transitionToStage and setStatus needed logging to cover all state changes
- advance-stage.ts calls issueRepo.updateStage directly (bypassing IssueService), so its transitions won't be logged through this path — covered by T-005 (spawn-coder tools)

### Next Steps
- T-005: spawn-coder 和工具日志迁移

## 2026-04-14 T-005 spawn-coder 和工具日志迁移

- Replaced console.log/error in spawn-coder.ts with Log.create({ service: 'spawn-coder' })
- Added log.time() in executeCoderTask and spawn_coder tool execute for duration tracking
- Replaced console.log in ask-user.ts with log.info/log.warn
- Replaced console.error in planner-agent.ts with log.error
- Note: used timer.stop() instead of `using` due to noUnusedLocals:true in tsconfig
- Typecheck passes, tests pass (2 pre-existing failures unrelated to this change)
- Next: T-006 Project / Worktree / DB 日志迁移

---

## T-006 - 2026-04-14T11:51:00Z

- Replaced console.log in project/manager.ts with log.info (service='project'): create, delete, use operations
- Replaced console.error in project/detector.ts with log.error (service='project'): config read failure
- Replaced console.warn in git/worktree-manager.ts with log.warn (service='worktree'): smartFetch failure
- Added log.info in WorktreeManager: create, remove, prune events
- Added log.info in db/database.ts (service='db'): database opened event
- Added log.info in db/migrations.ts (service='db'): migration start/complete, initialization start/complete
- Note: kept existing console.warn in smartFetch alongside log.warn for backward compatibility (CLI output)
- Typecheck passes, tests pass (2 pre-existing failures unrelated to this change)
- Next: T-007 CLI 内部操作日志清理
