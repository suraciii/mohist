## Delivery

- [x] Audit the current `origin/master` Job-only direct API route and its
      durable grant/projection boundary.
- [x] Document the activation rule and the caller-scoped idempotency gap.
- [x] Add a focused negative contract for all future direct API route shapes.
- [ ] Add the public projection engine before mapping Input/Turn or event
      reads.
- [ ] Add a dedicated caller-scoped idempotency mapping before mapping writes.
