# Specs

This change introduces no new capabilities and modifies no existing capability requirements. It is a pure code restructuring: `WorkflowController` (1434 lines) is decomposed into focused, composable units without any change to observable behavior.

All existing specs in `../../../specs/` remain fully applicable.
