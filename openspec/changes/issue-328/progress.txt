# Issue 328 — Progress

## Codebase Patterns

- `Mohist.Server.SystemInfo` is the namespace used by every file under
  `packages/server/src/Mohist.Server/SystemInfo/`. Every relocated type
  keeps this namespace, the type name, and (for records) the positional
  parameter list unchanged so DI wiring (`MohistServiceRegistration`)
  and HTTP contracts (DTOs in `SystemInfoDtos.cs`) keep working.
- The static `JSON` helper used by `FileSystemSystemUpdateStore`
  lives in `Mohist.Server.Infrastructure` — the
  `using Mohist.Server.Infrastructure;` import is mandatory in any new
  file that calls `JSON.Options` (the `Mohist.Server.Infrastructure.Hosting`
  import alone is not enough).
- `IEnvironmentVariableProvider` / `SystemEnvironmentVariableProvider`
  come from `Mohist.Server.Infrastructure.Hosting`, not from the
  `EnvironmentAbstractions` NuGet directly — the project aliases them.
- `ISingletonService` is the registration marker for application-level
  singletons (see `SystemUpdateService : ISingletonService`). DI
  lifetimes for the relocated types (singleton for store/runner,
  `AddHttpClient` for the probe) stay on `MohistServiceRegistration`
  unchanged.
- `SystemUpdateService.IsActive(state)` is referenced from
  `FileSystemSystemUpdateStore.TryAcquireLockAsync`. It must stay on
  `SystemUpdateService` (now `internal static`), not move with the
  store — that's why the store file references the orchestrator.

## Task T-001 (split into per-type files)

- Pure mechanical relocation of cohesive collaborator types out of
  `SystemUpdateService.cs` (1074 lines → 751 lines orchestration).
- New files (all in `packages/server/src/Mohist.Server/SystemInfo/`):
  - `SystemUpdateJobState.cs` (record + `ActiveStatuses` /
    `TerminalStatuses`).
  - `FileSystemSystemUpdateStore.cs` (`ISystemUpdateStore` + impl,
    owning the file lock, atomic temp-file rename, and
    `SaveIfCurrentAsync` optimistic-concurrency logic).
  - `ProcessSystemUpdateCommandRunner.cs`
    (`ISystemUpdateCommandRunner` + impl + `SystemCommandRequest` /
    `SystemCommandResult`).
  - `HttpSystemReadinessProbe.cs` (`ISystemReadinessProbe` + impl +
    `SystemReadinessResult`, owning the health / root / bundled-asset
    HTML parsing).
- Sibling files untouched:
  `RuntimeBuildInfo.cs`, `ServiceStatusChecker.cs`,
  `GitSourceInspector.cs`, `PhysicalFileSystem.cs`,
  `SystemdInstallDetector.cs`, `SystemdUnitParser.cs`,
  `SystemInfoService.cs`, `SystemInfoDtos.cs`.
- `MohistServiceRegistration` unchanged: registrations still resolve
  `ISystemUpdateStore → FileSystemSystemUpdateStore`,
  `ISystemUpdateCommandRunner → ProcessSystemUpdateCommandRunner`,
  `ISystemReadinessProbe → HttpSystemReadinessProbe` with original
  lifetimes; `SystemUpdateService` stays a singleton
  `ISingletonService`.
- Verification: `dotnet build Mohist.sln` succeeds with 0 warnings
  (`TreatWarningsAsErrors` is on); full server test suite passes
  (3661 passed, 13 pre-existing skips, 0 failed) — including all
  `SystemUpdateServiceSpecs` (32) unchanged.
- The CQS-violating logic at `GetLatestStatusAsync` (supersede /
  persist-waiting / ready→restart→succeeded→release) is intentionally
  preserved verbatim in T-001; T-003 will split it into a pure query
  + explicit command.

## Gotchas

- The original file imported `using Mohist.Server.Infrastructure;`
  solely for `JSON.Options`. When relocating `FileSystemSystemUpdateStore`
  to its own file, that import must be carried along — the
  `Infrastructure.Hosting` import alone does not surface the `JSON`
  helper.
- Record positional parameter order matters: `SystemUpdateJobState` has
  16 positional parameters (14 required, 2 optional). Reordering
  would silently change the persisted JSON shape via System.Text.Json
  property name binding — keep order identical to the original.
- `HttpSystemReadinessProbe.ProbeAsync` returns early with distinct
  failure messages per step (`API health endpoint is not ready`,
  `Web root is not ready`, `Web root did not reference a bundled asset`,
  `Bundled asset is not ready`) — those strings are part of the
  public contract surfaced via `SystemUpdateStatusResponse.Reason`,
  so any future refactor must keep them stable.

## T-001 verification (this run)

- `dotnet build Mohist.sln` succeeded with 0 warnings / 0 errors.
- `dotnet test Mohist.sln -p:SkipWebBuild=true --no-build` passed:
  3661 passed, 13 skipped, 0 failed (includes all `SystemUpdateServiceSpecs`
  unchanged).
- `npm test` runner suite passed: 58 files / 805 tests passed.
- `git diff --name-only` shows only
  `packages/server/src/Mohist.Server/SystemInfo/SystemUpdateService.cs`;
  sibling files in `SystemInfo/` are untouched, and the four new files are
  untracked additions.

## T-001 re-scheduled (this run)

- The runner re-scheduled T-001 even though it had already landed
  (commit `6d237876`). Verified the current tree still satisfies every T-001
  acceptance criterion without re-running the full server suite:
  - `SystemUpdateJobState.cs`, `FileSystemSystemUpdateStore.cs`,
    `ProcessSystemUpdateCommandRunner.cs`, `HttpSystemReadinessProbe.cs`
    all exist under `packages/server/src/Mohist.Server/SystemInfo/`,
    declare the same types/members, and keep the `Mohist.Server.SystemInfo`
    namespace.
  - `FileSystemSystemUpdateStore` owns the file-lock acquisition, the
    atomic temp-file rename (`File.Move(tempPath, _statePath, overwrite: true)`),
    and `SaveIfCurrentAsync`.
  - `HttpSystemReadinessProbe` owns the health/root/bundled-asset HTML
    parsing (health → root → asset regex → asset probe).
  - `SystemUpdateService.IsActive` stays as `internal static bool IsActive`
    on the orchestrator (the store references it at
    `FileSystemSystemUpdateStore.cs:68`), per the relocation note.
  - `git diff 6d237876^ -- .../RuntimeBuildInfo.cs .../ServiceStatusChecker.cs
    .../GitSourceInspector.cs .../PhysicalFileSystem.cs
    .../SystemdInstallDetector.cs .../SystemdUnitParser.cs
    .../SystemInfoService.cs .../MohistServiceRegistration.cs` returns
    empty — siblings and DI wiring unchanged.
  - `dotnet build Mohist.sln` succeeds with 0 warnings / 0 errors against
    the current tree (which now also includes T-002 consolidation).
- No new commits produced for this re-run; the tree is clean on
  `mohist/run-wr_17865cb3816543c5a9f58589a2b76a2d`.

## Task T-002 (consolidate transition templates into shared helpers)

- `PersistTransitionAsync` (private) owns the shared tail at
  `SystemUpdateService.cs:652-673`:
  `append log → set UpdatedAt → SaveAsync/SaveIfCurrentAsync → optional
  ReleaseLockAsync` (release runs strictly after save, line 670).
- `FailAsync` (private) generalized at `SystemUpdateService.cs:675-701`
  with optional `stage` / `outcome` / `unavailableCapability` /
  `logStage` / `logMessage` / `releaseLock`; it is the *single* definition
  of the failed transition (`Status = "failed"` literal at line 689).
- `MaxLogEntries = 200` (`SystemUpdateService.cs:9`) is the only 200
  literal in the file and is referenced once at `:707-708`.
- Every persisted transition (`StartAsync` initial save at `:81`,
  `GetLatestStatusAsync` supersede/waiting/ready/succeeded at
  `:118, :141, :157, :175`, `RunCommandAsync` stage/result at
  `:630, :646`, `RunUpdateAsync` waiting at `:490`, `TryRestoreRunnerAsync`
  restoring/recovered at `:526, :550`, `SupersedeStaleWebJobsAsync`
  CLI supersede at `:293`, `RecordCliOutcomeAsync` CLI outcome at `:275`)
  goes through `PersistTransitionAsync`. The only remaining `SaveAsync`
  call is in `StartAsync` (initial state before `RunUpdateAsync`) — that
  is the pre-existing first save and is not a transition tail.
- Every catch block and non-zero-exit branch delegates to `FailAsync`:
  `StartAsync` catch at `:93`, `RunUpdateAsync` catch at `:503`,
  `RevalidateInstallAsync` install-changed failure at `:609, :615`,
  `RunCommandAsync` non-zero-exit at `:643`, `TryRestoreRunnerAsync`
  restore-failure at `:564-573`.
- New unit/spec tests (39 total in `SystemUpdateServiceSpecs.cs`):
  - `PersistTransitionAsync_ReleasesLockOnlyAfterSave` — uses
    `OrderTrackingStore` to record `Save` / `ReleaseLock` order and
    asserts save index < release index.
  - `SourceAudit_FailedStateIsDefinedOnlyInFailAsync` — regex over the
    orchestrator source pins exactly one `state with { ... Status =
    "failed"` match, and that match is inside `FailAsync`.
  - `SourceAudit_SaveAsyncOnlyInSharedHelpersAndStartAsync` — every
    `_store.SaveAsync(` call sits inside `PersistTransitionAsync` or
    `StartAsync`.
  - `SourceAudit_AppendLogSaveAsyncSequenceOnlyInSharedHelper` — every
    inline `AppendLog(...); await _store.SaveAsync(...)` sequence sits
    inside `PersistTransitionAsync`.
  - `SourceAudit_SaveIfCurrentAsyncOnlyInPersistTransitionAsync` —
    every `_store.SaveIfCurrentAsync(` call sits inside
    `PersistTransitionAsync`.
  - `SourceAudit_ReleaseLockAsyncOnlyInSharedHelpersAndRunUpdateFinally`
    — every `_store.ReleaseLockAsync(` call sits inside
    `PersistTransitionAsync` or `RunUpdateAsync`'s `finally` cleanup.
  - `SourceAudit_LogCapDefinedOnce` — only one `200` literal and it is
    the `MaxLogEntries` constant.
- Re-verification of an already-landed T-002 commit `7a34aa66` (no
  source edits produced): `dotnet build Mohist.sln` 0 warnings /
  0 errors; filtered `SystemUpdateServiceSpecs` 39 passed /
  0 failed; runner suite 63 files / 875 tests passed; web + runner
  typechecks clean.

## Gotchas (T-002)

- The CLI-outcome branch (`RecordCliOutcomeAsync`) used to inline an
  extra `SystemUpdateLogEntry` for the CLI report *plus* the
  helper's transition log, double-recording. After T-002 the call
  site hands only one log entry (`outcomeEntry`) to
  `PersistTransitionAsync`, so the helper owns the appended log line
  and the duplicate is gone — this is a behavior-preserving deletion
  that is part of the consolidation, not a semantic change to the
  persisted log content.
- `PersistTransitionAsync` infers the `UpdatedAt` timestamp from
  `logEntry.At` when a log entry is supplied (so a single wall-clock
  read drives both the log entry and `UpdatedAt`), and falls back to
  `DateTimeOffset.UtcNow` when no log entry is supplied (e.g. the
  `TryRestoreRunnerAsync` "Restoring runner" stage bump at `:526`,
  which records the transition through the helper rather than the
  state shape).
- The `TryRestoreRunnerAsync` path keeps the recovered transition
  shape (`Status = "recovered"`, `Outcome = "recovered"`,
  `UnavailableCapability = null`, `Stage = "Recovered"`, a `Recovered`
  log entry) on the call site and only delegates the persist tail to
  `PersistTransitionAsync`, per the design decision.
