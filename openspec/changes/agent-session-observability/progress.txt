# Ralph Progress Log
Started: 2026-04-20T09:23:46.092Z

---

## T-001 — 2026-04-20
- Added `issueNumber?: number` and `onSessionUpdate?: (notification) => void` to both `AcpSessionOptions` and `AcpConnectionOptions`
- `runAcpSession`: SSE emits now use `String(issueNumber ?? issueId)` via `sseIssueId` variable; DB ops unchanged
- `createAcpConnection`: same SSE dual-track + onSessionUpdate branching — when set, skips coder_text_chunk/coder_tool_call emits and calls onSessionUpdate instead; when not set, behavior unchanged
- All 5 acpOptions in `api/issues.ts` now include `eventBus` singleton and `issueNumber: issue.number`
- Typecheck passes
- Learnings: The onSessionUpdate callback in createAcpConnection needed to handle non-text_chunk events too (they still get forwarded via onSessionUpdate, just no internal coder_* emits)
- Next: T-002 (register plan_round_start/plan_session_update event types)

---

## T-002 — 2026-04-20
- Added `plan_round_start` and `plan_session_update` to EventBus EventMap with correct payload shapes
- Added both event names to `events.ts` ALL_EVENT_TYPES, `agent-events.ts` AGENT_DETAIL_EVENTS, `useSSE.ts` eventTypes
- Added `PlanRoundStartEvent` and `PlanSessionUpdateEvent` types to frontend types.ts
- Added `PlanRoundStartEvent` and `PlanSessionUpdateEvent` to `AgentDetailEventMap` 
- Added `rawInput?`, `rawOutput?`, `title?` optional fields to `ToolCallEntry` interface
- CLI typecheck passes; web has pre-existing node_modules errors only
- Learnings: Straightforward registration task — 5 files, each just adding the two event names to existing arrays/maps
- Next: T-003 (bridge Plan/Review stage events via onSessionUpdate in workflow-controller)

---

## T-003 — 2026-04-20
- `runPlanStage`: clones acpOptions with `executionId: 'plan-${issue.number}'`, uses `roundState` object pattern for onSessionUpdate callback, emits `plan_round_start` before each round's `conn.prompt()`, emits `plan_session_update` via onSessionUpdate callback with round metadata and full data payload. Self-review round included as round index 4.
- `runPipelineReviewStage`: clones acpOptions with `executionId: 'review-${issue.number}'`, sets onSessionUpdate callback emitting `plan_session_update` with `roundType='review'`, emits `plan_round_start` before the review prompt
- `runPipelineBuildStage`: now receives acpOptions, passes `this.eventBus`, `workflowLogRepo`, `coderSessionRepo`, `issueNumber`, and `executionId: 'build-${issue.number}'` to RalphExecutor
- `RalphExecutorContext`: extended with `workflowLogRepo`, `coderSessionRepo`, `issueNumber` fields
- `runRalphLoop`: generates per-task `taskExecutionId` (e.g., `build-1-T-001`) for `_acpSessionRunner` and `emitTaskUpdate`; `emitLoopProgress` uses loop-level `context.executionId` (e.g., `build-1`). SSE issueId uses `String(context.issueNumber ?? context.issueId)`. All emit calls wrapped in try/catch (fire-and-forget).
- `_acpSessionRunner` calls now forward `workflowLogRepo`, `coderSessionRepo`, `issueNumber` from context
- All EventBus emit calls wrapped in try/catch (fire-and-forget) across both files
- Typecheck passes, all 514 tests pass (2 skipped)
- Learnings: The roundState pattern (D13) works cleanly — single object mutated by the for loop and read by the callback. The taskExecutionId generation inside the while loop is straightforward since it's scoped per task iteration.
- Next: T-004 (enrich coder_tool_call events with rawInput, rawOutput, title)

---

## 2025-04-20 T-004: Enrich coder_tool_call events with rawInput, rawOutput, title

- Added `title?`, `rawInput?`, `rawOutput?` fields to `coder_tool_call` event type in:
  - `packages/cli/src/services/event-bus.ts` (EventMap)
  - `packages/cli/web/src/lib/types.ts` (AgentDetailEventMap)
- Enriched `eventBus.emit('coder_tool_call', ...)` in BOTH handlers in `acp-session.ts`:
  - `runAcpSession`: includes title always, rawInput on 'started', rawOutput on 'completed'
  - `createAcpConnection`: same enrichment (only fires when onSessionUpdate NOT set)
- Typecheck passes
- Learnings: The data was already available in `toolCallData` (extracted from `update.toolCall`) — just needed 3 extra fields forwarded. The `as string` cast for title is safe because ACP provides string or undefined.
- Next: T-005 (Create useSessionTimeline hook for history + live data)

## T-005 — $(date -Iseconds)
- Created `packages/cli/web/src/hooks/useSessionTimeline.ts` — new hook
- Added `api.getWorkflowLogs()` to `packages/cli/web/src/lib/api.ts`
- Fixed `agentStatus.issueId === String(issueNumber)` → `agentStatus.issueNumber === issueNumber` in `packages/cli/web/src/hooks/useAgentSession.ts`
- Hook reconstructs rounds from workflow_log entries by splitting on `user_message_chunk` boundaries
- Historical round reconstruction handles: plan_session_update (agent text), coder_text_chunk, coder_tool_call (via Map dedup), plan_round_start (label)
- Live SSE subscriptions: plan_round_start (new round), plan_session_update (RAF-throttled 100ms batch), coder_text_chunk (append to last round), coder_tool_call (Map<toolCallId> dedup)
- Uses `setRoundsRef` pattern to avoid stale closure over setRounds in event handlers
- agentStatus detection uses `issueNumber` field (number comparison) instead of `issueId` (UUID)
- Typecheck passes cleanly
- Learnings: Using a ref to store the current setState function avoids the stale closure problem when event handlers need to call setState without re-subscribing on every state change. The RAF throttling pattern from useAgentSession was reusable.
- Next: T-006 (Build SessionTimeline component with round-based layout)

## 2026-04-20 T-006 Build SessionTimeline component with round-based layout
- Created packages/cli/web/src/components/SessionTimeline.tsx
- Component renders rounds as collapsible sections with correct labels
- Agent text shows streaming cursor animation when live (blue pulse cursor)
- Tool calls show name, status icon, duration; expandable to show rawInput/rawOutput (using rawInput/rawOutput with fallback to args/result)
- PipelineStatusTimeline shows stage progress above rounds (Plan/Build/Check/Done with visual indicators)
- Build rounds detected by T-xxx label pattern, rendered with purple color scheme
- RoundSection uses different color schemes: purple for build rounds, blue for live plan rounds, gray for completed
- Reused ToolCallTimelineEntry pattern from AgentSessionPanel with enhanced rawInput/rawOutput display
- Typecheck passes clean
- Next: T-007 integrate SessionTimeline into IssueDetailPage

## 2026-04-20 T-007 Integrate SessionTimeline into IssueDetailPage

### What was done
- Replaced `useAgentSession` hook with `useSessionTimeline` in IssueDetailPage
- Replaced `AgentSessionPanel` component with `SessionTimeline` component
- Fixed `isAgentRunningOnThis` to use `agentStatus?.issueNumber === issueNumber` instead of `agentStatus?.issueId === issue.id` (UUID vs number comparison fix per design D6)
- Updated rendering condition: removed `!sessionLoading` guard (SessionTimeline handles loading internally) and simplified data check to `rounds.length > 0`
- Old `AgentSessionPanel` and `useAgentSession` kept as dead code per task notes
- Typecheck passes, all 514 tests pass (37 files)

### Learnings
- `isAgentRunningOnThis` was using UUID comparison (`agentStatus.issueId === issue.id`) which was part of the original bug (design D6). Fixed to use `issueNumber` numeric comparison.
- SessionTimeline handles empty/loading/error states internally, simplifying the parent rendering condition.

### Next steps
- All 7 tasks complete. Change is ready for archival.
