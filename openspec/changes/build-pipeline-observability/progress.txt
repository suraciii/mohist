# Ralph Progress Log
Started: 2026-04-21T09:03:53.682Z

---


## T-001: Add logging and events to runPipelineBuildStage
- Timestamp: 2026-04-21T09:12:00Z
- What was done:
  - Added 4 new SSE event types to EventBus EventMap: build_stage_started, build_tasks_snapshot, build_stage_completed, build_stage_failed
  - Added emitSafe() helper method for fire-and-forget event emission
  - Added writeLog() helper method for fire-and-forget workflow_log writes
  - Added structured logging for detectOpenSpecChange result (INFO when found, WARN when null)
  - Added tasks snapshot logging before ralph loop (total/pending/passed counts)
  - Added ralph loop result logging after completion (completed/failed/total/success/duration)
  - Added zero-work detection: when completed===0 && total>0, emits WARN and returns success:false
  - Added SSE event emissions at all key points (started, snapshot, completed/failed)
  - Added workflow_log writes for build_started, build_completed, build_failed events
  - Wrapped tasks.json reading in try-catch to handle parse failures gracefully
- Learnings: The existing test mocks fs.readFileSync as vi.fn() with no return value, causing JSON.parse("undefined") to fail. Wrapped tasks reading in try-catch to handle this gracefully.
- Next steps: T-002 (Add logging to runRalphLoop), T-003 (Fix swallow catch), T-004-T-007


## T-002: Add logging and events to runRalphLoop
- Timestamp: 2026-04-21T09:20:00Z
- What was done:
  - Added `Log` import and `const log = Log.create({ service: 'ralph' })` at top of ralph-executor.ts
  - Added `writeTaskLog` helper function for fire-and-forget workflow_log writes
  - Added tasks state logging at INFO on loop entry (total, pending, passed counts)
  - Added WARN log when findNextPendingTask returns null and completed===0 && failed===0
  - Added INFO log for each task attempt start (taskId, attempt number)
  - Added INFO log for task completion (taskId, attempt)
  - Added WARN log for task attempt failures (taskId, attempt, category, error summary)
  - Added workflow_log writes: task_started, task_completed, task_failed, task_retrying
- Learnings: All 45 existing tests pass without modification. The WARN log for "no pending tasks" is visible in test output, confirming the logging works.
- Next steps: T-003 (Fix swallow catch), T-004 (API endpoints), T-005 (emitPersistent), T-006 (ACP session logging), T-007 (Tests)


[2026-04-21T09:27:00Z] T-003 Fix swallow catch in workflow-controller and ralph-executor
- Added WARN-level logging to all eventBus.emit catch blocks in workflow-controller.ts (7 locations: plan_session_update x2, plan_round_start x3, emitSafe helper, writeLog helper)
- Added WARN-level logging to eventBus.emit catch blocks in ralph-executor.ts (ralph_task_update, ralph_loop_progress, writeTaskLog)
- Added ERROR-level logging to writeTasksFile catch block in ralph-executor.ts
- All catch blocks preserved fire-and-forget semantics — only logging added, no behavior change
- Typecheck passes, all 514 tests pass
- Next: T-004 (build-status and tasks API endpoints)


[2026-04-21T09:35:00Z] T-004 Add build-status and tasks API endpoints
- Added `import * as fs from 'fs'` to issues.ts for tasks.json reading
- Added GET /api/issues/:number/build-status endpoint: returns stage, status, progress (completed/failed/total/currentTask), tasks array
- Added GET /api/issues/:number/tasks endpoint: returns tasks.json current state (raw file content)
- Both endpoints handle missing issue (404), missing project (400), and server errors (500)
- Used detectOpenSpecChange + fs.readFileSync to read tasks, consistent with workflow-controller approach
- Returns graceful empty state when no change found or tasks file missing
- Typecheck passes, all 514 tests pass
- Next: T-005 (emitPersistent helper), T-006 (ACP session lifecycle logging), T-007 (Tests)

[2026-04-21] T-005: Added emitPersistent method to EventBus class in event-bus.ts. Method accepts (event, data, { issueId, sessionId?, workflowLogRepo? }), emits via EventBus and optionally writes to workflow_log, catches/logs errors from both operations. Imported Log and WorkflowLogRepo types. Typecheck passes. No existing behavior changed. Next: T-006 (ACP session lifecycle logging).

[2026-04-21T09:42:00Z] T-006 Add ACP session lifecycle logging
- Added `writeSessionLog` helper function (fire-and-forget workflow_log writes, consistent with writeTaskLog pattern)
- Added lifecycle logging to `runAcpSession` (single-round): session start (cwd, timeout, issueId), timeout events with duration (initialize/newSession/prompt phases), process errors, session completion (success/failure with duration)
- Added lifecycle logging to `createAcpConnection` (multi-round): session start (cwd, timeout, issueId, mode), timeout events with duration, process errors, connection close with duration
- All lifecycle events written to workflow_log: acp_session_start, acp_session_timeout, acp_session_process_error, acp_session_completed
- Changed log service name from 'session' to 'acp-session' for clarity
- Typecheck passes, all 514 tests pass
- Next: T-007 (Test build pipeline logging and API endpoints)
