# Ralph Progress Log
Started: 2026-04-30T13:08:28.754Z

---


[2026-04-30T21:10:35Z] T-001 Create formatAgentPrompt function
- Created packages/cli/src/agents/agent-prompt-schema.ts with AgentPromptParts interface and formatAgentPrompt function
- Created packages/cli/tests/agent-prompt-schema.test.ts with 16 unit tests covering all field combinations
- formatAgentPrompt outputs <mohist-task> XML envelope with sections in defined order
- Optional fields (projectContext, rules, contextFiles, spec, contract, template, instruction) are omitted when not provided
- project_context and rules include "Do NOT include this section in your output" annotation
- Typecheck passes, all 16 tests pass
- Learnings: Simple interface + function, no external deps needed. XML escaping not required for current use case.
- Next steps: T-002 (loadAgentConfig), then T-003/T-004 (migrate callers)


[2026-04-30T21:13:45Z] T-002 Add loadAgentConfig to workflow-loader
- Added AgentConfig interface and loadAgentConfig() to workflow-loader.ts
- Reads agent.context (string) and agent.rules (Record<string, string[]>) from workflow.yaml
- Returns empty object when agent section absent, no error thrown
- Checks both workflow.yaml and .mohist/workflow.yaml, prefers root
- Created packages/cli/tests/workflow-loader-agent-config.test.ts with 11 tests covering: absent config, present config, partial config, empty agent section, .mohist fallback, preference order, non-string context, non-array rules, malformed YAML
- Typecheck passes, all 11 tests pass
- Learnings: YAML colons in unquoted values cause parse errors — always quote values with colons in test fixtures.
- Next steps: T-003 (rewrite buildTaskContext), T-004 (rewrite artifact-prompt)

[2026-04-30T21:20:00Z] T-003: Rewrite buildTaskContext to use formatAgentPrompt
- Rewrote buildTaskContext to use formatAgentPrompt from agent-prompt-schema.ts
- Proposal and design are now <context-files> references (not inlined)
- Spec content stays inline in <spec> tags
- Learnings are listed as <context-files> file references via new listLearningFiles()
- Added <role> with task position info (e.g. "task T-003 of 5 for issue #42")
- Added <contract> with commit instructions as first item
- WIP resume and retry failure reason inlined within <task>
- Removed old formatLearningsForPrompt, formatTaskForPrompt, formatRetryContext exports
- Added formatTaskBlock (replacement for formatTaskForPrompt)
- Added totalTasks and issueNumber to BuildContextOptions and ContextAssembler.assembleTaskContext
- All 37 tests pass, typecheck passes
- Next: T-004 (artifact-prompt.ts rewrite)

[2026-04-30T21:26:00Z] T-004: Rewrite artifact-prompt.ts buildXxxPrompt functions
- Rewrote all 8 buildXxxPrompt functions to use formatAgentPrompt from agent-prompt-schema.ts
- All functions now output <mohist-task> XML envelope with <role>, <task>, <instruction>, etc.
- Added optional AgentConfig parameter to all functions (wired in T-005)
- buildArtifactPrompt: <role> describes artifact creation, <task> contains issue info + dependencies + output, <template> and <instruction> preserved
- buildSelfReviewPrompt, buildReviewerPrompt, buildReviewSelfCheckPrompt: instruction content in <instruction>
- buildReviewerPrompt: includes <rules> from agentConfig.rules.review and <spec> with spec context
- buildAutoFixPrompt: <contract> with "Apply ONLY the fixes described" + "Do NOT modify review.md"
- buildConflictResolutionPrompt: <contract> with conflict-only constraint
- buildExplorePrompt: proposal content embedded in <task> instead of separate heading
- buildReVerifyPrompt: review report embedded in <task>
- Removed unused buildIssueTaskBlock helper
- Updated tests: 66 tests all pass, added tests for all 8 functions with AgentConfig
- Typecheck passes
- Learnings: All function signatures are backward-compatible (agentConfig is optional). Callers don't need changes until T-005 wires the config through. The <dependencies> and <output> sub-tags inside <task> are preserved for buildArtifactPrompt since they're task-specific structure.
- Next steps: T-005 (wire agent config into call sites), T-006 (add workflow.yaml agent config)

[2026-04-30T21:34:30Z] T-005: Wire agent config into prompt assembly call sites
- Added agentConfig?: AgentConfig to BuildContextOptions in context-assembler.ts, wired to formatAgentPrompt's projectContext and rules.build
- Added agentConfig?: AgentConfig to RalphExecutorContext in ralph-executor.ts, wired to both buildTaskContext calls (initial + retry)
- Added getAgentConfig() method to WorkflowController (caches loadAgentConfig result)
- WorkflowController passes agentConfig to: buildArtifactPrompt, buildSelfReviewPrompt, buildReviewerPrompt, buildReviewSelfCheckPrompt, buildAutoFixPrompt, buildReVerifyPrompt, and RalphExecutor
- explore-acp-service.ts calls loadAgentConfig(worktreePath) and passes to buildExplorePrompt (both run() and runOnIssue())
- conflict-resolution.ts calls loadAgentConfig(worktreePath) and passes to buildConflictResolutionPrompt
- Agent config now flows through the full call chain: workflow.yaml → loadAgentConfig → all prompt builders → formatAgentPrompt
- Typecheck passes, all 96 directly affected tests pass (context-assembler, artifact-prompt, workflow-loader-agent-config, ralph-executor)
- Learnings: WorkflowController uses cached agentConfig; services that don't have a controller (explore-acp, conflict-resolution) call loadAgentConfig directly. No new tests needed since the wiring is covered by existing integration patterns.
- Next steps: T-006 (add workflow.yaml agent config for mohist project)

[2026-04-30T21:40:00Z] T-006: Add workflow.yaml agent config for mohist project
- Created workflow.yaml at project root with agent.context and agent.rules
- agent.context: tech stack (TypeScript, Node.js, SQLite, Hono, Vitest, Commander.js), build/test/typecheck/lint commands, directory layout, agent runner info
- agent.rules.build: 6 rules (scoped changes, typecheck, tests, no comments, follow conventions, no new deps)
- agent.rules.plan: 4 rules (clear acceptance criteria, independently verifiable, what not how, edge cases)
- agent.rules.review: 5 rules (all dimensions, acceptance criteria met, typecheck/tests, scope check, no unjustified deps)
- Typecheck passes, loadAgentConfig successfully reads all fields
- Learnings: YAML colons in unquoted strings cause parsing as mapping — quote values containing colons (same lesson as T-002)
- All 6 tasks complete
