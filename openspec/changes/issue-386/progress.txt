## Codebase Patterns

- `System.CommandLine` name alias (`cmd.Aliases.Add("name")`) is the established
  pattern for renames (`rm` on label remove, `ls` on list commands, `repo` on
  repository). It shares the handler/option set by construction, so the alias
  *cannot* diverge — exactly what we need for `show`-as-alias-of-`get`.
- The `-o yaml` template-definition contract in `get -o yaml` hits
  `GET .../yaml` (not the JSON read model). This is honored by
  `PrintWorkflowRunYamlAsync` in `MohistCliCommands.Workflow.Reads.cs`.
- `BuildArchive` in `MohistCliCommands.Issue.Lifecycle.cs` already establishes
  the local-failure pattern for required args: declare with
  `Arity = ArgumentArity.ZeroOrOne` + `DefaultValueFactory = _ => null`, then
  have the `SetAction` handler check `string.IsNullOrWhiteSpace(value)` and
  print `<arg-name> is required`. Same pattern was applied to `RunIdArg` in
  `MohistCliCommands.Workflow.cs` so the existing `"<run-id> is required"`
  path inside `BuildGet` becomes reachable (previously System.CommandLine's
  required-arg parse error fired first and the handler never ran).
- `MohistCliCommands.OutputOption(defaultValue: ..., formats: ...)` lets you
  set per-command default. Most commands default to `json`; a few (`agent
  create/list/get/...`, `issue session ...`, `system info`, `opencode
  models`) default to `table`. We use `defaultValue: "table"` for `get` so
  `mo workflow get <runId>` (and its `show` alias) renders the table summary
  view by default — matching the spec's "default table renders the summary"
  requirement.
- Help output from System.CommandLine surfaces aliases comma-joined on the
  canonical row, e.g. `get, show <run-id>`. Don't assert the bare
  `<verb> <arg>` substring alone — that's only correct for non-aliased
  commands. For aliased commands assert the full row, e.g.
  `get, show <run-id>`.
- Aliases registered via `cmd.Aliases.Add` do NOT cause System.CommandLine to
  emit separate subcommand rows — the alias only appears in the alias
  annotation on the canonical row. This is exactly what we want for the spec
  scenario "show is a transitional alias of get" — only `get` is a positional
  subcommand.

## Decisions / Discoveries

1. **`show` becomes a name alias of `get` (D5, alias strategy D1)**: single
   `System.CommandLine.Command` whose constructor takes `"get"`, with
   `cmd.Aliases.Add("show")`. Handler is shared by construction. This is the
   existing pattern (`rm` on label, etc.), no new mechanism.
2. **`status` is hard-deleted (D1 redundant-command rule)**: `BuildStatus`
   removed from `MohistCliCommands.Workflow.Reads.cs` and from the group
   registration in `MohistCliCommands.Workflow.cs`. The associated table
   shape `MohistCliApi.TableShape.WorkflowRunStatus` and its
   `RenderWorkflowRunStatus` method were removed together — they had no
   other caller. The class-level `WorkflowRunsDetail` comment was updated so
   `show` is described as the alias, not the primary; the comment also no
   longer mentions `status` (the now-removed redundant command).
3. **`RunIdArg` becomes `ZeroOrOne`**: required-arg behavior was hiding the
   custom `"<run-id> is required"` local-failure message — System.CommandLine
   would emit its own `"Required argument missing for command: 'get'"` and
   the SetAction path was unreachable. Mirrors `BuildArchive`'s precedent.
4. **`get -o` defaults to `table`**: per
   `workflow-run-reads/spec.md#get-is-the-canonical-single-resource-read-for-a-workflow-run`
   "default table output renders the summary view". Previously the default
   was `json` (inherited from the global `OutputOption(defaultValue: "json")`
   default). Setting `defaultValue: "table"` only for the `get` command —
   does NOT touch the global default, leaves every other command's JSON
   default intact.
5. **Class-level doc comment updated**: describes `show` as the alias of
   `get`, mentions `show` exists "so scripts written against the
   previously-landed surface (see #381 / issue-386 D5) keep working", and
   records that the redundant `status` command was removed. Counts the
   remaining four reads (get, variables, events, list-sessions).

## Test plan

`CliWorkflowReads.cs` was rewritten:

- Kept the `WorkflowHelp_DoesNotExposeSingleSessionSubActions`,
  `Variables_*`, `Events_*`, `ListSessions_*`, and `ReadCommands_*` tests
  basically intact (with `ReadCommands_*` adding the `show` alias row and
  dropping the `status` row).
- Renamed `Show_*` tests to `Get_*` (canonical) for the bare-GET, table,
  yaml, help, and missing-run paths.
- Added three `Show_IsAliasOfGet_*` parity tests that run the same scenario
  under both names with a shared canned response, capturing requests after
  each invocation and asserting identical (method, URI) pairs, stdout, stderr,
  exit code. This is the alias-parity scenario from the spec
  (`show behaves identically to get`) enforced by diffing requests
  programmatically — closes "alias cannot diverge" into a behavioral test.
- Added `Status_IsNotARegisteredSubcommand` and `Status_IsNotAdvertisedInWorkflowHelp`
  to assert `status` is gone (no requests issued, no help row).
- Added `Get_MissingRunId_FailsLocallyWithoutHttp` to assert the local
  `"<run-id> is required"` failure path now reachable via
  `Arity.ZeroOrOne`.
- Added `Show_Help_DocumentsYamlOutputFormat` for the alias's help block.
- Replaced `Show_InvalidOutputFormat_*` → `Get_InvalidOutputFormat_*`.

Test harness helper `RunUnderBothNames` is a small local utility used by all
three parity tests; it captures the request list after each invocation
(`handler.Requests.Skip(canonicalRequests.Count)`) and diffs them.

## Verification

`dotnet test packages/cli/tests/Mohist.Cli.Tests` final tally:

- Before task: 725 pass, 0 fail
- After task: 729 pass, 3 fail (same 3 pre-existing `CliReferenceDocsSpecs`
  failures on master related to `docs/cli-reference.md` text mismatches
  covered by other items in this issue + T-006 finalization — out of scope
  for T-001).

CLI Workflow Reads suite: 31/31 pass.

Manual smoke tests:

- `mo workflow get <runId>` hits `/api/workflow-runs/{runId}` (or `/yaml` with `-o yaml`)
- `mo workflow show <runId>` — identical behaviour (same handler)
- `mo workflow status` — fails as unrecognized command
- `mo workflow get` (no runId) — prints `<run-id> is required` to stderr,
  exits non-zero, no HTTP request

## Gotchas for next tasks

- **`RunIdArg` is now `Argument<string?>`** with `Arity.ZeroOrOne`. All
  existing call sites already handle a nullable return via
  `string.IsNullOrWhiteSpace(runId)` checks inside `SetAction`. No callers
  needed updating — verified by a clean dotnet build with TreatWarningsAsErrors.
- The server-side `WorkflowRunStatus` enum (in `WorkflowRun.cs`) is a
  separate concept from the CLI's `TableShape.WorkflowRunStatus` we removed
  here. Do NOT confuse them.
- The web's `WorkflowRunStatus` (TS type alias for the WorkflowRunStatus enum)
  is also separate.
- The `MohistCliApi.TableShape.WorkflowRunStatus` enum value was only used by
  `BuildStatus`. Confirmed via `grep` before deletion — no other
  code/dispatch path referenced it.
- The doc-string failure tests in `CliReferenceDocsSpecs.cs` will be
  reconciled by T-006 (gap-table cleanup) + the per-task docs updates that
  fall out of items 4-6 (when their corresponding tasks ship). Do NOT touch
  them in T-001.

## T-002: Profile toggle (enable/disable) — code & test patterns

### Code

- `BuildProfileEnable` / `BuildProfileDisable` live in
  `MohistCliCommands.ProjectWorkflow.cs` alongside the existing
  `BuildProfileList`. Both are added to the `profile` subgroup in
  `BuildProfile`.
- Argument follows the `<run-id>` precedent from T-001: `Argument<string?>`
  with `Arity.ZeroOrOne` + `DefaultValueFactory = _ => null`. Handler
  checks `string.IsNullOrWhiteSpace(profileId)` and writes
  `"<profile-id> is required"` to stderr. Without this `ZeroOrOne` arity
  the SetAction would be unreachable because System.CommandLine's own
  required-arg parse error would fire first.
- Project scoping uses the standard `--project` / `--project-id` pair from
  `MohistCliCommands.ProjectRefOption()`. Unresolvable project fails locally
  via the existing `api.ResolveProject(project, projectId)` path — no extra
  guard needed, the conflict-msg / no-active-project stderr already covers
  the acceptance criteria.
- Bodies are `new { profileId }`. URL is built from
  `ProjectWorkflowProfilePath(resolvedProjectId, "/enable")` /
  `"/disable"` (already a private helper at the top of the file, used by
  `BuildConfigGet` etc.).
- Wire call goes through `api.PrintPostAsync` (not `PrintPostWithOutputAsync`)
  because there's no body / table to render — endpoint returns `200 OK` with
  empty payload. This keeps the existing success-path of
  `PrintResponseAsync` (writes `"OK"` to stdout) and the existing
  error-path (writes `error (code)` to stderr + returns
  `FailureExitCode(status)` — 4 for 404, 1 for everything else). That
  faithfully surfaces the verbatim server error per the spec scenario
  "Unknown profile on disable surfaces the server error" with no
  CLI-side translation.
- Description for `disable` mentions the `last_enabled_workflow_profile`
  guard so `--help` advertises the contract.

### Tests (`CliProjectWorkflowProfileSpecs.cs`)

Added 12 tests covering the acceptance criteria:

- `ProfileEnable_PostsProfileIdToEnableEndpoint` — happy-path POST shape
  (`{"profileId":"<profileId>"}` body + correct URL).
- `ProfileDisable_PostsProfileIdToDisableEndpoint` — disable counterpart.
- `ProfileEnable_MissingProfileId_FailsLocallyWithoutHttp` / `...Disable...`
  — assert `Assert.Empty(handler.Requests)` (no HTTP) + exit code 1 +
  stderr contains `"<profile-id> is required"`.
- `ProfileEnable_HonorsProjectIdFlag` / `ProfileDisable_HonorsProjectFlag`
  — verify `--project-id` and `--project` route to the right
  `/api/projects/<id>/workflow-profile/{enable,disable}` URL. Same parser
  path as other project-scoped commands — the existing
  `ResolveProject(project, projectId)` does the merge.
- `ProfileEnable_UnknownProfileServerError_SurfacesCodeAndMessage` /
  `...Disable...` — server returns `400` + `{ success:false, error, code:
  "unknown_workflow_profile" }`; assert the message and code both end up
  in stderr and exit code is 1.
- `ProfileDisable_LastEnabledProfileServerError_SurfacesCodeAndMessage` —
  the `last_enabled_workflow_profile` 400 path on disable.
- `ProfileEnable_NoProjectResolvable_FailsLocallyWithoutHttp` — covers the
  acceptance criteria's "unresolvable project fails locally" via the
  existing `CliTestHarness.CreateSync(activeProjectId: null)` setup; assert
  `Assert.Empty(handler.Requests)` and stderr contains the standard
  "Run 'mo project use ...'" hint.
- `ProfileEnable_ConflictingProjectFlags_FailsLocallyWithoutHttp` — covers
  the `--project`/`--project-id` conflict case (out of the spec's explicit
  list but a real failure path worth pinning).
- `Profile_HelpAdvertisesEnableAndDisableSubcommands` — `--help` shows
  both new subcommands (a safety net against future regressions where
  someone adds another `profile` subcommand and accidentally shadows).

### Verification

`dotnet test packages/cli/tests/Mohist.Cli.Tests`:

- Before task: 729 pass, 3 fail (pre-existing `CliReferenceDocsSpecs`
  failures from master; T-006 scope).
- After task: 741 pass, 3 fail (same 3 pre-existing — `CliReferenceDocsSpecs`
  unrelated to this task). Net: +12 tests, all green.

`CliProjectWorkflowProfileSpecs`: 26/26 pass (14 pre-existing + 12 new).

### Gotchas for next tasks

- The `ProfileEnable_UnknownProfileServerError_SurfacesCodeAndMessage`
  test asserts the literal `unknown_workflow_profile` string in stderr.
  If the server renames the code, this breaks — that's intentional per
  D4's risk call (server contract change is itself a coordinated change).
- The new `enable` / `disable` commands do not declare an `-o` option —
  they hit `PrintPostAsync` (no output mode plumbing). The success path
  therefore always writes the JSON envelope (or `"OK"` when no data) to
  stdout. Adding `-o table` would be nice-to-have but out of scope for
  the additive caller; the server returns no meaningful body to render.
- The `disable` command description explicitly names
  `last_enabled_workflow_profile` so the guard is discoverable from
  `--help`. If a future server adds more guards on these endpoints,
  update the description in lockstep.
- The `profile` subgroup's `--help` test (`Profile_HelpAdvertises...`)
  relies on System.CommandLine's default help-text rendering; no custom
  help writer is in play. Watch for changes to that renderer if you
  add more subcommands — the assertion is on substring presence of
  `enable` and `disable`, which the renderer emits as part of the
  subcommand listing.

## T-003: Issue rerun — add --from-stage, keep rerun-from-stage as peer alias

### Code

- New `BuildRerun` lives in `MohistCliCommands.Issue.Lifecycle.cs` right
  above `BuildRerunFromStage`. The previous generic
  `BuildAction("rerun", "Rerun issue", api)` registration in
  `MohistCliCommands.Issue.cs:21` is replaced with a direct
  `BuildRerun(api)` call. The generic `BuildAction` factory is left in
  place — it's still used for `start`/`approve`/`close`/`reopen`/`retry`/
  `force-stop`/`resume`/`unarchive` (those have no flags).
- `BuildRerun` mirrors `MohistCliCommands.Workflow.cs:174` (workflow's
  `BuildRerun`) structurally: `--from-stage` as `Option<string?>`,
  `fromStageProvided` from `IsOptionProvided(ctx, fromStageOpt)`, then
  `suffix = fromStageProvided ? "/rerun-from-stage" : "/rerun"`. The
  empty-body branch sends `new { }` (serializes as `{}` to the server,
  matching the rerun-from-start contract); the present branch sends
  `new { stage = fromStage! }`.
- `fromStageProvided` uses `IssueCommands.IsOptionProvided(ctx, ...)`
  (already declared at `MohistCliCommands.Issue.cs:54`) — returns
  `true` only when the user actually typed `--from-stage` on the
  command line (NOT when its default applies). Workflow's rerun does
  the same with the same helper pattern; the empty-vs-absent distinction
  is what makes `--from-stage ""` fail locally while bare `rerun` still
  POSTs to `/rerun` successfully.
- Local-failure message is `--from-stage is required and must not be
  empty` (verbatim spec phrasing from
  `specs/issue-rerun/spec.md#scenario-empty---from-stage-fails-locally`).
  Workflow uses the same text — symmetric error surfaces.
- `BuildRerunFromStage` is kept intact (D3: name-aliases share option
  sets so cannot honor both `--stage` and `--from-stage`). Its
  description was prefixed with `[transitional alias of 'mo issue rerun
  --from-stage']` so `--help` advertises it as a peer, not a primary.
  Wire behavior is unchanged: POSTs `{stage}` to `/rerun-from-stage`.

### Tests (`CliIssueRerunFromStageSpecs.cs`)

Added 7 tests on top of the existing 4 (rerun-from-stage peer behavior):

- `IssueRerun_NoFromStage_PostsEmptyBodyToRerunEndpoint` — bare rerun
  hits `/rerun` with `{}` body (acceptance #1).
- `IssueRerun_WithFromStage_PostsStageBodyToRerunFromStageEndpoint` —
  rerun --from-stage hits `/rerun-from-stage` with `{stage}` body
  (acceptance #2). Mirrors `CliWorkflowControlSpecs.Rerun_WithFromStage`.
- `IssueRerun_EmptyFromStage_FailsLocallyWithoutHttp` — `--from-stage ""`
  exits 1, no HTTP, stderr contains the exact spec phrasing
  (acceptance #3).
- `IssueRerun_BlankFromStage_FailsLocallyWithoutHttp` — `--from-stage "   "`
  same path (acceptance #3 — whitespace case).
- `IssueRerun_NoFromStage_HonorsProjectIdFlag` — `--project-id` reroutes
  to `/api/projects/<id>/issues/42/rerun` (acceptance #5).
- `IssueRerun_WithFromStage_HonorsProjectIdFlag` — same for the
  rerun-from-stage path (acceptance #5).
- `IssueRerun_FromStageAlias_PostsSameRequestAsRerunFromStagePeer` —
  runs both commands back-to-back against the same harness, captures
  requests, asserts method/URI/body all identical and exit codes equal
  (acceptance #4 parity). Different shape than `RunUnderBothNames` (workflow
  get/show) because the two commands have different flag names
  (`--from-stage` vs `--stage`) so we cannot pre-build a shared arg
  list; instead we invoke each command separately and diff the captured
  requests.

### Verification

`dotnet test packages/cli/tests/Mohist.Cli.Tests`:

- Before T-003: 741 pass, 3 fail (the 3 pre-existing `CliReferenceDocsSpecs`
  failures from T-002's report).
- After T-003: 748 pass, 3 fail (same 3 pre-existing — T-006 scope).
  Net: +7 tests, all green.

`CliIssueRerunFromStageSpecs`: 11/11 pass (4 pre-existing + 7 new).

### Gotchas for next tasks

- The `BuildAction` factory is left in place (used by 8 other issue
  lifecycle verbs). Don't remove it — only `rerun` needed the dedicated
  handler because it's the only one with a flag-driven variant. If a
  future issue lifecycle verb gains a flag, prefer adding a dedicated
  `Build<Verb>` (mirrors `BuildRerun`/`BuildReject`/`BuildStop`/`BuildRebase`
  /`BuildArchive` pattern) over extending `BuildAction`.
- The `fromStageProvided` check uses `IssueCommands.IsOptionProvided`,
  which lives in `MohistCliCommands.Issue.cs:54`. Workflow-side rerun
  doesn't use that helper — it inlines the same logic with `ctx.GetResult
  (fromStageOpt) is not null`. The two are equivalent; either could be
  promoted to `MohistCliCommands.IsOptionProvided` later.
- The transitional alias description for `rerun-from-stage` starts with
  the literal `[transitional alias of 'mo issue rerun --from-stage']`
  prefix. Future cleanup issues can grep for this exact prefix to find
  every transitional alias in one pass.
- Both `rerun` and `rerun-from-stage` show up as separate rows in
  `mo issue --help` (System.CommandLine doesn't merge them — they're
  sibling `Command` instances). That's intentional per D3; the
  `[transitional alias ...]` description prefix is how `--help` users
  discover the relationship.

## T-004: Agent delete->archive — code & test patterns

### Code

- `BuildDelete` in `MohistCliCommands.Agent.cs` was renamed to
  `BuildArchive`: `new Command("archive", "Archive an agent")` with
  `cmd.Aliases.Add("delete")`. Inner closure renamed `DeleteAsync` →
  `ArchiveAsync` for symmetry. The `DeleteAgentAsync` helper that does
  the actual `SendAsync(HttpMethod.Delete, ...)` call was kept intact
  — it still wires `HttpMethod.Delete` to `/agents/<id>` on the wire.
  The internal helper name is irrelevant to the wire/CLI surface; the
  verb on the URL path is `/agents/{id}` (no `archive` segment — the
  spec keeps the `ArchiveAsync` server method but it's still a `DELETE`
  on the agent resource, mirroring issue archive which is also
  POST-DELETE style).

### Tests (`CliAgentCommandSpecs.cs`)

Added 5 tests on top of the existing 16 (which all still pass):

- `AgentArchive_ResolvesByIdAndDeletes` — covers the spec scenario
  "Archive by id resolves and deletes". The id-form path issues TWO
  requests: a `GET /agents/<id>` (to read the agent's name) then the
  `DELETE /agents/<id>`. Test asserts both URIs are equal and the
  second is a DELETE, plus the stdout contains `Agent reviewer
  (agent_123) archived`.
- `AgentArchive_ResolvesByNameAndDeletes` — covers "Archive by name
  resolves via the agent list". First request is the `GET
  /agents?all=true` list lookup, second is the `DELETE /agents/<id>`.
- `AgentArchive_UnresolvedFailsLocallyWithoutHttp` — covers "Unresolved
  agent fails locally". Name-lookup path: a single `GET` to the list
  endpoint returns empty, then the CLI prints `Agent 'missing' not
  found` to stderr and exits 1 WITHOUT sending a DELETE. Asserts
  exactly 1 request in the handler (the listing GET only — proves
  the failure path skips the DELETE).
- `AgentArchive_DeleteAlias_ProducesIdenticalRequestAndOutput` — the
  parity test. Runs `archive <name>` then `delete <name>` back-to-back
  against a shared harness (clearing output/error buffers between
  invocations). Captures the canonical request list after the first
  invocation, then `handler.Requests.Skip(canonicalRequests.Count)`
  for the alias's requests, and diffs method+URI per index. Asserts
  stdout, stderr, exit code all equal. This is the exact `RunUnderBothNames`
  pattern from T-001 generalized inline — `archive` and `delete` are
  a true System.CommandLine `cmd.Aliases.Add` pair, so the parity is
  enforced by construction; this test turns "cannot diverge" into a
  behavioral assertion.
- `AgentArchive_AliasDelete_HonorsProjectIdFlag` — covers "produces
  same request/output/exit under any project scoping flags" by
  invoking `delete agent_123 --project-id proj_other` and asserting
  the URI is `/api/projects/proj_other/agents/agent_123` (proves the
  `--project-id` flag flows through the alias correctly).

Renamed the existing `AgentDelete_ArchivesByResolvedName` →
`AgentDelete_ArchivesByResolvedName_LegacyDeleteVerbStillWorks`. Same
body, pinned explicitly as the legacy entry-point contract.

Updated `AgentHelp_ListsSubcommands`: now asserts the full
`archive, delete <name-or-id>` row (canonical + alias annotation,
matching System.CommandLine's emission) AND verifies there is no
standalone `delete <name-or-id>` row (the alias collapses onto the
canonical). This is the same assertion pattern T-001 used for
`get, show <run-id>` in `CliWorkflowReads.cs`.

### Verification

`dotnet test packages/cli/tests/Mohist.Cli.Tests`:
- Before T-004: 748 pass, 3 fail (same 3 pre-existing
  `CliReferenceDocsSpecs` failures from T-006 scope).
- After T-004: 753 pass, 3 fail. Net: +5 tests, all green.

`CliAgentCommandSpecs`: 21/21 pass (16 pre-existing + 5 new).

`npm run build`: clean (.NET + Web + Runner).

### Gotchas for next tasks (T-005 label, T-006 docs)

- The `archive` command's `--help` row emits `archive, delete
  <name-or-id>` exactly the way T-001's `get, show <run-id>` does —
  this is a transitive property of System.CommandLine's `cmd.Aliases.Add`
  pattern, not per-command customization. T-005 should follow the
  same pattern in `MohistCliCommands.Label.cs`: rename
  `new Command("remove", ...)` to `new Command("delete", ...)`,
  `cmd.Aliases.Add("remove")` and `cmd.Aliases.Add("rm")` together.
  Both aliases collapse onto the canonical row — System.CommandLine
  emits `delete, remove, rm <key>`.
- The label spec already names `delete` as canonical and `rm`/`remove`
  as aliases (per `docs/cli-reference.md:220`) — no spec movement
  needed for T-005 other than the gap-table cleanup in T-006.
- Test parity pattern for label will be a single harness with
  three back-to-back invocations: `delete`, `remove`, `rm`. The
  `handler.Requests.Skip(last.Count)` capture still works because the
  recording handler is a `List<CapturedRequest>` that grows across
  invocations on the same harness. The label spec text in
  `specs/label-delete/spec.md` requires parity across all three names.
- The `agent --help` test (`AgentHelp_ListsSubcommands`) now asserts
  the full alias-collapse row `archive, delete <name-or-id>`. If a
  future cleanup drops the `delete` alias, this test will fail with
  the bare `archive <name-or-id>` row — that's the audit point for
  T-006's alias inventory.
- The transitional aliases introduced across this issue are (so far):
  - `workflow show` (alias of `get`, T-001)
  - `issue rerun-from-stage --stage` (peer of `rerun --from-stage`,
    T-003 — peer not alias because flag name differs)
  - `agent delete` (alias of `archive`, T-004)
  - `label remove` / `label rm` (aliases of `delete`, T-005)
  T-006 should consolidate this inventory in an issue comment.

