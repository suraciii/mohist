# issue-617 plan repair progress

`self-review.md` is intentionally untouched; the next self-review regenerates it.

## Must-fix dispositions in progress

- MF-1 — Slack-origin context can degrade to ordinary work: FIXED IN PLAN. The revised artifacts require a persisted/transported `executionSource`, enforce the Slack/context and non-Slack/no-context pair at Server, Runner control, AgentJob, and follow-up boundaries, and test omitted/null context on both paths.
- MF-2 — Batched follow-ups lack a complete anchor: FIXED IN PLAN. The revised artifacts persist the effective bound thread root, including the initial DM message, define the first durable input in a combined turn as the representative triggering message, and cover multi-input and DM follow-up dispatches.
- MF-3 — Same-version Skill immutability/parity is not enforceable: FIXED IN PLAN. The lock, drift rejection, and parity mechanism now use the exact post-change v1 fixture and digest.
- Review finding 1 — The pinned digest described the incomplete pre-change Skill: FIXED IN PLAN. `design.md` defines the complete v1 instruction bytes first, records their exact UTF-8/LF digest, and `tasks.json` plus both parity contracts use the same value.
- Review finding 2 — Strict source validation was deployed before Server emission: FIXED IN PLAN. `design.md`, the injection spec, and T-002 define a bounded dual-read gate, capability routing, legacy drain/reconciliation, strict activation prerequisites, mixed-version tests, and rollback order.

## Verification

- Canonical post-change asset fixture is now defined exactly in `design.md` with LF line endings and one trailing LF; its SHA-256 is `dedf18a796543ade06a9e0ece00c086577153e1e633f868c099b01cf910d641b`. The implementation must make the embedded file and catalog fixture byte-for-byte equal to it before claiming the digest is verified.
- No changes are made to `self-review.md`.
- Incremental commits: `98949a924` and `7712ca9ad` cover the canonical Skill lock repair; `df5b56129` covers the source-discriminator and batched-anchor repair; `f26be6fd0` covers the bounded rollout repair.
- Mechanical checks pass: `tasks.json` parses, the canonical fixture re-hashes to its pinned digest, `git diff --check` is clean, spec anchors and mixed-version rollout scenarios are present, and `self-review.md` is untouched. `npm run docs:check` could not run because `tsx` is not installed in the workspace (`exit 127`).
