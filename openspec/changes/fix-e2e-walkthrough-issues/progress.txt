# Ralph Progress Log
Started: 2026-04-23T15:22:38.091Z

---


[2026-04-23T15:24:32] T-001 COMPLETED
- Added Completed = 'completed' to IssueStatus enum in types/index.ts
- Imported IssueStatus in workflow-controller.ts and added updateStatus(issueId, IssueStatus.Completed) after updateStage(Done) and clearApprovalState()
- Added guard in issue-service.ts resume() to return null when issue.status === IssueStatus.Completed
- Typecheck passes, all 580 tests pass
- Learnings: workflow-controller.ts didn't previously import IssueStatus at all — status was only managed by agent-runner-service and api layer
- Next: T-002 (DB fallback for approve endpoint) can proceed in parallel

[2026-04-23T15:27:45] T-002 COMPLETED
- Added DB fallback in approve endpoint (api/issues.ts): when hasPendingGate(number) returns false, queries issueRepo.findPendingApprovalByIssueId(issue.id) — if approval_state.status === 'awaiting', proceeds with approval instead of returning 400
- Moved issueRepo fetch before the hasPendingGate check so it's available for the fallback query
- Normal approve path (gate in memory) is completely unchanged
- Typecheck passes, all 580 tests pass
- Learnings: The issue object already has approvalState from DB (fetched via getByNumber), so no need to reconstruct gate info — just need to bypass the hasPendingGate memory check. The issueRepo.findPendingApprovalByIssueId() method already existed and handles the DB query correctly
- Next: T-003 (recoverIssues preserve awaiting state) depends on T-002

[2026-04-23T23:31:00] T-003 Fix recoverIssues to preserve awaiting approval state
- Modified recoverIssues() in agent-runner-service.ts to check approvalState.status === 'awaiting' before resetting
- Awaiting issues now get their pendingGates Map entry restored instead of being reset to Blocked/Draft
- Non-awaiting crashed issues are still reset to Blocked/Draft as before
- Updated existing test that incorrectly tested old behavior (was resetting awaiting issues)
- Added 3 new tests: restore pending gates for awaiting, reset crashed issues, mixed scenario
- Learning: The existing test had approval_state set to 'awaiting' but was testing reset behavior — it was actually testing the wrong scenario
- All 581 tests pass, typecheck clean
- Next: T-004 (git commit after build stage)

[2026-04-24T00:34:06] T-004 COMPLETED
- Added commitBuildChanges() private method to WorkflowController
- Uses execFileAsync('git', ...) pattern matching existing codebase convention (not simple-git which isn't a dependency)
- Filters openspec/changes/ paths from git status --porcelain output before deciding to commit
- Skips commit when no relevant changes detected (logs info message)
- Wraps everything in try-catch: commit failure logs warning but doesn't block pipeline
- Commit message format: 'build(issue-N): issue title', uses --no-verify
- Typecheck passes, all 581 tests pass
- Learnings: simple-git is NOT a dependency — the codebase uses raw child_process.execFile for git ops (worktree-manager.ts pattern). Also, the spec said "excluding openspec/changes/" — implemented by filtering git status --porcelain lines that contain that path
- Next: T-005 (ACP EPIPE fix)

[2026-04-23] T-005 Fix ACP EPIPE errors by destroying streams on process exit
- Added separate proc.on('exit') handler in both runAcpSession and createAcpConnection that destroys proc.stdin and proc.stdout wrapped in try-catch
- Handler fires first (registered before existing exit handler), ensuring streams are destroyed before any cleanup abort() calls
- Existing spawn-failure exit handler logic unchanged
- All 581 tests pass, typecheck clean
- Next: T-006 (unit tests for all fixes)
