# Progress Notes — Issue 401

## T-001 (Decision-adjacent evidence)

### Codebase patterns discovered
- `useIssueWorkflowArtifacts` in `entities/issue/api/queries.ts:53` uses query key `['issues', issueNumber, projectId, 'workflow-artifacts', params]`. Same params (default `{}`) on both call sites means TanStack Query dedupes — no double-fetch even with two ArtifactOpener instances mounted (full panel + compact in surface).
- `ArtifactContentViewer` already takes `contentHook` injection. The shared ArtifactOpener preserves that hook for both modes so unit tests can stub content.
- `LatestArtifactsPanel` had `data-testid="latest-artifacts-list"` on the list container and `data-testid="latest-artifact-item"` on each row button. The compact mode in the surface uses `data-testid="runtime-evidence-list"` on the wrapper container but keeps the same `latest-artifact-item` on each row so existing content-viewer tests apply to both call sites.

### Component design
- `ArtifactOpener` extracted from `LatestArtifactsPanel` is the new reusable primitive (`packages/web/src/widgets/issue-workflow/ui/ArtifactOpener.tsx`). Two modes: `full` (card chrome + "Latest Artifacts" title, identical to the original panel) and `compact` (inline list with "Plan / check evidence" caption, hosted inside the surface). Each instance owns its own `selectedArtifactId` so only one viewer opens at a time.
- `LatestArtifactsPanel` now composes `ArtifactOpener` with `mode="full"` — renders byte-for-byte unchanged (10 existing `WorkflowArtifacts.test.tsx` tests still pass, including card chrome, error state, loading state, empty state, directory rendering).
- Compact mode returns null (no wrapper, no DOM) when `workflowRunId` is absent, the query is still loading, the query errored, or no artifacts exist — so the surface never shows an empty "Plan / check evidence" caption.

### Surface slot wiring
- New `DecisionEvidence` type on `RuntimeDecisionSurface`: `{ issueNumber, workflowRunId, artifactsHook?, contentHook?, compactLimit? }`. Surface accepts an optional `evidence` prop and renders the compact ArtifactOpener only when `decision.summary` is `approval-required | blocked | failed` AND `evidence.workflowRunId` is truthy.
- Non-applicable summaries (`running`, `queued`, `done`) render identically to before — `runtime-evidence` is absent in those paths and every pre-existing `data-summary`, `data-tone`, `data-testid` is preserved.
- The compact ArtifactOpener also surfaces `data-summary={decision.summary}` on its `runtime-evidence` wrapper so spec tests can assert the summary that gated the evidence.

### IssueDetailPage wiring
- `IssueDetailPage` now passes `evidence={{ issueNumber, workflowRunId: issue.workflowRunId ?? null }}` into `RuntimeDecisionSurface`. No new network call beyond the deduped artifact query — the surface's `ArtifactOpener` and the reading-flow `LatestArtifactsPanel` share the same TanStack Query cache entry.
- `compactLimit` defaults to 3 inside `ArtifactOpener`; the page does not pass a custom limit, so the surface shows up to 3 most recent artifacts.

### Test coverage
- Added 10 new `RuntimeDecisionSurface` tests covering: in-surface evidence for approval-required, blocked, and failed; clicking a compact item opens the modal (same modal as the full panel); evidence is omitted for running, queued, done; `compactLimit` truncation; absent-`workflowRunId` omission; empty-artifacts omission.
- Added 11 new `IssueDetailPage.decision-adjacent-evidence` tests covering: end-to-end placement inside the status-header tier (not reading-flow/rail) for approval-required/blocked/failed, full LatestArtifactsPanel still renders unchanged in reading-flow when the surface evidence is present, content-viewer opens from a compact item, deduped query between surface and reading-flow.
- All 4548 web tests pass; typecheck clean.

### Decisions made
- The compact ArtifactOpener renders its own wrapper element with `data-testid="runtime-evidence"` instead of the surface wrapping it. This means the wrapper correctly disappears when there are no artifacts (avoids a stray empty caption) and keeps the test anchor single-sourced.
- `evidenceSummary` is the `decision.summary` value passed through the ArtifactOpener so the wrapper carries `data-summary` for spec assertions without coupling the ArtifactOpener to RuntimeDecision knowledge.
- `LatestArtifactsPanel` is kept as a thin shell composing ArtifactOpener (mode="full") to preserve its public API and ensure every existing call site continues to work without change.

### Gotchas
- When clicking a compact evidence item, the test must mock the artifact content endpoint (`/workflow/artifacts/:artifactId/content`) or MSW will reject the unhandled request. Added this in the spec.
- The `ArtifactOpener` early-returns null when no artifacts exist, so the surface-level evidence wrapper is no longer needed (the ArtifactOpener now owns its own wrapper in compact mode).
## T-002 (Compact runner/session execution signal)

### Codebase patterns discovered
- `useWorkflowRunSessions` in `entities/coder-session/model/useWorkflowRunSessions.ts` already powers the full `WorkflowSessionsPanel`. Query key `['workflow-runs', workflowRunId, 'sessions']` is the same on both call sites, so calling the hook from the page-level (in addition to the panel) does not double-fetch — TanStack Query dedupes. The page hook call returns the same `sessions` array the panel renders.
- An "active" coder session in this codebase is any session with `status === 'active' | 'running' | 'probing'` (matches `useCoderSessions.ts:44,103,127` and `useGenericSessionDataSource.ts:82` patterns). `completed | failed | cancelled` are terminal.
- `buildWaitReason` in `widgets/issue-workflow/model/derive-runtime-decision.ts:92` is the single source of truth for runtime wait reasons (draft, waiting-for, runner-unavailable, capacity-full). Exporting it (now `export function buildWaitReason`) lets `IssueDetailPage` reuse the same text for the compact execution signal so the in-surface runner reason and the existing `runtime-wait-reason` stay byte-for-byte consistent.
- `toProjectPath` (already in scope from `useProjectPath`) is the path-builder the full `WorkflowSessionsPanel` uses for transcript links (e.g. `/issues/{n}/workflow/sessions/{name}`). Reusing it keeps the compact link consistent with the full panel.

### Type & wiring design
- `ExecutionSignal` lives on the surface as three named exports: `ActiveSessionCue` (`{ sessionName, transcriptPath }`), `RunnerGatingReason` (`{ reason, kind: 'runner-unavailable' | 'capacity-full' }`), and `ExecutionSignal` (`{ activeSession?, runnerGating? }`). The surface accepts `executionSignal?: ExecutionSignal | null` and renders nothing when the prop is omitted or both fields are falsy — non-applicable states render identically to before.
- The page-level helper `buildExecutionSignal` lives at `pages/issue-detail/model/buildExecutionSignal.ts`. It derives the runner-gating reason by calling `buildWaitReason` directly so the text matches the `runtime-wait-reason` exactly; the helper also checks `summary === 'queued'` to suppress the signal on non-decision-relevant summaries and excludes draft/waiting-for blockers (which would otherwise take precedence in `buildWaitReason`).
- `deriveRuntimeDecision` and `RuntimeDecision` are NOT extended with session detail — that would mix adjudication with execution-plane evidence (per design D3). The execution signal is purely page-computed and passed down.

### IssueDetailPage wiring
- `IssueDetailPage` now calls `useWorkflowRunSessions(workflowRunId)` alongside the existing hooks and finds the first session with status `'active' | 'running' | 'probing'`. The active session name + transcript path are passed into `buildExecutionSignal` along with `agentStatus`, the issue `blocker`, and `decision.summary`.
- The signal is passed to `RuntimeDecisionSurface` only on desktop (consistent with how the surface is gated by `!isNarrowViewport`); on narrow viewports, the existing `MobileActionBar` continues to carry the primary action.
- `_issueDetailMsw.tsx` adds `mockWorkflowRunSessions([...])` so tests can mount the page with a session list without re-stubbing the route.

### Surface slot rendering
- The signal renders as a single inline row with two optional spans: session cue (icon + "Session:" label + monospace session name + Link to transcript) and runner reason (icon-less "Runner:" label + reason text). When both apply, both render together; when only one applies, only that one renders; when neither applies, the row is omitted entirely.
- The signal sits between `runtime-drift-note` and the optional evidence slot — order in the section: rationale → next action → wait-reason → drift-note → execution-signal → evidence → actions.

### Test coverage
- Added 9 new `RuntimeDecisionSurface` tests in `widgets/issue-workflow/ui/RuntimeDecisionSurface.test.tsx` covering: active session alone; runner-unavailable alone; capacity-full alone; both together; omitted when prop absent; omitted when both fields null; omitted for queued without gating; external `runnerMessage` text override; signal co-located with action buttons inside the surface.
- Added 10 new tests in `pages/issue-detail/model/buildExecutionSignal.test.ts` covering the helper directly: null when no session + no gating; session-only; runner-unavailable; default fallback message; capacity-full; zero-max placeholder; draft blocker; waiting-for blocker; non-queued summary; both together.
- Added 12 new tests in `pages/issue-detail/ui/IssueDetailPage.execution-signal.test.tsx` covering end-to-end placement inside the status-header tier for active session, full WorkflowSessionsPanel still in reading-flow, normal running omits signal, runner-unavailable matches wait-reason text, capacity-full matches wait-reason text, external runnerMessage, done path omits signal, waiting-for blocker suppresses runner signal, capacity-gating consistency (Start disabled, reason matches), Start enabled + no signal when capacity available, normal running + completed sessions omits signal, done omits signal.
- All 4579 web tests + 1031 runner tests pass; typecheck clean.

### Decisions made
- Runner-gating is gated to `summary === 'queued'` (the only summary where the runner state "gates a decision"). Showing the runner reason during, e.g., `approval-required` would be misleading — the runner state doesn't block that approval.
- The helper accepts `blocker: unknown` (rather than the full `IssueStartBlocker` type) so it stays decoupled from the `Issue` model and the type imports stay small. The helper explicitly checks the blocker kinds it cares about (`draft`, `waiting-for`) and lets `buildWaitReason` handle the rest.
- The compact signal sits BETWEEN `runtime-drift-note` and the evidence slot, so both signals are visible in the same context (decision rationale → wait reason → drift note → execution signal → evidence → actions).
- The session cue uses the `ActivityIcon` (same icon the WorkflowSessionsPanel uses for live sessions) so the visual language is consistent across the page.

### Gotchas
- `useWorkflowRunSessions(workflowRunId)` must be called BEFORE the conditional `if (isLoading || !issue) return ...` early return, otherwise the hook order would violate the Rules of Hooks. The hook handles `null` workflowRunId correctly via `enabled: !!workflowRunId`.
- The "Project 1" project name in tests becomes URL-encoded as `Project%201` in the transcript link (because `toProjectPath` uses the project's display name, not the id). Tests assert the path with a regex `toMatch(/\/issues\/401\/workflow\/sessions\/review-repair$/)` rather than an exact match.
- The runner span renders a "Runner:" label prefix, so `runner.textContent` is `"Runner:<reason>"` while `runtime-wait-reason.textContent` is `"Waiting on: <reason>"`. The consistency test strips both prefixes and compares the reason substring — that's what the acceptance criterion requires (matches the text produced by buildWaitReason).

## T-003 (First-screen drift recovery entry point with shared rebase hook)

### Codebase patterns discovered
- The rebase mutation in `BranchBar.tsx` is built from four cooperating pieces: `useWorkspaceStatus` (workspace query, key `['issues', issueNumber, projectId, 'workspace-status']`), `useLiveTask().rebaseConflict` (cross-issue conflict state), `useState<boolean>` for `rebaseQueued`, and `useMutation({ onSuccess: data.status === 'queued' })`. Hoisting these into a single hook prevents logic drift between the two call sites (BranchBar + surface slot).
- `buildDriftNote` in `derive-runtime-decision.ts:250` already returns null when `drift?.drifted` is falsy and otherwise returns a per-decision string (`Base drift requires attention.` for `needs-attention`, `Base drift is currently deferred.` for `defer`, etc.). So `decision.driftNote !== null` is the natural gate for showing the first-screen drift slot.
- The decision is also gated on the drift decision value itself (`'needs-attention'` only) — `defer` does not block progress and should NOT promote to first-screen rebase. The page-level `buildDriftRecoveryAction` checks this so the surface stays simple and policy lives in one place.
- `useWorkspaceStatus` returns `baseBranch: data?.baseBranch ?? 'master'` (the hook itself falls back). The page-level helper `buildDriftRecoveryAction` lets the page pass a `baseBranchFallback` (the issue's repository.baseBranch) but the hook's value wins when present — this matches BranchBar's existing `data?.baseBranch ?? fallbackBaseBranch ?? 'master'` chain.
- `useLiveTask().rebaseConflict` is shared via React Context (`LiveTaskContext` defaulting to `{ activeTaskId: null, rebaseConflict: null }`), so any component that calls the hook without a provider (some tests) sees `null` for the conflict and the rebase hook's `isConflictResolving`/`isConflictFailed` derive to `false` correctly.

### Shared hook (`useRebaseRecovery`)
- New file: `packages/web/src/widgets/issue-workflow/model/useRebaseRecovery.ts`. Exports `RebaseRecovery` type and `useRebaseRecovery(issueNumber)` hook returning `{ trigger, isPending, isQueued, isRebasing, isConflictResolving, isConflictFailed, canRequest, hasConflicts, error, rebaseConflict, workspace: { data, isLoading, isChecking, hasAheadBehind, isUpstreamUnknown, isBehind, ahead, behind, branch, baseBranch } }`.
- Hook owns: workspace query read, conflict state read, `rebaseQueued` local state, mutation lifecycle (mutationFn, onSuccess, onSettled, query invalidation for `workspace-status` / `issues/{n}` / `issues`).
- Hook intentionally does NOT own `allowRebase` (BranchBar-only policy) or `baseBranchFallback` (page-level fallback). Each call site composes its own policy.
- BranchBar was refactored to compose the hook: the existing 9 BranchBar tests still pass byte-for-byte (same DOM, same data-testids, same `branch-bar-frame`/`branch-bar` chrome, same `Rebasing...`/`Rebase queued`/`Resolving conflicts...` labels, same conflict list rendering, same `Conflicting files:` + rebase error + conflict-failed banners).
- The hook's `canRequest` is `hasAheadBehind && !isPending && !isConflictResolving && !isQueued` — the BranchBar keeps `allowRebase` policy outside the hook by AND-ing `rebase.canRequest && allowRebase` for its own `canRequestRebase` (so BranchBar's gating matches the original logic exactly).

### Surface drift slot
- New prop on `RuntimeDecisionSurfaceProps`: `driftRecovery?: DriftRecoveryAction | null`. Exported type `DriftRecoveryAction` from `widgets/issue-workflow/index.ts` so the page can import it without reaching into the surface file directly.
- Surface renders the block (data-testid `runtime-drift-recovery`) immediately after `runtime-drift-note`, with the existing primary `runtime-actions` row sitting below it. Order in the section: rationale → next action → wait-reason → drift-note → drift-recovery → execution-signal → evidence → actions.
- Gate: `driftRecovery && decision.driftNote` — both must be present. The page decides when to pass the slot (only on `drift.decision === 'needs-attention'`); the surface stays agnostic about drift policy.
- The block carries: heading "Base drift needs attention", a `runtime-drift-recovery-action` button labeled `Rebase onto {baseBranch}` (or `Rebase queued`/`Rebasing...`/`Resolving conflicts...` per state), a small "Current branch <branch> onto <baseBranch>" hint, optional conflict-files line, optional conflict-failed banner, optional rebase-error banner. Button disabled when `!canRequest`.
- No new `RuntimeActionKind` is added — there is no `runtime-action-rebase` button. The drift-recovery action uses its own dedicated testid and lives inside its own block, not in the `runtime-actions` action set.

### Page wiring
- `useRebaseRecovery(issueNumber)` is called unconditionally before the `if (isLoading || !issue) return ...` early return (rules of hooks). When `issue` is null/undefined, the hook handles `workspaceStatusQueryOptions(... enabled: issueNumber > 0)` correctly — the workspace query is disabled but the mutation is still callable.
- `buildDriftRecoveryAction` in `pages/issue-detail/model/buildDriftRecoveryAction.ts` mirrors `buildExecutionSignal`: takes `drift`, `rebase`, optional `baseBranchFallback`; returns the action only when `drift?.drifted && drift.decision === 'needs-attention'`; otherwise returns null.
- `IssueDetailPage` passes `driftRecovery={driftRecovery}` to the surface, scoped to desktop (consistent with how the surface itself is gated by `!isNarrowViewport`). The narrow-viewport `MobileActionBar` still carries the primary action; the first-screen drift entry is a desktop-first workspace promotion (per the spec's narrow-viewport non-goal in T-002 / T-003 design).

### Test coverage
- Added 9 new `buildDriftRecoveryAction` unit tests in `pages/issue-detail/model/buildDriftRecoveryAction.test.ts` covering: null when no drift, null when `drifted: false`, null for `defer`/`suggest`/`enqueue` decisions, action shape when `needs-attention`, base-branch fallback forwarding, rebase-state forwarding (canRequest, isPending, isConflictFailed, hasConflicts).
- Added 10 new `RuntimeDecisionSurface` tests in `widgets/issue-workflow/ui/RuntimeDecisionSurface.test.tsx` under `describe('RuntimeDecisionSurface driftRecovery slot', ...)` covering: drift-recovery block renders inside surface when `decision.driftNote` is set; omitted when prop absent; omitted when `decision.driftNote` is null even with prop; coexists with primary action buttons (retry + stop remain available); button disabled when `canRequest: false`; queued label when `isQueued: true`; conflict info rendering inside the block; conflict-failed banner; in-progress label when `isPending`; click triggers the shared mutation; no `runtime-action-rebase` button added.
- Added 7 new `IssueDetailPage.drift-recovery` tests in `pages/issue-detail/ui/IssueDetailPage.drift-recovery.test.tsx` covering: in-surface block reachable from control region for needs-attention drift; reference-rail `IssueDriftCard` retained with full drift detail (base SHA, defer reason) — click-toggle expands the body so the existing detail is preserved; no in-surface block when not drifted; no in-surface block when drift decision is `defer`; clicking the in-surface action POSTs to the same `/issues/:number/rebase` endpoint BranchBar uses; existing blocked recovery actions (retry, stop) remain available alongside the new drift block; no `runtime-action-rebase` button added.
- All 9 existing BranchBar tests pass unchanged after hook extraction; all 4579 → 4605 web tests pass; all 1031 runner tests pass; typecheck clean.

### Decisions made
- The drift-recovery slot sits AFTER `runtime-drift-note` and BEFORE `runtime-execution-signal` so the drift explanation + action stay co-located, then the session/runner signal follows, then evidence, then actions. This makes the control region's read order: rationale → next action → wait → drift (with action) → session/runner → evidence → actions.
- The hook's `workspace.baseBranch` already falls back to `'master'`, so `buildDriftRecoveryAction`'s `baseBranchFallback` is rarely needed. Kept the param so the page can override when the issue's `repository.baseBranch` is known but the workspace query hasn't returned yet — the page already passes `baseBranchFallback: issue.repository?.baseBranch ?? null` to preserve the issue's repo branch.
- The drift-recovery block uses the same `runtime-drift-recovery-action` button for all rebase states (queued, rebasing, conflict-resolving, idle) so the surface stays one button, not four buttons with different labels. State changes the label and disabled, not the layout. This matches the design's "compact" framing.
- The shared hook does NOT know about `allowRebase` — that is BranchBar-specific policy (the full BranchBar gating logic from page-level `!isBacklog && !!issue.workflowRunId`). The hook is therefore usable by any future caller that wants the rebase action without the BranchBar policy.

### Gotchas
- `useWorkspaceStatus` (and therefore `useRebaseRecovery`) must be called BEFORE the `if (isLoading || !issue) return ...` early return in `IssueDetailPage`, otherwise the hook order would violate Rules of Hooks. The hook handles a not-yet-loaded issue correctly (workspace query is enabled only when issueNumber > 0 and projectId is set; mutation is always callable).
- When writing the page-level `buildDriftRecoveryAction` test that exercised `baseBranchFallback`, the test initially expected `baseBranchFallback` to override `workspace.baseBranch`, but the helper uses `workspace.baseBranch ?? baseBranchFallback ?? 'master'` — `workspace.baseBranch` (which already defaults to `'master'`) always wins. Adjusted the test to verify the realistic case (workspace exposes `'develop'`, helper forwards it) rather than the override path.
- The `reference-rail-drift` card is collapsed by default — `CollapsibleRailCard` has `defaultCollapsed: true` for the drift card. To assert "full drift detail retained" (base SHA, defer reason), the test must click the toggle to expand the card body, then verify the existing fields. Verifying just the collapsed summary (`reference-rail-drift-summary` shows `needs-attention`) plus the toggle-then-expanded body proves both: the rail card is still present AND its full content is still rendered.
- `deriveRuntimeDecision` returns `failed` summary when `recovery?.latestAttemptState === 'failed'`. So the drift+blocked test had to use `latestAttemptState: null` (or omit it) to land in the `blocked` summary — otherwise the surface produces `terminalActions(ctx, 'start')` (retry, resume, rerun, **start**, inspect) instead of `terminalActions(ctx, 'stop')` (retry, resume, rerun, **stop**, inspect). The acceptance criterion asks for "blocked recovery actions (retry/resume/rerun/stop)" so the test must pin the summary to `blocked`.


## T-004 (Control workspace composition, content demotion, and cross-cutting spec verification)

### Composition verification
- No production-code changes to `IssueDetailPage.tsx` were needed: the page composition already has every slot (`evidence`, `executionSignal`, `driftRecovery`) wired, the `runtime-decision-surface-frame` lives inside `status-header-tier`, and description/comments live in `reading-flow` below the surface. The pre-existing tier-weight anchors (`status-header-tier`, `reading-flow[data-tier-weight="reading-flow"]`, `reference-rail[data-tier-weight="reference-rail"]`), single-sticky `status-headline`, and `lg:col-span-2` / `lg:col-span-1` split all remain in place. The 3-slot integration is verified to compose into one coherent control workspace without leaking operational content above reading-flow.
- The `runtime-decision-surface-frame` slot wraps `RuntimeDecisionSurface` (with new `evidence`/`executionSignal`/`driftRecovery` props) and is gated by `!isNarrowViewport` for desktop control-workspace; narrow viewports continue to use `MobileActionBar` for the primary action, preserving prior scope per design D1.
- The 3 new anchors (`runtime-evidence`, `runtime-execution-signal`, `runtime-drift-recovery`) follow the existing `runtime-*` naming convention and live **inside** the surface, so they do not introduce duplicates in any tier.

### New cross-cutting spec test file
- Added `IssueDetailPage.control-workspace.test.tsx` (22 tests across 12 describes) as the integration verification point. It exercises the full control workspace contract across every conditional path, the invariants, the lifecycle-action preservation, and the no-new-actions invariant.
- Coverage map:
  - **running** — identity, stage, progress, primary action (Stop), no fabrication of evidence/signal/drift slots, full LatestArtifactsPanel still in reading-flow with `latest-artifacts-list` data-testid preserved.
  - **approval-required** — awaiting stage in surface, Approve and Send-back as adjacent buttons inside the same decision context, primary carries default-variant emphasis. Send-back form opens `runtime-send-back-form` inside the surface (no navigation away).
  - **blocked** — recovery actions (retry/resume/rerun/stop) inside surface, `runtime-next-action`/`runtime-rationale` present, primary action is one of the valid recovery kinds.
  - **interrupted health** — surfaced as `summary: blocked` in the headline, `runtime-rationale` cites "interrupted", no standalone `workflow-interrupted-card` introduced.
  - **drift (needs-attention)** — `runtime-drift-recovery` inside surface AND `reference-rail-drift` retained with full collapse behavior; **defer** drift — no in-surface block (non-blocking decision does not promote).
  - **failed** — `summary: failed` AND `runtime-evidence` slot present, multiple recovery actions inside surface, all anchored in status-header-tier.
  - **queued/backlog** — identity + queued summary, NO fabricated stage/progress figure, NO primary `runtime-action-...` block (only the disabled `runtime-action-start` is offered for a draft backlog), draft pill in identity row, runner-gating signal surfaces (`runtime-execution-signal-runner` with `data-gating-kind="capacity-full"`) and matches the wait reason.
  - **done (non-archived)** — terminal Done summary, no start/stop/approve/retry/resume/rerun offered.
  - **archived done** — `archived-banner` and `archived-pill` in status-header-tier, terminal state, no active controls, `back-to-archived` instead of `back-to-board`, description/comments still readable in reading-flow.
  - **secondary-content demotion** — description, comments, `reference-rail-configuration`, `reference-rail-prerequisites`, and `reference-rail-details` (model) all anchored outside `status-header-tier`; description and comments appear AFTER the surface in document order.
  - **lifecycle preservation** — across 6 conditional scenarios the only `runtime-action-*` testids produced are the existing set (`approve`, `send-back`, `retry`, `resume`, `rerun`, `stop`, `start`, `inspect`); no `runtime-action-rebase`/`close`/`archive`/`mark-ready` ever leaks into the surface. `rebase` flows through `runtime-drift-recovery-action`, NOT through the `runtime-actions` row. Close/archive/mark-ready remain in the reference-rail `reference-rail-actions` card untouched.
  - **stop confirmation** — clicking Stop shows `runtime-stop-confirmation-copy` inside the surface (no drawer/navigation) and the stop button remains mounted.

### Cross-cutting invariants asserted
- `assertControlWorkspaceBasics()` helper is the single integration entry point that the test suite uses to verify, on every conditional path:
  - Exactly 3 `data-tier-weight` containers (`status-header > reading-flow > reference-rail`).
  - Single `data-sticky="true"` element (`status-headline`).
  - `grid` uses `lg:grid-cols-3`, `reading-flow` uses `lg:col-span-2`, `reference-rail` uses `lg:col-span-1`.
  - All `status-header-tier` blocks (headline, identity, runtime-decision-surface) live inside the header tier, not in reading-flow or rail.
  - Reading-flow blocks (workflow-view, description, comments, diff-files, commits) live inside reading-flow, not header/rail.
  - Reference-rail blocks (details, workflow-profile, configuration, actions, prerequisites) live inside rail, not header/reading-flow.
- `everyActionAnchorInHeaderTier()` ensures every `runtime-action-*` button is in `status-header-tier` (not reading-flow or rail).
- `expectNoNewActionKinds()` walks all `[data-testid^="runtime-action-"]` and asserts each is in the known set; specifically asserts `runtime-action-rebase` does not exist (rebase flows through `runtime-drift-recovery-action` per design D4).
- `noExtraOperationalNodesLeakAboveReadingFlow()` ensures description and comments never appear inside the header tier.

### Test results
- All 4627 web tests + 1031 runner tests pass.
- TypeScript clean (no `tsc --noEmit` errors in packages/web or packages/runner).
- 22 new IssueDetailPage.control-workspace.test.tsx tests pass; 278 prior issue-detail tests remain green (no updates needed — the new slots sit inside the surface and do not affect document-order or cross-tier invariants the per-feature tests already assert).

### Decisions made
- Treated `inspect` (the disabled "View transcript" placeholder) as part of the existing action set in the runtime-action universe; did NOT exclude it from "no new actions" verification. `inspect` is part of the existing `RuntimeActionKind` set in `derive-runtime-decision.ts:30` and is unchanged.
- Created one focused test file for the control workspace contract rather than scattering more tests across the existing `cross-tier.test.tsx`. Existing `cross-tier.test.tsx` covers tier-weight invariants; the new file layers on the slot-specific, lifecycle-preservation, and no-new-actions invariants without duplication.
- The test file uses local helpers (`assertControlWorkspaceBasics`, `everyActionAnchorInHeaderTier`, `expectNoNewActionKinds`, `noExtraOperationalNodesLeakAboveReadingFlow`) shared across every describe. This keeps each scenario test compact and prevents drift between describe blocks on what "correct composition" means.

### Gotchas
- `mockArtifacts` items must use `artifactId`, `workflowRunId`, `taskRunId`, `path`, `kind`, `recordedAt` (the `WorkflowArtifact` shape). Passing items with the wrong shape caused a "Each child in a list should have a unique key" warning because `ArtifactOpener` uses `key={artifact.artifactId}`. Discovered this when the first run of the approval-required test failed on the artifacts list. Fixed by using the same shape that `IssueDetailPage.decision-adjacent-evidence.test.tsx` uses (`makePlanArtifact`/`makeReviewArtifact`).
- `runtime-action-inspect` is ALWAYS rendered (disabled, "View transcript") — driven by `presentation.inspect` in `runtime-presentations.ts:74` which is built regardless of allowed actions. So "no inspection" assertions must accept its disabled presence; "no inspection offered" is implicitly a no-op across all paths. The "no-new-actions" assertion correctly excludes `inspect` from the unknown set but does NOT exclude it from the rendered set.
- `ArtifactOpener` returns null in compact mode when there are no artifacts (line 84), so an empty mock (or missing workflowRunId) means `runtime-evidence` is absent — the in-surface artifact slot is correctly absent for the running summary test where artifacts exist but the summary is `running` (the gate is on `summary` not on artifact presence).
