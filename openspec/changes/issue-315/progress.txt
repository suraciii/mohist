## Codebase Patterns

### LiveTaskProvider test harness
- `packages/web/src/app/providers/LiveTaskProvider.test.ts` has a `vi.hoisted` `mocks` block that mocks `useEventsConnection`, `useAgentStatus`, and `sonner.toast.{info,error,success}`.
- The `mountWith(queryClient)` helper renders `LiveTaskProvider` under `QueryClientProvider` + `ProjectProvider` and pulls back the captured `handleEvent` callback from `mocks.useEventsConnection.mock.calls[0][1]`.
- Calling `handleEvent(eventName, data)` from the test drives the in-file `handleEvent` switch exactly as `useEventsConnection` would on the SignalR bus.
- `beforeEach` calls `vi.clearAllMocks()` so every test gets a fresh spy baseline.

### Re-observing `LiveTaskContext` state in tests
- `useLiveTask` (from `entities/issue/model/live-task`) reads the context that `LiveTaskProvider` populates.
- To capture `rebaseConflict` (and the rest of the state) from a test, mount a tiny child consumer under `LiveTaskProvider` that writes the state object to a mutable ref on every render. Read the ref's latest value after `act()` to assert on the post-event state.
- Pattern used in new D2 tests:
  ```ts
  const stateProbe = { current: null as { rebaseConflict: unknown } | null }
  function StateProbe() { stateProbe.current = useLiveTask(); return null }
  // pass `stateProbe` into `mountWith` and let it render <StateProbe/> as the child
  ```

### Seeding the issue cache for `findIssueNumber`
- `findIssueNumber` reads `queryClient.getQueriesData<Issue[]>({ queryKey: ['issues'] })` and matches on `Issue.id` to return the issue's `.number`.
- Tests can seed via `queryClient.setQueryData(['issues'], [makeIssue(id, number)])` before driving `handleEvent`.
- A minimal `Issue` for seeding requires the required scalar fields (`id`, `number`, `title`, `status`, `health`, `projectId`, `labels`, `createdAt`, `updatedAt`, `isDraft`, `canStart`, `blocker`); use `IssueStatus.Backlog` / `IssueHealth.Active` for status/health since these are string-valued enums.

### `dispatchRebaseEvent` / `onRebaseEvent` in tests
- `onRebaseEvent(handler)` (from `entities/issue/model/rebase-events`) is the public subscribe entry — it returns an unsubscribe function.
- Tests can subscribe, push events into a captured array, and assert on the captured sequence; remember to call the off function at end-of-test.

## Task T-001 outcomes

- Added 10 new characterization tests in `LiveTaskProvider.test.ts` covering the currently-untested branches of:
  - `handleReverseDnsIntegrationOutcome` — rebase-completed (clears conflict via state probe, dispatches `rebase_completed`, invalidates `['issues']`), merge-success (toast.success, invalidates `['issues']`), rebase-conflict (WorkflowRunFailed + StageFailed arms — sets state via probe, dispatches `rebase_conflict`, toast.error, invalidates `['issues']`), merge-failure (toast.error, invalidates `['issues']`, no rebase dispatch), no-match fallthrough (returns false — no rebase dispatch, no toast, default switch-arm invalidations fire).
  - `notifyRunLifecycleToast` — pause arm (toast.info `'Issue #N needs approval'`), error arm (toast.error `'Issue #N encountered an error'`), suppression when the event's issue is currently viewed, suppression when `findIssueNumber` returns null.
- All 33 tests in `LiveTaskProvider.test.ts` green; full web suite (240 files / 3727 tests) green; `npm run typecheck -w packages/web` clean.
- NO production code change — tests pin the CURRENT in-file implementation per design D2.

## Gotchas for downstream tasks

- The `IssueWorkCompleted + non-rebase non-merge` fallthrough test must use `IssueWorkCompleted` (not `WorkflowRunFailed`) as the carrier event because the workflow-run switch arm also fires `notifyRunLifecycleToast` for paused/failed events, which would taint the "no toast was fired by the outcome handler" assertion. `IssueWorkCompleted` has no toast side-effects outside the outcome handler itself.
- When the seeding step (`WorkflowRunFailed + rebase_conflict`) is needed to populate `rebaseConflict` before a `rebase_completed` clearing test, the seeding step also fires `toast.error('Rebase conflict on Issue #N')`. The test resets `mocks.toastInfo/Error/Success` via `mockClear()` between seed and the assertion step to keep the assertion focused on the post-seed handler's behavior.
- The check `expect(invalidateSpy).toHaveBeenCalledWith({ queryKey: ['issues', 'detail', issueId] })` only fires in the issue-arm when the outcome handler returns false; the rebase-completed arm returns true and breaks out of the issue-arm without firing the detail invalidation. Pin this.
- The rebase-failed toast outcome (`'Rebase conflict on Issue #21'` etc.) is `toast.error`; the merge-failed outcome (`'Merge failed for Issue #21'`) is also `toast.error` but with different copy. Both must be tested separately to keep the strings distinct.

## Task T-002 outcomes

- Pure envelope/normalization helpers extracted to `packages/web/src/app/providers/model/event-envelope.ts` (178 lines, no React/TanStack/sonner imports — only `entities/agent` for `AGENT_DETAIL_EVENTS` / `AgentDetailEventMap` types):
  - `unwrapEnvelope`, `readEnvelopeField`, `asRecord`, `normalizeToolState`, `normalizeTranscriptDetail`, `unwrapTranscriptEnvelope`, `routeTranscriptEventName`, `isAgentDetailEvent`.
- Pure timeline helpers extracted to `packages/web/src/app/providers/model/timeline-live-event.ts` (37 lines):
  - `readIssueNumber`, `readTimelineEventId`, `readTimelineTime`, `buildTimelineLiveEvent`.
- `LiveTaskProvider.tsx` slimmed 601 → 394 lines. Imports the helpers and re-exports the same `__testing__` key set; `LiveTaskProvider.test.ts` still imports `./LiveTaskProvider` byte-identical.
- All 33 LiveTaskProvider tests green; full web suite (240 files / 3727 tests + 1 skipped) green; `npm run typecheck -w packages/web` clean.

## Gotchas for downstream tasks

- Module path from `app/providers/model/*.ts` to `entities/*` is `../../../entities/*` (three levels up to `src/`), NOT `../../entities/*` — the latter resolves to `app/entities/*` which does not exist. Easy off-by-one when extracting helpers out of `LiveTaskProvider.tsx` (which itself sits at `app/providers/` and uses `../../`).
- `AgentDetailEventMap` (type-only) is still needed in `LiveTaskProvider.tsx` because the `dispatchAgentEvent` calls cast through `AgentDetailEventMap[typeof eventName]` / `AgentDetailEventMap[typeof routedName]`. `AGENT_DETAIL_EVENTS` is NOT needed there anymore — only in `event-envelope.ts` where `isAgentDetailEvent` lives.
- `TimelineLiveEvent` type import is no longer needed in `LiveTaskProvider.tsx` — the function that uses it (`buildTimelineLiveEvent`) is fully encapsulated in `timeline-live-event.ts`.
- `readIssueNumber` is consumed by `handleReverseDnsIntegrationOutcome` (still in `LiveTaskProvider.tsx`) — `readIssueNumber` must be exported and imported back so the body keeps working unchanged. T-003 will move the consumer to `reverse-dns-outcome.ts` and the import will move with it.

## Task T-003 outcomes
- Decoupled `handleReverseDnsIntegrationOutcome` (plus `readOutcome` / `isRebasePayload` / `isMergePayload`) into `packages/web/src/app/providers/model/reverse-dns-outcome.ts` (133 lines, zero runtime imports — only types `RebaseConflictState` / `RebaseEvent`, the `REVERSE_DNS_EVENT_TYPES` constant, and the pure `readIssueNumber`).
- New declarative result type: `ReverseDnsOutcome = { handled: false } | { handled: true, invalidations: ReverseDnsInvalidationKey[], rebaseConflict?: RebaseConflictState | null, rebaseEvent?: ReverseDnsRebaseEvent, toast?: ReverseDnsToast }`. `null` `rebaseConflict` clears; `undefined` means "no change".
- Picked a custom `ReverseDnsInvalidationKey = readonly string[]` (NOT `QueryKey` from `@tanstack/react-query`) so the decider doesn't transitively pull TanStack types — keeps it pure-of-TanStack, not just pure-of-Runtime.
- New `decideReverseDnsOutcome(eventName, parsed)` is a pure function of `(eventName, parsed)` — no `queryClient`, no `sonner.toast`, no React setter. Mirrors the original logic exactly.
- `LiveTaskProvider.tsx` slimmed further: replaced the inline `handleReverseDnsIntegrationOutcome` (~45 lines) with a 23-line `applyReverseDnsOutcome` helper that consumes the declarative result. The helper is the sole side-effect point, sitting next to the closure-fixing call sites. Canonical application order: `invalidate → setRebaseConflict → dispatchRebaseEvent → toast` (was non-uniform across arms).
- `readIssueNumber` import removed from `LiveTaskProvider.tsx` entirely (now exclusively used inside `model/reverse-dns-outcome.ts`).
- New `reverse-dns-outcome.test.ts` (26 unit tests, no provider mount) exercises every arm + fallthrough + helper directly. Pins behavior at the decider layer so future readers get coverage without a provider mount.
- T-001's 10 characterization tests pass unchanged against the refactored LiveTaskProvider.tsx (33 tests → still 33 — no test churn required). Full web suite now: 241 files / 3753 tests passing (+1 skipped). `npm run typecheck -w packages/web` clean.

## Gotchas for downstream tasks (post T-003)
- The `ReverseDnsInvalidationKey` is a `readonly string[]`, not TanStack's `QueryKey` (`readonly unknown[]`). When the caller applies it, it casts to `unknown[]` to satisfy `invalidateQueries` — see `applyReverseDnsOutcome` line 154. Keep `ReverseDnsInvalidationKey` narrow-by-intent (string segments only); broadening to `readonly unknown[]` would force the decider to import a type from a runtime dep.
- The apply function holds the canonical-order invariant — **never reorder** the four lines; rebase-conflict arm in particular relied on `setRebaseConflict` followed by `dispatchRebaseEvent` followed by `toast.error` followed by `invalidate`. Re-running the existing T-001 outcome tests is the regression check that pins this; if any of them flip behavior, the canonical order is broken.
- `outcome.rebaseConflict !== undefined` is the null-clear gate. Setting to `null` is the legacy "clear" semantics; `undefined` is "leave state unchanged" (used by merge-success / merge-failure arms). Do not change this without re-running the rebase-completed-clears-conflict assertion.
- When T-005 collapses the switch into a ROUTE table, the three `applyReverseDnsOutcome(decideReverseDnsOutcome(...), ...)` call sites each gate the switch-arm default invalidations (else the switch arm falls through to `['issues']`+detail). That gate must remain in the per-domain handler — the decider returning `handled: false` is the signal "this event is a plain issue event, run the default".
