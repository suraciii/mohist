{
  "project": "mohist-local",
  "description": "Deliver the Workflow-direct mohist/pi path end to end: pin and verify the bundled Pi SDK, implement the in-process PiRuntime, make Workflow AgentSession coordination and binding runtime-aware without breaking OpenCode, durably report Session facts, and expose Pi-backed Session milestones without adding AgentJob, Session-command, or model-catalog UI scope.",
  "tasks": [
    {
      "id": "T-001",
      "title": "Pin Pi SDK and verify the runtime/toolchain contract",
      "spec": "specs/pi-runtime/spec.md#pi-is-a-pinned-in-process-runner-capability",
      "description": "Pin @earendil-works/pi-coding-agent to an exact Runner dependency, raise every documented and enforced Node floor to 22.19, and make local version managers, source-install docs, CI, and Docker select a compatible concrete release. Run the real-SDK smoke before any product implementation, record the verified service, Session, Prompt, event, trust, model, and interruption surfaces, and reconcile upstream drift into the canonical runtime call map.",
      "acceptanceCriteria": [
        "packages/runner/package.json pins @earendil-works/pi-coding-agent to an exact version, package-lock.json is updated through the Runner workspace npm command, and no ACP/RPC adapter dependency is added",
        "The root and Runner package manifests require Node >=22.19; .nvmrc pins a concrete compatible release; CONTRIBUTING.md and docs/self-host.md advertise >=22.19; every setup-node workflow including ci.yml and web-test-shuffle.yml plus the Docker builder install an explicit compatible release rather than an unspecified Node 22 minor",
        "openspec/changes/issue-450/sdk-smoke-verification.json records only pinned package/Node versions, operation names, pass/fail booleans, and sanitized field-name/type summaries for ModelRuntime/catalog, project-untrusted resources, SessionManager create/open, sessionFile identity, createAgentSession, literal Prompt completion, messages/events, model/thinking selection, steer, abort, and stop confirmation",
        "The smoke proves repository-local .pi settings/extensions/packages/skills/prompts are not loaded while global Pi configuration and AGENTS.md/CLAUDE.md context remain available",
        "The smoke identifies stable message/tool/usage/retry identities, the final Session message surfaces usable for restart reconciliation, and the option that disables prompt-template expansion without recording environment/auth values, provider responses, Prompt/message bodies, or raw SDK objects",
        "A secret scan of the smoke artifact and staged change passes before commit; a sentinel credential used by the smoke wrapper is absent from the artifact even on a failed operation",
        "Any SDK drift found by the smoke is reflected in design/runtimes/pi.md and openspec/changes/issue-450/design.md before T-002 starts",
        "A repository-wide search accounts for every active Node version declaration and finds no manifest, lockfile, version-manager file, source-install document, workflow, or container path that can select or advertise a release below 22.19",
        "A clean npm ci succeeds on Node >=22.19, and npm run typecheck -w packages/runner passes with the pinned dependency"
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "CONFIG",
      "output": "Pinned Pi SDK and Node toolchain updates in package manifests, lockfile, .nvmrc, CONTRIBUTING.md, docs/self-host.md, every CI workflow, and Docker, plus sdk-smoke-verification.json and any required call-map reconciliation",
      "dependsOn": [],
      "passes": false,
      "notes": "Plan-wide hard gate before every implementation task. The smoke uses real Pi and may require configured provider credentials; if the environment cannot verify the required surface, record the blocker and do not start T-002 through T-007 from assumptions. Do not add product code, AgentJob routing, Session commands, or Pi catalog reporting here."
    },
    {
      "id": "T-002",
      "title": "Implement the in-process PiRuntime deep module",
      "spec": "specs/pi-runtime/spec.md",
      "description": "Create packages/runner/src/runtime/pi as a complete deep module with Mohist-owned boundary types, injectable SDK services, project-untrusted resource setup, credential confinement/redaction, readiness/catalog state, physical Session create/open/cache/dispose, literal Prompt execution, per-turn model/thinking selection, event projection/reconciliation, deadline/cancellation/provider interruption, execution quarantine, provider policy, and normalized errors. Parse Runner provider policy once and supply the same pure object to OpenCode and Pi without introducing a generic AgentRuntime interface.",
      "acceptanceCriteria": [
        "All @earendil-works/pi SDK imports are confined to packages/runner/src/runtime/pi, and callers/tests consume only Mohist-owned Pi request/result/diagnostic types",
        "start()/ready()/diagnostic() become ready only after SDK services initialize and getAvailable completes; an empty catalog is ready with a warning, while initialization/catalog failure stays not-ready with an actionable diagnostic and retryable start path",
        "cli.ts parses MOHIST_PROVIDER_RETRY_THRESHOLD as a positive integer defaulting to 5 and MOHIST_PROVIDER_NON_RECOVERABLE_TERMS as a JSON array of non-empty literal strings; invalid values block Pi readiness and operator text is escaped rather than evaluated as regular expressions",
        "Runner startup configuration carries one parsed provider policy object; OpenCode consumes that object and PiRuntime accepts the same object, with tests proving non-default threshold/literal terms reach both runtime boundaries and preserve existing OpenCode defaults",
        "The SDK factory fixes project trust to false, gives global authentication only to Pi's auth manager, loads repository instruction files, and supplies no repository-local .pi execution resources; Mohist-owned boundary types contain no credential fields or raw SDK objects",
        "createSession returns the normalized absolute session-file path and rejects a missing path as incompatible-runtime; restoring uses SessionManager.open and a missing/corrupt file returns missing-session without implicit creation",
        "runTurn applies supplied model and thinking level on the existing Session, preserves the complete model remainder after the first slash, submits slash-prefixed Prompts literally, awaits prompt() as the only completion authority, reads final assistant text from messages, and never issues a second wait or replays an uncertain Prompt",
        "Runtime tests prove omitted model does not call setModel, omitted variant does not call setThinkingLevel, each independently preserves a restored Session's current selection, and a new Session with either or both omitted retains Pi defaults",
        "The event projector maps assistant/reasoning/tool/model/usage facts with separate input/output/cache-read/cache-write/thought dimensions, persists smoke-verified fingerprints through caller-owned state, reconciles final messages, and treats unknown events as diagnostics only",
        "Fake-timer tests cover a caller-declared duration converted to an absolute deadline from the injected clock immediately before SDK Prompt, one wrap-up warning, result fixation, external cancellation, late completion, confirmed abort, and no replay; Workflow's fixed duration/context wiring is owned by T-006",
        "An unconfirmed timeout, cancellation, or provider-triggered abort creates a generation-scoped physical quarantine and synchronously publishes ordered entered before returning; observed stop publishes matching cleared, stale-generation clear cannot remove a newer quarantine, other paths remain available, and Runner restart starts with no in-process execution quarantine",
        "Provider-policy tests cover first-event quota/credit/billing failure, recoverable retries below the configured threshold, threshold-N failure, Pi-native terminal error normalization, confirmed and unconfirmed provider abort, logical/physical quarantine signal, and binding preservation",
        "Pi-boundary credential tests inject sentinel environment/auth/provider values and prove they are absent from PiRuntime requests/results/diagnostics and task-log output while surfaced text contains the existing CredentialMasker placeholder; downstream registration/event/outbox/Action sinks are verified when those integrations exist in T-006",
        "Runtime tests use fake SDK services, virtual paths, and fake timers only; npm run typecheck -w packages/runner and npm test -w packages/runner pass"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/runtime/pi deep module, validated shared provider-policy configuration, and deterministic fake-SDK runtime tests",
      "dependsOn": [
        "T-001"
      ],
      "passes": false,
      "notes": "Implements design D1, the Pi-runtime half of D3, D7, and the projector half of D8. It does not register mohist/pi, alter Workflow Session routes, implement AgentJob Pi execution, or add Session commands."
    },
    {
      "id": "T-003",
      "title": "Make Workflow Session coordination and binding runtime-aware",
      "spec": "specs/pi-workflow-session/spec.md#the-physical-pi-binding-is-persisted-before-the-first-prompt, specs/pi-workflow-session/spec.md#current-pi-bindings-are-reused-without-model-driven-rotation, specs/pi-workflow-session/spec.md#invalid-persisted-bindings-fail-without-silent-replacement, specs/pi-workflow-session/spec.md#one-workflow-work-turn-executes-per-logical-agentsession, specs/pi-workflow-session/spec.md#pi-turn-facts-populate-the-existing-session-audit-record",
      "description": "Introduce WorkflowSessionTurnCoordinator and its already-held WorkflowSessionTurnLease token contract for complete Workflow task lifetimes; make Workflow open/bind and a new Action-owned event route runtime-aware; add AgentSession Action stream state plus atomic drained-stream rebind transitions; and implement only the narrow Server-side command capability admission/read needed to keep Pi commands unavailable. Define and fake-test the executor/Action lease boundary without wiring production WorkExecutor or Actions, which T-006 migrates atomically; leave command wire DTOs, Runner handler registration/execution, journals, and Runtime operations unchanged.",
      "acceptanceCriteria": [
        "WorkflowSessionTurnCoordinator serializes by projectId/WorkflowRunId/sessionName from before open through Action execution, successful completion checks, any worktree cleanup turn, and a caller-supplied final-report phase; acquisition returns an already-held WorkflowSessionTurnLease exposing only its immutable key plus generation-safe physical-execution registration/quarantine operations, removes settled task tails, and allows different keys to run independently without exposing queue, release/reacquire, Runtime, or Session-command APIs",
        "Fake executor/OpenCode/Pi adapters prove only the executor acquires/releases the coordinator lease, Actions receive the held token, concurrent OpenCode/OpenCode and OpenCode/Pi operations on one key cannot overlap, queued same-name work cannot intervene between a turn and cleanup, and different keys can; production WorkExecutor and Action migration is owned solely by T-006",
        "Workflow Session open requires runtime opencode or pi and returns observed runtime/binding/workDir/Runner plus current authority-issued actionStreamId, lastAppliedSequence, applied projector checkpoint, and latest Action-turn metadata; open and bind preserve existing active-state and expected-binding rejection as actionable session-binding-failed, and the existing OpenCode caller sends runtime opencode atomically with the Server contract change",
        "AgentSession persists nullable current Action stream state as stable JSON fields: first bind creates an opaque authority-issued ID at cursor 0 with empty projector checkpoint/no latest turn, repeated open/attach returns it unchanged, each accepted sequence atomically updates cursor/checkpoint/latest turn with facts, and open atomically backfills one empty stream state for a legacy OpenCode binding without changing binding or lineage",
        "Workflow bind carries target runtime, expected current runtime/session/actionStreamId, and for rebind the current owning Runner plus drainedThroughSequence; an unbound Session has no stream until first bind",
        "A guarded cross-runtime rebind in either direction preserves logical identity/workDir, appends one lineage entry, and succeeds only when expected binding/stream/Runner match and the current Server cursor equals drainedThroughSequence; it atomically installs a fresh authority-issued stream at cursor 0",
        "The successful rebind seals the old stream and installs the new binding in one AgentSession transition; mismatch, gap, stale expectation, another Runner's attestation, same-runtime different physical ID, or existing authoritative active state rejects without either mutation",
        "A dedicated Workflow Action runtime-event DTO/route carries route runnerId, authority-issued stream identity, physical binding, actionTurnId, sequence, latest-turn metadata, and projectorCheckpointAfter; the route passes runnerId into the grain, which rejects a non-owner before duplicate/gap handling or mutation and never treats stream identity as authorization; the existing AgentSessionRuntimeEventsRequest plus Workflow Follow-up/failure-outbox and generic routes remain wire-compatible and unsequenced",
        "The dedicated Action-event request requires caller-supplied actionTurnId/projector checkpoint and validates that session.input establishes latest-turn metadata for its input sequence while later facts preserve it; transcript turns gain nullable ActionTurnId with a filtered unique per-Session index; the Action-event transition acknowledges an accepted cursor + 1 only after one AgentSession state/event-store transaction commits the cursor, projectorCheckpointAfter, latest turn, usage/model/lifecycle state, domain events, and ordered idempotent PendingTranscriptEvidence keyed by actionStreamId/sequence/fact index; evidence gains stable JSON fields for actionTurnId and exact start-turn/append-part operations including runtime/prompt metadata or part/correlation/text/payload/timestamps, projection upserts the stable turn and parts in stream order, removes evidence only after successful projection plus guarded state commit, replays idempotently after transcript-save/evidence-removal failure, returns stored checkpoint/turn state for sequence <= cursor without reapplying or deleting pending evidence, and rejects gaps, sealed streams, stale physical bindings, or inconsistent turn IDs before mutation; aggregate commit failure returns no acknowledgement and forces activation reload, while existing Session-command events remain binding-validated outside the Action cursor; outbox derivation/checkpoint persistence is owned by T-004",
        "AgentUsageSummary, Session grain state/transition and surrogate, runtime-event parser, AgentUsageDto/read models, activity projections, and DTO mappers add nullable CachedWriteTokens/cachedWriteTokens with new append-only serializer IDs and no renumbering of existing fields",
        "The authoritative workDir remains immutable and mismatch rejects before binding mutation; Workflow bind accepts opencode or pi through a Workflow-specific validator while the command-capable runtime predicate remains OpenCode-only; BeginFollowup and Compact/Reset preparation check it atomically before creating a lease/reservation and Reset's unregistered-runtime OpenCode fallback is removed; Cancel uses a non-mutating grain admission read that rejects an observed Pi binding or returns one immutable OpenCode target which may finish after a later rebind; generic AgentJob DTO/routes remain unchanged",
        "Server and Runner coverage includes unchanged OpenCode Session-command and Workflow Follow-up/failure-outbox regression paths, Action-versus-command DTO routing, first/idempotent bind, concurrent legacy bootstrap, zero-event rebind, symmetric lineage, expected-binding/stream/owner races, non-owning Runner rejection even with current stream/binding IDs, atomic seal/fresh-stream failure and success, crash immediately after Action acknowledgement, complete evidence old-state deserialization, transcript failure after cursor commit across two successive Action turns, pending-evidence retry after reactivation, crash after transcript save before evidence-removal commit, stable-turn/part idempotency, duplicate delivery while evidence is pending, duplicate/gap/sealed/stale Action events, separate cache-read/cache-write accumulation, workspace mismatch, Pi registration, and coordinator cleanup concurrency",
        "A Server API/read-model spec applies different cachedReadTokens and cachedWriteTokens values, reads the existing Session JSON surface, and asserts both nullable fields survive DTO mapping with their distinct values",
        "Race tests use a fake caller to supply drainedThroughSequence and prove the coordinator excludes concurrent same-key Workflow work through the atomic bind; production outbox drain/fencing is owned by T-004. They also cover concurrent OpenCode/Pi runtime choices, rejection of queued Workflow work during the gap between original and cleanup Prompts, Pi Follow-up/Compact/Reset capability rejection before reservation, Reset fallback removal after reactivation, and controlled Cancel barriers proving an observed Pi binding is not dispatched while an admitted OpenCode Cancel may finish its immutable old target after rebind; no Workflow Prompts overlap",
        "Coordinator quarantine tests register logical-to-physical tracking before Runtime execution, apply entered before lease release, clear only the matching generation after observed stop, ignore stale clear, unregister normal turns, and start empty when reconstructed after Runner restart",
        "Coordinator identity tests prove identical session names remain isolated across different projects and WorkflowRuns as well as different names, while Session Action events including final/end facts never mutate TaskRun status or advance Workflow",
        "npm run typecheck -w packages/runner, npm test -w packages/runner, and npm test pass with deterministic fakes only"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Shared Workflow Session coordinator/held-lease token contract, guarded runtime binding, and idempotent durable Server event cursor/seal, with production executor/Action migration deferred to T-006",
      "dependsOn": [
        "T-001"
      ],
      "passes": false,
      "notes": "Implementation is SDK-independent but deliberately ordered after T-001 because the smoke is the plan-wide pre-code gate and may reconcile shared design contracts. Deliver this as an OpenCode-preserving Workflow Session infrastructure change. Split the new Action event route instead of changing existing Session-command/generic DTOs. Do not add command coordination, command Runtime methods, command journal behavior, or Pi command routing, and do not let one Runner attest another Runner's local Action stream."
    },
    {
      "id": "T-004",
      "title": "Implement the durable Workflow Session event outbox",
      "spec": "specs/pi-workflow-session/spec.md#pi-turn-facts-populate-the-existing-session-audit-record",
      "description": "Implement WorkflowSessionEventOutbox as the runtime-neutral owner of Workflow Action stream manifests, encoding, fingerprints, sequencing, acknowledgement compaction, reporting quarantine, and restart repair over injected byte storage. Add a separately testable atomic-replace protocol over low-level file operations, the thin production adapter, standalone initialization/readiness and drain services, injected Pi reconciliation hooks, and fenced prepareRebind using T-003's Action cursor/seal protocol. Do not compose RunnerHost or production Actions in this task.",
      "acceptanceCriteria": [
        "WorkflowSessionOutboxStorage exposes list/read/replaceAtomically/remove byte operations; AtomicSnapshotReplaceProtocol owns write -> file sync -> same-directory rename -> directory sync -> cleanup ordering over injected low-level operations, while the production adapter only supplies Node filesystem operations",
        "The real outbox codec/state machine runs over injected storage and keys one rebuildable crash-buffer manifest by logical AgentSession plus authority-issued actionStreamId; Workflow open's cursor/projector checkpoint/latest-turn metadata are applied authority, the Runner never derives stream identity, and tests use in-memory bytes without host filesystem access",
        "The outbox exposes one runtime-neutral binding-stream API for session.input plus required pre-Workflow-turn and pre-rebind drain/fencing; fake OpenCode and Pi binding contract tests prove the same API against T-003's authority-issued stream state without modifying Session-command handlers",
        "Before Prompt admission the outbox atomically creates or refreshes a manifest at any returned cursor after Runtime-specific reconciliation, containing authority-issued stream identity, physical binding/repair target, next sequence cursor + 1, Server-applied projector checkpoint/latest turn, and an active-turn checkpoint with runtime, requiredFactsPolicy (pi-canonical or opencode-input-only), inputSequence, and deterministic actionTurnId; session.input carries that turn ID plus projectorCheckpointAfter, is Server-acknowledged, and the checkpoint is durably marked admitted before the Runtime call; every later fact carries the same turn ID and updated checkpoint",
        "If local manifest state is missing, corrupt, stale, or gapped at any Server cursor, preserve corrupt bytes for diagnostics and rebuild from Server stream state; Pi opens the bound file and reconciles retained canonical facts against the Server checkpoint, OpenCode restores input-only latest-turn state without Runtime projection, and admission/rebind resumes only after required reconciliation/drain; original Runner workspace restoration is never required",
        "Each required canonical Pi audit fact and updated projector fingerprint is one atomic snapshot replacement; final checkpoint closure occurs only after final assistant/reasoning, completed tool/result, model, and input/output/cache-read/cache-write/thought usage facts supplied by Pi are durable, and delivery occurs in monotonic sequence order",
        "Ambiguous acknowledgement retries the same sequence, duplicate acknowledgement compacts once, and a Server gap retains/resends the missing prefix; post-turn transport failure preserves the completed Action result when records are durable and background/startup drain retries without Prompt replay",
        "prepareTurn and prepareRebind hold the stream fence and drain every locally issued sequence before Runtime entry; rebind supplies expected identity plus final sequence to T-003's atomic replacement, and failure admits no Workflow Runtime operation",
        "A post-admission local append failure fixes session-reporting-failed, interrupts an active Pi turn, quarantines that physical Session from later Prompt/rebind admission, retains the fact in memory, and does not replay the Prompt",
        "Startup rebuilds missing/corrupt local manifests from Workflow open's Server cursor/checkpoint/latest-turn state; closes prepared-but-not-admitted checkpoints without Runtime replay; for admitted Pi checkpoints opens the persisted Session through an injected repair hook, reconciles retained required facts against the Server checkpoint, and clears reporting quarantine only after drain; for admitted OpenCode input-only checkpoints restores/drains durable input then atomically closes as execution-outcome-unknown before host registration/polling without invoking/querying either Runtime, after which later same-Session admission and OpenCode-to-Pi rebind are allowed; repair itself never submits a Prompt, while a separately redelivered Workflow work item may execute a duplicate turn under the canonical crash-window limitation; an absent/unreadable Pi file records uncertain submission and leaves runtime-session-missing with Reset guidance without replacement or Runtime/outbox replay",
        "A corrupt committed manifest is preserved and quarantines only its logical Session while unrelated streams remain drainable; root/list/atomic-capability initialization failure makes the standalone outbox readiness state not-ready with a stable workflow-session-outbox-unavailable diagnostic naming the failed operation/capability and an actionable masked message, and successful re-initialization clears the diagnostic; Runner registration/claim gating is owned by T-006",
        "Atomic protocol tests over fake operations cover every write/sync/rename/directory-sync failure and classify rename-success/directory-sync-failure as commit-uncertain; a sentinel in a storage error is masked from readiness diagnostics, recovery clears that diagnostic, and fixed virtual paths assert same-directory rename and supported filesystem assumptions without instantiating the physical adapter",
        "Codec/state-machine tests cover cursor-0 bootstrap producing first sequence 1, deterministic actionTurnId from input sequence retained by every fact/restart checkpoint, missing/corrupt manifest rebuild at nonzero cursor from Server projector/latest-turn state, surviving contiguous local pending suffix compaction/resume, gapped suffix discard/reconciliation, first Pi bind, pending-fact drains before later Workflow turns, zero-event and non-empty OpenCode -> Pi -> OpenCode stream drain/rebind, Server/local identity or cursor disagreement, atomic old-or-new generations, append failure before/after admission, global storage readiness failure, duplicate/gap recovery, crash after OpenCode admission before closure, startup input drain/execution-outcome-unknown close before polling, later same-Session admission and OpenCode-to-Pi rebind, Pi restart reconciliation/drain recovering required but not provisional facts, zero Runtime Prompt calls during either repair path, pre-admission/pre-rebind failure, all usage dimensions, and no Runtime/outbox replay",
        "npm run typecheck -w packages/runner, npm test -w packages/runner, and npm test pass"
      ],
      "priority": 3,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Crash-consistent Workflow Session outbox, injected byte storage and production file adapter, standalone readiness/drain/repair services, reporting quarantine, and deterministic recovery tests",
      "dependsOn": [
        "T-002",
        "T-003"
      ],
      "passes": false,
      "notes": "Implements D8's transport and local-persistence guarantees before the Pi Action consumes them. The outbox owns its format and recovery policy; the physical adapter owns only byte persistence. Never turn event-delivery or append failure into automatic Prompt replay."
    },
    {
      "id": "T-005",
      "title": "Recognize Pi as a Workflow Inline Agent Action",
      "spec": "specs/pi-workflow-action/spec.md#mohistpi-is-a-workflow-inline-agent-action",
      "description": "Update Server Workflow task validation, classification, translation, serialization, and parent-context selection to use the architecture-specified explicit Inline Agent Action set containing mohist/opencode and mohist/pi. Reject mohist/pi in check definitions because #450 provides only task-lifecycle lease ownership. Keep the change at the dispatch/presentation boundary so runtime implementation, AgentJob routing, and Session commands remain untouched.",
      "acceptanceCriteria": [
        "Server static validation accepts uses=mohist/pi with prompt/session/options and rejects legacy with.agent, with.kind, and with.type before dispatch with an Action-specific message",
        "Server check validation rejects uses=mohist/pi before dispatch with an actionable task-only diagnostic, while existing non-Pi checks and mohist/opencode check behavior remain unchanged",
        "TaskRun.DeriveClassification, WorkflowItemTranslator, and WorkflowYamlSerializer classify mohist/pi as UserFacing Inline Agent without substring matching or changing non-inline Actions",
        "Plan-stage parent issue context is attached for mohist/opencode and mohist/pi only under the existing task/stage/parent guards defined in design/workflow/task-dispatch.md; checks, other stages, AgentJobs, and unrelated Actions remain excluded",
        "Validation and dispatch diagnostics refer to the selected Action rather than hard-coding OpenCode",
        "Server specs cover accepted Pi dispatch, every legacy field rejection, both Inline Agent classifications, serialization, parent-context inclusion/exclusion guards, and unchanged OpenCode behavior",
        "npm test passes"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Pi-aware Server Workflow validation, explicit Inline Agent classification, parent-context dispatch, and regression specs",
      "dependsOn": [
        "T-001"
      ],
      "passes": false,
      "notes": "The authoritative parent-context design has already been updated during planning. Do not register the Runner Action, add Pi AgentJob routing, or change model catalog APIs in this task."
    },
    {
      "id": "T-006",
      "title": "Deliver the mohist/pi Workflow execution path",
      "spec": "specs/pi-workflow-action/spec.md, specs/pi-workflow-session/spec.md",
      "description": "Integrate PiRuntime and the durable reporter into RunnerHost and WorkExecutor, register the standalone mohist/pi Action, and atomically migrate WorkExecutor plus the production OpenCode/Pi Actions to the shared logical-Session held-lease contract, guarded binding, Workflow completion/promise projection, recovery, and cleanup reuse. Compose the Workflow-turn/rebind outbox protocol into both Actions while leaving Session-command handlers and Runtime operations unchanged. This is the final Runner vertical slice; it consumes the already-tested runtime, Session wire, outbox, and Server dispatch boundaries without touching AgentJob or Session-command implementation.",
      "acceptanceCriteria": [
        "RunnerHost creates one provider policy and credential masker, supplies them to OpenCode/Pi, is the sole owner that composes T-004 outbox initialization/background drain with both runtimes before Server registration, shuts them down together, periodically retries failed Pi/storage startup, and gates registration/new polling while still draining awaitingAck; Pi catalog data is not added to RunnerRegistration",
        "WorkExecutor alone receives WorkflowSessionTurnCoordinator, resolves the logical key after recursive input expansion with the shared session-name helper, acquires/releases the complete task lease, and passes only the already-held WorkflowSessionTurnLease plus piRuntime through task Inline Agent Action/base/cleanup contexts; check contexts receive neither dependency and reject mohist/pi before registry resolution, Actions cannot access or reacquire the coordinator, createDefaultRegistry resolves mohist/pi for task execution, and ownerKind=agent-job continues to branch before Action resolution into OpenCode AgentJobExecutor",
        "mohist/pi recursively consumes only expanded prompt/session/options; omitted or null session uses Work ID, explicit strings are trimmed, whitespace/non-string session fails before Session creation, options must be an object, model/variant accept only string or null, model requires a non-empty first-slash provider/model split, unknown option keys are diagnostic only, and top-level with.timeout/with.deadline is invalid",
        "Workflow tests prove omitted model and variant independently avoid Runtime setters, preserve current selections on reused and restart-restored Sessions, and leave a new Session on Pi defaults when both are omitted",
        "The Action has no hidden vars.agent fallback, Server-rejected legacy fields never execute, and slash-prefixed resolved Prompt text reaches Pi literally",
        "T-006 atomically migrates production WorkExecutor, mohist/opencode, and mohist/pi to T-003's held-lease token contract: both Actions consume the binding-stream API for Workflow turns/rebinds and use token-scoped physical execution/quarantine registration; OpenCode reports session.input without its full observer and closes its input-only checkpoint after the Runtime result settles, while a first Pi turn binds, creates its pi-canonical manifest, acknowledges input, marks admission, then calls prompt()",
        "Session-command regression tests prove current OpenCode command execution, handler registration, Runtime surface, journal behavior, and event DTOs are unchanged; command admission observing Pi rejects before reservation/dispatch without OpenCode fallback, an already-admitted immutable OpenCode Cancel may finish after rebind, no Pi Session-command handler is registered, and no command path acquires Workflow coordinator state or mutates an Action stream in this issue",
        "Same-name tasks, retries, cleanup, and Runner restart reuse one logical and physical Pi Session; model/variant changes preserve path and lineage; different logical names run independently; missing/corrupt bound Pi files fail without replacement and diagnose Reset as the future same-Session remedy plus a new logical session name as the recovery available now",
        "The reporter durably projects required final assistant/reasoning, completed tool/result, model, and separate input/output/cache-read/cache-write/thought usage facts and closes the turn checkpoint before Action return; post-turn transport failure drains later without flipping success, while local append failure or a required pre-admission/pre-rebind drain failure maps to session-reporting-failed",
        "Successful final assistant text reaches expect, failIf, artifact, recovery, and promise evaluation only through private turnFact; Action failure bypasses files/markers/missing-promise checks; public output is null or exactly {promise} and contains no runtime facts",
        "All stable error mappings are covered: unavailable-runtime -> runtime-unavailable, missing-session -> runtime-session-missing, deadline-exceeded -> timeout, plus invalid-input, session-workspace-mismatch, session-binding-failed, session-reporting-failed, incompatible-runtime, interrupted, and turn-failed; binding failure submits no Prompt",
        "WorkExecutor supplies immutable turnTimeoutMs=3600000 through private Action context regardless of input; PiRuntime starts it immediately before each original/cleanup SDK Prompt, and fake-timer tests prove queue/bind/input/model preparation does not consume the budget, each Prompt gets a fresh duration, warning occurs at 55 minutes, and timeout/abort at 60 minutes",
        "External cancellation maps to interrupted, provider exhaustion remains turn-failed even when abort is unconfirmed, and reporting failure preserves its code; before runTurn the Action registers logical/physical tracking, every unconfirmed interruption installs matching logical quarantine before lease release, observed stop clears both physical and logical generations, stale clear is ignored, and Runner restart begins empty",
        "Worktree enforcement reinvokes the already-resolved mohist/pi Action under the original task lease on the same physical Session; a queued same-name task cannot run or rebind between work and cleanup",
        "Fake-timer RunnerHost specs cover pre-registration Pi/storage readiness, empty-catalog warning, periodic retry, registration/poll suppression, credential-redacted actionable workflow-session-outbox-unavailable diagnostic propagation, diagnostic clearing and registration recovery, unrelated per-stream corruption remaining available, continued awaitingAck drain, and an admitted OpenCode checkpoint preventing registration/polling until T-004 drains input and commits execution-outcome-unknown closure",
        "Fake-backed Workflow specs cover open -> bind -> manifest/input acknowledgement -> Prompt/events -> final text -> expect/promise, exact input and every error mapping, Server and Runner rejection of mohist/pi checks with zero Session/lease/Prompt side effects, same-session/cross-runtime Workflow serialization, cleanup intervention exclusion, project/WorkflowRun/name isolation, runtime-switch rejection during unconfirmed stop, observed-stop logical admission recovery, stale quarantine generations, empty execution quarantine after host restart, cursor-0 first sequence, nonzero-cursor missing-manifest rejection, retry/reporting recovery, missing-file guidance/bind failure, all usage dimensions, event non-completion, timeout/cancellation/provider/reporting failure, no Runtime/outbox resubmission within one delivery, and a controlled Runner-crash/Server-redelivery case documenting that the new delivery may execute a duplicate turn",
        "End-to-end credential tests inject a sentinel through fake Pi auth/provider surfaces and prove it is absent from Runner registration, Action-event requests, serialized outbox bytes, diagnostics, task logs, private turn facts, and public Action output while sanitized provider text retains the mask",
        "npm run typecheck -w packages/runner, npm test -w packages/runner, and npm test pass"
      ],
      "priority": 4,
      "mode": "AFK",
      "type": "WRITE",
      "output": "End-to-end Workflow-direct mohist/pi implementation across Runner host, Action, completion, Session binding/reporting, recovery, and cleanup with fake-backed Workflow coverage",
      "dependsOn": [
        "T-002",
        "T-003",
        "T-004",
        "T-005"
      ],
      "passes": false,
      "notes": "Consumes and composes all preceding boundaries; this is the only task that wires the outbox into RunnerHost or production Actions. The temporary runtime-bearing ActionContext and name-based promise/cleanup extension is explicitly owned for removal by issue #447; do not invent a competing capability abstraction. Keep Pi AgentJob execution, Pi Follow-up/Compact/Reset/Cancel routing, runtime-aware catalog reporting/UI, ACP/RPC, and a generic AgentRuntime out of scope."
    },
    {
      "id": "T-007",
      "title": "Expose Pi Workflow Session presentation and close implementation gaps",
      "spec": "specs/pi-workflow-session/spec.md#pi-turn-facts-populate-the-existing-session-audit-record",
      "description": "Teach the Web Workflow milestone classifier to recognise mohist/pi and verify that existing runtime-neutral Session transcript, tools, model observations, usage, and lineage render Pi facts correctly. After the user-visible path is verified, update only implementation-gap footnotes in the Pi product/runtime documents to describe the delivered Workflow-direct path and retain explicit sister-issue gaps.",
      "acceptanceCriteria": [
        "Web isInlineAgentTask recognises mohist/pi with a non-empty Session name and continues to exclude non-inline Actions and AgentJob work",
        "AgentSessionUsage and every Web API adapter gain cachedWriteTokens; a Pi Workflow Session fixture renders assistant text, tool lifecycle, resolved model, distinct supplied input/output/cache-read/cache-write/thought values, reporting diagnostics, and Pi lineage through shared components without a Pi-specific duplicate view",
        "Task-log model/session milestones are derived for mohist/pi and remain correctly ordered with operation log rows",
        "No Pi model selector, provider credential form, Agent runtime selector, or Session-command control is added",
        "docs/actions/pi.md and design/runtimes/pi.md implementation-gap sections mark the Workflow-direct PiRuntime/Action/Session reporting path implemented while leaving AgentJob, Session-command, and runtime-aware catalog/UI gaps explicit",
        "Web tests cover Pi Inline Agent classification and Session/milestone rendering; npm run typecheck -w packages/web and npm run test:run -w packages/web pass",
        "Final repository verification passes: npm run typecheck -w packages/runner, npm test -w packages/runner, npm test, npm run typecheck -w packages/web, and npm run test:run -w packages/web; verification leaves no Git-visible changes"
      ],
      "priority": 5,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Pi-aware Workflow Session presentation and tests in packages/web, plus accurate remaining-gap footnotes in docs/actions/pi.md and design/runtimes/pi.md",
      "dependsOn": [
        "T-006"
      ],
      "passes": false,
      "notes": "Final user-facing slice and closeout. Existing Session DTOs/components remain runtime-neutral; add only the classification/fixtures needed for Pi. Product/design bodies are target specs, so edit implementation-gap footnotes only."
    }
  ]
}
