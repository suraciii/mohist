{
  "project": "mohist-local",
  "description": "Land a self-driven, self-healing Orleans cluster-singleton event dispatcher that pulls undelivered event rows from all four truth tables each tick, fans them out by type to matching [Subscription] handlers with per-handler retry, dead-letters poison messages, and marks each row delivered — reactivating the dormant cross-aggregate reactions. Re-creates the DeadLetters persistence layer as a prerequisite.",
  "tasks": [
    {
      "id": "T-001",
      "title": "Re-create the DeadLetters persistence layer",
      "spec": "specs/dead-letter/spec.md#Dead-letter rows queryable",
      "description": "Re-introduce the DeadLetters persistence layer deleted in ba50c2089. Add a DeadLetterRow entity with event snapshot, failing-handler diagnostics, and explicit Pending/Redelivering/Resolved recovery state. Add handler-keyed uniqueness and an atomic settlement operation that upserts exhausted handlers while marking the source event in one transaction. Back query/get/recovery operations with DeadLetterStore plus test fakes and EF migrations.",
      "acceptanceCriteria": [
        "DeadLetterRow entity exists and DeadLetters DbSet is registered in MohistDbContext with PK DeadLetterId and indexes HasIndex(DeadLetteredAt) and HasIndex(FailingHandler, DeadLetteredAt).",
        "EF migrations create the table, recovery-state columns, and unique (Source, Id, FailingHandler) index; they apply cleanly against MigratedSqliteTemplate and MohistDbContextModelSnapshot has no drift.",
        "IDeadLetterStore exposes direct write/query/get operations plus SettleAsync, StartRedeliveryAsync, RecordRedeliveryFailureAsync, ResolveAsync, and DeleteAsync; test fakes implement the same state semantics.",
        "SettleAsync commits dead-letter upserts and source DispatchedAt atomically; a source-mark failure commits neither side, and retrying the same event/handler retains one row.",
        "Recovery persists Redelivering before handler invocation, returns failures to Pending, marks success Resolved, and excludes resolved rows from unresolved queries.",
        "Store and migration specs cover write/query/get/delete, atomic rollback, uniqueness, and recovery-state transitions; the server test suite is green and TreatWarningsAsErrors stays clean."
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Infrastructure/Data/Events/DeadLetterRow.cs, Infrastructure/Events/IDeadLetterStore.cs, Infrastructure/Data/Events/DeadLetterStore.cs, test fakes, MohistDbContext DeadLetters registration, creation + hardening EF migrations, and store tests.",
      "dependsOn": [],
      "passes": true,
      "notes": "The historical row shape is extended with recovery state. The first migration creates DeadLetters; the hardening migration adds status/timestamps and unique (Source, Id, FailingHandler). DispatchedAt already exists on all four source truth tables."
    },
    {
      "id": "T-002",
      "title": "Implement the self-driven cluster-singleton event dispatcher",
      "spec": "specs/event-dispatch/spec.md#Cluster-singleton self-waking sole notifier",
      "description": "Build the sole, self-healing notifier as an Orleans cluster-singleton grain over a pure-DI fan-out service. Pull all truth tables, deliver matching subscriptions with per-handler retry, atomically settle poison outcomes, and preserve serial FIFO. Persist AgentJob trigger inputs and stable runner work identities before acknowledgement so activation loss resumes one job/work. Keep documented best-effort handlers best-effort, scope closed-generic Epic queries per delivery, and expose direct-loopback-only redacted API/CLI recovery.",
      "acceptanceCriteria": [
        "IDispatcherGrain (IGrainWithStringKey, IRemindable) and DispatcherGrain exist with fixed key \"dispatcher\"; the grain registers a persisted reminder (~1s, cadence configurable) on activation, and both ReceiveReminder and PulseAsync run exactly one pull–fan-out–mark cycle by delegating to EventDispatcherService.DispatchAsync.",
        "DispatcherActivationService activates the fixed-key grain during host startup, and an integration spec verifies that a fresh host has registered the dispatcher reminder without an explicit producer Pulse.",
        "EventDispatcherService is a plain DI singleton taking IEventStore, IEnumerable<Subscription>, IDeadLetterStore, TimeProvider, ILogger; every DispatchedAt/DeadLetteredAt timestamp is sourced from the injected TimeProvider (no DateTimeOffset.UtcNow in the dispatch path).",
        "Each tick calls IEventStore.ListUndeliveredAsync (the single UNION over WorkflowRun/Issue/Epic/AgentSession truth tables), processes the batch serially in (Source, Id) order, and applies CloudEventTypeMatcher.Matches(subscription.Type, event.Type) to decide fan-out per subscription.",
        "Per-handler retry uses a fixed attempt cap; exhausted handler rows and the original event DispatchedAt are committed through one atomic dead-letter settlement.",
        "Per-handler isolation holds: when an event matches two handlers and one exhausts, the sibling handler still receives the event independently and its outcome does not depend on the failing handler.",
        "Per-row DispatchedAt is set only after all matching handlers have settled (succeeded or dead-lettered); no undelivered row is marked before delivery.",
        "Settlement uses the EventOrigin returned by ListUndeliveredAsync to select the persisted truth table; custom/future source URIs routed through the WorkflowRun fallback remain markable after delivery.",
        "A MarkDispatchedAsync failure propagates and stops the current batch so a higher row in the same stream cannot overtake the unmarked row.",
        "The closed-generic discovery bug in AddCloudEventHandlersFromAssembly is fixed (proper ICloudEventHandler<> closed-generic scan) so EpicAutoDoneHandler and EpicCancelledReconcileHandler are registered and invoked in the fan-out.",
        "InMemoryEventBus no longer reads DateTimeOffset.UtcNow; it uses an injected TimeProvider for the envelope timestamp.",
        "RedeliverAsync(deadLetterId) retries only the recorded failing handler, persists recovery state, marks success resolved, and remains available through direct-loopback-only server API and mo CLI surfaces that reject proxy forwarding and redact exception stacks.",
        "Required durable handler side effects are awaited and propagate failure; documented best-effort Hermes notifications and Agent subscription launches remain log-and-swallow and never enter retry/dead-letter flow.",
        "Agent subscription launches use stable session/job identities; AgentJob input, candidate runner, work id, retry state, and terminal state are durable, and Runner acceptance replay reuses the same work id.",
        "Closed-generic Epic handlers resolve scoped EpicQuerier instances inside an async scope per delivery; production scope validation succeeds.",
        "EventDispatcherService, IDeadLetterStore→DeadLetterStore (with NoopDeadLetterStore via TryAdd), subscriptions, and AgentJob observer defaults are registered once in the host-shared application service graph; MohistSiloRegistration adds only Orleans infrastructure.",
        "Unit tests (fake IEventStore + fake/Noop IDeadLetterStore + FakeTimeProvider) cover: pull→match→invoke→mark; per-stream FIFO (Source,Id) ordering with no skip; per-handler retry-then-succeed; exhaustion → dead-letter written + DispatchedAt set + not retried next tick; per-handler isolation; deliver-before-mark crash → row stays undelivered → re-delivered on next tick; CloudEventTypeMatcher fan-out including a closed-generic handler.",
        "Spec/integration tests cover reminder-callback delivery without Pulse using fake time and deterministic signals, fixed-key recovery on another silo after host-silo loss, Pulse latency, closed-generic Epic delivery, Agent best-effort settlement, proxy rejection, and AgentJob deactivation/crash checkpoints.",
        "Server npm test is green and TreatWarningsAsErrors stays clean."
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "IDispatcherGrain.cs, DispatcherGrain.cs, DispatcherActivationService.cs, EventDispatcherService.cs, handler outcome/idempotency hardening, the discovery/filter fix in CloudEventBusServiceCollectionExtensions.cs, the InMemoryEventBus TimeProvider edit, silo/host registrations, DeadLetterRoutes + mo event dead-letter commands, and unit + spec/integration/CLI tests.",
      "dependsOn": ["T-001"],
      "passes": true,
      "notes": "Follow design.md decisions D1–D13. Retry is hand-rolled with a fixed attempt cap and no backoff/jitter. Fresh host startup calls EnsureStartedAsync. Durable handler failures propagate; documented best-effort handlers absorb their own failures. Poison settlement is atomic. Agent launch replay resumes durable stable job/work identities. Dead-letter recovery targets one handler and is available only through a direct loopback API + mo CLI connection."
    }
  ]
}
