# Ralph Progress Log
Started: 2026-04-16T12:10:01.067Z

---


[2026-04-16] T-001: Create artifact instruction and template files
- Created 5 artifact instruction files in src/agents/prompts/artifacts/: proposal.md, specs.md, design.md, tasks.md, self-review.md
- Created review stage prompt at src/agents/prompts/review.md
- tasks.md includes guidance on granularity, AFK/HITL (mode), task types (WRITE/TEST/MIGRATE/CONFIG/REVIEW), output, dependsOn, dependency ordering
- Content derived from OpenSpec ralph-driven/schema.yaml instructions and existing YAML prompt files
- Typecheck passes
- Learnings: Build script only copies .yaml files to dist/ - future task (T-002) will need to update build script to also copy .md files
- Next: T-002 Implement AcpConnection and artifact prompt builder

## 2026-04-16T00:00:00Z - T-002 Implement AcpConnection and artifact prompt builder

### What was done
- Added `createAcpConnection()` to `acp-session.ts` returning an `AcpConnection` with `prompt()` and `close()` methods
- AcpConnection uses closure-based factory pattern, reuses ClientSideConnection across multiple prompt rounds
- Per-round timeout (15 min default) with `createTimeout()` helper
- Agent text tracked per-round via `roundStartIndex` offset
- Created `src/agents/artifact-prompt.ts` with:
  - `buildArtifactPrompt(artifactType, issue, changeDir)` - loads instruction from `artifacts/{type}.md`
  - `buildSelfReviewPrompt(issue, changeDir)` - loads from `artifacts/self-review.md`
  - `buildReviewerPrompt(issue, changeDir)` - loads from `review.md`
- Updated `agent-runtime/index.ts` exports
- Updated build script to copy `.md` prompt files and `artifacts/` subdirectory to dist/

### Learnings
- Existing `runAcpSession` uses a single shared timeout promise for all phases (potential bug)
- AcpConnection uses per-round timeouts via `createTimeout()` factory
- Instruction files are cached in module-level Map for performance

### Next steps
- T-003: Implement pipeline plan stage using AcpConnection multi-round prompts

---

## 2026-04-16T00:00:00Z - T-003 Implement pipeline plan stage with multi-round ACP

### What was done
- Added `runPlanStage(issue, acpOptions)` method to `WorkflowController`
- Uses `createAcpConnection()` to open a multi-round ACP session
- 5 rounds: proposal → specs → design → tasks → self-review
- Each round builds prompt via `buildArtifactPrompt()` (rounds 1-4) or `buildSelfReviewPrompt()` (round 5)
- Each round verifies artifact exists on disk before proceeding (`fs.existsSync`)
- On any round failure, closes connection and returns `StageResult` with which artifact failed
- Added `cleanChangeDir(changeDir)` module-level function: removes all entries except `.openspec.yaml`
- Added `PlanRoundConfig` interface to configure round type, verify function, and label
- `AcpConnectionOptions` passed as parameter to `runPlanStage()` — caller (future T-004 pipeline) provides cwd, issueId, etc.
- Typecheck and build pass

### Learnings
- Kept existing `executePlanStage()` alongside new `runPlanStage()` to avoid breaking callers until T-004 rewrites the pipeline
- The `cleanChangeDir` function is intentionally simple — just removes everything except `.openspec.yaml` before starting plan stage
- `AcpConnectionOptions` is passed in rather than stored on the controller, keeping the controller stateless w.r.t. ACP connections

### Next steps
- T-004: Implement pipeline core loop, gate controller, and review stage

---

## 2026-04-16T00:00:00Z - T-004 Implement pipeline core loop, gate controller, and review stage

### What was done
- Added `PipelineResult` interface to `workflow-controller.ts` with `completed`, `stage`, `gateRequired`, `message` fields
- Added `run(issue, acpOptions)` method to `WorkflowController` — the main pipeline loop:
  - Fixed order: plan → gate → build → review → gate → done (no LLM decisions)
  - Plan gate: sets `approvalState` to `awaiting`, emits `approval_requested`, returns
  - Build → Review: auto-advance without gate (per spec: "No gate after build")
  - Review gate: same pattern as plan gate
  - Completion: updates stage to Done, clears approvalState
- Added `runPipelineBuildStage(issue)` — uses `RalphExecutor` via `detectOpenSpecChange()` (unchanged logic from existing `executeBuildStage`)
- Added `runPipelineReviewStage(issue, acpOptions)` — uses `AcpConnection` with `buildReviewerPrompt()`, single prompt round
- Extended `WorkflowControllerOptions` with optional `issueRepo`, `eventBus`, `projectId` (pipeline-only dependencies)
- Made `plannerAgent`/`reviewerAgent` optional in options (backward compatible)
- Added to `AgentRunnerService`:
  - `pendingGates` Map for tracking issues at gates (replaces `pausedSessions` for pipeline path)
  - `hasPendingGate(issueNumber)` — check if issue is waiting at a pipeline gate
  - `startPipeline(...)` — creates WorkflowController and calls `run()`
  - `resumePipeline(...)` — re-invokes `run()` after gate approval
  - `executePipeline()` — shared private method that runs pipeline and handles gate tracking, error handling, event emission
- Exported `PipelineResult` from `workflow/index.ts`
- Typecheck and build pass

### Learnings
- Pipeline `run()` reads current `issue.stage` from parameter, so re-invocation naturally picks up from the right stage after gate approval
- `pendingGates` tracks gate state by issue number, mirroring the `pausedSessions` pattern for backward compatibility
- Old `start()`/`resume()` methods preserved untouched — they'll be removed in T-006/T-007 when MainAgent is deleted
- `AcpConnectionOptions` passed to `run()` as parameter since it contains execution-specific context (issueId, projectId, etc.)

### Next steps
- T-005: Implement ExploreService

---

## 2026-04-16T00:00:00Z - T-005 Implement ExploreService

### What was done
- Created `agents/prompts/explore.md` — explore prompt instruction for ACP agent
- Added `buildExplorePrompt(issueInfo, changeDir, existingProposal?)` to `artifact-prompt.ts`
  - Accepts `ExploreIssueInfo` (title, body?, number?) for both new and existing issue scenarios
  - Includes existing proposal content when updating
- Created `services/explore-acp-service.ts` with `ExploreAcpService` class:
  - `run(issueTitle, projectId, acpOptions?)` — creates issue + change dir, runs `runAcpSession()` to generate proposal.md
  - `runOnIssue(issue, acpOptions?)` — ensures change dir exists, reads existing proposal, runs `runAcpSession()` to update/generate proposal.md
  - Both methods verify proposal.md was created after ACP session
- Exported `ExploreAcpService`, `ExploreAcpServiceOptions`, `ExploreResult` from `services/index.ts`
- Typecheck passes

### Learnings
- Existing `ExploreService` (DB CRUD for interactive explore sessions) kept intact — new `ExploreAcpService` serves the pipeline-oriented explore use case
- `run()` creates the issue first to get a number for the change dir, then runs ACP session — matches the design's flow
- `runOnIssue()` reads existing proposal and passes it to the prompt so the agent can update rather than start from scratch

### Next steps
- T-006: Delete MainAgent, PlannerAgent, ReviewerAgent, and agent-loop
