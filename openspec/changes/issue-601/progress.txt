## Review Rework

- 2026-08-14: Read the first self-review and repository lifecycle code. All five must-fix findings are accepted for correction; `self-review.md` remains untouched.
- Completed design/spec/task updates: the migration is fail-closed, dirty/unconfirmed state and actions are concrete, WorkspaceVerification is separate from CommitReceipt, operator-fenced source adoption is defined, and all Workflow task terminal paths are covered.
- Consistency checks passed: tasks JSON parses, all spec anchors resolve, dependencies are present and acyclic, and `git diff --check` is clean.
- Final clarification added: clean stage/run transitions and the tracked/declared source-path distinction are explicit.
- Verification: `npm run verify` was attempted but stopped at `tsx scripts/test-duration/canonical.ts` because `tsx` is not installed (`command 127`).
- Final audit complete: `self-review.md` is unchanged since the first review commit, and the disposition work is committed in three incremental checkpoints.
- 2026-08-16 T-002: The existing runner had cleanup convergence inside `WorkExecutor.executeOne`, which rewrote valid Action results to `failed`. Workflow task execution now uses an outer completion-boundary path; checks and AgentJob execution remain on their separate contracts.
- 2026-08-16 T-002: `completion-boundary.ts` probes branch, HEAD, HEAD tree, and porcelain status through the injected Git runner. Authoritative clean evidence requires non-null matching workspace identity/generation, branch, HEAD, tree, and empty status. Pre-Action failures force `authoritative=false` with a phase reason.
- 2026-08-16 T-002: `WorkResultJournal` stores the immutable boundary beside the result and rejects exact-identity replays with changed boundary/result payloads. `executeWork` persists the boundary through a host callback before terminal task-log delivery; pending local persistence retains the in-flight fence.
- 2026-08-16 T-002: Full runner verification passed after updating cleanup-era tests to the new contract: 156 test files and 1687 tests with one Vitest worker; runner production and test typechecks also pass. Dependency installation was required because the checkout initially had no `node_modules`.
- 2026-08-16 T-002 follow-up: Receipt authority requires identity and generation from the resolved workspace binding as well as matching dispatch expectations; missing resolved binding cannot become clean merely because the dispatch supplied expected values. Receipt-probe Git output is logged under its own task-log source.
- 2026-08-16 T-002 ordering: after execution returns, incremental task-log delivery is detached and drained before the completion-boundary journal write. Terminal task-log persistence/delivery starts only after the boundary is durable; a pending boundary leaves the started fence retained.
