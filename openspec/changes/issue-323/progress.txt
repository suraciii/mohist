# Issue #323 — Insights M2 chart migration — working memory

## Codebase Patterns

- **Dashboard grid layout** — `pages/dashboard/ui/DashboardPage.tsx` uses a 2-column responsive grid (`md:grid-cols-2`) that already renders two items cleanly. Going from 3 → 2 surviving zones does NOT cause layout regression.
- **DashboardPage zone mounting** — iterates `DASHBOARD_ZONES` (array of `{id, name}`) and mounts each via `<DashboardZone>`. Wrapping the iterand in a single-expression ternary by `zone.id` (instead of a chained ternary chain) is the simpler, narrower shape after removing the productivity branch.
- **DashboardPage empty-state path** — the `!projects || projects.length === 0` branch is the only place `dashboard-empty-state` renders; none of the chart mocks fire there, so tests in the empty-projects branch can keep asserting every zone is absent (including `dashboard-zone-productivity`) unchanged.
- **Colocated tests** — every chart component has a `*.test.tsx` next to it (not in a separate `tests/` dir); relocating the component means moving its test file with it, single command (`git mv`).
- **`../charts` import pattern preserved** — every `pages/dashboard/productivity/*.tsx` imports `'../charts'` (ChartContainer/ChartAxes/etc.) or specific primitives. Keeping `charts/` and `panels/` as siblings under `pages/insights/` (D1) preserves every import byte-for-byte.
- **`<chart>-window` testid convention (from T-002 spec)** — D4 will add `data-testid="<chart>-window"` to six charts in T-002; the chart's existing data-testid was specifically generic (e.g. `productivity-quality` for `QualityPanel`, `throughput-chart` for `ThroughputChart`). The window badge naming must be `<chart-name>-window` to match `ThroughputChart → data-testid="throughput-chart-window"` etc. (T-002 not done yet; assert it does not collide with any existing testid via grep before adding the badge.)

## Task T-001 specifics

- The T-001 acceptance criteria were precise: it bundles D1 (relocate) with D5 (Dashboard narrowing) because ProductivityZone's deletion requires unmounting from DashboardPage. Doing them together avoids an intermediate broken state.
- After relocation but before T-003, the relocated panels live under `pages/insights/panels/` but are not yet mounted anywhere — `InsightsPage` still renders the M1 `<ChartPlaceholder />`. This is the accepted intermediate state described in the task note.
- All chart component data-testids keep their historical `productivity-*` prefix even though the components no longer live under `pages/dashboard/productivity/` (e.g. `productivity-quality`, `productivity-trend`, `productivity-epic-list`). Test files reference those ids. Renaming them is out of T-001 scope and belongs to a separate cleanup; the design.md decision D1 explicitly preserves the byte-for-byte import graph and implicit testid stability.
- `ProductivityZone.test.tsx` was the only test that asserted the ProductivityZone component itself as a unit (with mocked child components). It was deleted along with `ProductivityZone.tsx`. Per-chart child ordering/positioning assertions that were in those tests are NOT preserved anywhere — T-003's `InsightsCharts.test.tsx` will cover the new group composition, not the legacy flat order.
- `model/` subdirectory under `productivity/` moved as part of the productivity move (D1 keeps the `model/` sibling relationship). It contains `throughput.ts` + tests, `delivery-time.ts` + tests. The relocated panels all import via relative paths under the same parent, so nothing else needed to change.

## Gotchas

- **`DashboardZoneId` type narrowing** — removing `'productivity'` from the union in `DashboardZone.tsx` means `DASHBOARD_ZONES` array literal must also drop that id. TypeScript's exhaustiveness in the ternary was a non-issue because the new code uses a non-exhaustive ternary (`zone.id === 'digest' ? <digest> : <pulse>`), but if you ever tighten back, reintroduce exhaustive checks.
- **`git mv` followed by `git rm`** — `git mv` stages the rename; `git rm` of those same paths then needs `-f` (force) because the index has staged entries. Workaround: `git rm -f <path>` after `git mv` works directly.
- **No third `dashboard-digest-empty` slot check** — in the prior "zone content in every slot" test, the third negation (`productivity.querySelector('[data-testid="dashboard-digest-..."]')`) tested that the productivity slot does not contain digest content. After deletion the new test must verify the surviving zones (`pulse` not mounting digest; `digest` not mounting digest-empty/loading/content is wrong — digest IS the digest mount point). Lesson: be careful not to over-add "absence" assertions on the surviving zones that ARE supposed to mount the digest widget.
- **`npm install` not pre-run** — this workspace ships `package-lock.json` but no `node_modules`; first run of any `npm run *` script triggers a `npm install` (took ~4s). Subsequent commands work normally.

## Hand-off for T-002 / T-003

- **T-002 (window badges)** — touches only six panel components; each badge goes in the existing header chrome next to the `<h3>`. Existing per-chart colocated tests get one new `expect(getByTestId('<chart>-window')).toBeInTheDocument()` per test that renders the populated state. Three charts (`CompletionTrend`, `QualityPanel`, `InvestmentPanel`) already show their window — no badge there.
- **T-003 (groups + ChartPlaceholder removal)** — introduces `ChartGroup` and `InsightsCharts` in `pages/insights/ui/`. Replaces `<ChartPlaceholder />` in `InsightsPage.tsx`. Deletes `ChartPlaceholder.tsx` and its `index.ts` export. Adds `InsightsCharts.test.tsx`. Flips the placeholder assertion in `InsightsPage.test.tsx` to assert the four groups render. The Signal Summary tests remain unchanged.
- The 10 colocated `*.test.tsx` files now live at `packages/web/src/pages/insights/panels/<Chart>.test.tsx`. They continue to mock `'../../../entities/...'` / `@/widgets/...` etc. with absolute/relative paths that don't reference the moved panel itself, so they should pass as-is. (Verified: 253 test files, 3961 tests passed after the move.)

## Task T-002 specifics

- **Badge testid naming:** used `<chart-name>-window` exactly as the convention planned (e.g. `throughput-chart-window`, `cycle-time-chart-window`, `cumulative-flow-chart-window`, `stage-duration-chart-window`, `ftr-trend-chart-window`, `cost-trend-chart-window`). Pre-flight grep confirmed no collisions with existing testids (`*-lens-*`, `chart-container-*`, etc.).
- **Badge style:** `inline-flex items-center rounded-md border border-border bg-muted/40 px-2 py-0.5 text-xs tabular-nums text-muted-foreground` — same chip pattern used elsewhere in the codebase (`SystemUpdateOutcomeView`, `SessionRecoveryActions`, `context-ref-chip-*`). The badge lives in the header chrome (alongside the existing `<h3>`, on the right of a `flex justify-between` row) and does not touch chart body, data-fetch, lens toggles, overlays, empty states, or `ChartAccessibility` summaries.
- **Charts with right-side lens toggles (CycleTimeChart, StageDurationChart):** the badge sits to the left of the lens toggle inside a wrapping `<div className="flex items-center gap-2">` so the layout doesn't break.
- **Charts with rework overlay toggle (FtrTrendChart):** the badge sits inside the existing right-side `<div className="flex items-center gap-2 text-xs text-muted-foreground">` so it shares the row with the overlay checkbox. The badge does not include `text-muted-foreground` since it inherits from the wrapper.
- **Empty-state hiding rule:** badge renders only when the chart's existing populated-data guard passes (`hasThroughputData` / `hasPlottablePoints` / `hasSnapshotData` / `hasPlottableStages` / `trend && bucketCount > 0` / `hasUsageData`) — same predicate the chart body uses to render its data, so the badge visibility mirrors the chart body's empty handling. Each colocated test asserts both the populated badge AND that the badge is absent in the empty state.
- **Window source per chart (matches design.md D4):**
  - `ThroughputChart` → fixed `30d` (completion `bucket=day` trailing window).
  - `CycleTimeChart` → fixed `30d` (delivery-time endpoint trailing window).
  - `CumulativeFlowChart` → derived from `data.rangeFrom`/`data.rangeTo`, formatted as `MMM D – MMM D` (e.g. `Apr 2 – Jun 30`). Reuses existing `formatDayLabel`.
  - `StageDurationChart` → derived from `data.window.from`/`data.window.to`, formatted as `MMM D – MMM D`. Added a new `formatStageWindow` helper that parses the ISO datetime prefix and formats via `toLocaleDateString`.
  - `FtrTrendChart` → derived from `trend.from`/`trend.to`, formatted as `MMM D – MMM D`. Reuses existing `formatBoundaryLabel`.
  - `CostTrendChart` → derived from `data.rangeFrom`/`data.rangeTo`, formatted as `MMM D – MMM D`. Reuses existing `formatLabel`. Note: `rangeTo` may be exclusive (one day after the last bucket, e.g. `2026-06-29T00:00:00` for buckets ending `Jun 28`); the badge reflects the endpoint as-is, not the displayed last bucket day — the task explicitly requires the badge to be "derived strictly from the range their endpoint already returns (no invented window)".
- **Function hoisting:** `formatDayLabel` (CumulativeFlowChart), `formatLabel` (CostTrendChart), and `formatBoundaryLabel` (FtrTrendChart) are all function declarations in the same module, so they hoist and can be referenced from the JSX returned earlier in the file. No imports needed.
- **No new types, no DTO changes, no chart-internal logic touched.** Pure additive header chrome per D4; charts' interaction, empty-state, accessibility, lens toggles, overlays, and `ChartAccessibility` summaries all unchanged.
- **Test results:** all six chart test files pass individually (27/27, 28/28, 23/23, 29/29, 27/27, 33/33). Full web test run: 253 files, 3972 passed / 1 skipped (the pre-existing skip). `npm run typecheck -w packages/web` clean.
- **Hand-off for T-003:** the spec test for `InsightsCharts` (D6) will assert the six new `<chart>-window` testids render in the populated state, plus the existing-label charts (`CompletionTrend`, `QualityPanel`, `InvestmentPanel`) carry their own window copy. T-003 can also assert `data-testid="throughput-chart-window"` etc. via `screen.getByTestId` on the mounted components.

## Task T-003 specifics

- **`ChartGroup` props:** `{ id, title, question, children }`; id is a `DimensionId` union (`'output' | 'delivery' | 'quality' | 'investment'`) so it matches the `data-dimension` attribute literally. `title` is the question-led dimension name (产出 / 交付效率 / 质量 / 投入), `question` is the short clause that the title asks (你交付了多少？ / 多快？ / 一次做对了吗？ / 花了多少？). The wrapper renders a `<section>` with `data-testid="insights-chart-group"` and `data-dimension="<id>"`, a `<h3 data-testid="insights-chart-group-title">` for the title, a `<p data-testid="insights-chart-group-question">` for the question, and a charts slot `<div data-testid="insights-chart-group-charts">`. The two-element header (title + question) is the "question-led heading" — the title states the dimension and the question states what it answers; the design.md decision calls out the title+question pattern.
- **`InsightsCharts` composition:** a single `CHART_GROUPS` array (4 readonly entries) drives the mount order. Each entry's `render()` returns the assigned charts in their fixed intra-group order. `InsightsCharts` maps the array and mounts each via `ChartGroup`. This makes "exactly four, fixed order, fixed membership" structural and trivially assertable — count groups, assert `data-dimension` order, assert each group contains its chart testids.
- **Group membership (matches design.md D2):** output = EpicProgressList, ThroughputChart, CompletionTrend, CumulativeFlowChart (EpicProgressList first per D3); delivery = CycleTimeChart, StageDurationChart; quality = QualityPanel, FtrTrendChart; investment = InvestmentPanel, CostTrendChart. EpicProgressList is exempt from the window annotation (no endpoint window, D3) and intentionally comes first in the 产出 group; the spec test asserts this explicit order.
- **`JSX.Element` namespace unavailability:** the project tsconfig uses `"jsx": "react-jsx"` with no `import React` anywhere — `JSX.Element` is not in scope. Switched the `GroupSpec.render` field to `() => ReactNode` (importing `ReactNode` as a type, the convention used elsewhere in `pages/insights/ui/`). The simpler array-of-renderers pattern replaces an early draft that tried per-group wrapper components.
- **`insights-charts` spec testid:** the top-level wrapper exposes `data-testid="insights-charts"` (the section in `InsightsPage.tsx` is unchanged at `data-testid="insights-charts-section"`). `App.test.tsx` was updated to assert `insights-charts` instead of the removed `insights-chart-placeholder` (the App routing-split test was the only other consumer).
- **Test surface:** `InsightsCharts.test.tsx` mocks all nine hook calls (`useCompletionThroughput`, `useCompletionTrend`, `useCumulativeFlow`, `useDeliveryTime`, `useStageDuration`, `useQualityMetrics`, `useEpics`, `useCostRollup`, `useAgentUsage`) with populated data so the six D4 window badges render. Asserts exactly four groups, fixed `data-dimension` order, each group's chart testid membership + order, each heading's question-led title, that the six window badges are present, that no time-range selector control renders anywhere (queries `-range-selector` testid, `<input type="range">`, and `<select name*="range|window">`), and that the placeholder zone does not render.
- **`getByTestId` with attribute selectors:** an early draft used `screen.getByTestId('insights-chart-group[data-dimension="output"]')` which the testing-library query parser does not understand as a CSS selector — it treats the whole string as the literal testid. Fixed by `getAllByTestId('insights-chart-group').find(g => g.getAttribute('data-dimension') === 'output')`. The InsightsPage test uses `getAllByTestId('insights-chart-group')` and reads `data-dimension` from each element directly.
- **Test results:** `InsightsCharts.test.tsx` 8/8; `InsightsPage.test.tsx` 14/14 (including the flipped four-groups assertion and the unchanged Signal Summary structure/empty/populated/graceful-degradation/investment-sub-trend tests); `App.test.tsx` 152/152 across all routing-split suites. Full web test run: 254 files, 3980 passed / 1 skipped. `npm run typecheck -w packages/web` clean.
- **M1 placeholder removal:** `ChartPlaceholder.tsx` deleted; `pages/insights/index.ts` no longer re-exports it; `InsightsPage.tsx` renders `<InsightsCharts />` instead. The `data-future="charts-m2"` marker is gone from the DOM. The Signal Summary structure/empty/populated/graceful-degradation tests are unchanged because the verdict content is untouched.
- **Hand-off:** T-003 closes the insights-charts "reachable exclusively on /insights" requirement together with T-001's Dashboard narrowing. M3 (time-range selector) remains out of scope; the explicit no-selector assertion in InsightsCharts.test.tsx locks that boundary until M3 lands.
