# Ralph Progress Log
Started: 2026-04-24T13:42:07.268Z

---


## 2026-04-24T13:44 — T-001 Fix build failed counter false-positive
- Removed unconditional `failed++` from line 525 of ralph-executor.ts
- Added `failed++` only in the abort path (user chooses to abort)
- Added `failed++` in the else branch for genuine failures without shouldPause
- Added `taskResults.pop()` before retry `continue` to prevent duplicate entries
- Auto-skip path: no failed++ (status changed to 'skipped')
- User-skip path: no failed++ (status changed to 'skipped')
- Retry path: pop the 'failed' entry, if retry succeeds only 'completed' is pushed
- Typecheck: passes
- Tests: 590 passed, 2 skipped
- Next: T-002 (stage regression fix)

## 2026-04-24 — T-002 Fix stage regression on pipeline failure
- Removed `updateStage(issue.id, Stage.Draft)` from 3 locations in agent-runner-service.ts:
  1. Pipeline failure path (was line 327-334): removed stage rollback, keeps current stage
  2. Exception catch handler (was line 377-384): removed stage rollback, keeps current stage
  3. Orphan recovery else branch (was line 135): removed stage rollback, keeps current stage
- All three paths already set status=Blocked; now they also preserve the current stage
- Updated orphan recovery log message to reflect "stage preserved" instead of "stage=draft"
- Typecheck: passes
- No `updateStage.*Draft` calls remain in agent-runner-service.ts
- Next: T-003 (stage timeout passing)

## 2026-04-24 — T-003 Pass stage timeout to ACP session runner
- Added `stageTimeoutMs?: number` to `RalphExecutorContext` in ralph-executor.ts
- In `runRalphLoop`: computed `perTaskTimeout = max(stageTimeoutMs / totalTasks, 300000ms)`, default 30min when no stage timeout configured
- Passed `perTaskTimeout` via `timeout` option to `_acpSessionRunner` call
- In workflow-controller.ts: added `getBuildStageTimeoutMs()` helper that loads workflow config and extracts build stage timeout in ms
- Passed `stageTimeoutMs` when constructing `RalphExecutor` in `runPipelineBuildStage`
- Added `import { loadWorkflow }` to workflow-controller.ts
- Typecheck: passes
- Tests: 588 passed, 2 failed (pre-existing T-002 test expectations for Draft stage), 2 skipped
- Learnings: acp-session.ts already accepts `timeout` in options with default 30min — only needed to thread the value through from workflow config
- Next: T-004 (skip-to-review deadlock)

## 2026-04-24 — T-004 Fix skip-to-review deadlock
- Added `setApprovalState({stage: Review, status: 'awaiting', output: null})` after transitionToStage/setStatus in skip-to-review endpoint
- Added `eventBus.emit('approval_requested', ...)` to notify CLI/frontend of the approval gate
- approve endpoint already has fallback: checks `approvalState.status === 'awaiting'` when no pendingGate exists (line 736)
- After approval, `approvalStage === Stage.Review` → `nextStage = Stage.Done` (line 757-758), pipeline proceeds to merge-back
- Typecheck: passes
- Learnings: ApprovalState requires `output` field (not optional). PendingGate registration is indeed optional — approve endpoint fallback works with just approvalState set.
- Next: T-005 (reopen resume from current stage)

## 2026-04-24 — T-005: Fix reopen to resume from current stage

**What was done:**
- Removed the `hasPendingGate` conditional branch in api/issues.ts reopen endpoint
- Removed the reset-to-draft else branch (transitionToStage Draft + clearApprovalState)
- Replaced with single code path: always preserve current stage and call resumePipeline
- Updated response message to show current stage: "reopened and resumed from stage {stage}"
- Updated CLI help text in issue.ts from "Reopen a closed issue" to "Reopen a blocked, closed, or paused issue"

**Files changed:**
- packages/cli/src/api/issues.ts (lines 520-552: removed two-branch logic, single resume path)
- packages/cli/src/cli/commands/issue.ts (line 252: updated description)

**Typecheck:** passes

**Next steps:** T-006 (unit tests for all fixes)

## 2026-04-24 — T-006: Add unit tests for all fixes

**What was done:**
- Fixed 2 pre-existing broken tests in agent-runner-service.test.ts that expected `Stage.Draft` after orphan recovery (now expects `Stage.Build` — stage preserved)
- Added 3 ralph-executor tests: auto-skip no failed increment, retry replaces failed with completed, abort increments failed
- Added 3 timeout calculation tests: 2 tasks gets 15min each, 10 tasks hits 5min floor, no config defaults 30min
- Added skip-to-review API test: verifies approvalState={stage: Review, status: awaiting} after skip
- Added reopen API test: verifies resumePipeline called with issue at stage=Build

**Files changed:**
- packages/cli/tests/agent-runner-service.test.ts (2 tests fixed)
- packages/cli/tests/ralph-executor.test.ts (6 new tests)
- packages/cli/tests/api-routes.test.ts (2 new tests, added fs/path/os imports)

**Typecheck:** passes
**Tests:** 598 passed, 2 skipped

**All tasks complete.**
