# Ralph Progress Log
Started: 2026-04-16T04:49:17.598Z

---


## 2026-04-16 T-001 Update detector and types
- Renamed prdPath→tasksPath, removed taskStatusPath in detector.ts
- detectOpenSpecChange now checks tasks.json only, returns null if missing
- Replaced PrdTask/PrdJson/PrdTaskStatus/TaskStatusValue with Task/TasksFile in change-artifacts-manager.ts
- New Task interface: id, order (required), title, description, acceptanceCriteria, dependsOn, spec, passes, attempts, error
- Removed: capability, requirement_ref, estimated_effort, status, startedAt, completedAt
- readPrd/writePrd→readTasks/writeTasks, updateTaskStatus→updateTaskPasses
- workflow-results.ts: PrdJson→TasksFile, prd→tasks field
- Updated downstream consumers: context-assembler.ts, ralph-executor.ts, workflow-controller.ts, planner-agent.ts, workflow-loader.ts, read-workflow.ts
- TypeScript compiles (only pre-existing log-tail.ts error)
- Learnings: Need to update all downstream consumers in same task to keep compilation passing
- Next: T-002 (ContextAssembler), T-003 (rename tools), T-004 (RalphExecutor refactor)

## 2026-04-16 T-002 Update ContextAssembler Task interface
- Task interface already had new schema fields (passes/attempts/error) from T-001
- formatTaskForPrompt already used acceptanceCriteria (correct field name)
- Added spec field output to formatTaskForPrompt so it references task.spec
- TypeScript compiles (only pre-existing log-tail.ts error, unrelated)
- Learnings: T-001 already migrated most of the Task interface; T-002 was mostly verification + adding spec to formatted prompt output
- Next: T-003 (rename read_prd tool, delete task-status tools)

## 2026-04-16 — T-003: Rename read_prd tool to read_tasks, delete task-status tools

### Done
- Created `read-tasks.ts` replacing `read-prd.ts`: tool name `read_tasks`, reads `tasks.json`, outputs passes/attempts/error status
- Deleted `task-status.ts` entirely (removed `update_task_status` and `get_task_status` tools)
- Updated `self-review.ts`: `generatePrd` → `generateTasksFile`, `createGeneratePrdTool` → `createGenerateTasksTool`, outputs `tasks.json` with new schema (Task/TasksFile types from change-artifacts-manager)
- Updated `main-agent.ts`: imports changed to `createReadTasksTool`, `createGenerateTasksTool`; removed task-status imports and registrations; updated system prompt tool descriptions
- Deleted `read-prd.ts`

### Learnings
- T-001 had already created Task/TasksFile types in change-artifacts-manager.ts, which read-tasks.ts and self-review.ts now import
- The `canGeneratePrd` function name in openspec/self-review.ts is intentionally left as-is — it will be renamed as part of a broader cleanup (T-006)

### Next Steps
- T-004: Refactor RalphExecutor to use tasks.json directly (remove task-status.json read/write logic)

## 2026-04-16 — T-004: Refactor RalphExecutor to use tasks.json directly

### Done
- Removed `TaskStatusFile`, `TaskStatusEntry` interfaces from ralph-executor.ts
- Removed `readTaskStatus`, `writeTaskStatus`, `updateTaskStatusEntry`, `initializeTaskStatus` functions
- Added `writeTasksFile(tasksPath, tasks)` helper to persist tasks.json with `{version, tasks}` wrapper
- Added `findNextPendingTask(tasks)` — finds first task with `passes === false` sorted by order
- Added `updateTaskInList(tasks, taskId, updates)` — mutates task in array for passes/attempts/error
- Refactored `runRalphLoop` from index-based iteration to `while(true)` loop finding next pending task:
  - On success: sets `passes: true`, writes back to tasks.json
  - On failure: increments `attempts`, sets `error`, writes back to tasks.json
  - On skip: sets `passes: true` with skip reason as error
- Updated tests: replaced `readTaskStatus`/`readPrdTasks` tests with `readTasks`/`findNextPendingTask` tests
- Updated test fixtures to use `tasksPath` and new schema (passes/attempts fields)
- Removed stale tests for string-order parsing (function no longer handles strings)
- TypeScript compiles, all utility tests pass (41/41)

### Learnings
- The `runRalphLoop` integration tests hang because `runAcpSession` isn't mocked — this was pre-existing
- Changing from index-based to while-loop approach simplified resume logic (no more `current_task_index`)
- `findNextPendingTask` is exported for testability

### Next Steps
- T-005: Update agents to reference new types and tools
- T-006: Cleanup and verify all references
