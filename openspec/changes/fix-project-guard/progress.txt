# Ralph Progress Log
Started: 2026-04-13T05:16:04.078Z

---
T-001 | 2026-04-13T05:20:00Z | 新增 useCurrentProject hook 和 AppContent 初始化
- 在 api.ts 中添加了 getCurrentProject() 方法，封装 GET /api/projects/current
- 在 useQueries.ts 中新增 useCurrentProject hook，使用 retry: false 避免404时重试
- 在 AppContent 中添加 useProjects 和 useCurrentProject 调用，通过 useEffect 同步到 ProjectContext
- 处理了三种情况：后端有 currentProject 时设置 projectId；404 但有项目列表时自动选第一个；API 失败时 gracefully 降级
- Typecheck 和 Build 均通过
Next: T-002 添加 ProjectGuard 全局守卫组件

---
T-002 | 2026-04-13T05:25:00Z | 添加 ProjectGuard 全局守卫组件
- 创建了 ProjectGuard 组件 (components/ProjectGuard.tsx)，使用 React Router layout route 模式
- 使用 useLocation 判断 /settings 路由进行豁免
- 处理四种状态：loading 显示 spinner；无项目显示引导页（含 CreateProjectDialog）；有项目但 projectId 为 null 时自动选择第一个；正常渲染 Outlet
- 更新 App.tsx 将 Routes 包裹在 ProjectGuard 的 layout route 中
- Typecheck 和 Build 均通过
Next: T-003 从 KanbanView 移除 projectId 初始化副作用

---
T-003 | 2026-04-13T13:23:58Z | 从 KanbanView 移除 projectId 初始化副作用
- 移除了 KanbanView 中的 useEffect，该 useEffect 调用 setProjectId 和 setProjects 进行 projectId 初始化
- 这些初始化工作现在由 AppContent 的 React Query hooks (useProjects, useCurrentProject) 和 ProjectGuard 负责
- 保留了 KanbanView 中无项目时的空状态展示（防御性编程）
- Typecheck 和 Build 均通过
Next: T-004 验证 Explore 页面不再永久 Loading

