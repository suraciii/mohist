## Codebase Patterns

- WorkflowRun state is persisted transactionally with Workflow events through WorkflowGrain.CommitAsync. CommitAsync([]) persists a state-only transition without appending events.
- Task attempts keep their dispatch identity on TaskRun.Id, WorkId, and WorkerId. Reports must not use the mutable current AgentSession metadata as an identity source.
- IWorkflowGrain test doubles live in RunnerWorkflowStatusRouterSpecs and must be updated when the grain interface grows.
- Server test source changes require synchronizing the SpecUnitMigration ledger, validator, and provenance source-tree file count and digest.

## T-001

- AgentResultSettlement is optional TaskRun JSON state and is created only for mohist/agent, mohist/opencode, and mohist/pi when StartTask receives the real work and Runner identity.
- BindAgentExecution and ObserveAgentExecution validate the persisted TaskRun.Id + WorkId + RunnerId tuple before checking the frozen Session, Turn, runtime, and runtime-session identity. Exact replay returns Accepted without writing state or Workflow events; mismatches return Stale.
- Observations move awaiting-result to unknown and retain physical facts only. They do not use task completion or failure paths. T-002 owns deadlines and blocked transitions; T-003/T-004 own the AgentSession binding and observation ingress; T-005 owns Runner-loss handling.
- FindReportableTaskAttempt uses the persisted attempt tuple. FindReportableWork is used by report ingress so the original running attempt remains discoverable for later authoritative reports.
- npm run test:fast passed after npm ci restored missing Node dependencies. The final Server-test source-tree baseline is files=836, digest=b1dd27a9405144bab7fcd9373f9d0a4eca9b4d047f8fa3aff209dcc04455e0f6.
- FindReportableWork must preserve active checks work before falling back to persisted task attempts; otherwise checks reports are incorrectly classified as stale and stages cannot advance.
- npm run verify passed: docs check, complete build, 175 workflow-definition tests, 1793 CLI tests, 2621 Server unit tests, 3855 Server spec tests, 69 architecture tests, 4697 Web tests, 1590 Runner tests, and 70 Slack tests.

## T-002

- The first unknown Agent result observation persists its absolute deadline with the injected TimeProvider and emits an observable unconfirmed-result event. The `agent-result-settlement` reminder reconciles persisted unknown, blocked, terminal, and explicitly stopped settlement states on both ticks and activation.
- Unknown and blocked work is fenced from new dispatch, desired redelivery, retries, stage reruns, and runtime task insertion while the Runner's reported in-flight or awaiting-ack key continues to consume capacity.
- Expiry writes blocked task, stage, and run events without a failure transition. An authoritative report remains eligible for the original running attempt and clears the settlement only after snapshot and reminder cleanup succeeds, so failed cleanup is repairable on activation.
- Explicit stop commits task cancellation and run stop together, then retries snapshot deletion, reminder removal, and stage-lock release through idempotent stop replay or activation. Later reports and observations for that cancelled attempt are stale.
- Reminder registration and removal failure specs use an overridden grain seam to prove the committed settlement is repaired on activation. The final Server-test source-tree baseline is files=836, digest=4e8fb8268b1cb7cfc56489f7a76eb018616572df11876392a5e8243669d3d687.

### Interrupted Handoff

- Focused Agent settlement, state-save recovery, and domain specs passed. The isolated Web suite also passed all 4,697 tests; the concurrent `npm run test:fast` run timed out two Web navigation tests under contention, then stopped its sibling jobs.
- The architecture suite then rejected `WorkflowGrainStateSaveFailureSpecs.cs` at 26,143 bytes. Its reusable profile-coordinator fake was extracted to `WorkflowGrainTestProfileCoordinator.cs`, but this adds a Server test source file. The migration source-tree baseline is now stale again and must be recalculated before rerunning the architecture and full fast gates.

## T-003

- Workflow `session.input` now persists a replay-idempotent delivery identity and frozen `SessionWorkflowExecutionBinding` on the created Agent turn before AgentSession calls Workflow's exact-task-run binding operation. The input receipt includes the bound AgentTurn only after both boundaries accept it; a stale Workflow binding returns no receipt.
- Runner outbox input records retain task-run, delivery, runtime, and turn metadata. Reporter execution waits for a matching Server receipt and stamps the acknowledged turn on all later records; restart replay preserves the original delivery id.
- Later events for a runtime with a Workflow-bound turn must carry a complete matching frozen binding. The Server rejects a missing or mismatched turn/task/work/runner/runtime identity.
- Focused checks passed: Server build, AgentSession input-boundary specs, migration-ledger architecture specs, Runner typecheck, and 51 Runner runtime-event/reporter tests. `dotnet test --filter` is ignored by the current Microsoft.Testing.Platform configuration and ran all 3,862 specs; 27 unrelated full-suite failures were reported, so the direct xUnit class runner was used for the focused Server spec instead.
- The migration source-tree baseline is files=837, digest=1efcb9cbd9d39dac5f5f3d7b268fd7c61358f81551d6ef8df088418ec175e01c.

## T-004

- Workflow runtime-event records now carry the immutable Runner, task-run, work, input-delivery, Agent-turn, runtime, and runtime-session identity. The outbox groups and production delivery batches by that complete identity, and `sendBatch` rejects mixed records before building an envelope from its head.
- AgentSession sends identity-fenced physical observations through `ObserveAgentExecutionAsync`; terminal, idle, stop, recovery, and disconnect paths use the binding frozen on the recorded turn rather than mutable Session work labels. Generic unbound turns have no Workflow observation target.
- Stop recovery preserves the existing operation id and sends it again only while the exact frozen target remains active. A stale, replaced, terminal, idle, or indeterminate target is observed without another stop dispatch.
- Focused Runner runtime-event tests passed (58 tests). Focused AgentSession input, stop-recovery, and Workflow settlement specs passed through the xUnit v3 runner. The migration source-tree baseline is files=837, digest=5b0192e1a5889f506eb39993c0737ae13bc20ab8838ca05bb037d573b04a253b.

### Interrupted Handoff

- The first `npm run test:fast` completed every lane except architecture, which correctly caught a Sessions-to-Workflow dependency through the observation enum. The enum is now owned by Sessions and mapped in `WorkflowSessionWorkPort`; the source-tree baseline is files=837, digest=d740a68880316b263daed1945f4bc6de159d53682af65703ea591e71bbbb80c4.
- The focused SpecTests project build and Runner typecheck pass after that boundary correction. Rerun the architecture suite and `npm run test:fast` after resuming; no further feature work is pending for T-004.

## T-005

- Runner `ActionResult.outcome` is an internal `unknown` projection. OpenCode and Pi map unconfirmed cleanup diagnostics to that outcome, preserve it through result validation and WorkExecutor projection, and skip recovery for unknown work.
- Pi now executes through the Agent-turn capability in Workflow work, so it uses the same receipt-gated `WorkflowAgentSessionReporter` outbox path as OpenCode. It retains its declared validated `timeout` as the Pi turn duration.
- `WorkflowItemTranslator` represents unknown as an inbound observation, not a third `TaskReportStatus`. `WorkflowReportService` observes the original bound Agent execution before output, artifact, or follow-up handling, and only retires the Runner result after that durable acknowledgement.
- Runner loss first records `runner-disconnected` against the active Workflow Agent settlement. Only a stale Agent observation falls back to existing `runner-lost` failure behavior for non-Agent tasks and checks.
- Stable candidate checks passed after T-005 became idle: Runner Pi/executor/follow-up/cancel 27/27, Server settlement/reconnect/identity/stop 76/76, full architecture 69/69, and `npm run test:fast` with Workflow 175, CLI 1,793, Unit 2,625, Arch 69, Web 4,697, and Runner 1,610; all failed/skipped counts are zero and typecheck passed. The current migration source-tree baseline is files=838, digest=d6284aeebdec678bfca87e4ccd20dce7097c3cba6fda9bec3dfd17317d1c0365.
- T-005 completion verification reran `npm run test:fast` successfully on the final branch state: Workflow 175, Server Unit 2,625, Architecture 69, CLI 1,793, Web 4,697, and Runner 1,610 tests all passed with TypeScript typechecks. The concurrent Server projects emitted one transient MSB3026 file-copy retry and then passed.

## T-006

- `WorkflowReportService` now performs only pure report-shape lookup and translation. Runner reports return the dispatch's persisted `TaskRunId`; the service uses `TaskRunId + WorkId` only to reconstruct the declaration and does not inspect mutable task eligibility or Runner ownership. The serialized Workflow grain remains the sole winner fence over `TaskRunId + WorkId + authenticated Runner` and rejects any envelope mismatch before artifact binding, follow-up projection, output mutation, events, or advancement.
- The result wire reuses the existing dispatch `TaskRunId`; no new execution state or compatibility path is introduced. Checks retain their checks WorkId and no task attempt. Actual active, unknown, or blocked eligibility is decided only by `FindReportableTaskAttempt` inside the grain turn.
- The artifact migration contract is exercised through the complete EF migration chain and proves nullable `SourceUploadId` plus the filtered unique index. A direct grain-envelope mismatch spec proves that an upload is neither bound nor consumed before arbitration.
- The first focused run exposed two deterministic defects in the candidate tests/design: an Orleans copier-incompatible collection expression and all-history ambiguity for a WorkId legally reused across stages. The collection uses an explicit array and report-shape selection now gives the current stage precedence; no timeout, retry, sleep, skip, or serialization workaround was added.
- Pre-rebase evidence on commit `9b07b0c3fe7081b2bb8321572bc72086780bc1a6`: focused Server specs passed 87/87, the full settlement domain class passed 19/19, the authoritative migration-ledger method passed 1/1, full architecture passed 69/69, and `npm run test:fast` passed with Unit 2,627, Architecture 69, Web 4,697, and Runner 1,610 tests plus all other lanes and typechecks. All reported failed/skipped/not-run counts were zero. The Server-test inventory was files=840, digest=330b82bd7be35451f78fe8f059a355b4783fc7b2d71c324212c494ae69acd632.
- Remote master advanced from `820d45b7c916cb8a7bf09bfbcafc249dae187768` to `21f283d66936a0556d324fa1706f6aad1ec9ff94` after that evidence. Its only overlap is the three migration-ledger metadata files, so all inventory values and test evidence above are explicitly stale until the protected rebase, recomputation, and final rerun complete.
- Final pre-rebase T-006.2 candidate fixes a run-identity defect exposed by the exact late-result fence: task definition ids remain stage-scoped, while a collision-only allocator now guarantees run-wide unique `TaskRun.Id`/`WorkId` values. The first candidate keeps its established id and a later collision receives the first deterministic `.runN` suffix. Initial stage tasks, manual retry, runtime insertion, and recovery continuation all pass the complete run inventory to the same allocator.
- The cross-stage repeated-definition regression proves the first attempts receive distinct task/work identities and that an unknown Runner result targets only the named later attempt. The report grain now uses one exact `TaskRunId + WorkId + Runner` lookup carrying settlement state, rather than a second ambiguous all-stage scan.
- Compiled focused evidence on the frozen pre-rebase source: Unit discovery 26 and actual 35/35; settlement Spec discovery and actual 8/8; broad T-006 Spec discovery 96 and actual 105/105. All TRX failed/error/timeout/not-executed counters are zero. The broad TRX is `/tmp/mohist-589-t6-broad.mJ0Muv/results.trx`, SHA-256 `ba3221319b7f2520bbdd8e6be8a2256d58765102720e4ded9d36e3028b04a94d`.
- Authoritative Server-test inventory is files=840, digest=`ddc34db9314c7fe726f10db5b6507ab6133fb98df02b942218351bb6def06193`. Final full architecture TRX `/tmp/mohist-589-arch-final.rOIqOs/results.trx` has 69/69 passed with failed/error/timeout/not-executed zero and includes the authoritative ledger method as passed; SHA-256 `94ee72d8f0dc603fdbd844cd4d4ff5b7a1f24fd5a7950fc701424b05458d6fca`, wall 47.10s.
- The first final quick run overlapped another worktree's full verify because the preflight printed a match without failing closed; although it exited green, it is excluded from acceptance evidence. The corrected structured preflight found no external verify/test-fast process. That exclusive `npm run test:fast` exited 0 in 51.27s: Workflow 175, CLI 1,793, Unit 2,627, Architecture 69, Web 4,697, Runner 1,610, all reported failed/skipped counts zero, with typecheck exit 0. Log `/tmp/mohist-589-test-fast-exclusive2.hDjW9Q/output.log`, SHA-256 `37425555fe2acaf08e4682a7912d3dd31d391bc068f0609582ef76d630a90f00`.
- This evidence is deliberately pre-rebase. It must not be reused after replaying the checkpoint onto `origin/master`; final ledger values, compiled focused results, full architecture, and quick gate will be regenerated on the rebased commit before Workflow continuation.

### Post-rebase verification

- The protected 17-commit series was rebased from `820d45b7c916cb8a7bf09bfbcafc249dae187768` onto `21f283d66936a0556d324fa1706f6aad1ec9ff94`; the resulting T-006 checkpoint is `5882cb41cd6b72f6ce4513ee5c9a26262b2bd93f`. Per-commit stable patch ids excluding the three migration-ledger metadata files match 17/17. Recomputed intermediate ledger values account for all range-diff changes; no product or behavioral patch changed during conflict resolution.
- The final authoritative Server-test inventory is files=841, digest=`1a3ab7d0448f20b053e998562fd91caa3f1fce537bac86098967e26eecbd6a53`. The compiled authoritative ledger method passed 1/1 with failed/error/timeout/not-executed zero; TRX `/tmp/mohist-589-ledger-final-compiled.56riEN/results.trx`, SHA-256 `d682d083c6e35807ecb7b2dbbfbb30f3cb2b8fd21ec3a7acf392b1cba8c20864`. An earlier post-write invocation used the pre-write apphost and is excluded; rebuilding the exact final source made the same method pass.
- Final compiled focused results: Unit discovery 26 and actual 35/35; settlement Spec discovery and actual 8/8; Runner report/artifact tests 24/24. The broad ten-class Server set discovered 96 cases and passed 105/105 after theory expansion, with all TRX failed/error/timeout/not-executed counters zero; TRX `/tmp/mohist-589-broad-post-rebase.bUOa5t/results.trx`, SHA-256 `d619f92fe0448cde1df4cefe4714fbc9407f85bbb17b973a323ba30a1a5493fa`.
- Full architecture passed 69/69 with failed/error/timeout/not-executed zero in 48.35s; TRX `/tmp/mohist-589-arch-post-rebase.DXHpNA/results.trx`, SHA-256 `43e1f7908ec1544f688197eeec13116d9b124f7761d651bd14258badf05493b9`.
- A fail-closed process preflight found no external `verify` or `test:fast` process. The exclusive `npm run test:fast` exited 0 in 18.31s: Workflow, CLI, Unit, Architecture, and typecheck lanes all exited 0; Web passed 4,697/4,697 and Runner passed 1,610/1,610. Log `/tmp/mohist-589-test-fast-post-rebase.4Tswdr/output.log`, SHA-256 `e0399c556d126f183b1ef4af51d5a800660a9bb683ba92425ea14f6e80b38886`.

### Full-gate fixture convergence

- The first post-rebase `npm run verify` exited 1 after 208.13s. Workflow 175/175, CLI 1,803/1,803, Unit 2,627/2,627, and Architecture 69/69 passed; Server Spec passed 3,856 and failed 28 with zero skipped. Node workspace tests did not run because the .NET aggregate failed. The raw log is `/tmp/mohist-589-verify-post-rebase.B9XpXG/output.log`.
- The 28 failures were deterministic fixture drift exposed by the new fail-closed boundary, not a reason to weaken it: 19 Workflow-labelled Session tests omitted acknowledged execution identity, four batched `session.input` with other events, one old-binding recovery test used the Workflow route, two historical migration tests queried a future EF column, and one translator test had no running task attempt. Session/transcript tests now use the Session-owned runtime-event route while the dedicated execution-boundary specs continue to prove that the Workflow route rejects missing identity. Historical migration assertions use their target schema directly, and the translator fixture establishes the normal started-stage/assigned-run/running-task preconditions.
- The repaired ten-class Server set discovered 60 cases and passed 60/60 with failed/error/skipped/not-run zero in 8.814s; TRX `/tmp/mohist-589-spec-fixtures-final.1fF3LX/results.trx`, SHA-256 `98e0370be877a2e992c64a36e411123e18b95c817c1fa6a86e21a8941006f0d9`. The authoritative inventory is files=841, digest=`747fa82d192ff7fd08f28764bd11e86bb26f91c359405f24e2b16a7fe8480e77`; its compiled ledger method passed 1/1, and full Architecture passed 69/69 with all failure counters zero in 19.876s.
- After the external aggregate gate released the host, a fresh `npm run test:fast` exited 0 in 58s: Workflow 175, CLI 1,803, Unit 2,627, Architecture 69, Web 4,697, and Runner 1,610 all passed with zero failed/skipped and typecheck exit 0. A different worktree started `npm run verify` 41 seconds into this run and overlapped its final 17 seconds, so this green result is retained as diagnostic evidence but excluded from final aggregate acceptance. Log `/tmp/mohist-589-test-fast-fixture-final.AA4g2B/output.log`, SHA-256 `1daf2aad291da0509e8e43d9bcc2a8f6e5eb2ae222fd562a99893b81236d975c`.
