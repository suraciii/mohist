# Progress — issue-521

## Artifacts
- proposal.md — committed (115949b48)
- specs/ (3 capabilities) — committed (d982f62c5)
- design.md — committed (be17f3f2d)
- tasks.json — committed (88c4d1107)
- self-review.md — FAIL (F1-F5); not committed yet (regenerated by next self-review task)

## Fix-self-review pass (in progress)
Fixing F1-F5 from self-review.md in plan artifacts (not editing self-review.md):

- [x] F1 — lease lifecycle / concurrent-followup rejection reconciled
  - design D8 (Begin/Confirm collapsed; FollowupOperationInProgressException removed;
    per-turn leases; recovery guard = no non-terminal follow-up turn)
  - turn spec: "Follow-up is never rejected merely because a turn is in flight"
  - tasks T-001: concurrent-acceptance + per-turn-lease criteria
- [x] F2 — removed un-specced "drain" promise; Risks now references D9;
      removed resolved redelivery Open Question
- [x] F3 — design D9 (retry re-dispatches while queued; identity-only when
      executing/terminal); input spec scenarios; T-001 criterion
- [x] F4 — design D7 (observation = status/identity only); call spec requirement;
      T-002/T-003/T-004 de-dup criteria
- [x] F5 — design D8 capacity note + Open Question; T-001 capacity-rejection criterion

## Verification
- tasks.json valid JSON; DAG valid (T-002→T-001, T-003/T-004→T-002).
- All 3 specs format-compliant (4 requirements each; scenarios use ####).
- Next: commit fixes; await next self-review to regenerate self-review.md.

## T-001 implementation
- Follow-up acceptance is implemented as a synchronous grain transition that persists accepted input/turn records and per-turn accepted leases.
- Queued follow-ups join the queued turn; follow-ups received after execution starts create a separate queued turn. Runtime `session.input` and idle `session.activity` events progress the correlated follow-up turn.
- Same-key retry is identity-preserving and re-dispatches only queued turns. Retry resolution now uses the original input's operationId even when multiple inputs share one queued turn.
- Accepted leases are retained until terminal turn correlation rather than expiring at the delivery window, so long queued turns remain recoverable.
- Added grain specs for original operationId retry correlation and long-queued lease durability.
- Focused verification: `AgentSessionFollowupGrainSpecs` 31 passed; follow-up/recovery API classes 54 passed; server test assembly build passed.
- Final server spec verification: 3353 passed, 0 failed.

## T-002 implementation
- Both canonical and issue-scoped follow-up routes now return the shared `accepted`/`rejected`/`unknown` result envelope.
- Confirmed rejection is represented in the response data without creating an input; persistence uncertainty is represented as `unknown`; runner delivery failure does not undo an accepted input.
- The `Idempotency-Key` header continues through both route entry points into the grain accept command, and API coverage verifies same-key identity replay.
- Generic and unified session summaries expose status/identity-only input and turn observations. Input text and turn results remain absent from the observation DTO.
- Added API coverage for accepted queued follow-ups, rejected routes, same-key replay, and observation status; direct xUnit verification passed for all affected follow-up/history classes. A full-assembly run still has one unrelated pre-existing cancel/reset timing failure.

## T-003 implementation
- Web follow-up API clients always send an `Idempotency-Key`; callers generate one per submission and retain it when the response is `unknown`, so retrying the preserved composer input reuses the same key.
- Follow-up status is projected from observation input acceptance and turn status. The composer distinguishes `Accepted — pending` for accepted queued turns from `Executing`, with executing observation taking precedence over local queued delivery state.
- Follow-up text remains owned by the transcript; observation data is used only for identity and status, avoiding a second message rendering path.
- Unknown follow-up results remain retryable and keep the input text in the composer; confirmed outcomes retire the retry key.
- Verification: `npm run typecheck -w packages/web`, `npm run test:run -w packages/web` (5158 passed, 1 skipped), and `npm run check:fsd -w packages/web` passed.

## T-004 implementation
- `mo session followup` now accepts `--idempotency-key`, generates and prints a key when omitted, and reuses that key for its one network retry.
- Follow-up table output now shows the accepted/rejected/unknown outcome, input and turn identities, input acceptance, and turn status. An accepted result without an observed turn status uses the shared queued/pending interpretation; observed `executing` is preserved.
- Follow-up output does not render the submitted message text, keeping transcript text and observation status as separate display concerns.
- Verification: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj --no-restore` and `dotnet test packages/cli/tests/Mohist.Cli.Tests/Mohist.Cli.Tests.csproj --no-restore` passed (1445 tests).

## Verification repair
- `npm test` initially failed only in `Mohist.Server.ArchTests.CSharpTestFiles_MustStayWithinSizeRatchet` (34 passed, 1 failed); all other .NET test assemblies passed, including 3354 server specs.
- The failure was caused by the intended issue-521 follow-up/recovery specs exceeding the checked-in size-ratchet allowances: `AgentSessionFollowupGrainSpecs.cs` had no baseline at 34022 bytes, and `AgentSessionRecoveryGrainSpecs.cs` grew to 27135 bytes from its 25091-byte allowance. Updated the ratchet baseline to the verified sizes; no assertions or tests were weakened.
- Focused reruns passed: ArchTests 35/35, Web typecheck and tests 5158 passed/1 skipped, CLI tests 1445 passed, and the complete Server SpecTests run 3354/3354 passed.
- Final `npm test` passed all suites: Workflow Definition 175, CLI 1445, Server Unit 1535, Server Arch 35, Server Spec 3354, Web 5158 passed/1 skipped, Runner 1443.
- Follow-up verification rerun from the committed repair passed unchanged: `npm test` completed with Workflow Definition 175, CLI 1445, Server Unit 1535, Server Arch 35, Server Spec 3354, Web 5158 passed/1 skipped, and Runner 1443. No verification-only worktree changes were produced.

## Review fixes (in progress)
- F1: follow-up delivery is now claimed once per queued AgentTurn by the session grain. The dispatcher sends the ordered turn input snapshot, releases a failed delivery for same-key retry, and runtime event routes trigger the next eligible queued turn after terminal state.
- F2: queued/executing is display state only in the Web composer; it no longer blocks additional follow-ups. Matching `session.input` events now invalidate the session observation query so turn execution state refreshes.
- F3: accepted follow-up responses carry authoritative input acceptance and turn status. CLI renders only supplied status, without inventing a queued fallback.
- The Dispatcher is an API-layer SignalR adapter so Session remains a leaf domain. Verification passed: Web typecheck and test suite (5158 passed, 1 skipped); Web FSD check; CLI tests (1445 passed); Server ArchTests (35 passed); Server SpecTests (3354 passed). A temporary grain claim test held a fixture activation and was removed.

## Review fix pass 2 (in progress)
- F1/F2: dispatch claim now atomically advances the selected follow-up turn to executing. A rejected SignalR delivery releases the claim and restores queued, allowing the same idempotency key to retry; once delivery is accepted, later inputs are assigned to a subsequent turn rather than omitted from an immutable dispatch payload.
- F3: issue-session follow-up acceptance invalidates metadata/transcript observations, and transcript event subscriptions remain active for idle sessions so the following session.input event refreshes executing status.
- Verification passed: Web typecheck, FSD boundary check, focused follow-up transcript/data-source tests (29), the full Web suite (5159 passed, 1 skipped), and CLI tests (1445 passed). Server SpecTests rebuilt successfully but their child test process was interrupted by the execution environment even with explicit `DOTNET_ROOT`; rerun it in a stable shell before final integration.

## Review fix pass 3 (in progress)
- F1: initial Turn terminal state now schedules the same queued-follow-up dispatcher used by the request and runtime-event paths. The scheduler runs outside the Session grain, so it cannot block recovery commands while Runner delivery is pending.
- F2: a delivery claim keeps the AgentTurn queued; only `session.input` moves it to executing. Grain activation clears an unconfirmed queued claim, allowing the same idempotency key and operation id to re-attempt delivery through the Runner journal without duplicate runtime work.
- F3: issue-session metadata now projects follow-up input acceptance and Turn status/identity, matching generic and unified session observations without exposing input text.
- F4: generic Web follow-up feedback distinguishes accepted, rejected, and unknown server outcomes instead of reporting every HTTP 200 as sent.
- Focused verification passed: Server build; follow-up grain specs (34); generic follow-up API specs (18); issue metadata observation spec; initial-turn terminal dispatch integration spec; Web typecheck, FSD check, and agent-session API tests (42).
- Commits: `daa59e8b9` (Server lifecycle/retry/observation), `e71bd1707` (Web outcome feedback).
- Complete .NET verification passed: Workflow Definition 175, CLI 1445, Server Unit 1535, Server Arch 35, Server Spec 3359. Runner test CI passed 1443 tests. Web typecheck, FSD check, and full Vitest suite passed (5161 passed, 1 skipped).
- Root `npm test` remains non-green solely because the pre-existing, untouched `packages/web/src/widgets/session-transcript/model/useSessionTranscript.test.tsx` is 516 lines and fails the 500-line file-size guard. No verification command created a Git-visible product change.

## Review fix pass 4
- Verified the current tree already resolves the recorded F1-F4 review findings: initial-turn terminal delivery scheduling, queued dispatch recovery, issue-session input/turn observation, and three-valued generic Web outcome feedback.
- Follow-up composer outcomes are visible: confirmed rejection uses the destructive tone and unknown outcome uses the warning tone.
- Generated idempotency keys remain available in table output, but no longer precede machine-readable `--json` output for follow-up or agent launch.
- Verification passed: Web typecheck; Web tests (5163 passed, 1 skipped); Web FSD check; CLI tests (1446 passed); Server SpecTests (3359 passed).

## Review fix pass 5
- A Runner dispatch claim now seals the queued turn input list. A follow-up accepted while that claim is awaiting the Runner result starts the next queued turn instead of mutating an already-snapshotted payload.
- Updated the issue design and call spec with the claim boundary. The integration regression holds the first `ReceiveFollowup`, accepts a second idempotency key, then verifies distinct turns and delivery of the second payload after the first turn becomes terminal.
- Verification passed: `dotnet build packages/server/tests/Mohist.Server.SpecTests/Mohist.Server.SpecTests.csproj --no-restore -p:SkipWebBuild=true`; direct Server Spec execution for `AgentSessionFollowupGrainSpecs` and `GenericAgentSessionCanonicalFollowupApiSpecs` (44 passed).

## Review fix pass 6
- A sealed payload now remains sealed when an in-flight attempt is canceled, times out, or is reclaimed after activation. `Dispatching` only records the retryable attempt; a new input always starts a distinct turn after the payload has been sealed.
- Web follow-up status retains the response input acceptance and turn status until a later observation advances it, so a response reporting `executing` is not rendered as pending.
- JSON-mode `mo session followup` keeps stdout valid JSON and writes an auto-generated retry key to stderr for later reconciliation.
- Verification passed: Web typecheck, full Web suite (5165 passed, 1 skipped), Web FSD check, CLI tests (1469 passed), and Server SpecTests (3434 passed).

## Review fix pass 7
- Web now renders each accepted terminal follow-up turn state instead of replacing it with an invisible placeholder.
- CLI table output now includes unknown-result details and a same-key reconciliation instruction; JSON output preserves stdout while emitting the generated-key retry instruction on stderr.
- Verification passed: Web typecheck, focused composer tests (38 passed), full Web suite (5169 passed, 1 skipped), Web FSD check, and CLI tests (1470 passed).
