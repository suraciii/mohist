# Ralph Progress Log
Started: 2026-03-26T16:03:14.626Z

---

## T-001: Fix Task.issueId mapping bug in TaskRepo (2026-03-27T00:00:00.000Z)

**Status**: COMPLETED

**Changes**:
- `types/index.ts`: Replaced `issueNumber: number` with `issueId: string` in Task interface
- `db/task-repo.ts`: Fixed `rowToTask()` to map `row.issue_id` to `Task.issueId` (was hardcoded to 0)
- `db/task-repo.ts`: Fixed `create()` to return `issueId` from input data (was hardcoded to 0)
- `server/task-queue.ts`: Updated `enqueue()` signature from `issueNumber: number` to `issueId: string`, updated log messages
- `api/issues.ts`: Updated `taskQueue.enqueue()` calls to pass `issue.id` instead of `number`

**Acceptance Criteria Met**:
- Task interface has issueId: string instead of issueNumber: number
- TaskRepo.findById returns Task with correct issueId
- TaskRepo.create stores and returns issueId
- Build passes without type errors
- All 173 tests pass

**Learnings**:
- The issueNumber→issueId change cascaded to 4 files beyond types and repo. The TaskQueue enqueue calls in api/issues.ts needed updating to pass issue.id (UUID) instead of the parsed URL number.
- TaskQueue is temporary (deleted in T-002) but must compile in the interim.

---

