## Codebase Patterns

- The dead state we're removing is `TaskProgressMap` (a `Map<string, TaskProgressEntry>`) + `LoopProgress` declared in `entities/coder-session/model/types.ts`. These are **distinct** from `TaskProgress` (singular, `{ completed, total }`) which lives on `AgentActivity`/`AgentProgress` and is used by `SessionCard`, `activity-cards`, `PulseZone`, `CompactSessionCard`, and inside `useSessionTimeline.ts:344-345` via `progress.taskProgress`. The two share a name fragment but are unrelated types; only the `TaskProgressMap` is dead state. Future tasks grepping must distinguish.
- `TaskProgressPanel` is an `export function` from `SessionTimeline.tsx` with parameter type `{ tasks: Array<{ taskId; status; error? }>; loopProgress: LoopProgress | null }` — `LoopProgress` import must remain in `SessionTimeline.tsx` even after dropping it from `SessionTimelineProps` (the panel itself still uses it). `TaskStatusIcon` is local (no `export`) and only used inside `TaskProgressPanel`.
- `useSessionTimeline.ts` exports many shared types/helpers (`Round`, `RecoveryEvent`, `RecoveryStatus`, `PlanStep`, `PlanProgress`, `ContextHealthState`, `ContextHealthStatus`, `CompactionEntry`, `deriveToolCallTitle`, `reconstructRoundsFromEvents`, `BASE_PLAN_STEPS`, `toContextHealthStatus`, `mapLivenessToRecoveryStatus`) that are imported by `SessionTimeline.tsx`, `PlanProgressPanel.tsx`, and tests via the deep path `../model/useSessionTimeline`. When T-003 moves them to `session-timeline-reducer.ts`, it must re-export them from the hook module (design D7) to avoid changing import paths.
- Test fixtures for `SessionTimeline` consistently use `isLive={false}` followed by the dead-state props; future test maintenance should leave the props-positioning simple (no extra blank lines between unrelated attributes).
## T-001 — Remove dead state taskProgress/loopProgress

- Zero behavioral risk (panel never rendered — Map always empty). 15 SessionTimeline tests + 22 useSessionTimeline tests pass unchanged.
- Hook return shape now exactly `{ rounds, isLoading: false, isStreaming, recoveryStatus, planProgress, contextHealth }` — matches the spec's preserved-contract list in `session-timeline-events/spec.md` §"The hook return shape is preserved except for the removed dead state".
- Files touched:
  - `packages/web/src/widgets/coder-session/model/useSessionTimeline.ts` — drop `TaskProgressMap`/`LoopProgress` from import; drop two `useState` declarations; drop two return fields.
  - `packages/web/src/widgets/coder-session/ui/SessionTimeline.tsx` — drop `TaskProgressMap` from import (keep `LoopProgress` for `TaskProgressPanel`); drop two props from interface; drop two destructured params; drop `taskEntries` derivation; drop `currentStage === 'build' && taskEntries.length > 0` render branch. `TaskProgressPanel` + `TaskStatusIcon` retained.
  - `packages/web/src/widgets/coder-session/ui/SessionTimeline.test.tsx` — drop both props from all 15 render fixtures.
- Gotcha: a naive `replaceAll` removing the two props left orphan blank lines between `isLive={false}` and `recoveryStatus={null}`; cleaned up with a follow-up replace on `isLive={false}\n        \n        recoveryStatus={null}` → `isLive={false}\n        recoveryStatus={null}`.
- Future reducer-extraction task (T-003) targets the `model/useSessionTimeline.ts:344-345` `progress.taskProgress` block — that's the live `AgentActivity.taskProgress` (singular), a *real* state field initialized from `agentStatus`, NOT the dead `TaskProgressMap`. Don't conflate.
## T-002 — Add event-wiring integration tests before reducer extraction

- Added 17 new `dispatchAgentEvent`-driven integration tests under a new `describe('useSessionTimeline event-wiring integration', ...)` block in `packages/web/src/widgets/coder-session/model/useSessionTimeline.test.ts`. All 39 tests pass (22 pre-existing + 17 new); `npm run typecheck -w packages/web` green.
- Test pin current (pre-extraction) hook behavior. Each test is a regression net that the subsequent reducer extraction (T-003) must not break.
- Coverage map (all asserted via the resulting hook state — `rounds` / `recoveryStatus` / `planProgress` / `contextHealth`):
  - `plan_round_start`: appends a round, marks the matching step running, appends a new step when `roundType` is not in `BASE_PLAN_STEPS`, drops events with wrong `issueId` or wrong `coderSessionId`.
  - `plan_round_complete`: stamps `duration` + `verdict`, marks step `failed` on `FAIL` / `completed` on `PASS`, appends `auto-fix` + `re-self-review` pending steps on `self-review FAIL` (each at most once across repeated FAILs).
  - `coder_recovery_status`: sets `recoveryStatus` from event, appends to last round's `recoveryEvents`, clears `recoveryStatus` to `null` on `recovered` (and the round event is still appended).
  - `session.liveness`: `probing → recovering`, `running → recovered`, `failed → failed`; ordered `attempt` fallback `activeProbeVersion → satisfiedProbeVersion → probeVersion → 1`; `failureReason` used when set; `recoveryStatus` cleared on `failed`.
  - `compaction_event`: appends `CompactionEntry` to last round's `compactions`, resets `contextHealth.contextWindowUsed = contextWindowUsedAfter` and `contextHealth.status = null`.
  - `com.mohist.agent-session.context-compacted`: same compaction semantics as `compaction_event` but gates on `detail.issueId === issueId` only (not `isCurrentSessionEvent`); synthesizes a placeholder `Compaction` round when no round exists.
  - `com.mohist.agent-session.context-health-updated`: applies server-provided values verbatim (gates on `issueId` only).
  - `tool_call.started` → `updated` → `completed` (live path): `started` creates entry, `updated` with `state: 'timeout'` maps to `failed` via `toToolCallEntryState` and re-clones the in-place-mutated ref entry into the round's `toolCalls`; `completed` mutates the ref entry and replaces in the round's `toolCalls`. Round's `toolCalls` length stays at 1 (no duplicate appends on update).
  - `plan_session_update` coalescing: two events dispatched within 100ms → agentText stays empty until the setTimeout+rAF cascade fires (advance 120ms covers setTimeout(100) + rAF(16)) → agentText becomes the concatenation (`'hello world'`), proving a single `setRounds` flush consumed the batch.
  - `plan_session_update` rAF branch: after the first flush at t≈116, advancing to t≈316 + dispatching one more event takes the rAF branch (elapsed ≥ 100ms); the second delta is applied within 30ms (one rAF tick), not 116ms, proving the elapsed-since-last-flush branch selection.
- Codebase patterns and gotchas discovered:
  - **`vi.advanceTimersByTime` boundary for the rAF polyfill**: the test env polyfills `requestAnimationFrame` as `setTimeout(..., 16)` (see `tests/setup.ts:60`). With `vi.useFakeTimers()`, the rAF needs ~28-30ms of advance to fire — 20-25ms is not enough even though the underlying setTimeout delay is 16ms. The first-flush cadence test uses 120ms (setTimeout 100 + rAF ~16 + 4ms buffer); the rAF-branch test uses 30ms to assert the second flush happens in one rAF tick.
  - **`vi.runAllTimers` is unsafe here**: the hook uses `useQuery({ refetchInterval: 5000 })` for `getAgentStatus`, and `vi.runAllTimers` will keep firing the refetch setTimeout forever (the 10k-timer vitest cap is hit, throwing "Aborting after running 10000 timers"). Always use bounded `advanceTimersByTime` for this hook.
  - **Live `tool_call.*` handler does NOT call `deriveToolCallTitle`** — only the batched path inside `flushPlanBuffer` does. The live path stores `detail.title` verbatim, so the test asserts `title: 'read'` for the `tool_call.started` event with `title: 'read', rawInput: { file_path: ... }`. Future reducer-extraction (T-003) must preserve this asymmetry (or fix it deliberately as a separate change).
  - **`compaction_event` vs `com.mohist.agent-session.context-compacted` scoping differ**: the legacy `compaction_event` uses `isCurrentSessionEvent` (session-bound), while the new `com.mohist.agent-session.context-compacted` uses `detail.issueId === issueId` only. Tests use `coderSessionId: 'coder-1', acpSessionId: 'acp-1'` for the legacy path and `issueId: '123'` for the new path.
  - **`session.liveness` clears `recoveryStatus` AFTER the round's `recoveryEvents` append** — the test for `failed` asserts `recoveryStatus === null` AND the round has the `failed` event. If the implementation reordered these, the test would catch it.
  - **`afterEach(vi.useRealTimers)` is mandatory** in this test group: `beforeEach(vi.useFakeTimers())` + a `setTimeout`-using `useQuery` would otherwise leak fake timers to the next test in the file (the original `describe('useSessionTimeline context health events', ...)` block uses real timers).
- The reducer extraction (T-003) target surface is now test-pinned. Future T-003 must keep every assertion in this block green after the move to `model/session-timeline-reducer.ts`.
## T-003 — Extract pure event reducers to model/session-timeline-reducer.ts

- 9 reducers + shared types/helpers + dedup extracted to `packages/web/src/widgets/coder-session/model/session-timeline-reducer.ts`. Hook slimmed from 826 to 443 lines; the hook now is a pure event-wiring thin layer (session-scoping predicate + `dispatch(reducer, detail)`) for the nine directly-extractable events; `plan_session_update` → `flushPlanBuffer` → `scheduleFlush` + `handleToolEvent` + `liveToolCallMapRef` sharing left in the hook unchanged (D5).
- Hook re-exports the reducer module symbols via `export * from './session-timeline-reducer'` (D7) so `SessionTimeline.tsx` (`../model/useSessionTimeline`) and `PlanProgressPanel.tsx` (`../../coder-session/model/useSessionTimeline`) imports resolve unchanged. `toToolCallEntryState` (live `tool_call.*` state mapper used only inside the hook) is kept in the hook file rather than re-exported from the reducer module — it's not a "shared symbol".
- Reducer signature follows D2: `(prev: SessionTimelineState, detail: D, env: SessionTimelineEnv) => SessionTimelineState` where `env = { now, isoNow, randomId }`. State shape follows D3: full `{ rounds, planProgress, recoveryStatus, contextHealth }` in / out, with reference-equality preserved for unchanged slices so the hook's `if (next.slice !== snapshot.slice) setX(next.slice)` lines are no-ops for untouched slices.
- `mergeContextHealth(prev, next)` (D4) is exported from the reducer module and used inside the four context-health reducers (`usageUpdatedReducer`, `contextHealthUpdateReducer`, `compactionEventReducer`, `contextCompactedReducer`, `contextHealthUpdatedReducer`).
- Hook-side state snapshot: the hook holds four refs (`roundsRef`/`planProgressRef`/`recoveryStatusRef`/`contextHealthRef`) that mirror state on each render. `dispatch` reads the snapshot from these refs, calls the reducer, then conditionally applies the result via the existing setters. This matches the existing `setRoundsRef` pattern — the snapshot is fresh by the time the next render commits.
- 31 new co-located reducer unit tests in `model/session-timeline-reducer.test.ts` exercise each of the 9 reducers + `mergeContextHealth` (dedup returns prev when all four fields match; returns next on any change) + `toContextHealthStatus` + `mapLivenessToRecoveryStatus` + `BASE_PLAN_STEPS`. Tests use a fixed `env` (`now: 1_700_000_000_000`, `isoNow: '2024-01-01T00:00:00.000Z'`, `randomId: () => 'abc123'`) and assert reference-stability of the returned `prev` slices for `plan_round_start` (one slice untouched returns the same reference).
- All 4410 web tests pass (up from 4379 by 31 new reducer tests). T-002's 17 new integration tests + 22 pre-existing tests in `useSessionTimeline.test.ts` remain green unchanged. `npm run typecheck -w packages/web` green.
- Codebase patterns and gotchas discovered during extraction:
  - **`coder_recovery_status` reducer semantics**: the original closure's two `setRecoveryStatus` calls (set value, then clear on `recovered`/`failed`) collapsed into one reducer returning the final value (`null` for `recovered`/`failed`, the new value otherwise). The round's `recoveryEvents` append happens for all four status types — confirmed by the existing test that asserts `recoveryStatus === null` AND a `recoveryEvents` entry with status `recovered` after the second event.
  - **`session.liveness` always appends to `recoveryEvents` regardless of terminal status**: same atomicity pattern as `coder_recovery_status`. The reducer computes the liveness→recovery mapping + ordered attempt fallback + reason string atomically, returns both new `recoveryStatus` (null for terminal) and updated `rounds`.
  - **`compaction_event` and `com.mohist.agent-session.context-compacted` share an `appendCompactionToRounds` helper inside the reducer module** — both produce a placeholder `Compaction` round when `prev.rounds.length === 0` and append the `CompactionEntry` to the last round otherwise. The two reducers differ only in the entry id prefix (`compaction-` vs `compaction-domain-`) and in optional vs nullable string fields on the detail.
  - **`roundLabel ?? 'Round N+1'` fallback uses `??` not falsy-coalescing**: a `roundLabel: ''` detail keeps the empty string (matches the original behavior). Tests assert the fallback against `undefined`, not `''`.
  - **Reducer module state slice preservation**: each reducer constructs the returned state object explicitly, copying through the unchanged slices (`recoveryStatus: prev.recoveryStatus, contextHealth: prev.contextHealth` for reducers that don't touch them). The hook's `dispatch` uses reference-equality `!==` to skip no-op setter calls.
  - **T-002 timing-related tests are flaky in parallel test runs**: when running just `useSessionTimeline.test.ts`, all 39 pass. When running all 294 test files in parallel, the `plan_session_update uses the rAF branch when at least 100ms has elapsed` test occasionally fails (`expected 'first ' to be 'first second'`). The root cause is unrelated to this change — it's a parallel-test pollution / fake-timer scheduling race. The T-002 `progress.txt` notes already flagged `vi.advanceTimersByTime` boundary sensitivity; a future cleanup issue may need to isolate this test in its own file or use `vi.runOnlyPendingTimers` + microtask scheduling.
- Acceptance criteria checklist (all green):
  - `model/session-timeline-reducer.ts` exports all 9 reducers + shared types/helpers + `deriveToolCallTitle` + `reconstructRoundsFromEvents` + `mergeContextHealth` ✓
  - No React / `@tanstack/react-query` imports; no `requestAnimationFrame`/`window.setTimeout`/`Date.now` references; no module-level `let`/`var` ✓
  - Each reducer is pure `(prev, detail, env) => next` returning a new value without mutating `prev` ✓
  - Hook's nine subscription closures delegate via `dispatch(reducer, detail)`; the hook body retains only the scoping predicate + `env` construction + `setX(updater)` wiring ✓
  - `plan_session_update` → `flushPlanBuffer` → `scheduleFlush` and `handleToolEvent` stay in the hook; 100ms rAF/setTimeout cadence and `liveToolCallMapRef` set/get/in-place mutation unchanged ✓
  - `useSessionTimeline.ts` re-exports shared symbols via `export * from './session-timeline-reducer'` so existing import paths resolve ✓
  - `model/session-timeline-reducer.test.ts` exercises each exported reducer as a pure function with a fixed `env` (31 tests, including `mergeContextHealth` dedup and self-review FAIL step extension) ✓
  - T-002 integration tests + pre-existing `usage.updated` / `context_health_update` / `compaction_event` cases pinned by issue-247 remain green unchanged ✓
  - `npm run typecheck -w packages/web` green ✓
  - `npm run test:run -w packages/web` green (4410 passed, 1 skipped) ✓
