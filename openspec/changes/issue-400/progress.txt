## Codebase Patterns

- The desktop kanban row (`data-testid="kanban-board-row"`) is a `hidden md:flex flex-row gap-4 overflow-x-auto p-4 flex-1 min-w-0`. Its children are themselves flex items; `StageColumn` already bakes `flex-1` into its class string, so the three core columns absorb the reclaimed slot via `flex-1` without the row needing to know.
- `StageColumn`'s column classes are `flex flex-col min-w-[280px] max-w-[420px] flex-1 rounded-xl border bg-card/50 ...` — the raised `max-w-[420px]` cap keeps the core groups from stretching into unreadable wide blocks at large viewports.
- `filterCancelledFromColumns` is intentionally an identity function (documented seam); visibility is a render-time concern owned by `KanbanBoard.tsx` + `StageColumn`. Cancelled column carries full issue set through the pipeline so tab counts / counts / "n hidden" affordances stay stable.
- State `showCancelled` is local React state. `cancelledCount` (derived from `allColumns`, pre-filter, pre-toggle) is what mobile tab counts and the in-list Show/Hide link read. This is the contract tested by the "Mobile cancelled tab count is independent of showCancelled" describe.
- For IssueCard density assertions, the title is a bare `<h3>` (no `data-testid`) so the test helper `getTitleElement(card: HTMLElement)` queries `card.querySelector('h3')` and returns the typed node. Keep this helper local to the density describe — extending it elsewhere risks coupling unrelated tests to the title-element selector.
- The six-dimension test deliberately uses an **active idle** card (no agent running, health=Active) so all six signals are independently observable: `issue-number`, `<h3>`, `priority-chip`, `workflow-stage-badge`, `workflow-stage-progress`, and `rerun-button`. A *running* card would fold stage+progress into the status pill and hide the rerun button, collapsing the independent signal set.
- Existing `data-testid` anchors cover the full signal set: `issue-number`, `priority-chip`, `status-pill`, `workflow-stage-badge`, `workflow-stage-progress`, `rerun-button`. The title `<h3>` is queried by tag. No new testids are needed for card-density assertions.

## T-001 — Desktop Cancelled column default-collapse to compact stub reclaiming space for core groups

- Replaced the showCancelled=false path of the Cancelled column: instead of a full `StageColumn` with `bodyHidden=true` + `emptyState="Cancelled issues hidden — N not shown"`, the desktop row now renders a lightweight collapsed stub (`data-testid="cancelled-collapsed-stub"`, `w-[140px] shrink-0`, `flex flex-col`) with the count chip + a single expand affordance (`data-testid="cancelled-collapsed-stub-expand"`).
- Removed `bodyHidden` / `emptyState` from the StageColumn instantiation in `KanbanBoard.tsx`. The StageColumn's `bodyHidden` / `emptyState` props remain on the API (not removed) so we don't churn callers outside the cancelled-render path; no other caller uses them today.
- Raised the column max-w cap in `StageColumn.tsx`: `max-w-[320px]` → `max-w-[420px]`. Min-w stays at `min-w-[280px]`. Together with the row's `flex-1` per column, the three core groups flex to fill the reclaimed Cancelled slot up to 420px without overflowing a common desktop width.
- The stub itself is `w-[140px] shrink-0` — the three core columns get the reclaimed space via flex-1 / max-w-[420px] / min-w-[280px].
- Preserved all existing testids for the expanded state (`stage-column-cancelled`, `cancelled-toggle`); new stub uses its own testids (`cancelled-collapsed-stub`, `cancelled-collapsed-stub-expand`) per design D6.
- The `kanban-board-row` retains `overflow-x-auto min-w-0` as the escape hatch; the `kanban-board-containment.test.tsx` is left untouched and still passes.
- Updated the cancelled-toggle bidirectional regression test in `kanban-board-query.regression.test.tsx` to assert the new default collapsed-stub path: stub renders with count, expand affordance click restores full `stage-column-cancelled` with `cancelled-toggle` reading 'Hide cancelled', and clicking 'Hide cancelled' collapses back to the stub (and `stage-column-cancelled` is gone).
- The mobile tab badge (and `cancelledCount`) reads from `allColumns` (pre-filter, pre-toggle), so the existing mobile tab count independence test (`Mobile cancelled tab count is independent of showCancelled`) remains untouched and still passes — mobile layout is unaffected.
- `npm run typecheck -w packages/web` passes; `npm run test:run -w packages/web` passes (4481 tests, 1 skipped pre-existing).

## T-002 — Codify six-dimension card-density invariant in regression tests

- The IssueCard source already renders all six decision dimensions; this task added purely additive regression coverage to `IssueCard.test.tsx`. No production code change to `IssueCard.tsx`.
- Added two tests inside the existing `IssueCard - stage folds into StatusPill instead of stacking` describe: one for the **blocked** fold (asserts `status-pill` present with `'Blocked · Build 2/5'`, no `workflow-stage-badge`, no `workflow-stage-progress`, and `issue-number` / `priority-chip` / `<h3>` title remain visible); one for the **approval-awaiting** fold (same shape with `'Approval · Plan 2/5'`).
- Added a new `IssueCard - six-dimension card-density invariant` describe with five tests:
  1. **All six dimensions exposed** for an active idle card (priority + workflow stage + progress, no agent running). Status indicator is null → no `status-pill`, but `workflow-stage-badge` carries the stage, `workflow-stage-progress` shows the fraction, and `rerun-button` is the health signal (idle/queued/rerunnable).
  2. **Active card with no status pill**: same shape but no progress; `workflow-stage-badge` stands alone as the stage dimension.
  3. **Title has bounded line clamp**: assert `style.webkitLineClamp === '2'`, `webkitBoxOrient === 'vertical'`, `overflow === 'hidden'`, `display === '-webkit-box'` on the `<h3>` title element — locked against future Tailwind/utility churn.
  4. **Sibling compactness**: render four cards inside a column mock and assert each card's `<h3>` keeps the same four clamp properties and full title text, locking the "cards do not push each other out of compact density" invariant.
  5. **Non-active card preserves number/title/priority**: a blocked card with `number: 211` and `priority: 'p1'` still exposes the issue number `#211` and `P1` chip alongside the folded status pill.
- `npm run typecheck -w packages/web` passes; `npm run test:run -w packages/web` passes (4488 tests, 1 skipped pre-existing; +7 new tests vs. the prior T-001 baseline of 4481).

## Gotchas

- The StageColumn class string is on a single line in the source — editing it means re-matching the full string, since conditional whitespace inside the templated class is easy to mistype.
- The DOM-trees-always-render split (desktop `hidden md:flex` + mobile `md:hidden`) means there's always both a desktop row and a mobile column in the document, even if jsdom-class-hidden. Tests that target the desktop board by testid (`kanban-board-row`, `stage-column-*`) still locate the right elements because each testid is rendered exactly once.
- `filterCancelledFromColumns` is an identity seam by design; do not move visibility decisions back into the model — that would break tab counts (already covered by the "cancelled tab count is independent of showCancelled" test).
- jsdom does NOT expose `WebkitLineClamp` via `getComputedStyle()` (it is a CSS-only runtime property, not on `CSSStyleDeclaration.prototype`). Card-density tests read `style.webkitLineClamp` directly from the inline `style` prop on the `<h3>` (set by `IssueCard.tsx:381-386`), not from computed style.
- For layout-class assertions (`.md\\:hidden.flex.overflow-x-auto.snap-x` etc.), `document.querySelector` returns the FIRST match — and `md:hidden flex` matches both the inner stage-tab strip AND its outer `md:hidden flex flex-col flex-1` wrapper. When the contract cares about a specific element, prefer testid-based parent lookup over class-based selectors.

## T-003 — Lock first-screen filter/search/sort reachability in regression tests

- Added `packages/web/src/widgets/kanban-board/ui/kanban-board-filter-reachability.test.tsx` with a new top-level describe `First-screen filter/search/sort reachability` (no production code change — the FilterBar already satisfies the spec; this task locks it with tests).
- Desktop section (4 tests):
  1. **Controls present in single row**: scopes to `.hidden.md\:flex.flex-wrap` and asserts `search-input`, three priority chips (`priority-chip-p0..p2`), `label-chip`, and all three sort buttons (`sort-priority`, `sort-number`, `sort-updated`) are inside that row. p3/p4 chips are also rendered (ALL_PRIORITIES = ['p0','p1','p2','p3','p4']) but the test asserts p0..p2 because "at least one priority-chip" was the bar in the acceptance criteria; p0..p2 keeps the assertion focused on the canonical three.
  2. **Single flex-wrap row**: class list contains `hidden md:flex flex-wrap` — locks the desktop single-row contract.
  3. **DOM order #1**: `kanban-board-root` contains both the desktop FilterBar row and `kanban-board-row`, and `compareDocumentPosition(filterBar, boardRow) & DOCUMENT_POSITION_FOLLOWING` is truthy — board row follows FilterBar in document order within `kanban-board-root`.
  4. **DOM order #2**: same DOM-position assertion as #3, kept as a focused single-intent test for readability.
- Mobile section (5 tests):
  1. **Search + toggle in mobile controls**: scopes to `.md\:hidden.px-3.py-2` and asserts `search-input` and `mobile-filter-toggle` are inside that section.
  2. **Mobile controls section is md:hidden**: class list contains `md:hidden` and does NOT contain `md:flex` (pure-CSS mobile-only split per design D2).
  3. **Panel revealed as a panel** (not inline beside card list): clicking `mobile-filter-toggle` renders `mobile-filter-panel`; the panel class list contains `space-y-2` and does NOT contain `inline-flex`/`inline-block`; the panel precedes the stage-tab strip in DOM order (`compareDocumentPosition(panel, stageTabs) & DOCUMENT_POSITION_FOLLOWING`).
  4. **FilterBar and stage-tab strip are distinct siblings**: both inside `kanban-board-root` and not nested inside each other; `filterBarRoot.parentElement === root`; `stageTabs.parentElement !== filterBarRoot`.
  5. **No shared flex-row side-by-side**: the FilterBar wrapper and the stage-tab strip are direct siblings under `kanban-board-root`; their class lists contain no `flex-row` (or `flex-row-*`) variant; the common ancestor of the two is `kanban-board-root` itself (computed via a small `findCommonAncestor` helper).
- `mobile-stage-tab-backlog` was used as the anchor for the stage-tab strip selector (its parent is the snap strip on line 577). The class `.md\:hidden.flex.overflow-x-auto.snap-x` is ambiguous — the outer mobile container at line 576 (`md:hidden flex flex-col`) also matches the `md:hidden flex` substring — so the testid-based parent lookup is more stable.
- The mobile filter controls class locator (`.md\:hidden.px-3.py-2`) is unique in the document (only line 330) — confirmed via grep: the only two `md:hidden` matches in the file are line 330 and line 576.
- The `getFilterBarRoot` helper walks up from the desktop FilterBar row to `[class*="bg-background"][class*="border-b"]` (which matches the wrapper at line 296), so DOM-order assertions compare the wrapper nodes, not the inner desktop-only/mobile-only rows.
- No new testids introduced (per design D6: preserve existing anchors; only the cancelled stub from T-001 got new testids).
- `npm run typecheck -w packages/web` passes; `npm run test:run -w packages/web` passes (4497 tests, 1 skipped pre-existing; +9 new tests vs. the T-002 baseline of 4488). All 143 kanban-board tests pass.

## T-004 — Lock mobile board navigation non-overlap in regression tests

- Added `packages/web/src/widgets/kanban-board/ui/kanban-board-mobile-non-overlap.test.tsx` with a new top-level describe `Mobile board navigation non-overlap` (no production code change — the mobile layout already satisfies the spec; this task locks it with tests).
- The board's pure-CSS `md:` split renders BOTH desktop and mobile trees in the same jsdom document, so all queries against `issue-card` / `rerun-button` MUST be scoped to the mobile board container (`.md\:hidden.flex.flex-col`) via `within(mobile, …)`. Using the unscoped `screen.getAllByTestId('issue-card')` would return both desktop and mobile cards and break the count/ownership assertions (caught by the first test run; one assertion failed before scoping).
- The mobile board container class `.md\:hidden.flex.flex-col` is unambiguous (the only other `md:hidden flex` in the file is `.md\:hidden.px-3.py-2` on the mobile filter controls at line 330; grep confirmed there are no other matches). The stage-tab strip `.flex.overflow-x-auto.snap-x` and the card list `.flex-1.overflow-y-auto` are each unique in the document once you anchor on the right parent.
- Stage tab strip section (3 tests):
  1. **All four tabs present**: scopes to `.md\:hidden.flex.flex-col` and asserts `mobile-stage-tab-backlog`, `-in_progress`, `-done`, `-cancelled` are all inside that container.
  2. **Tabs are direct children of the snap strip**: pulls the parent of `mobile-stage-tab-backlog` (the strip), checks its class list contains `flex overflow-x-auto snap-x`, and asserts `querySelectorAll('[data-testid^="mobile-stage-tab-"]')` returns exactly 4 inside the strip — locks "all four tabs are siblings in the snap scroll strip".
  3. **Strip is a direct child of the mobile container**: `strip.parentElement === container` — locks the strip's position in the flex-col stack.
- Sibling / vertical-stack section (5 tests):
  1. **Strip classes**: contains `border-b` and `shrink-0` — locks the "thin header strip" contract.
  2. **Card list classes**: contains `flex-1` and `overflow-y-auto` — locks the "fills remaining vertical space, scrolls vertically" contract.
  3. **Distinct siblings**: `strip.parentElement === container` AND `cardList.parentElement === container` AND neither contains the other — locks non-nesting.
  4. **DOM order**: `strip` precedes `cardList` via `compareDocumentPosition` & DOCUMENT_POSITION_FOLLOWING — locks vertical stacking direction.
  5. **No shared flex-row**: the mobile container's class list contains `flex-col` and no `flex-row(-*)`; the common ancestor of strip and cardList (computed via the `findCommonAncestor` helper from T-003) is the container itself.
- Card navigation / unobstructed tap target section (4 tests):
  1. **Card is an anchor with issue number in href**: scopes to the mobile container, asserts each `issue-card` is `tagName === 'A'` and that hrefs for both #211 and #212 are present (asserted via `hrefs.some(...)` rather than positional index — `sortIssues` is default `priority` then `updatedAt desc` then `number desc`, so within a same-priority same-updated bucket the DOM order is number desc, not input order; the positional-index pattern from IssueCard.test.tsx would have flaked on this).
  2. **Card is not nested inside the filter bar**: walks the document for `[class*="bg-background"][class*="border-b"]` (the FilterBar wrapper from line 296) and asserts it does not contain the card.
  3. **Card is not nested inside the stage tab strip**: `strip.contains(card)` is false, plus an explicit parent walk that asserts `cur !== strip` at every level.
  4. **Card is not pointer-events:none**: `getComputedStyle(card).pointerEvents !== 'none'` — locks the "tap target is unobstructed" contract (the IssueCard source does have a `pointer-events-none` overlay for cancelled cards; this test uses non-cancelled cards to assert the default tap-target contract).
- Primary board action section (5 tests):
  1. **rerun-button renders for an interrupted card**: scopes `within(cardList).getByTestId('rerun-button')` (per IssueCard.tsx:422-451 the rerun-button is rendered when `isInterrupted` OR a non-cancelled/non-done/non-draft backlog card with a workflow stage).
  2. **rerun-button renders for a rerunnable active backlog card**: `health: Active, status: Backlog, workflowStage: 'plan'` exercises the non-interrupted rerun path.
  3. **rerun-button is inside its own card surface, not a sibling's**: maps `cards[i].contains(buttons[i])` — DOM order is deterministic when priority and updatedAt tie (number desc), so the index alignment is stable. Belt-and-braces: also asserts both buttons are inside the card list.
  4. **rerun-button is not inside the stage tab strip**: `strip.contains(button)` is false.
  5. **rerun-button is clickable (not disabled)**: `expect(button).not.toBeDisabled()` plus `cardList.contains(button)` — locks the "primary action is reachable" contract.
- Existing mobile regression tests (cancelled tab count, mobile filter disclosure from T-003 and the prior kanban-board-query.regression tests) remain untouched and still pass — the new test file is purely additive and shares the same `vi.mock` setup as the existing colocated tests.
- `npm run typecheck -w packages/web` passes; `npm run test:run -w packages/web` passes (4514 tests, 1 skipped pre-existing; +17 new tests vs. the T-003 baseline of 4497).
