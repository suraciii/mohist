## Codebase Patterns
- Ordinary Slack DM ingress persists an inbox route, launches through `IAgentLauncher.LaunchConnectionAsync`, sets the route Session and `SlackDmSessionMappingStore`, then uses `AcceptFollowupAsync` plus `AgentSessionFollowupDispatcher` for later messages.
- `AgentSlackExecutionContext` is built from durable Session input provenance and the Runner injects the pinned collaboration Skill for every Slack execution. Manager uses the synthetic `__mohist_slack_manager__` project and Enrollment id as the Slack owner/connection identity.
- Inbox identity is `(ConnectionId, SlackMessageIdentity)` and Manager uses Enrollment id as `ConnectionId`; the mapping stores workspace and actor facts and is read with a workspace-bound lookup for Manager ingress.
- Slack terminal delivery must finalize liveness only. Assistant output is not a reply source; Manager terminal envelopes now omit the extracted `assistantText` value entirely.

## T-001 Decisions
- Manager prompts are passed unchanged as ordinary natural-language Agent input. Manager-specific JSON/protocol instructions and synthesized acknowledgement/fallback text are removed.
- Manager initial context dispatch refs follow the ordinary `slack:{sessionId}:{inputId}` convention; follow-up refs use the durable Session follow-up `OperationId`.
- Manager reply-anchor extensions are append-only `projectId` and `ownerKind` fields. Existing ordinary Connection anchors remain compatible; Manager anchors explicitly carry synthetic project and `manager` owner values.
- The retired `SlackManagerToolTurnProcessor`, `SlackManagerToolExecutor`, and execution-fence store are retained only as unregistered compatibility source; no runtime ingress or terminal path resolves them.
- `SlackStatusProjection.FinalizeLivenessAsync` now closes receipt state and adds terminal reaction even if no progress row was persisted, preserving silence as a valid Agent outcome.

## Verification Notes
- `progress.txt` was absent at task start.
- Existing Manager tests that asserted the retired JSON protocol were replaced with natural-language, replay, replacement, and no-terminal-text coverage.
- A replacement inbox route must be updated conditionally from the mapped runtime Session to the deterministic replacement Session; unconditional `SetRouteSessionIdAsync` correctly rejects stale routes but was too strict for this recovery transition.
- Manager status projections explicitly select `OwnerKind.Manager`; Manager reaction payloads have no Server-authored fallback text, preserving Agent-only reply ownership.

## Verification
- `dotnet build packages/server/src/Mohist.Server/Mohist.Server.csproj --no-restore -p:SkipWebBuild=true` passes.
- Focused Server specs pass: Manager conversation/replacement, terminal delivery, ordinary Slack reply-anchor, DM mapping, and managed-bot admission classes (20 tests).
- Focused Server unit classes pass: built-in catalog and Slack execution context contracts.
- `npm run build -w packages/runner` and `npm run build -w packages/web` pass.
- `npm run test:run -w packages/runner -- src/runtime/slack-execution-context.test.ts` passes (15 tests).

## T-002 Decisions
- `ManagerCapabilityCatalog` lives in the shared `Mohist.Workflow.Definition` assembly so the CLI preflight and Server admission resolve the same logical capabilities and HTTP route shapes. The catalog contains only workspace status, Agent/Connection inspection, Agent create-or-mount, access policy, enable/disable, diagnostics, and owner claim/transfer.
- Manager mode is process-scoped through `MOHIST_MANAGER_MODE=1` (the Runner launcher can provide this later) or the explicit `--manager` flag. The CLI strips the flag before parser dispatch, rejects unlisted commands before HTTP, and adds only a non-secret `X-Mohist-Manager-Mode` marker to requests.
- `ManagerCapabilityAdmissionMiddleware` runs after authentication and before endpoint filters, so a Manager-marked request to setup, credential, recovery, binding-removal, permanent-delete, arbitrary API, or any other unlisted route is rejected before Project or resource lookup. Unmarked operator/Web requests retain their existing route behavior.
- Existing `mo slack view` is the diagnostic projection, so its catalog capability is `connection.diagnostics`; `slack diagnostics` is an explicit alias. `slack create` mounts an existing Agent through `SlackManagerApplicationService` or creates the Agent through the existing Agent route before mounting it.
- Owner claim and transfer responses are authoritative but one-time values are protected: Manager mode suppresses `code`, `claimCode`, `value`, and `token` from CLI output while preserving non-secret expiry/bot projection. Ordinary operator output is unchanged.

## T-002 Verification
- Complete CLI suite passes: `dotnet test packages/cli/tests/Mohist.Cli.Tests/Mohist.Cli.Tests.csproj --no-restore -p:UseSharedCompilation=false` (1,950 tests).
- Complete Server unit suite passes: `dotnet test packages/server/tests/Mohist.Server.UnitTests/Mohist.Server.UnitTests.csproj --no-restore --no-build` (3,115 tests).
- Complete Server spec suite including direct Manager admission coverage passes: `dotnet test packages/server/tests/Mohist.Server.SpecTests/Mohist.Server.SpecTests.csproj --no-restore --filter FullyQualifiedName~SlackManagerCapabilityAdmissionSpecs` (3,706 tests; the current test platform executes the full assembly).
- Server build passes: `dotnet build packages/server/src/Mohist.Server/Mohist.Server.csproj --no-restore -p:SkipWebBuild=true`.

## T-003 Decisions
- Manager credentials are issued only while assembling the Server poll response from a Manager Slack anchor. The durable `WorkDispatch`, AgentJob ledger, Session state, Slack context, and Runner journal never gain a credential field.
- `ManagerExecutionLeaseStore` retains only SHA-256 hashes plus immutable origin, actor/Enrollment, execution, epoch, scope, issue, expiry, and active metadata. `ManagerDeploymentEpoch` invalidates the store on graceful shutdown and rejects older values after a restart; store or epoch unavailability fails closed.
- The Runner consumes the response-only grant into a per-work in-memory wrapper. Its private broker exposes the matching management or reply value only to the scoped `mo` launcher; generic commands inherit a broker locator and private PATH but no bearer. Pi uses the pinned SDK Bash operations hook, and Manager OpenCode turns create a separate server/client process with the same non-secret boundary environment.
- Manager CLI transport selects `MOHIST_MANAGER_MANAGEMENT_TOKEN` or `MOHIST_MANAGER_REPLY_TOKEN` by request path and never falls back to an operator credential. Runner capability registration/gating requires grant, epoch, broker, Pi, isolated OpenCode, and redaction support.
- Epoch changes abort Manager turns and dispose brokers/runtimes; acknowledged AgentJob reports revoke both lease records for the work identity. Final Runner outputs, runtime events, task logs, exceptions, and terminal messages are passed through the Manager redactor.

## T-003 Verification
- Server, Runner, and CLI projects build.
- Runner broker/transport tests pass: `npm run test:run -w packages/runner -- src/runtime/manager-execution-boundary.test.ts src/server/connection.test.ts`.
- Full Runner suite passes: 156 files / 1,683 tests, including host lifecycle, recovery, reporting, runtime, cleanup, and new broker tests.
- Full Server unit suite passes: 3,120 tests; full Server spec suite passes: 3,706 tests; CLI suite passes: 1,950 tests.
- A compatibility seam was preserved: `RunnerHost` continues to call `ServerConnection.poll`; the connection stores its grant-bearing view separately so existing test fakes and ordinary dispatch journals never receive grant fields.
- Lease-backed HTTP requests reauthenticate the current Slack actor and active/ready Enrollment through `ManagerActorAccessDecider` before reaching an endpoint; invalid current state fails closed with a non-secret response.

## T-004 Decisions
- Manager CLI reply sends are a separate `manager.reply` capability. Manager mode routes `mo slack message send` to `/api/slack-manager/reply`; the existing project-scoped Connection reply route remains unchanged for ordinary callers.
- Manager reply validation is lease-bound and durable: the route compares the requested conversation/thread and optional full-anchor fields with the immutable lease origin, then requires the matching active Enrollment, accepted Manager inbox origin, and Enrollment/workspace/actor/Session DM mapping before any outbox write.
- `SlackManagerReplyAnchor` carries the validated origin into `SlackOutboxStore.EnqueueManagerAgentReplyAsync`. The outbox uses the synthetic Manager project, `OwnerKind.Manager`, Enrollment id, and the stable origin-derived progress dispatch ref. A pending progress row is promoted in place; repeated sends return the same terminal row without appending text. A missing progress row receives a Manager-owned terminal row so fast replies still share terminal convergence.
- Manager ingress persists receipt before Session dispatch and queues working state before invoking the launcher/follow-up path. The working projection queues receipt removal first, so fast completion and adapter redelivery cannot leave a receipt as the final visible state or show working before the receipt transition.
- Terminal reaction mutations are idempotent by stable phase dispatch refs. Terminal payloads carry non-user-visible `terminalStatus` metadata so `unknown` remains distinguishable for operator/recovery handling; terminal delivery still never reads assistant text or authors fallback text.

## T-004 Verification
- Full Server spec suite passes: `dotnet test packages/server/tests/Mohist.Server.SpecTests/Mohist.Server.SpecTests.csproj --no-restore` (3,709 tests), including Manager route anchor validation, reply promotion/deduplication, fast completion, terminal outcomes, restart/recovery, and ordinary Slack regressions.
- Full Server unit suite passes: `dotnet test packages/server/tests/Mohist.Server.UnitTests/Mohist.Server.UnitTests.csproj --no-restore --no-build` (3,120 tests).
- Full CLI suite passes: `dotnet test packages/cli/tests/Mohist.Cli.Tests/Mohist.Cli.Tests.csproj --no-restore -p:UseSharedCompilation=false` (1,950 tests), including dedicated Manager reply route selection.
- `mohist-slack` adapter suite passes: `npm run test:run -w packages/mohist-slack` (7 files, 76 tests).
- Server build passes: `dotnet build packages/server/src/Mohist.Server/Mohist.Server.csproj --no-restore -p:SkipWebBuild=true`.

## Verify Repair
- The failed `npm run verify` output was recovered from the workflow result: one Server architecture test and one Server unit test failed; all 3,709 Server specs and 1,950 CLI tests passed.
- The architecture failure was a real T-004 regression: `SlackManagerIngressRoutes` directly injected `MohistDbContext` and read inbox, Session mapping, and enrollment rows. Those origin checks now live in the transactional `SlackOutboxStore` reply enqueue path, preserving validation-before-write while respecting the API/data boundary.
- The credential failure was a real restart-epoch reporting defect: `ManagerDeploymentEpoch.Advance()` revoked leases before validation, so an old grant was reported as `manager_credential_invalid` instead of `manager_epoch_changed`. Validation now distinguishes retained revoked metadata from unknown credentials and reports the deployment epoch transition correctly.

## Final Verification
- Focused repairs pass: Server architecture 51/51, Server unit 3,120/3,120, Manager reply integration, Runner typecheck, Runner Manager boundary and Slack context tests (17/17), and the Runner host readiness test (1/1).
- The first post-repair full Server spec run also exposed one unrelated Runner recovery-spec race; the exact named test passed 1/1 in isolation and was not changed.
- The clean `npm run verify` gate passes all tracks: Workflow Definition 178, CLI 1,950, Server Unit 3,120, Server Architecture 51, Server Spec 3,709, Web, Runner, and `mohist-slack`, plus docs, format, file-size, script, and boundary checks.
- File-size ratchet failures were resolved with behavior-preserving partial/module splits for issue-618 growth; no baseline or assertion was weakened. Verification created no Git-visible changes, and the final worktree is clean.
