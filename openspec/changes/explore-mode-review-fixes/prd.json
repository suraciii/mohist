{
  "project": "mohist",
  "description": "Fix bugs and issues found in explore-mode code review: security, message persistence, frontend styling, error handling, and code quality improvements.",
  "tasks": [
    {
      "id": "T-001",
      "title": "Fix path traversal boundary check in read-file tool",
      "description": "Fix the startsWith check in read-file.ts to include path separator boundary, preventing /projectA from matching /projectAB/file.txt.",
      "acceptanceCriteria": [
        "read-file.ts path check uses startsWith(projectPath + path.sep) || resolved === projectPath",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": "Simple one-line fix in packages/cli/src/tools/read-file.ts line 34."
    },
    {
      "id": "T-002",
      "title": "Add try/catch to grep-tool walkFiles",
      "description": "Add try/catch around readdirSync in walkFiles function in grep-tool.ts, consistent with globWalk in glob-tool.ts. Catch permission errors and continue silently.",
      "acceptanceCriteria": [
        "walkFiles has try/catch around readdirSync",
        "Permission-denied subdirectories do not crash the grep",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": "Follow the same pattern as globWalk in glob-tool.ts lines 54-57."
    },
    {
      "id": "T-003",
      "title": "Refactor create-issue-tool to inject eventBus via context",
      "description": "Add eventBus to CreateIssueToolContext and pass it from createExploreRoutes, removing the direct singleton import. Update server/index.ts wiring.",
      "acceptanceCriteria": [
        "create-issue-tool.ts imports eventBus type, not singleton",
        "CreateIssueToolContext includes eventBus field",
        "createExploreRoutes passes eventBus to the tool context",
        "eventBus.emit call uses context.eventBus instead of imported singleton",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": "Follow add-comment.ts pattern which receives eventBus via context."
    },
    {
      "id": "T-004",
      "title": "Fix explore-agent return type",
      "description": "Replace the 'any' return type of runExploreAgent with the actual streamText return type from the Vercel AI SDK.",
      "acceptanceCriteria": [
        "runExploreAgent return type is not 'any'",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": "Use ReturnType<typeof streamText> or import the specific type from the AI SDK."
    },
    {
      "id": "T-005",
      "title": "Fix explore-service addMessage session validation and remove dead code",
      "description": "Add session existence check in ExploreService.addMessage. Remove unused touch() method from ExploreSessionRepo.",
      "acceptanceCriteria": [
        "addMessage throws or returns error if session does not exist",
        "ExploreSessionRepo.touch() method is removed",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": "getSession() already returns null for non-existent sessions. Use it for validation."
    },
    {
      "id": "T-006",
      "title": "Fix explore API message persistence timing and issue ID",
      "spec": "specs/http-api/spec.md",
      "description": "Move user message save to after agent succeeds, but include user message in agent history before calling agent. Capture issue number from create_issue tool result string via regex instead of re-reading from DB. Send issue number (not UUID) in done event.",
      "acceptanceCriteria": [
        "User message is added to agent history before calling agent",
        "User message is NOT persisted to DB before agent runs",
        "Both user and assistant messages are persisted together only after successful stream completion",
        "createdIssueId captured from tool-result when tool name is create_issue",
        "Fallback to DB read if regex parse fails",
        "done event issueId contains issue number string",
        "On stream error or DB write failure, user message is not persisted and error is returned",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": "The create_issue tool returns 'Issue #${issue.number} created...'. Use /Issue #(\\d+)/ regex. Key file: packages/cli/src/api/explore.ts. Must adjust history building: add user message to history array in memory before agent call, then persist both messages after success."
    },
    {
      "id": "T-007",
      "title": "Fix frontend error handling and typography",
      "spec": "specs/web-ui/spec.md",
      "description": "Install @tailwindcss/typography@^0.6.0 for Tailwind v4 compatibility and add @plugin directive. Add streamError to useExploreStream hook. Display error in ExplorePage. Fix ExploreRedirect error handling.",
      "acceptanceCriteria": [
        "@tailwindcss/typography@^0.6.0 installed in package.json",
        "@plugin '@tailwindcss/typography' added to CSS entry file (index.css)",
        "useExploreStream returns streamError state",
        "streamError set on done event with error field and on stream catch",
        "streamError cleared on new send() call",
        "ExplorePage displays error banner when streamError is set",
        "ExploreRedirect handles session creation failure",
        "Build succeeds"
      ],
      "priority": 7,
      "passes": false,
      "notes": "Must use typography v0.6.0+ for Tailwind v4 compatibility. Add @plugin directive to packages/cli/web/src/index.css. Use existing error banner pattern from IssueDetailPage."
    }
  ]
}
