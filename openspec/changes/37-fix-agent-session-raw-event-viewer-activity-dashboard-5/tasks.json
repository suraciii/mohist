{
  "version": 1,
  "tasks": [
    {
      "id": "T-001",
      "title": "Add deriveToolCallTitle utility and new types",
      "spec": "specs/tool-call-context-display/spec.md#tool-call-display-title-derived-from-rawinput",
      "description": "Add a `deriveToolCallTitle(toolName, title, rawInput)` function to `useSessionTimeline.ts` that parses rawInput JSON and extracts meaningful display labels per tool kind (read→file basename, bash→command, glob→pattern, etc). Also add `TaskProgressEntry`, `LoopProgress` types to `types.ts` and add `thoughtText` field to the `Round` interface.",
      "acceptanceCriteria": [
        "deriveToolCallTitle('read', 'read', '{\"file_path\":\"packages/cli/src/server.ts\"}') returns 'server.ts'",
        "deriveToolCallTitle('bash', 'bash', '{\"command\":\"npm run build\"}') returns 'npm run build'",
        "deriveToolCallTitle('read', 'packages/cli/src/main.ts', '...') returns 'packages/cli/src/main.ts' (title already meaningful)",
        "deriveToolCallTitle('bash', 'bash', 'npm test') returns 'npm test' (non-JSON fallback)",
        "deriveToolCallTitle('unknown', 'unknown', null) returns 'unknown'",
        "TaskProgressEntry and LoopProgress types exist in types.ts",
        "Round interface has thoughtText: string field",
        "Typecheck passes"
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/hooks/useSessionTimeline.ts, packages/cli/web/src/lib/types.ts",
      "dependsOn": [],
      "passes": true,
      "notes": "Foundation task — provides the shared utility and types used by all subsequent tasks.",
      "attempts": 2,
      "order": 999999,
      "error": null
    },
    {
      "id": "T-002",
      "title": "Fix completed event title/rawInput propagation + historical title derivation",
      "spec": "specs/tool-call-context-display/spec.md#completed-tool-call-update-propagates-title-and-rawinput",
      "description": "In `reconstructRoundsFromLogs` (line 92-97): when processing completed/failed tool_call_update, also copy `title` and `rawInput` from the event data onto the existing ToolCallEntry. For initial tool_call entries where title equals kind, call `deriveToolCallTitle` to set a meaningful title. In the live `coder_tool_call` handler (line 301-318): when state !== 'started', also propagate `detail.title` and `detail.rawInput` onto the updated entry.",
      "acceptanceCriteria": [
        "reconstructRoundsFromLogs: tool_call_update completed event copies title and rawInput to existing entry",
        "reconstructRoundsFromLogs: tool_call with title==='read' derives title from rawInput via deriveToolCallTitle",
        "reconstructRoundsFromLogs: tool_call_update title overrides the derived title from initial tool_call",
        "Live coder_tool_call completed event propagates detail.title and detail.rawInput",
        "Historical page load shows 'server.ts' instead of 'read' for completed read tool calls",
        "Typecheck passes"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/hooks/useSessionTimeline.ts",
      "dependsOn": [
        "T-001"
      ],
      "passes": true,
      "notes": "Fixes 断层 2 (completed title discarded) and 断层 5 (historical data quality). The event payload already has these fields, they are just being ignored at lines 92-97 and 301-318.",
      "attempts": 2,
      "order": 999999,
      "error": null
    },
    {
      "id": "T-003",
      "title": "Handle plan_session_update tool_call, tool_call_update, and agent_thought_chunk",
      "spec": "specs/session-timeline-ui/spec.md#sessiontimeline-appends-live-sse-events",
      "description": "Extend `flushPlanBuffer` in useSessionTimeline.ts to handle `sessionUpdate === 'tool_call'` (create ToolCallEntry), `sessionUpdate === 'tool_call_update'` (update existing entry with title/rawInput/rawOutput), and `sessionUpdate === 'agent_thought_chunk'` (accumulate into lastRound.thoughtText). Also handle `agent_thought_chunk` in `reconstructRoundsFromLogs` by accumulating into `thoughtText` instead of `agentText`.",
      "acceptanceCriteria": [
        "flushPlanBuffer handles sessionUpdate==='tool_call' by creating a ToolCallEntry in the last round",
        "flushPlanBuffer handles sessionUpdate==='tool_call_update' by updating existing ToolCallEntry with title, rawInput, state, rawOutput",
        "flushPlanBuffer handles sessionUpdate==='agent_thought_chunk' by appending to lastRound.thoughtText",
        "reconstructRoundsFromLogs handles eventType==='agent_thought_chunk' by accumulating into thoughtText, not agentText",
        "Plan stage tool calls appear in real-time via plan_session_update SSE",
        "Typecheck passes"
      ],
      "priority": 3,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/hooks/useSessionTimeline.ts",
      "dependsOn": [
        "T-001"
      ],
      "passes": false,
      "notes": "Fixes 断层 1 (main_tool_call dead event — Plan stage tool calls now visible via plan_session_update). The onSessionUpdate bridge in workflow-controller.ts already forwards tool_call/tool_call_update/agent_thought_chunk.",
      "attempts": 1,
      "order": 999999,
      "error": "Auto-skipped (no onAskUser): Timed out after 300s"
    },
    {
      "id": "T-004",
      "title": "Add ralph_task_update and ralph_loop_progress subscriptions to useSessionTimeline",
      "spec": "specs/build-task-progress-ui/spec.md#usesessiontimeline-subscribes-to-ralph-task-update-and-ralph-loop-progress",
      "description": "Add `onAgentEvent('ralph_task_update')` and `onAgentEvent('ralph_loop_progress')` subscriptions in the main useEffect of useSessionTimeline. Maintain `useState` for `TaskProgressMap` (keyed by taskId) and `LoopProgress | null`. Map SSE status 'started'→'running', 'completed'→'passed'. Filter by issueId. Expose taskProgress and loopProgress in the hook return value.",
      "acceptanceCriteria": [
        "onAgentEvent('ralph_task_update') subscription added and filtered by issueId",
        "onAgentEvent('ralph_loop_progress') subscription added and filtered by issueId",
        "ralph_task_update status 'started' maps to TaskProgressEntry status 'running'",
        "ralph_task_update status 'completed' maps to TaskProgressEntry status 'passed'",
        "ralph_task_update status 'failed' stores error and attempt fields",
        "loopProgress state updates with { completed, failed, total } from ralph_loop_progress",
        "taskProgress and loopProgress exposed in useSessionTimeline return value",
        "Typecheck passes"
      ],
      "priority": 4,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/hooks/useSessionTimeline.ts",
      "dependsOn": [
        "T-001"
      ],
      "passes": false,
      "notes": "Fixes 断层 3 (ralph_task_update unconsumed). SSE events are already dispatched by useSSE.ts — just need consumption in useSessionTimeline.",
      "attempts": 1,
      "order": 999999,
      "error": "Auto-skipped (no onAskUser): Timed out after 300s"
    },
    {
      "id": "T-005",
      "title": "Add TaskProgressPanel component and update SessionTimeline props",
      "spec": "specs/build-task-progress-ui/spec.md#taskprogresspanel-renders-build-stage-task-list",
      "description": "Add a `TaskProgressPanel` function component in `SessionTimeline.tsx` that renders when `currentStage === 'build'`. Display a summary line (e.g. '3/4 tasks passed'), a compact grid of task IDs with colored status icons (green check=passed, blue spinner=running, red X=failed, orange retry=retrying). Update `SessionTimelineProps` to accept `taskProgress` and `loopProgress`. Wire the new props from the parent component that uses `useSessionTimeline`.",
      "acceptanceCriteria": [
        "TaskProgressPanel renders when currentStage is 'build' and taskProgress is non-empty",
        "TaskProgressPanel is hidden when currentStage is not 'build'",
        "Summary line shows 'X/Y tasks passed' format",
        "Each task shows task ID with correct status icon color (green/blue/red/orange)",
        "Failed tasks display error message",
        "SessionTimelineProps includes taskProgress and loopProgress fields",
        "Parent component (IssueDetailPage or equivalent) passes new props from useSessionTimeline return",
        "Typecheck passes"
      ],
      "priority": 5,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/components/SessionTimeline.tsx",
      "dependsOn": [
        "T-001",
        "T-004"
      ],
      "passes": false,
      "notes": "Fixes 断层 3 UI component. The data layer is in T-004, this task adds the visual rendering.",
      "attempts": 1,
      "order": 999999,
      "error": "Auto-skipped (no onAskUser): Timed out after 300s"
    },
    {
      "id": "T-006",
      "title": "Add thought text collapsible section to RoundSection and ToolCallTimelineEntry context display",
      "spec": "specs/session-timeline-ui/spec.md#sessiontimeline-component-renders-rounds",
      "description": "In `RoundSection` component: render `round.thoughtText` in a default-collapsed `<details>/<summary>` element with label 'Thinking...' and size indicator for large text (>500 chars shows KB size). Message text (`agentText`) renders normally above thoughts. In `ToolCallTimelineEntry`: use `deriveToolCallTitle` to compute the display label shown next to the tool name, replacing the raw `entry.title` rendering.",
      "acceptanceCriteria": [
        "RoundSection renders thoughtText in a <details> element collapsed by default",
        "Summary label shows 'Thinking...' and includes KB size when text > 500 chars",
        "Clicking the 'Thinking...' toggle expands to show full thought content",
        "Message text (agentText) renders normally above the thought section",
        "ToolCallTimelineEntry uses deriveToolCallTitle for display label instead of raw entry.title",
        "Tool calls show 'server.ts' instead of 'read' for read tool with file path rawInput",
        "Tool calls show 'npm run build' instead of 'bash' for bash tool with command rawInput",
        "Typecheck passes"
      ],
      "priority": 6,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/cli/web/src/components/SessionTimeline.tsx",
      "dependsOn": [
        "T-001",
        "T-003"
      ],
      "passes": false,
      "notes": "Fixes 断层 4 (text unclassified) and completes the UI rendering for 断层 2 (tool call context display).",
      "attempts": 1,
      "order": 999999,
      "error": "Auto-skipped (no onAskUser): Timed out after 300s"
    },
    {
      "id": "T-007",
      "title": "Verify all changes with build and manual review",
      "spec": "",
      "description": "Run `cd packages/cli && npm run build` to verify the web frontend compiles. Run `cd packages/cli && npm test` to verify no regressions. Review the complete diff of useSessionTimeline.ts, SessionTimeline.tsx, and types.ts to confirm all 5 pipeline gaps are addressed and no existing behavior is broken.",
      "acceptanceCriteria": [
        "npm run build succeeds in packages/cli with no errors",
        "npm test passes in packages/cli with no regressions",
        "All 5 pipeline gaps verified: (1) Plan stage tool calls visible, (2) completed titles propagated, (3) task progress panel renders, (4) thoughts collapsed, (5) historical titles derived",
        "No console errors or TypeScript warnings"
      ],
      "priority": 7,
      "mode": "AFK",
      "type": "TEST",
      "output": "Build and test output",
      "dependsOn": [
        "T-002",
        "T-003",
        "T-004",
        "T-005",
        "T-006"
      ],
      "passes": false,
      "notes": "Final verification task. Depends on all implementation tasks.",
      "attempts": 2,
      "order": 999999,
      "error": null
    }
  ]
}