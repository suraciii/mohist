# Ralph Progress Log
Started: 2026-04-23T05:16:29.386Z

---

## T-001 — 2026-04-23T13:34:00Z
- **What**: Added corrupted-state detection and reset in `runRalphLoop()` (ralph-executor.ts:296-306). When all tasks have `passes: true`, resets all to `false` and persists via `writeTasksFile`. Mixed passes states are left untouched (per design review refinement).
- **Tests**: Updated existing test to reflect new behavior (all-passes=true triggers reset). Added test for mixed-passes scenario. Utility tests pass. `runRalphLoop` integration tests have pre-existing hang (unrelated to this change — verified by reverting and confirming same hang).
- **Typecheck**: Passes
- **Learnings**: The design review refined the original task desc (unconditional reset) to only reset when ALL tasks have passes=true. This preserves normal resume scenarios while still fixing the self-review corruption bug.
- **Next**: T-002 (persist pipeline failure in approvalState)


[2026-04-23T06:04:30Z] T-002 Persist pipeline failure message in approvalState
- Added issueRepo.setApprovalState() with status:'error' in both result-failure branch (~line 291) and catch branch (~line 335) of executePipeline()
- Removed clearApprovalState() calls in both error paths (was erasing the error state we just wrote)
- ApprovalStatus type already included 'error' (fixed in review phase)
- Typecheck passes, 8/8 agent-runner-service tests pass
- Learning: The original code called clearApprovalState after setting the error, which would erase it. Removed clearApprovalState from both error paths so the error persists.
- Next: T-003 (show error in mo issue show CLI)

[2026-04-23T14:10:00Z] T-003 Show error state in mo issue show
- Modified `cli/commands/issue.ts` show action: added 'error' to statusColors map, added conditional display of error message when approvalState.status === 'error' with output.error
- Error state shows `Error: <message>` in red; other states (awaiting/approved/rejected) show self-review notes as before
- No output changes when approvalState is empty (the outer `if` guard unchanged)
- Typecheck passes
- Learning: The existing approvalState display code already handled awaiting/approved/rejected. Just needed to add 'error' color mapping and error message display. Separated error display from self-review notes display with an else-if to avoid confusing output.
- Next: T-004 (add pipeline events to mo issue logs)
