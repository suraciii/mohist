Issue 619 plan repair progress

- F1 fixed: proposal, design, spec, and tasks now cover enabled non-disabled Connection unavailability (incomplete setup, unhealthy/degraded health including service-offline and backpressure, or OfflineGapAt), with Connection-gate precedence, safe required nudge, deduplication, no execution resources, and existing Disabled audited-discard behavior.
- F2 fixed: proposal, design, spec, and tasks now treat a first Bot mention in an unbound channel thread as a new launch, gate it before launch side effects, target the nudge at body.ThreadTs, and test deduplication; bound-thread follow-ups remain exempt.
- Commit: 11117ec14 (Clarify issue 619 unavailable Slack launch paths)
- Final consistency checks completed: tasks JSON parses, spec anchors are current, no stale scope wording remains, and self-review.md is unchanged.
- T-001 implementation: Slack admission now resolves accepted inbox replays before new-launch gates, applies persisted Connection availability before canonical AgentReadinessService executability, and leaves DM/bound-thread follow-ups on their existing routes.
- T-001 implementation: blocked launches persist one required UserAction/post_message setup nudge using dispatch reference `slack-setup-nudge:{connectionId}:{identity.AsKey()}`, identity client message id, safe owner/operator wording, and DM/root/thread targeting; no inbox or execution resources are created.
- T-001 verification: updated DM/channel ingress specs cover blocked Agent resource isolation, safe nudge payloads, targeting, deduplication, offline-gap/backpressure behavior, and follow-up continuity. `dotnet test` SpecTests passed 3876 tests and UnitTests passed 2767 tests with `-p:SkipWebBuild=true`.
- T-002 implementation: `AgentInfo.Executability` is now projected into Slack Connection list/detail models, `ConnectionDiagnosticResult`, manager status/projections, and the authorized manager diagnostics text; legacy `AgentReadiness` and Connection setup/desired/health/offline facts remain separate and unchanged.
- T-002 decision: the diagnostic API carries canonical executability as top-level `executability`; list/detail and manager read projections use the same field, while the fixed setup nudge continues to render only the category-safe summary.
- T-002 verification: added canonical model-missing and execution-config-failure diagnostic cases, project scoping, list/detail compatibility assertions, ConnectionDiagnostic independence coverage, and privileged-detail exclusion assertions for not-executable Slack nudges. Full Server SpecTests pass 3881 and UnitTests pass 2768 with `-p:SkipWebBuild=true`.
- Web verification: installed existing lockfile dependencies with `npm ci --ignore-scripts --no-audit --no-fund`; `npm run typecheck -w packages/web` passed and focused diagnostic/API web tests passed 8/8.
- Repair diagnosis: the first reproduced `npm run verify` failed in the concurrent `archtest` phase at `format:check`, before test execution. Biome reported unformatted T-002 changes in `ConnectionDiagnosticPage.tsx` and `ConnectionDiagnosticPage.test.tsx`; the build sibling was cancelled by the canonical gate and left no Git-visible side effects.
- Repair applied: ran the repository-prescribed `npm run format`, which changed only those two T-002 Web files; `npm run format:check` now passes.
- Focused post-repair verification: Web typecheck and all 8 connection-diagnostic Web test files pass (38 tests); the Server SpecTests and UnitTests projects pass (3,881 and 2,768 tests). Microsoft Testing Platform reports that the VSTest filter is ignored in these project runs, so each complete project was executed.
- Second canonical diagnosis: after formatting was fixed, `npm run verify` reached `check:filesizes` and rejected T-001 growth of `SlackConnectionRoutes.cs` (2,399 -> 2,490 counted lines) and `SlackChannelThreadIngressSpecs.cs` (959 -> 1,039 counted lines). This was an implementation structure defect, not a generated verification side effect.
- Structural repair: moved the existing Manager adapter mapping into `SlackConnectionRoutes.ManagerAdapter.cs` and the three issue-619 channel admission specs into `SlackChannelThreadIngressAdmissionSpecs.cs`; the ratchet now reports clean sizes (2,393 and 870 lines in the retained files, with both new files under 1,000). Full Server SpecTests pass again (3,881).
- Third canonical diagnosis: all tests passed, but the duration ratchet rejected two OTEL registration specs at 5.00s each because disposing the real OTLP exporters waited on the unreachable `collector.test` endpoint. Direct class reproduction confirmed 10.263s for 8 tests.
- Test repair: the two affected OTEL specs now retain their endpoint and provider assertions while using the existing in-process OTLP handler for exporter transport. The rebuilt class passes all 8 tests in 0.258s with zero build warnings.
- Final verification: committed repair `daa798a72`, then `npm run verify` completed all 9 tracks with 0 failures, cancellations, or timeouts; Server SpecTests passed 3,881 and the duration ratchet reported 0 unallowlisted over-budget tests. Diagnostics were written under `/tmp` only; no verification-generated Git-visible changes remained.
- Independent repair verification at HEAD `b5421981d`: the OTEL class passed 8/8 in 0.214s, issue-619 Server classes passed 38/38, related Web files passed 44/44, and `npm run verify` passed all 9 tracks with 0 failures/cancellations/timeouts and 0 unallowlisted duration violations. Non-fatal OTLP port-4318 bind warnings came from concurrent test hosts; the worktree remained clean after verification.
