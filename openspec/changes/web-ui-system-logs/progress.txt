# Ralph Progress Log
Started: 2026-04-15T05:05:53.732Z

---

## 2026-04-15T05:12:30Z — T-001 改造 Log 系统为 JSONL 格式输出
**Done:** Modified `packages/cli/src/util/log.ts`:
- File output changed from plain text to JSONL format (each line = JSON with level, time, diffMs, service, message + extra fields)
- Log filename changed from `YYYY-MM-DDTHHMMSS.log` to rolling `mohist-YYYY-MM-DD.log`
- Cleanup strategy changed from "keep 10 files" to "delete files older than 24h"
- `--print-logs` mode preserved as plain text stderr output
- Added `printMode` flag to route between text (`print:true`) and JSONL (`print:false`) builds
- Updated `log.test.ts` with 4 new JSONL-specific tests (format validation, extra fields, level values, rolling filename)
**Learnings:** Namespace `export {}` syntax not allowed in TS namespaces — need to use individual function/const exports instead.
**Next:** T-002 (log-tail read function)

---

## 2026-04-15T05:20:00Z — T-002 实现 log-tail 读取函数
**Done:** Created `packages/cli/src/util/log-tail.ts`:
- `readLogTail()` main function — reads JSONL log lines from current log file
- `resolveLogFile()` — when primary file doesn't exist and matches rolling pattern, finds newest `mohist-YYYY-MM-DD.log` in logs dir by mtime
- `readLogSlice()` — core file reading with cursor-based pagination, limit, maxBytes
- Handles: no cursor (tail from end), cursor (incremental), cursor > size (reset), file not found (empty), truncation
- Lines returned as raw strings (unparsed)
- Added inline `clamp()` helper (project has no shared clamp util)
**Learnings:** TS target ES2022 doesn't have `Array.prototype.toSorted()` — must use mutable `.sort()`. `os` import not needed since `Log.file()` provides the path.
**Next:** T-003 (GET /api/logs/tail API route)


[2026-04-15] T-003 完成
- 创建 packages/cli/src/api/logs.ts，实现 GET /tail Hono 路由
- 解析 cursor/limit/maxBytes query params，验证参数有效性
- 调用 readLogTail，返回 { success: true, data: { file, cursor, lines, truncated } }
- 在 server/index.ts 注册 /api/logs 路由
- 在 http-server.ts middleware 中排除 /api/logs/* 路径（跳过日志记录）
- 在 web/src/lib/api.ts 添加 getLogTail(cursor?, limit?, maxBytes?) 方法
- 在 web/src/lib/types.ts 添加 LogTailResult 接口
- CLI typecheck 通过
- Learnings: rate limiter 当前仅在 providers.ts 中按路由使用，非全局 middleware；logs 路由自然不受限制
- Next: T-004 实现 useLogs hook

[2026-04-15] T-004: Implemented useLogs hook
- Created packages/cli/web/src/hooks/useLogs.ts with:
  - parseLogLine: JSONL parser with fallback for invalid JSON
  - useLogs hook: 3s polling, cursor-based incremental reads, 2000 entry buffer cap
  - Page Visibility API integration: pauses polling when tab hidden, immediate fetch on return
  - Proper cleanup of intervals and event listeners on unmount
- Returns: entries, loading, error, refresh, cursor, truncated, file
- Fixed naming bug (parseLogEntry -> parseLogLine) and typed setEntries callback
- Typecheck passes
- Next: T-005 (LogsPage component and routing)

## 2026-04-15 — T-005: 实现 LogsPage 组件和路由集成

**Done:**
- Created `packages/cli/web/src/components/LogsPage.tsx` with:
  - Structured log display (time | level with color | service tag | message)
  - Level filter chips (DEBUG/INFO/WARN/ERROR) — toggleable
  - Text search filtering across message, service, raw fields
  - Auto-follow toggle with scroll-based pause/resume (10px threshold)
  - Truncation warning banner when API returns truncated=true
  - File path display
  - Export button (downloads filtered logs as .txt, disabled when empty)
- Added `/logs` route in `App.tsx`
- Added Logs nav link in `Header.tsx` between Explore and Settings (text hidden on narrow screens via `hidden sm:inline`)

**Learnings:**
- The pre-existing TS6133 errors in CustomProviderDialog.tsx and ProviderConnectDialog.tsx are not related to this change
- `parseLogLine` is already exported from useLogs.ts but only needed inside the hook; the page component uses the pre-parsed entries directly

**Next steps:**
- All 5/5 tasks complete for web-ui-system-logs change
