## Codebase Patterns

- AgentSession state and domain events are persisted by AgentSessionGrain; runtime transcript parts are accumulated and flushed separately by TranscriptAccumulator.
- Runner workflow facts are durable through AgentSessionRuntimeEventOutbox; turn reporters enqueue input before execution and settle produced facts after the turn.
- DTO status fields remain compatibility shims in T-001; T-004 owns the breaking API activity field.

## T-001

- Added persisted AgentSessionActivity (Idle, Active, Unknown), defaulting to Idle.
- session.input applies idle to active and session.activity applies runner-reported idle/unknown/active transitions.
- Runner Pi, workflow reporter, and follow-up paths now emit session.activity; failed turns also emit turn.failed for context exhaustion classification.
- Removed grain terminal-close special handling and ReadTerminalStateAsync status derivation. Legacy constants remain only for old persisted/test data compilation and are not written by runner paths.
- ContextExhaustionClassifier now runs on turn.failed payloads; old terminal-event fixtures are expected to be migrated with the activity contract.
- Full legacy server spec suite still contains assertions for removed terminal lifecycle behavior and needs migration to the T-001 contract before it can be green.

## T-002

- RebindRuntimeSession now takes full AgentRuntimeBinding expected/replacement values, requires idle activity, validates reset/runtime-change/missing-recovery reasons, and clears only context-window usage while preserving cumulative usage.
- Reset, CompleteReset, and runtime-changing attach use the shared replacement transition; reset markers are persisted as session.context_reset transcript evidence with reason/observedAt only and no physical session IDs.
- Removed RuntimeSessionLineage, RuntimeSessionLineageEntry, PreviousAgentRuntimeSessionId, and lineage DTO projection. Orleans JSON deserialization ignores the removed legacy field.
- Updated unit and focused grain coverage for full-binding CAS, stale/active rejection, context clearing, attach replacement, legacy state deserialization, and context-reset payload shape. The remaining full spec failures are the pre-existing T-001 terminal/status contract assertions.

## T-003

- Added shared runner `resolveOrRecoverBinding`: only deterministic `missing-session` creates one empty candidate; all other resolve failures preserve the binding, and failed CAS returns a candidate-unbound diagnostic without submission.
- Added explicit OpenCode/Pi runtime resolve operations and wired recovery before Workflow task, AgentJob, and idle Follow-up input recording; the server confirms full-binding CAS through dedicated recover-missing runner routes before input submission.
- Added the server missing-recovery grain command using the T-002 idle-only rebind transition and `session.context_reset` reason, without Workflow recovery or work-attempt changes.
- OpenCode Reset now creates an empty session through `OpenCodeRuntime.createSession`; Compact and Cancel remain non-recovering. Recovery-related runtime_session_missing responses no longer carry hint:reset, and missing Cancel returns not-cancellable.
- Runner typecheck and all 1,379 runner tests pass; `npm run build` passes. Focused missing-recovery server specs pass. Full `npm test` still has the pre-existing T-001 legacy terminal/status failures noted above; route tests expecting old reset hints also need migration to the new T-003 contract.

## T-004

- AgentSessionSummaryDto/GenericAgentSessionSummaryDto and WorkflowSessionDto now expose the persisted activity value directly; Web session metadata and list models consume the shared idle/active/unknown mapper.
- Removed Web subscriptions to session.closed and session.followup_* for status patches. Session activity events now reconcile transcript/list queries, and Compact/Reset eligibility is idle-only while Follow-up remains available for idle and active sessions.
- Removed the runtime-session lineage component and historical runtime matching path from the transcript hook. The remaining legacy test fixtures still mention removed lineage fields but no production consumer reads them.
- Added direct activity-mapper coverage for idle/active/unknown, unknown fallback, follow-up eligibility, and idle-only recovery eligibility.
- `npm run typecheck -w packages/web` passes. The focused activity tests pass. Full Web tests still contain pre-T-004 terminal/history expectations; full `npm run build`/`npm test` also remain blocked by the pre-existing T-001/T-002 server spec compilation failures and the runner-disconnect architecture-rule failure.

## Verification Repair

- Initial `git status` and `git diff` were clean; the failed verification did not leave generated changes.
- Reproduced `npm test`: the first failure was stale SpecTests compilation against the removed `Status`/time-window APIs, followed by the architecture rule rejecting the concrete Runner tracker dependency from `AgentSessionGrain`.
- Migrated the directly affected server specs to `activity`/`session.activity`/CAS semantics and registered a Sessions-owned `IAgentSessionConnectionRegistry`; the grain no longer depends on the Runner concrete type, so ArchTests pass.
- Added follow-up idle activity handling that clears the matching accepted follow-up lease, which is required now that legacy follow-up terminal events are removed.
- Focused server verification passes: `GenericAgentSessionSummarySpecs` and `AgentSessionRecoveryGrainSpecs` (35 tests). Runner typecheck and all 1,379 runner tests pass.
- Full server/Web suites still fail in legacy tests that assert removed `session.closed`, `session.followup_*`, terminal status values, reset hints, runtime lineage, or Session-ended UI labels. Those failures are contract-test migration work, not verification side effects.
