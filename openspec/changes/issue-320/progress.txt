# Issue #320 — Build Progress

## Codebase Patterns

- Web tests use `vitest` + `@testing-library/react`. `npm run test:run -w packages/web` runs the full suite (~3700 tests, <15s). Targeted runs accept a path: `npm run test:run -w packages/web -- <path>`.
- Web typecheck is `npm run typecheck -w packages/web` → `tsc -b`. Catches unused imports / dead code; must stay green.
- Dashboard attention hero lives at `packages/web/src/widgets/attention-hero/ui/AttentionHero.tsx`. The `AllClearState` is a private sub-component; its `data-testid="dashboard-zone-attention"` is shared with the attention/loading variants by design.
- Testid `productivity-placeholder` is the only place in the source tree that referenced the stale productivity-preview copy; deleting the `<div>` in `AllClearState` was sufficient — no other consumers.

## T-001 — Remove Productivity placeholder from All-clear

- Source: removed the `<div data-testid="productivity-placeholder">…</div>` block inside `AllClearState`. `ApprovalWaitSummary` and the all-clear message remain. No imports became orphaned.
- Tests: flipped two positive `getByTestId('productivity-placeholder')` assertions to `queryByTestId(...).not.toBeInTheDocument()` in `AttentionHero.test.tsx` (around the "renders the all-clear state with All clear message and Productivity placeholder…" test and the "renders the all-clear state when agentStatus is undefined…" test). Both tests renamed to drop the now-misleading "and Productivity placeholder" suffix.
- Verification: `npm run typecheck -w packages/web` clean; `npm run test:run -w packages/web` → 240 files / 3717 tests passing. Targeted `AttentionHero.test.tsx` → 32 tests passing.

## T-002 — Remove Dashboard hero Ask Agent button

- Source `DashboardPage.tsx`: removed the `dashboard-hero` inner `<div className="mt-4 flex justify-end">` containing the `<Button data-testid="ask-agent-project">Ask Agent</Button>` that called `navigate(toProjectPath('/agent-sessions/new'))`. The hero now contains only `<AttentionHero />`.
- Imports cleaned: dropped `useNavigate` from `react-router-dom`, `useProjectPath` from `entities/project`, and `BotIcon` from `lucide-react`. Also removed the `const navigate = useNavigate()` and `const toProjectPath = useProjectPath()` declarations. `useState`, `useProject`, `Button` (still used by `dashboard-create-project`) retained — confirmed no remaining references in the file.
- Tests `DashboardPage.test.tsx`: deleted the `const mockUseNavigate = vi.hoisted(() => vi.fn())` hoist, the `vi.mock('react-router-dom', ...)` override that returned `useNavigate: () => mockUseNavigate`, and the entire `describe('DashboardPage Ask Agent entry (T-005)', ...)` block (3 tests covering button render + navigation behavior). Added one new test inside the main `describe('DashboardPage', ...)`: `'does not render the Ask Agent entry in the dashboard hero'` — asserts both that `dashboard-hero` contains no `ask-agent-project` descendant and that the testid is absent anywhere on the page.
- The `useActivityCards` mock still returns `statusCounts` because T-003 (Pulse pills) will consume it; harmless either way per design Decision 3.
- Verification: `npm run typecheck -w packages/web` clean; `npm run test:run -w packages/web` → 240 files / 3715 tests passing (1 skipped, pre-existing). Targeted `DashboardPage.test.tsx` → 10 tests passing (9 original + 1 new).

## T-003 — Remove Pulse lifecycle status pills

- Source `PulseZone.tsx`: deleted the `PILL_STYLE` and `PILL_LABEL` constant maps, the `<div data-testid="pulse-status-pills">` block (which contained four `pulse-pill-{active,waiting,completed,failed}` spans), and removed `statusCounts` from the `useActivityCards()` destructure (now `{ activeCards, slotUsage }`). The `pulse-capacity-header` with `pulse-slots` ("X/Y slots used") indicator, the empty state, the card list, and the overflow link all remain unchanged. No imports became orphaned.
- `useActivityCards` model itself (`src/widgets/coder-session/model/activity-cards.ts:150`) is untouched — `statusCounts` is still produced. `ActivityPage.tsx:39-42` continues to consume it. Per design Decision 2 the shared model stays intact.
- Tests `PulseZone.test.tsx`: replaced the single "renders the four status pills with counts sourced from the activity summary" test (≈ line 102) with a new test "'does not render lifecycle status pills and keeps the slot-usage indicator'" that seeds a summary with non-zero counts and asserts `pulse-status-pills` + all four `pulse-pill-*` testids are absent while `pulse-slots` still renders "2/8 slots used". Capacity-header / empty-state / card-cap / overflow-link / rerender-on-update / shared-source tests are unchanged. The `within` import from `@testing-library/react` stays in use (line 99: `within(header).getByTestId('pulse-slots')`).
- Verification: `npm run typecheck -w packages/web` clean; `npm run test:run -w packages/web` → 240 files / 3715 tests passing (1 skipped, pre-existing). Targeted `PulseZone.test.tsx` → 10 tests passing.

## T-004 — Remove Productivity zone SnapshotRow

- Source `ProductivityZone.tsx`: dropped the `import { SnapshotRow } from './SnapshotRow'` line and removed `<SnapshotRow />` from the rendered tree. The `<div data-testid="productivity-zone">` now begins with `<EpicProgressList />`; the remaining ten child components (CompletionTrend, ThroughputChart, CycleTimeChart, StageDurationChart, CumulativeFlowChart, QualityPanel, FtrTrendChart, InvestmentPanel, CostTrendChart, EpicProgressList) are unchanged in order. No imports became orphaned in this file.
- Files deleted: `pages/dashboard/productivity/SnapshotRow.tsx` and `pages/dashboard/productivity/SnapshotRow.test.tsx`. Verified `SnapshotRow` has zero remaining references in `packages/web` (was only consumed from ProductivityZone.tsx).
- `useCompletionSnapshot` / `completion-snapshot.ts` and `entities/issue/lib/completion-snapshot.test.ts` untouched — design/proposal preserved them as the entities/issue public export. `entities/issue/index.ts:16-17` still exports `deriveCompletionSnapshot`, `useCompletionSnapshot`, and `CompletionSnapshot`. Recent digest (`recent-digest.test.tsx`) cross-check remained valid because it asserts against `completion-snapshot` conceptually, not the deleted component.
- Tests `ProductivityZone.test.tsx`: removed the 3-line `vi.mock('./SnapshotRow', () => ({ SnapshotRow: () => <div data-testid="snapshot-row" /> }))` block at the top of the file. The existing seven structural tests (mounting order of quality/completion/investment + relative order of throughput/cycle/stage/cumulative/quality/ftr) still pass without modification — none of them referenced the snapshot-row mock. All other eleven `vi.mock('./…')` blocks (EpicProgressList, CompletionTrend, QualityPanel, FtrTrendChart, InvestmentPanel, ThroughputChart, CycleTimeChart, StageDurationChart, CumulativeFlowChart, CostTrendChart) and the `import { ProductivityZone }` order intact.
- Verification: `npm run typecheck -w packages/web` clean (no orphan import warnings); targeted `ProductivityZone.test.tsx` → 7 tests passing. Full `npm run test:run -w packages/web` → 239 files / 3711 tests passing (1 skipped, pre-existing). Total dropped from 240→239 files (the deleted SnapshotRow.test.tsx) and 3715→3712 tests (4 deleted SnapshotRow tests).
- Acceptance: productivity zone no longer renders `productivity-snapshot-row` or any `productivity-snapshot-*` descendant (the only producer was SnapshotRow, now deleted). All other Productivity zone charts unmounted identically.