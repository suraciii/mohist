# Review Report

## Result: PASS

The auto-fix resolves the prior review failures. Generated `SKILL.md` files now keep AgentSkills frontmatter at byte 0, the Mohist marker/checksum are moved after the closing frontmatter delimiter, and all required `skills` help pages distinguish coder agent skills in `.agents/skills` from internal Mohist skills in `.mohist/skills`.

## Dimensions

### Correctness: PASS

- `buildMarkedContent()` now inserts `<!-- Generated by Mohist -->` and `<!-- checksum: ... -->` after the first closing frontmatter delimiter at `packages/cli/src/agent-skills/shared-agent-skills.ts:367-375`, so generated files begin with `---\n` and remain compatible with frontmatter-first AgentSkills loaders.
- Runtime verification with `node bin/mo skills install --path <tmp>` produced `.agents/skills/mohist/SKILL.md` starting with frontmatter, followed by the generated marker/checksum after the closing `---` block.
- `parseFrontmatter()` now anchors to the start of the file at `packages/cli/src/agent-skills/shared-agent-skills.ts:20-21`, matching the intended AgentSkills layout instead of accepting frontmatter after arbitrary leading content.
- Checksum recognition remains compatible with the new marker location: `extractContentWithoutMarker()` removes marker and checksum lines regardless of whether they appear after frontmatter, and `isMohistGenerated()` validates the checksum against the marker-stripped template at `packages/cli/src/agent-skills/shared-agent-skills.ts:39-59`.
- Install/update behavior remains correct: missing files are created, recognized generated files are refreshed or left unchanged, protected non-generated files are skipped, and `install --force` overwrites protected files at `packages/cli/src/agent-skills/shared-agent-skills.ts:398-491`.

### Complexity: PASS

- The auto-fix is minimal: it changes marker extraction/insertion and help text only, without adding new abstractions or changing command flow.
- The implementation remains centralized in `packages/cli/src/agent-skills/shared-agent-skills.ts`, while `packages/cli/src/cli/commands/skills.ts` only wires Commander options, help, result formatting, and exit behavior.
- Embedded templates make `shared-agent-skills.ts` long, but runtime branching is still straightforward and no unnecessary service class, manifest store, or per-agent directory logic was introduced.

### Test Coverage: PASS

- Focused shared skill tests passed: `npm test -- shared-agent-skills.test.ts` with 16 tests passing.
- Full test suite passed: `npm test` with 93 test files passing, 1646 tests passing, and 6 skipped.
- Type checking passed: `npm run typecheck`.
- Build passed: `npm run build`, including the web build. The build reported existing npm audit warnings for web dependencies, but did not fail.
- New regression coverage verifies generated content starts with AgentSkills frontmatter and the first frontmatter `name` equals the skill directory name at `packages/cli/tests/shared-agent-skills.test.ts:65-77`.
- New regression coverage verifies help text for `skills`, `skills install`, and `skills update` includes `.agents/skills`, `coder agent skills`, `.mohist/skills`, and that the commands do not execute/scan/modify internal skills at `packages/cli/tests/shared-agent-skills.test.ts:234-251`.
- Existing tests still cover first install, repeated install/update, partial repair, protected user edits, forced overwrite, explicit `--path`, command registration, options, and `mohist-walkthrough` exclusion.

### Security: PASS

- No command injection path was introduced. `--path` is only used as a filesystem base path, and writes are constrained to `.agents/skills/<skill>/SKILL.md` under the selected path at `packages/cli/src/agent-skills/shared-agent-skills.ts:399-405` and `packages/cli/src/agent-skills/shared-agent-skills.ts:446-452`.
- The generated skill list is fixed to `mohist` and `mohist-explore` at `packages/cli/src/agent-skills/shared-agent-skills.ts:362-365`; user input does not control generated skill names.
- No secrets, credentials, network calls, shell execution, or server/database access were added to the new command path.

### Spec Compliance: PASS

- Install shared agent skills: PASS. `installSharedAgentSkills()` writes `.agents/skills/mohist/SKILL.md` and `.agents/skills/mohist-explore/SKILL.md`, each template has frontmatter `name` and `description`, and generated content now starts with the frontmatter block.
- Install to explicit path: PASS. `projectPath` selects the base path and writes under `<repo>/.agents/skills`, with test coverage verifying the current working directory is not written when a different path is selected.
- Re-run generated install or update: PASS. Recognized generated files are checksum-validated, unchanged files remain unchanged, and stale generated files are rewritten from templates.
- Update repairs partial generated installation: PASS. `updateSharedAgentSkills()` creates missing distributed skills and continues to skip protected existing files.
- Protect user-modified skill files: PASS. Existing files that are not recognized as unchanged Mohist-generated files return `skipped-protected` for both install and update unless `install --force` is used.
- Force overwrite protected skill files: PASS. `installSharedAgentSkills({ force: true })` overwrites protected files with current generated templates and marker/checksum metadata.
- Help distinguishes skill types: PASS. `node bin/mo skills --help`, `node bin/mo skills install --help`, and `node bin/mo skills update --help` all state that the commands manage coder agent skills under `.agents/skills` and do not execute, scan, or modify internal Mohist skills under `.mohist/skills`.
- Internal Mohist skills are untouched: PASS. The new command path imports only the shared agent skills utility from `packages/cli/src/cli/commands/skills.ts:3-7`, does not reference `SkillService`, and does not create, update, delete, or scan `.mohist/skills`.
- Walkthrough exclusion: PASS. `SHARED_SKILLS` only includes `mohist` and `mohist-explore`, and runtime verification confirmed no `.agents/skills/mohist-walkthrough/SKILL.md` is created.

## Fix Suggestions

None.

<promise>PASS</promise>
