# Ralph Progress Log
Started: 2026-04-22T05:14:13.835Z

---


[2026-04-22] T-001 Remove skip permission from design.md prompt
- Removed "When to Include" conditional section and "you may skip this file" language
- Replaced with mandatory "You SHALL always generate this file" instruction
- Added guidance for simple changes to keep content minimal
- Build and typecheck both pass
- Learnings: The build copies .md files from src/agents/prompts/artifacts/ to dist/, so changes to the source are reflected in dist
- Next: T-002 (self-review prompt), T-003 (server startup recovery)

[2026-04-22] T-002 Update self-review prompt to treat design.md as required
- Changed `design.md (if it exists)` to `design.md` in self-review.md line 10
- Build and typecheck both pass
- Learnings: Simple one-line change, consistent with T-001's mandatory design.md approach
- Next: T-003 (server startup recovery), T-004 (verify with tests)

## 2026-04-22 T-003
- **What done**: Added `recoverIssues()` method to AgentRunnerService that queries all active non-draft issues, sets status=blocked, stage=draft, clears approval state, and logs each action. Wired `agentRunner.recoverIssues()` call in server/index.ts after construction. Clears internal recoverableIssues array after recovery.
- **Learnings**: The existing pipeline failure path (lines 262-279) uses the same pattern — good consistency. `recoverableIssues` array is cleared in-place via `.length = 0` to keep the reference valid for getStatus().
- **Next steps**: T-004 — run tests to verify no regressions.

## 2026-04-22 T-004
- **What done**: Ran `npm run typecheck` (clean), `npm run build` (clean), and `npm test` (41 files, 552 passed, 2 skipped — all pass). No regressions found.
- **Learnings**: All prior changes (T-001, T-002, T-003) are non-breaking. The EPIPE error at end of test output is a known Bun issue with pipe closure, not a test failure.
- **Next steps**: All 4/4 tasks complete. Change is ready for archive.
