issue-370 progress notes
========================

T-001 (AgentSessionDtoMapper extraction) — completed.

## Codebase Patterns

- `internal static class` mappers in `Sessions/Services/` are the established
  precedent (`AgentSessionJsonHelper`, `ContextHealthClassifier`,
  `ContextExhaustionClassifier`, `TranscriptEventSummaryProjector`). Same
  namespace + `internal` visibility + `Mohist.Server.Tests` via
  `InternalsVisibleTo` — `AgentSessionDtoMapper` follows the same pattern.
- `IssueRowMapper` / `WorkflowStatusMapper` are the explicit precedent for
  *pure static* type-as-mapper (no DI, no clock, no DB). `AgentSessionDtoMapper`
  matches: every member is a pure function from a session / summary / part row
  into a DTO. `ToUsageDto(AgentSession)` takes the session and resolves the
  usage summary + history via the existing `AgentSessionJsonHelper` / its own
  `BuildUsageHistoryDto`, so it stays a one-input, side-effect-free projection.
- Test re-targeting pattern: pre-existing specs that called
  `AgentSessionQuerier.X` directly are renamed to `AgentSessionDtoMapper.X`
  with assertion text preserved verbatim (the rename is the entire diff).
  `AgentSessionRecoveryDomainSpecs.cs` had 8 such calls (BuildLineageDto ×3,
  BuildUsageHistoryDto ×3, ToUsageDto ×2) — all re-targeted, all 27 specs
  in that file still pass with no assertion change.
- The "byte-alignment" invariant between the three consumers (querier
  workflow / current / metadata / generic-summary paths, activity feed
  assembler, generic summary) used to live in prose comments; the new
  cross-consumer identity specs assert it via property-by-property
  equality (`AssertUsageDtoEqual` / `AssertEventSummaryDtoEqual` /
  `AssertLineageDtoListEqual`) because the `record.Equals` path doesn't
  propagate cleanly through `IReadOnlyList<...Dto>` constructed by
  `.ToList()` on a type where the runtime list element is the same but
  the property type widens. Field-by-field comparison keeps the assertion
  deterministic and explicit about which fields must agree.

## Gotchas

- `AgentSessionQuerier` is in `Mohist.Server.Sessions.Services`, but the
  DTOs (`AgentUsageDto`, `AgentEventSummaryDto`,
  `RuntimeSessionLineageEntryDto`) are in `Mohist.Server.Sessions`. The
  test file `AgentSessionDtoMapperCrossConsumerIdentitySpecs.cs` needs
  `using Mohist.Server.Sessions;` to reference the DTOs in its assertion
  helpers — caught on first build with `CS0246`.
- `IssueWorkflowProfileStorageIntegritySpecs.DefensiveCopyVariables_AfterCommit_VerificationReportsReachable`
  flaked once during the full `npm test` run (failed in parallel, passes
  in isolation on both pre-change and post-change trees). Pre-existing
  flake, unrelated to this task. Full re-run is green (3808 passed,
  13 skipped, 0 failed).
- The `AgentSessionQuerier.ReconcileActiveSessionsAsync` /
  `LoadEventSummariesAsync` / `LoadIssueTitlesAsync` / `IssueTitle` /
  `Label` / `IssueNumber` / `Annotation` statics remain on the querier
  for now — those are owned by T-002, T-003, T-004. This task only
  removed the six DTO-projection statics + the duplicate `Labels`.
- `ToUsageDto(AgentSession)` on the new mapper is `internal static`,
  matching the visibility of the original. `IssueRowMapper` is
  `internal static` for the same reason (test assembly is the only
  out-of-assembly caller via `InternalsVisibleTo`).

## Decisions

- Followed the design's additive-first sub-strategy: introduced
  `AgentSessionDtoMapper` (with `Labels` authoritative) → swapped the
  three consumers (`AgentSessionQuerier` private helpers,
  `AgentActivityFeedAssembler`, `AgentUsageReporter`) → deleted the
  duplicate `Labels` at `AgentActivityFeedAssembler.cs:302-311` →
  removed the six DTO statics from `AgentSessionQuerier` → re-targeted
  the 8 spec calls → added the three cross-consumer identity specs.
  Each sub-step left the build green.
- `AgentSessionDtoMapper` is `internal static` matching the design
  decision (D1) and the `IssueRowMapper` precedent. No DI registration
  change needed.
- The cross-consumer identity spec file uses four facts instead of
  the "three" called out in the task description: one
  usage-DTO-querier-vs-assembler, one event-summary-DTO-querier-vs-assembler,
  one event-summary-DTO-non-exhaustion-flag-null (the
  "flags remain null (not false) when unmatched" branch of the
  byte-identity spec), and one lineage-DTO-metadata-vs-generic-summary.
  The three-scenario split maps to the three acceptance criteria in
  the agent-session-dto-mapping spec: same projection path
  byte-alignment, flag-null behaviour, and lineage byte-alignment.

T-002 (record accessors + Annotation forwarder removal) — completed.

## Codebase Patterns (T-002)

- The record's existing `Label(string key)` instance method was upgraded to
  perform the record-first-then-metadata fallback inline
  (`Labels[key] ?? Session.Metadata.Label(key)`) — design Open Question 1
  lean (a). Production records are built with `session.Metadata.Labels`
  inside `AgentSessionQuery.ToRecords`, so the two dictionaries coincide
  and the fallback is a defensive no-op; the only caller that observed
  the fall-back semantics was `WorkflowActivityQuerier.cs:118-124`'s
  private duplicate (now removed). `RunnerRoutes.cs:362-363` previously
  read the record-only `Label(key)`; the new method returns the same
  value for production records, verified via
  `AgentSessionContextAssociationApiSpecs` (6 specs pass).
- Method group context: `sessions.Select(AgentSessionQuerier.IssueNumber)`
  became `sessions.Select(r => r.IssueNumber())` — the static-to-instance
  shape change forced the lambda form. Lambdas are also future-proof if
  the method gains parameters.

## Gotchas (T-002)

- The querier's `Label(record, key)` static and the
  `WorkflowActivityQuerier.cs:118-124` private duplicate were
  byte-for-byte identical. Re-targeting `WorkflowActivityQuerier` to
  `record.Label(...)` removed a third copy of the fallback semantics
  that the proposal did not enumerate.
- `Annotation` was a pure forwarder (`=> session.Metadata.Annotation(key)`);
  inlining `session.Metadata.Annotation(key)` at the three call sites
  (`ToSummaryDto`, `BuildSessionMetadataDtoAsync` ×2) keeps zero behaviour
  drift while shrinking the querier surface.
- The `<c>AgentSessionQuerier.IssueNumber</c>` mention inside
  `AgentSessionRecord.IssueNumber`'s XML doc is intentional — it links
  the instance method to the spec section that pinned its semantics;
  it's a `<c>` text reference, not a `<see cref>` cross-reference, so
  no doc-repointing is required (per the T-002 notes).

## Decisions (T-002)

- Followed the design's additive-first sub-strategy: added the new
  instance methods to `AgentSessionRecord` → swapped the four consumers
  (`AgentSessionQuerier`, `AgentActivityFeedAssembler`,
  `AgentSessionContextRefs`, `WorkflowActivityQuerier`) → inlined the
  three `Annotation` call sites → removed the three statics
  (`Label`, `IssueNumber`, `Annotation`) from `AgentSessionQuerier`.
  Each sub-step left the build green.
- `AgentSessionRecordAccessorSpecs.cs` is a focused unit spec that
  exercises all three spec scenarios (record-precedence,
  metadata-fallback, absent-returns-null) plus the issue-number parse
  cases — numeric, empty/whitespace, non-numeric, decimal, alphanumeric.
  13 facts total; all pass.
- Followed the issue-370 design's "tuck the fallback XML doc on the
  method" guidance: the `<summary>` documents the fallback semantics
  explicitly and points the reader at the `AgentSessionQuery.ToRecords`
  construction site where the production-dictionary-coincides invariant
  holds.

T-003 (transcript reductions relocation) — completed.

## Codebase Patterns (T-003)

- `internal static class` for reductions that take `MohistDbContext` as a
  parameter (no DI, no clock) matches the existing mapper precedent
  (`AgentSessionDtoMapper`, `IssueRowMapper`). `TranscriptReductions` is a
  peer to `TranscriptPartLoader` so the transcript read-region is now
  bounded by two co-located types: the loader returns raw materials, the
  reductions project/summarise/reconcile them.
- Re-target pattern: each call site swap is a one-token rename
  (`AgentSessionQuerier.LoadEventSummariesAsync` →
  `TranscriptReductions.LoadEventSummariesAsync` and the same for
  `ReconcileActiveSessionsAsync`). Two call sites in the querier
  (`ListCurrentAsync`, `ListAgentSessionsAsync`) and two in the
  assembler (`GetActivityAsync` reconciliation + event-summaries); all
  four were renamed in lockstep. The assembler's `<see cref>` doc
  references were repointed to `TranscriptReductions.*` as part of the
  same swap, satisfying T-005's pre-emptive doc-cleanup note.
- The reductions consume the relocated primitives correctly:
  `LoadEventSummariesAsync` uses `AgentSessionDtoMapper.ToProjection` (T-001)
  for projection and `TranscriptEventSummaryProjector.Summarize` for
  per-session roll-up; `ReconcileActiveSessionsAsync` reads
  `WorkflowRunId` / `WorkId` labels via the record-instance `Label(key)`
  method (T-002), so the fallback semantics ride along for free.

## Gotchas (T-003)

- `TranscriptReductions` keeps its `using System.Text.Json;` and
  `using Microsoft.EntityFrameworkCore;` for the
  `DeserializeWorkflowRun` helper (typed deserialisation) and the
  `LoadWorkflowRunsForReconciliationAsync` LINQ-to-EF query respectively;
  those stay on the file because the reductions *do* talk to the DB
  through `MohistDbContext`, unlike `AgentSessionDtoMapper` which is a
  pure projection type.
- `LoadIssueTitlesAsync` / `IssueTitle` remain on `AgentSessionQuerier`
  for now — T-004's responsibility (issue-title lookup relocation to
  `Issue/Services/IssueTitleLookup`). The T-003 acceptance criteria
  scope explicitly excludes them; the verification gate
  `rg "internal static.*AgentSessionQuerier\b"` will only reach zero
  after T-004 lands.
- Existing `AgentSessionRecoveryDomainSpecs` (27 specs) and
  `AgentActivityFeedAssemblerSpecs` (6 specs) exercise both reconciliation
  branches via the end-to-end paths that the reductions now serve — the
  reduction logic itself is unchanged (pure move, not a rewrite), so the
  byte-alignment invariant holds without re-targeting any test calls.

## Decisions (T-003)

- Followed the design's additive-first sub-strategy: introduced
  `TranscriptReductions` with both reductions + helpers → swapped the
  four call sites (`AgentSessionQuerier.ListCurrentAsync`,
  `AgentSessionQuerier.ListAgentSessionsAsync`,
  `AgentActivityFeedAssembler.GetActivityAsync` ×2) → repointed the
  assembler's `<see cref>` doc references → removed the six members
  (`LoadEventSummariesAsync`, `ReconcileActiveSessionsAsync`, plus four
  private helpers: `DeserializeWorkflowRun`,
  `LoadWorkflowRunsForReconciliationAsync`,
  `IsSessionAssociatedWithRun`, `IsActiveSession`) from
  `AgentSessionQuerier`. Each sub-step left the build green and the
  324 Sessions specs passing.
- Chose `internal static` for `TranscriptReductions` (matching design D4
  and the `IssueRowMapper` / `AgentSessionDtoMapper` precedent). The
  reductions take `MohistDbContext` as a parameter, which is sufficient
  to keep them testable via the in-memory test DB factory the existing
  `AgentActivityFeedAssemblerSpecs` already exercises end-to-end.

T-004 (issue-title batch lookup relocation) — completed.

## Codebase Patterns (T-004)

- `internal static` for cross-domain read queries that take
  `MohistDbContext` as a parameter (no DI, no clock) matches the
  `TranscriptReductions` precedent set in T-003 and the `IssueRowMapper` /
  `AgentSessionDtoMapper` mapper precedent. `IssueTitleLookup` is the
  natural home: it sits on the Issue read side because both inputs
  (`db.Issues` + `IssueRowMapper.ByNumber`) are Issue-domain data, so
  Session callers read it directly with the `(project, numbers)` tuple
  instead of going through `AgentSessionQuerier`.
- Re-target pattern: each call site swap is a one-token rename
  (`AgentSessionQuerier.LoadIssueTitlesAsync` →
  `IssueTitleLookup.LoadTitlesAsync` and `AgentSessionQuerier.IssueTitle` →
  `IssueTitleLookup.Resolve`). Two call sites — one in
  `AgentSessionQuerier.ListCurrentAsync` (querier path) and one in
  `AgentActivityFeedAssembler.GetActivityAsync` (assembler path) — were
  renamed in lockstep, satisfying the cross-consumer identity invariant
  the spec requires ("both consumers obtain identical number→title maps
  by invoking the same Issue read-side capability").
- The assembler's `<remarks>` was updated to reference
  `IssueTitleLookup` alongside the existing references to
  `TranscriptReductions` / `TranscriptPartLoader`, so the activity-feed
  assembler's read-side dependency list (loader + reductions + issue-side
  capability) is now co-located in one block.

## Gotchas (T-004)

- `IssueTitleLookup` is the final cross-domain lookup that was still
  living on `AgentSessionQuerier` after T-001/T-002/T-003; with it gone,
  the querier's `internal static` member count is now zero (the global
  acceptance criterion for the issue). T-005's verification gate
  `rg "internal static.*AgentSessionQuerier\b"` should now return zero
  matches on `packages/server/src`.
- The `Resolve` method's pre-change fallback string `$"Issue #{number}"`
  is byte-identical in the new home — the unit spec exercises
  absent / empty / whitespace / zero cases so a future regression that
  changed the literal format (e.g. trimmed whitespace) trips an
  assertion.
- The existing `AgentSessionQuerier.IssueTitle` / `LoadIssueTitlesAsync`
  callers were all internal to the Session namespace
  (`AgentSessionQuerier.cs`, `AgentActivityFeedAssembler.cs`); no test
  called the statics directly, so no test re-targeting was needed — only
  the new `IssueTitleLookupSpecs` was added to cover the spec scenarios.

## Decisions (T-004)

- Followed the design's additive-first sub-strategy: introduced
  `IssueTitleLookup` (with both `LoadTitlesAsync` and `Resolve`) →
  swapped the two call sites (`AgentSessionQuerier.ListCurrentAsync`,
  `AgentActivityFeedAssembler.GetActivityAsync`) → updated the assembler's
  `<remarks>` to reference the new home → removed the two members
  (`LoadIssueTitlesAsync`, `IssueTitle`) from `AgentSessionQuerier`.
  Each sub-step left the build green and the 324 Sessions specs passing.
- `IssueTitleLookup` is `internal static` matching design D5 and the
  `IssueRowMapper` / `TranscriptReductions` precedent. No DI
  registration change needed; the test assembly's `InternalsVisibleTo`
  makes `IssueTitleLookup` directly accessible from the new spec file.
- `IssueTitleLookupSpecs` is a service-tier spec (uses `MohistDbFixture`)
  for the DB-touching scenarios (empty / dedup / project-filter /
  cross-consumer) and a unit-tier spec for the pure
  `Resolve(titles, number)` function. The acceptance criterion "internal
  static 成员数降为零" for the querier is pinned by a reflection-based
  spec (`AgentSessionQuerier_NoLongerExposesLoadIssueTitlesAsyncOrIssueTitle`)
  so any future regression that adds the statics back trips the test.
- 10 new facts total: 4 DB scenarios (empty input, dedup, load-all,
  cross-project drop) + 4 `Resolve` scenarios (stored title, absent,
  whitespace ×3 sub-cases, zero) + 1 cross-consumer identity spec + 1
  reflection spec asserting the querier no longer exposes the statics.
  All 10 pass; full `npm test` is green (3831 passed, 13 skipped,
  0 failed on the server; 908 passed on web).
