# Ralph Progress Log
Started: 2026-04-08T11:56:48.315Z

---

### 2026-04-08T20:07:00Z | T-008 | 实现 Change 自动检测

**What was done:**
- Added `OpenSpecDetection` and `WorkflowConfigWithDetection` interfaces to `workflow-loader.ts`
- Implemented `detectOpenSpecForIssue(cwd, issueNumber)` that checks `.mohist-specs/changes/{num}-*/prd.json` existence
- Implemented `loadWorkflowWithDetection(cwd, issueNumber)` combining workflow loading with detection
- Updated `read-workflow.ts` to use `loadWorkflowWithDetection` and include OpenSpec detection section in output
- Updated `main-agent.ts` to pass `issueNumber` to read-workflow context, updated system prompt
- Wrote 9 unit tests covering all detection scenarios (no dir, no match, no prd, with prd, etc.)
- All 275 tests pass, build succeeds

**Learnings:**
- Detection has 3 states: not detected, detected-but-no-prd (plan in progress), detected-with-prd (use Ralph)
- The `detectOpenSpecForIssue` function only needs issue number, not full Issue object, keeping workflow-loader decoupled from types

**Next steps:** T-009 (mo propose command), T-010 (resume --skip-to-review), T-011 (check stage extension)

