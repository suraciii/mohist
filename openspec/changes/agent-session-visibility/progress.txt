# Ralph Progress Log
Started: 2026-04-15T10:23:07.116Z

---


[2026-04-15T10:27:30Z] T-001 COMPLETED
- Created agent-session-message-repo.ts with AgentSessionMessageRepo class (insert, findByIssueId methods)
- Added migration v12 creating agent_session_message table with all required columns
- Added index idx_agent_session_message_issue_step on (issue_id, step_index, message_index)
- Exported from db/index.ts
- npm run build succeeds, all tests pass
- Learning: followed existing repo patterns (WorkflowLogRepo, ExploreMessageRepo) for consistency
- Next: T-002 (coder_session table) or T-003 (persist messages from agent-loop)

[2026-04-15] T-002 completed
- Created coder-session-repo.ts with CoderSessionRepo class (insert, updateStatus, findByIssueId methods)
- Added migration v13 to create coder_session table with index on issue_id
- Exported from db/index.ts
- npm run build succeeds
- Next: T-003 (persist Main Agent messages) and T-004 (record coder session mapping)

[2026-04-15] T-003 completed
- Added persistMessage helper to agent-loop.ts that extracts role/content/tool_calls/tool_result from ModelMessage
- Assistant messages: extracts text content and tool_call parts (with toolCallId, toolName, args as JSON)
- Tool messages: extracts each tool-result part (toolCallId, toolName, output as toolResult)
- System/user messages: stored as-is with content stringified if needed
- Added AgentSessionMessageRepo to AgentLoopOptions, MainAgentContext
- Wired repo through StateManager -> AgentRunnerService -> MainAgentContext -> runAgentLoop
- step_index aligns with result.steps array index, message_index aligns within step.response.messages
- npm run build succeeds
- Next: T-004 (record coder session mapping) or T-005 (REST API endpoints)

[2026-04-15] T-004 Record coder session mapping on ACP spawn and completion
- Added CoderSessionRepo to StateManager with getter
- Wired CoderSessionRepo through AgentRunnerService constructor -> MainAgentContext -> SpawnCoderContext -> AcpSessionOptions
- In runAcpSession: insert coder_session row with status='running' after sessionId is obtained
- On success: update to 'completed'; on prompt timeout: update to 'failed'; on catch after session creation: update to 'failed'
- No row is created if ACP fails before session creation (initialize/newSession timeout) - per spec
- Build succeeds, all tests pass (e2e failures are pre-existing)
- Learning: coderSessionId must be declared before try block since catch block needs access to it
- Next: T-005 Add REST API endpoints for session data

[2026-04-15] T-005 completed.
- Added GET /issues/:number/agent-session endpoint returning agent_session_message rows with fields: id, role, content, toolCalls, toolCallId, toolName, toolResult, stepIndex, createdAt
- Added GET /issues/:number/coder-sessions endpoint returning coder_session rows with nested workflowLogs (filtered by acpSessionId)
- Both endpoints return empty arrays when no data exists (repos return empty arrays from findByIssueId/findBySessionId)
- Extended createIssueRoutes signature with agentSessionMessageRepo and coderSessionRepo params
- Wired repos through server/index.ts
- Build passes successfully
- Next: T-006 (frontend types and SSE subscription)

[2026-04-15] T-006 completed
- Added AgentDetailEventMap type with all 6 event payloads (agent_text_chunk, main_tool_call, coder_text_chunk, coder_tool_call, ralph_task_update, ralph_loop_progress) to types.ts
- Merged AgentDetailEventMap into existing EventMap via intersection type
- Created lib/agent-events.ts: global EventTarget-based event emitter with dispatchAgentEvent, onAgentEvent, and AGENT_DETAIL_EVENTS list
- Updated useSSE.ts to subscribe to all 6 new events and dispatch them through the global emitter
- Components can now subscribe via onAgentEvent() without creating duplicate SSE connections
- Text buffer and tool call timeline accumulation will be handled in T-007 (useAgentSession hook)
- Learnings: Pre-existing TS errors in CustomProviderDialog.tsx and ProviderConnectDialog.tsx (unused imports) - not related to this change
- Next: T-007 (useAgentSession hook)
