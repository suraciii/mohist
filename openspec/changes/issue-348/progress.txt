## Codebase Patterns

- Grain tests use `new AgentGrain(new AgentStore(factory), new AgentQuerier(factory), new FakeTimeProvider(...)) { GrainKeyForTest = GrainKey.Agent(projectId, agentId) }` (AgentGrainSpecs.cs). The fixed-time `FakeTimeProvider` makes time-related assertions awkward — `CreateGrain` was refactored to expose an overload that accepts the FakeTimeProvider so tests can `Advance` between operations and verify `UpdatedAt` progression. The original signature (fixed time, just projectId + agentId) is preserved for the other tests.
- `AgentStore` serializes the `Domain.Agent` to a single `State` JSON column (Infrastructure/Data/Agent/AgentStore.cs:24-43); `Deserialize(state)` is `public static` and reads it back, perfect for asserting the persisted row matches the in-memory view in tests.
- `AgentDefinitionRoutes` uses `IGrainFactory grains.GetGrain<IAgentGrain>(GrainKey.Agent(projectId, id))` then awaits the grain method, mapping `null` to `NotFound` via the `result is null ? NotFound : Ok(result)` shape. The new `POST /{id}/unarchive` mirrors the DELETE archive route's exact shape (AgentDefinitionRoutes.cs:86-95 → 97-104), satisfying D1+D4 (mirror Issue precedent path, return 200 with data).
- `ApiTestClient.PostDataAsync<T>(path)` accepts a null body and issues POST with no body — used by the unarchive route since the request carries no payload.
- `MohistIntegrationFixture` exposes `TimeProvider` (FakeTimeProvider) which API spec tests can `Advance` to assert `UpdatedAt` progression deterministically (no wall clock).

## Decisions Applied

- D2: `IAgentGrain.UnarchiveAsync` returns `Task<AgentInfo?>`, matching `ArchiveAsync` (null = not-found) instead of the Issue domain's throw shape. Reuses the existing `result is null ? NotFound : Ok(result)` route plumbing.
- D3: `AgentGrain.UnarchiveAsync` short-circuits when `_agent.Status == Active` — returns `AgentQuerier.ToInfo(_agent)` without bumping `UpdatedAt` and without calling `_agentStore.SaveAsync`. No write occurs; the persisted row's `UpdatedAt` is unchanged. Archive does NOT short-circuit (it re-writes & bumps), preserving the asymmetry that motivated D3.
- D1/D4: route verb is `POST /api/projects/{projectRef}/agents/{id}/unarchive`, response body is the unarchived `AgentInfo` (200), DELETE stays archive, PATCH stays unchanged.
- The new route pre-checks via `AgentQuerier.GetByIdAsync` to avoid the round trip when the agent id is unknown in the project (consistent with DELETE archive and PATCH). The grain itself still returns null as a defense-in-depth; the route covers both layers.

## Verification

- `dotnet build Mohist.sln -p:SkipWebBuild=true` — 0 warnings, 0 errors (TreatWarningsAsErrors).
- Targeted: `dotnet test --filter "FullyQualifiedName~AgentGrainSpecs|FullyQualifiedName~AgentDefinitionApiSpecs"` — 18 passed (11 pre-existing + 3 grain specs + 4 API specs).
- Full suite: `dotnet test Mohist.sln` — 3758 passed, 0 failed, 13 skipped (skipped entries are pre-existing Architecture/Issue-Profile work, unrelated to issue-348).

## Test Coverage

Grain (AgentGrainSpecs.cs):
- `Archive_then_unarchive_round_trips_agent_to_active_and_advances_updated_at` — verify round-trip + UpdatedAt progression + persisted row reflects active.
- `Unarchive_of_already_active_agent_is_a_no_op_without_advancing_updated_at_or_persisting` — verify no-op (UpdatedAt unchanged, persisted row unchanged).
- `Unarchive_of_unknown_agent_returns_null_and_creates_no_row` — verify not-found contract + no row created.

API (AgentDefinitionApiSpecs.cs):
- `Unarchive_ReversesArchiveAndAdvancesUpdatedAt` — end-to-end via DELETE → POST unarchive, asserts 200 with status=active + UpdatedAt > archive time + the agent re-appears in the default list (active-only).
- `Unarchive_IsNoOpForActiveAgentAndReturnsNotFoundForUnknown` — covers the no-op deterministically + 404 on unknown id and 404 on cross-project id (project resolution filter rejects it before the grain).
- `Unarchive_DoesNotAlterPatchSemanticsForStatusField` — regression guard: PATCH `status=archived` is silently ignored, agent stays active, UpdatedAt still advances (because PATCH description moved it).
## T-002 Web Data Layer (unarchive + archived list visibility)

### Codebase Patterns
- `useAgents` consumers: `AgentListPage` (filters archived via `filter(a => a.status === 'archived')` for the section, expects archived agents to be in the data), `AgentSessionComposerPage` (the only consumer that must show launchable-only). No other page consumes `useAgents`. Filter applied at the consumer (single `['agents']` cache key).
- `AgentDetailPage` reads its own agent via `useAgent(agentRef)` with query key `['agents', projectId, agentRef]`, so the detail-page row is already covered by the `['agents']` prefix invalidation in the unarchive mutation.
- `useArchiveAgent` already invalidates both `['agents']` and `['agent-status']` — `useUnarchiveAgent` mirrors this (D7) so the runner re-evaluates the agent on the way back to active.
- `AgentSelector` in `AgentSessionComposerPage.tsx` previously rendered an "Archived" badge inline; with the filter, the picker no longer contains archived rows. Two test cases were reshaped: the old "allows selecting archived agents but shows warning" was replaced by "excludes archived agents from the launcher picker" + "shows archived warning when ?agent= points at an archived agent even though it is not in the picker" (preserves the deep-link warning invariant).
- `AgentListPage` already had a dormant `"Archived (n)"` UI; promoted the section wrappers to `data-testid="active-section"` / `data-testid="archived-section"` for cleaner component assertions without coupling to text content.
- Test pattern for client functions (`unarchiveAgent` POST test) follows `entities/issue/api/client.test.ts`: stub `fetch` with a `vi.fn<typeof fetch>()` and assert path + method via `fetchMock.mock.calls[0]`.

### Decisions Applied (T-002)
- D4: `unarchiveAgent` issues `POST /api/projects/{ref}/agents/{id}/unarchive` and types the response as `AgentInfo` (matches `archiveAgent`'s contract).
- D5: `useAgents` calls `listAgents(projectId!, { all: true })`; the session composer is the only consumer filtered to launchable. Single `['agents']` cache key, no `useAllAgents` split.
- D7: `useUnarchiveAgent` invalidates `['agents']` and `['agent-status']`, shows `Agent restored` toast — mirrors `useArchiveAgent`.

### Verification
- `npm run typecheck -w packages/web` — clean.
- `npm run test:run -w packages/web` — 259 files / 4091 tests pass, 1 skipped (pre-existing); 4 newly-added unarchive tests, 4 newly-added Archived-section tests, 1 reshaped composer test, 1 reshaped archived-warning composer test.

## T-003 Web Detail Page Actions Card

### Codebase Patterns
- The destructive-confirm pattern from `AgentProfileEditor.tsx:251-275` (second `<Dialog>` driven by a local `archiveConfirmOpen` useState) is the established affordance for irreversible actions — `handleArchive` calls the mutation with an `onSuccess` that closes the confirm dialog. Reused verbatim on the detail page.
- `useArchiveAgent` / `useUnarchiveAgent` expose `{ mutate, isPending }`; the detail-page button uses `disabled={...isPending}` and a `<Loader2Icon className="size-4 animate-spin" />` while pending, mirroring the editor's pattern. Both mutations already invalidate `['agents']` (and `['agent-status']`) from T-002, so the detail-page agent query (`['agents', projectId, agentRef]`, queries.ts:67) auto-refreshes the disabled-state of New Session after unarchive — no extra invalidation needed.
- The shared `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription` primitives (shared/ui/components/dialog.tsx) accept `data-testid` on `DialogContent`, so the destructive-confirm dialog gets `data-testid="agent-detail-archive-confirm-dialog"` for direct DOM targeting without coupling to text.
- Test pattern for component-level mutation mocks: extend the existing `vi.mock('../../../entities/agent', ...)` block in the page test to also stub `useArchiveAgent` and `useUnarchiveAgent`. The stubs expose `mutate: vi.fn()`-shaped spies (here ad-hoc closures pushing into a `vi.hoisted` array) and `isPending` flags, so component assertions can verify which mutate was called with which id and confirm-state transitions.

### Decisions Applied (T-003)
- D6: Actions card layout is `{active: Archive button -> local confirm dialog -> mutate}`, `{archived: Unarchive button -> mutate}`. Testid `agent-detail-archive-btn` preserved; new testid `agent-detail-unarchive-btn`; confirm dialog testid `agent-detail-archive-confirm-dialog` with `agent-detail-archive-confirm` / `agent-detail-archive-cancel` actions.
- The static archived notice string `"This agent is archived and cannot be launched."` is fully removed (acceptance: "static archived notice is replaced by an Unarchive control"). The "cannot be launched" semantics are still expressed by the existing top-level disabled New Session button.
- The detail-page confirm dialog copy states the real effect: "leaves the Active group, will not be launchable, remains visible in the Archived group, and can be restored from this page" — the latter sentence is now backed by the same dialog's sibling Unarchive control, so D8's "reversibility claim is backed by a working affordance" invariant is satisfied. The pre-fix T-004 dialog copy in `AgentProfileEditor` is intentionally not touched here (its own task).
- Detail-page Unarchive button uses `RotateCcwIcon` (already in lucide-react via `TemplateEditor.tsx:2,447`) — the conventional restore affordance across the web.
- "New Session" disabled-for-archived guarantee from the agent-archive spec is preserved verbatim (lines 222-232). The mutation-callback-on-unarchive path naturally re-enables New Session because `useAgent` invalidation refreshes `agent.status` to `'active'`.

### Gotchas
- `useMutation`'s `mutate` second arg accepts `(variables, options?)` — the onSuccess option here is what closes the confirm dialog. Hooking `onSuccess: () => setArchiveConfirmOpen(false)` (instead of letting the unmount/remount cycle handle dialog state) keeps the test pattern simple: the mock's `mutate` immediately invokes `options.onSuccess` and the test asserts the dialog is gone.
- The `unarchiveAgent` POST has no body. The component passes `agent.id` as the only argument to `mutate(id)` — no payload object — which matches T-002's `useUnarchiveAgent` signature `(agentRef: string) => Promise<AgentInfo>` (queries.ts:122).
- The top-level Edit button (`177-185` pre-T-003 / `203-211` post-T-003) is intentionally untouched: it's the canonical edit entry regardless of archive state. The detail-page Unarchive control does NOT replace Edit for archived agents — both exist; Edit stays open for name/instructions/skills editing even on archived agents (unchanged behavior).

### Verification
- `npm run typecheck -w packages/web` — clean.
- `npm run test:run -w packages/web -- src/pages/agent-detail` — 18 tests pass (10 pre-existing + 8 newly-added under the `Actions card (agent-archive + agent-unarchive specs)` block).
- Full web suite: `npm run test:run -w packages/web` — 259 files / 4099 tests pass (1 pre-existing skip). T-002 baseline was 4091; T-003 added 8 detail-page Actions card tests, all green.
- Runner (`npm test -w packages/runner`) — pre-existing flake in `tests/runner-host-task-log.spec.ts > RoutesConcurrentIncrementalUploadsToEachWorkItemCollector` (5s timeout) when the whole suite is run back-to-back; passes in isolation. Unrelated to T-003 (T-003 touches only `pages/agent-detail/ui/AgentDetailPage.{tsx,test.tsx}`).

### Test Coverage (T-003)
Detail-page component tests, under the new `Actions card` block:
- `for an active agent, the Archive button does not open the Edit dialog on click` — fixes the label/behavior mismatch regression.
- `for an active agent, clicking the Archive button opens a confirm dialog (not a direct archive)` — direct-trigger with confirm.
- `cancelling the archive confirm does NOT archive` — local-state confirm gate works.
- `confirming the archive invokes useArchiveAgent.mutate with the agent id and closes the confirm dialog` — end-to-end of the active-agent archive path.
- `for an archived agent, the static archived notice is replaced by an Unarchive control` — UI loop closed.
- `for an archived agent, clicking the Unarchive control invokes useUnarchiveAgent with the agent id` — detail-page unarchive wiring.
- `for an active agent, the Unarchive control is NOT rendered (no mismatch)` — no archived-only affordance leaks into the active state.
- `for an archived agent, the New Session control remains disabled (archived-cannot-launch invariant)` — no regression of the existing constraint.
