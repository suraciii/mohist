# Ralph Progress Log
Started: 2026-04-21T16:27:56.429Z

---


## 2026-04-22 T-001
- Implemented resolveOpencodeBinPath() in config-loader.ts with 3-tier priority: env var > config.jsonc > auto-detect
- Added opencode.binPath to config-schema.ts
- Added opencodeBinPath optional field to AcpSessionOptions and AcpConnectionOptions
- Updated both spawn() calls in acp-session.ts to use opencodeBinPath || 'opencode'
- Updated server/index.ts to resolve opencode path at startup and pass to route creators
- Updated issues.ts (5 acpOptions constructions) and propose.ts (1 acpOptions) to pass opencodeBinPath
- npm run build passes
- Learning: The acpOptions construction is duplicated across 6 call sites in issues.ts + propose.ts; future refactor could centralize this.
- Next: T-002 (already done as part of T-001 since it was merged), T-003 spawn call update (also done), T-004/T-005 error propagation

[2026-04-22] T-002: config-schema.ts already had opencode.binPath field (added in T-001). Verified build passes. No additional changes needed.

## 2026-04-22 T-003
- Verified both spawn() calls in acp-session.ts already use `opencodeBinPath || 'opencode'` (lines 105 and 445)
- Both AcpSessionOptions and AcpConnectionOptions already have opencodeBinPath optional field
- This was completed as part of T-001 (which merged original T-001/T-002/T-003)
- npm run build passes
- No additional changes needed
- Next: T-004/T-005 spawn error propagation

## 2026-04-22 T-004
- Implemented spawn error propagation in createAcpConnection() in acp-session.ts
- Added `initialized` flag and `rejectOnInit` deferred for event-driven spawn failure detection
- proc.on('error') now calls rejectOnInit with [SPAWN_FAILED] prefixed error
- Added proc.on('exit') handler that rejects on premature exit (code !== 0) before initialize
- connection.initialize() now races against spawnFailure promise via Promise.race
- .catch() on the race ensures cleanup is called before re-throwing spawn errors
- After successful initialize: sets initialized=true, clears rejectOnInit to prevent unhandled rejections
- npm run build passes
- Learning: TypeScript control flow narrowing correctly identifies that spawn errors set in async callbacks are unreachable at synchronous pre-check points. The race-based approach is both more correct and avoids TS narrowing issues.
- Next: T-005 (runAcpSession spawn error propagation, similar pattern), T-006/T-007/T-008
