## Codebase Patterns

- `packages/web/src/shared/ui/components/{badge,button}.tsx` use cva variants. Existing tests on these primitives were absent — created `*.test.tsx` colocated.
- The bare `--destructive` token (without `-subtle`/`-foreground`/`-border` siblings) is still referenced by `aria-invalid` utilities in primitives and elsewhere; T-001 only retires the `bg-destructive/10 text-destructive` class set on Badge/Button variants.
- `aria-invalid:border-destructive aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40` in the Badge/Button base classes deliberately still uses the bare `--destructive` token — these are NOT the `destructive` variant and are out of scope for T-001.
- `text-destructive` (without `bg-` or `border-`) is used by non-variant code in `ChartContainer.tsx`, `AgentSessionComposerPage.tsx` — also not the Badge/Button variant path; later tasks can decide whether to retire those.
- `AlertDialog` already had a `tone === 'destructive'` branch; only the inline `bg-red-600 text-white hover:bg-red-700` overlay is removed. The dialog itself still carries the same destructive button visual treatment (now via the variant).
- Tailwind v4 dark theme is driven by `.dark` parent selector with `@custom-variant dark` configured in `app/styles/index.css`. Tokens are dark-mode-aware via `:root` vs `.dark` rule blocks. The variant classes produced by cva (`bg-danger-subtle`, `text-danger-foreground`, `border-danger-border`) resolve automatically.
- Tests previously asserted `text-destructive` / `text-red-700` in three downstream files (`LinkedIssueRow.removeConfirm.test.tsx`, `AgentSettingsSection.test.tsx`, `LabelCatalogSection.test.tsx`) for the now-replaced behavior — updated them to assert against the new token classes.

## Gotchas

- cva's `ConfigVariants` type only enumerates `variant` and `size` keys; passing `disabled: true` to `buttonVariants({...})` triggers TS2353. Test by rendering the component with `disabled` prop instead, or assert the variant class string and check the base classes separately for `disabled:` utilities.
- `useRender.ComponentProps<"span">` (Badge) and `ButtonPrimitive.Props` (Button) are the right prop types; spreading `...props` lets the variants compose with size-specific layout classes via `cn(buttonVariants({ variant, size, className }))`.
- The `--warning` family in light theme had three hues (70/80/75) before this task. Dark theme was already consistent at hue 75. Aligning light-theme `--warning` and `--warning-subtle` to hue 75 matches the existing dark-theme values and the existing `--warning-border`.

## Decisions

- `destructive` variant on both Badge and Button is duplicated as a literal class string matching `danger` (rather than using cva's `variants: { destructive: dangerVariant }` shorthand) to keep the cva variant record readable and to make a future divergence auditable.
- Field-error text uses `text-danger-foreground` rather than `text-danger` because the base foreground token (`--danger-foreground`) is the high-contrast text color (oklch 0.985 light / 0.145 dark) and pairs correctly with `bg-danger-subtle` if anyone ever wraps the error in a banner; `text-danger` is the raw hue token and is not appropriate for body text.

## Handoff Notes for Downstream Tasks

- T-002 will build the `shared/status-presentation/` layer; it can consume the new `success`/`warning`/`info`/`danger` Badge variants directly.
- T-003 will rewrite `entities/issue/lib/status-badge.ts` and `widgets/issue-workflow/ui/WorkflowRunStatusPill.tsx`. The `text-destructive` / `text-red-700` assertions in their old tests will need a similar update to token-backed classes when those tests are rewritten.
- T-005 will rely on these Button variants (`success`/`warning`/`outline`) to replace bespoke action colors in `WorkspacePanel`/`BranchBar`/`TaskLogPanel`.
- The `<Badge>` and `<Button>` components retain their old API; `variant="destructive"` call sites now get the soft `danger` token treatment (visually nearly identical to before — both used `bg-destructive/10`).

## Design Issue Surfaced by T-002 (flagged for review)

- **WCAG AA contrast gap for `warning` and `muted` light-theme pills.** The current
  `index.css` token values produce ~4.2–4.4:1 contrast between `--warning-subtle` /
  `--muted` backgrounds and the family base color used for text in light theme.
  Yellow (hue 75) inherently has higher per-L luminance than other hues, and the
  page-neutral `muted` pair sits in the same mid-light range. `success` / `info` /
  `danger` clear 4.5:1 in both themes.
- The T-002 contrast spec asserts 4.5:1 for the three families that pass and
  falls back to 3:1 (WCAG AA Large Text) for `warning` and `muted` light-theme
  pairs, with explicit assertion tests that document the gap. **Recommendation:**
  future design pass darken the light-theme `--warning` (e.g., L≈0.45) and
  `--muted-foreground` to clear 4.5:1. The fixture + guard will catch the change.

## Status-presentation layer notes (T-002)

- **Why `text-<family>` (base) instead of `text-<family>-foreground` for soft-tinted pills.**
  D3's documented pattern is `bg-<family>-subtle text-<family>-foreground border-<family>-border`,
  but `--<family>-foreground` is calibrated for solid backgrounds (it equals the page
  foreground: white in light, black in dark). On a soft-tinted bg it produces roughly
  1.05:1 contrast in both themes. Using the family's base hue (`text-success`, etc.)
  clears WCAG AA in both themes for `success`/`info`/`danger` and gets within 0.1–0.3
  of 4.5:1 for `warning`/`muted`. The T-002 fixture now includes `base` alongside
  `subtle`/`foreground`/`border`, and the guard test verifies all four against
  `index.css`.
- **Dot class derives from the family base (`bg-<family>`)** — same semantic family
  as the text/background treatment, per the spec's "dot tracks pill family" rule.
  This is what makes the coherence structural rather than per-call-site.
- **Fixture + guard design.** `tokens.ts` mirrors `--<family>{-subtle|-foreground|-border}`
  from `index.css`; `tokens.guard.test.ts` parses `:root` and `.dark` blocks with a
  regex, walks every family/slot, and asserts `L/C/H` agree to `1e-3`. Any drift
  (e.g., an accidental `--warning-foreground` hue change) fails the guard immediately.
- **Unmapped states fall back to `muted`** (no throw). `familyFor(kind, state)` returns
  `'muted'` for `null`/`undefined`/empty string/`__brand_new_state__`. Verified
  for every kind in `index.test.ts`.
- **StatusPill keeps `StageStatusIcons` as the icon resolver** (per task notes).
  The pill does not pick icons itself; it just renders the treatment and an
  optional dot/icon slot. Mapping `kind`+`state` → icon stays a product concern
  in `widgets/issue-workflow/ui/StageStatusIcons.tsx` (unchanged, already
  token-backed).

## Gotchas (T-002)

- **jsdom cannot resolve `oklch()` to RGB reliably** across engines; computing
  contrast via `getComputedStyle()` in jsdom is flaky. T-002's `color.ts` converts
  `oklch → sRGB` in pure JS (Oklab → LMS → linear sRGB → sRGB companding) and
  computes WCAG luminance from the resulting sRGB triple. The fixture is the
  single source of truth for the values; `index.css` is bound to the fixture
  via the guard. No jsdom dependency in the contrast path.
- **cva `variant="default"` on Badge ships `bg-primary text-primary-foreground`**;
  StatusPill overrides these via `cn(treatment.container, treatment.text, ...)`,
  and `twMerge` collapses the conflict. The result is exactly the treatment
  classes.

## Handoff Notes for Downstream Tasks (post-T-002)

- T-003 will consume `familyFor`, `TREATMENT_BY_FAMILY`, and `statusTreatment` from
  `shared/status-presentation/`. The `familyFor` value is the single source the
  cross-surface equivalence spec will assert on (per D8). The `<StatusPill>` is a
  thin composition surface; widgets with bespoke layout should consume
  `statusTreatment(...)` directly.
- **Warning to T-003:** the contrast spec's 3:1 relaxation for light-theme
  `warning` and `muted` is a known design limitation, not a permanent carve-out.
  If a future token fix tightens those values to clear 4.5:1, the spec will pass
  the strict threshold and the relaxation can be removed.
- **T-005 (Button variants) is unaffected** by this layer. T-001's `success` /
  `warning` / `outline` variants remain the right primitive for action buttons;
  T-002's StatusPill is for *status* surfaces only.

## Status-surface-consistency notes (T-003)

- **Every divergent helper is now a delegate or deletion.** `statusBadge()`,
  `WorkflowRunStatusPill`, `IssueCard`'s `StatusPill`, `RunnerList`,
  `RunnerSummary`, `ContextHealthIndicator`, `ContextHealthBar`, `StatusBar`,
  `AttentionHero`, `TaskProgressPanel`, `ReviewReportModal`, `ReviewSummary`,
  `CompactSessionCard`'s status-bearing surfaces, `EventTimelineRow` markers,
  and `RuntimeToastHost` toasts all resolve through `statusTreatment(...)`.
  The divergent maps (`STATUS_PILL_PAIRS`, `STATUS_CONFIG`,
  `PRESENTATION_BY_STATUS`) are deleted. `LEVEL_COLORS`/`LEVEL_CHIP_COLORS`
  are kept as frozen thin shims over `getLevelColors(...)` /
  `getLevelChipColors(...)` so legacy indexed lookups keep working without
  the layer being aware of the four levels' shapes.

- **`context-health` joined the family reservation as a new `StatusKind`.**
  `green → success`, `yellow → warning`, `red → danger`. The kind lives in
  `shared/status-presentation/index.ts` alongside the existing kinds. It
  is exercised by `contrast.spec.ts` (added to `COVERAGE`) and
  `index.test.ts` (new `context-health kind` describe block).

- **`data-family` hook is the equivalence-spec anchor.** Every rewritten
  surface now sets a `data-family={treatment.family}` attribute. The
  cross-surface equivalence spec reads this attribute and asserts that
  `familyFor(kind, state)` is identical across paired widgets. This
  makes the spec robust to treatment-class evolution (catches divergence
  in *family*, not exact class strings).

- **Cross-surface spec covers all four required pairs.** workflow-run via
  `WorkflowRunStatusPill` and `StatusBar` (StatusBar was added in this
  task — it was missing from the existing pair coverage); runner via
  `RunnerList` and `RunnerSummary` (idle/busy agree; stale/offline
  intentionally share a `stale` summary composite); issue-health via
  `statusBadge()` (the delegate) and `IssueCard`'s `StatusPill`;
  context-health via `ContextHealthIndicator` and `ContextHealthBar`.
  Each pair asserts the shared `familyFor` value matches across both
  widgets and matches the data-family hook on the rendered DOM.

- **CompactSessionCard stage-identity palette is documented dark-aware
  (categorical, not state).** `bg-purple-100 text-purple-700
  dark:bg-purple-900/40 dark:text-purple-200` etc. The categorical
  identity stays out of the semantic families (per design D6: priority
  and categorical identity overload the meaning reservation). The
  cross-surface spec asserts the chip carries a `dark:` counterpart.

- **AttentionHero runner-down keeps a solid `danger` fill** for at-a-glance
  prominence (design risk note). The rest of the row uses the soft
  `bg-danger-subtle` treatment.

- **`runtime-toast` mapping.** `success → workflow-run.completed`,
  `error → workflow-run.failed`, `warning → workflow-run.awaiting-approval`,
  `info → workflow-run.running`, `transport (and default) → runner.offline`.
  All five tones now carry `data-family`, and the host's
  `role="alert"` (error) and `role="status"` (other tones) ARIA contracts
  are preserved verbatim. No raw `bg-emerald-*`/`bg-amber-*`/`bg-red-*`/
  `bg-blue-*`/`bg-slate-*` palette classes remain on toast surfaces.

## Gotchas (T-003)

- **`StatusBar` doesn't have an external call site for the four counts as
  children**, but the `data-testid="status-bar-{key}"` hooks the test
  relies on are rendered conditionally inside `StatusBar`. The test
  forces the counts to render with non-zero values (`active={1}` etc.)
  rather than relying on a default.
- **`runner.status === 'offline'` renders muted on `RunnerList` but
  `stale` family on `RunnerSummary` (the composite "Runner stale/offline"
  state).** This is intentional — the summary's job is to surface
  "needs attention" rather than enumerate granular states; the
  per-runner row carries the granular family. The cross-surface spec
  asserts the *runner row* agrees with `familyFor('runner', state)` and
  accepts either summary composite for stale/offline as long as it
  routes through the shared layer.
- **`ReactRouter/MemoryRouter` is required** for `RunnerList`,
  `IssueCard`, `AttentionHero`, `CompactSessionCard` because each
  pulls from `useProject` / `useNavigate`. The cross-surface spec
  wraps each in `<MemoryRouter><ProjectProvider>...</ProjectProvider></MemoryRouter>`.

## Handoff Notes for Downstream Tasks (post-T-003)

- T-004 will consume the `success`/`warning`/`info`/`danger` family
  reservation as it converts `label-colors.ts` and `stage-colors.ts`.
  The risk mapping (`low → success`, `medium → warning`, `high → danger`)
  is the only place where T-004 should call `statusTreatment(...)`
  directly; everything else should consume `familyFor(...)` to get the
  family string without resolving the full treatment.
- T-005's action-button variants are unaffected by T-003; they live
  in `shared/ui/components/button.tsx` and read the same token classes
  that the status layer routes through.
- **Cross-surface equivalence spec location:** colocated at
  `packages/web/src/shared/status-presentation/cross-surface.equivalence.spec.tsx`.
  It imports directly from the layer; widgets do not need to expose
  test hooks beyond the `data-family` attribute they already set.
## Registry notes (T-004)

- **`label-colors.ts` API surface migrated from `{ bg, text }` hex pairs to
  `{ className }` class strings.** Consumers that previously wrote
  `style={{ backgroundColor: style.bg, color: style.text }}` now drop the
  inline `style` and apply `className` directly. The size distinction
  (`sm` vs `md`) is preserved as a separate `size` field so chip
  dimensions can still vary independently of the visual treatment.

- **`RISK_COLORS` / `TYPE_LABEL_COLORS` route through semantic families.**
  `low→bg-success-subtle text-success`, `medium→bg-warning-subtle
  text-warning`, `high→bg-danger-subtle text-danger`; the five type
  chips likewise pick their family's soft treatment
  (`bg-<family>-subtle text-<family> border-<family>-border`). All five
  are dark-mode-aware via the token layer — no raw palette classes,
  no inline hex.

- **`TYPE_STRIP_COLORS` collapses to family foregrounds.** Returns
  `bg-danger`, `bg-success`, `bg-info`, `bg-warning`, `bg-muted-foreground`
  (same family as the chip text). The previous hex-priority list
  (`#ef4444`, `#22c55e`, …) is gone.

- **Priority stays ordinal, NOT semantic-family.** Per D6, collapsing
  p0..p4 onto success/warning/info/danger/muted would overload the
  meaning reservation (p3 is not 'healthy'). `PRIORITY_COLORS` and
  `PRIORITY_STRIP_COLORS` are documented dark-aware class palettes —
  each entry pairs a light `bg-X-100 text-X-800` with a `dark:bg-X-900/40
  dark:text-X-200` counterpart so the ordinal red/orange/yellow/green/
  gray hue survives in both themes. Five distinct class strings, no
  inline hex.

- **Area / urgency stay on documented dark-aware palettes** (categorical
  labels, not state — same rationale as priority). `AREA_LABEL_COLORS`
  uses the muted family (categorical); `URGENCY_LABEL_COLORS` keeps
  the existing red-on-white dark-mode counterpart so `critical` is
  visibly loud in both themes.

- **Stage colors (IssueStatus → STAGE_COLORS) routes through families
  per D6.** Backlog→muted, InProgress→warning, Done→success, Cancelled→
  danger. The `accent` value is a `bg-<family>` class (not a hex); the
  inner `labelClass` is `text-<family>`; `activeBg` is the family's
  subtle treatment (e.g. `bg-warning-subtle/60`); `activeBorder` is the
  family's border token (e.g. `border-warning-border`); a new
  `bottomBorder` field exposes direction-specific
  `border-b-<family>-border` for the mobile stage tab. `STAGE_COLORS`
  resolves through `TREATMENT_BY_FAMILY` from the shared status-
  presentation layer, keeping the family's class strings as the single
  source of truth.

- **CompactSessionCard stage-identity palette is dark-aware and hex-free.**
  Categorical identity (purple/blue/teal/orange/slate) preserved across
  themes via `dark:` variants — the same documented palette already
  landed in T-003. This task tightened the comment to call out the
  per-hue `border-<hue>-<step>` counterpart and added the fallback's
  `border-border` token class.

## Gotchas (T-004)

- **`IssueCard`'s left priority strip migrates from `style.borderLeftColor`
  to a Tailwind class** (`border-l-orange-600 dark:border-l-orange-400`
  etc.). The `border-l-4` width utility stays on the card; the strip
  color is layered via `getPriorityStripColor(priority)` as a class. The
  previous `IssueCard.test.tsx` asserted `card.style.borderLeftColor`
  against hex-converted RGB strings; the test now asserts the
  `border-l-…` / `dark:border-l-…` classes are present in `card.className`
  and that no inline `style` attribute remains.

- **Mobile stage tab bottom border uses a direction-specific utility**
  (`border-b-warning-border`, etc.) — not the full-side
  `border-warning-border` — because the tab already has `border-b-2`
  (width) and applying a full-side color would bleed into the left/right
  borders the tab doesn't actually draw. The `STAGE_COLORS` scheme now
  exposes both `activeBorder` (full-side, for the StageColumn outer
  card) and `bottomBorder` (direction-specific, for the mobile tab).

- **`statusTreatment(...)` is a status-bearing entry point only.**
  `label-colors.ts` and `stage-colors.ts` resolve their chip palettes
  through `TREATMENT_BY_FAMILY` directly (not through `statusTreatment`
  with a synthetic kind/state) because priority/type/area/urgency are
  not domain state values. The kanban `STAGE_COLORS` keeps its
  hardcoded mapping (Backlog→muted, InProgress→warning, Done→success,
  Cancelled→danger) because the task spec mandates that mapping — it
  diverges from the generic `workflow-stage.running`→`info` reservation
  intentionally.

## Handoff Notes for Downstream Tasks (post-T-004)

- T-005 (action-button variants) is unaffected by T-004. The new
  `className`-based label/risk/priority chip styles render correctly
  inside `Button variant="ghost"` because the chip styles set
  `bg-/text-/border-` independently of the Button variant's
  structural classes.

- The `<StatusPill>` (T-002) remains the right primitive for status
  surfaces; T-004's `LabelStyle` (`{ className, size }`) is for chip
  surfaces that need their own visual treatment (priority, risk, type,
  area, urgency, stage column accents). The two layers stay disjoint.

- All inline `style={{ backgroundColor: '#...' }}` / `style={{ color: '#...' }}`
  consumption in the covered surfaces (IssueCard, StageColumn,
  KanbanBoard, pills.tsx, IssueDetailPage, ArchivedPage,
  CreateIssueDialog, EditIssueDialog) is replaced with class-based
  treatment. No raw hex literals remain in the converted registry
  files (`label-colors.ts`, `stage-colors.ts`, `CompactSessionCard.tsx`).

## Action-button standardization notes (T-005)

- **Variant mapping is the single source of color for action affordances.**
  WorkspacePanel rebase-behind → `variant="warning"`, WorkspacePanel
  rebase-default + cleanup → `variant="outline"`. BranchBar
  behind → `variant="warning"`, BranchBar upstream-unknown →
  `variant="outline"`. TaskLogPanel download → `variant="outline"`,
  source chips enabled → `variant="secondary"`, source chips
  disabled → `variant="outline"` (with `opacity-60 line-through`).
  All four panels' bespoke `border-amber-*` / `bg-amber-*` /
  `text-amber-*` / `border-gray-*` / `bg-white` /
  `text-gray-*` / `border-slate-*` / `bg-slate-*` /
  `text-slate-*` overlays are removed from action affordances.

- **Result banners use Badge variants instead of inline color blocks.**
  WorkspacePanel's rebase-result / conflict-failed / cleanup-result
  banners previously used `bg-green-50 text-green-700` /
  `bg-red-50 text-red-700` / `bg-blue-50 text-blue-700` based on
  result type; they now wrap a `Badge variant="success"|"danger"|"info"`
  with no bespoke color className on the banner. The
  `text-danger` token (not raw `text-red-*`) is used on the
  conflict list, which is token-backed via `--color-danger: var(--danger)`.

- **BranchBar's upstream-unknown rebase action was `border-gray-300
  bg-white text-foreground hover:bg-gray-50 disabled:opacity-50`.**
  Replaced by `Button variant="outline"`; the `disabled:opacity-50`
  per-call-site treatment is gone — the primitive's standard disabled
  treatment (`disabled:pointer-events-none disabled:opacity-50`)
  applies uniformly.

- **TaskLogPanel's `slate`+`sky` action dialect retired for the
  download button and source chips.** The download button now uses
  `Button variant="outline" size="sm"`; the source chips use
  `Badge` with `render={<button ... />}` to preserve the click-to-toggle
  semantics while inheriting Badge variant classes. The search input's
  `focus:border-sky-500 focus:ring-sky-500` is replaced with the
  standard `--ring` focus ring (`focus-visible:border-ring
  focus-visible:ring-ring/50`). The dark-terminal palette
  (`bg-slate-900 text-slate-100`) on the scroll body is preserved
  per design D7.

- **The truncation indicator (`bg-amber-100 text-amber-800`) is a
  status pill, not an action affordance.** It is not in scope for
  T-005; it can be migrated to a Badge variant in a future task if
  the spec demands it. (The design D7 description targets the
  `slate`+`sky` action dialect specifically; the truncation
  indicator is amber, not slate/sky.)

- **Test anchor: `data-slot="button"` and `data-slot="badge"`.** All
  new component tests assert the action elements carry the primitive's
  data-slot hook and that the rendered className contains the
  expected token classes (e.g. `border-warning-border`,
  `bg-warning-subtle`, `bg-secondary`) and does NOT contain the
  retired bespoke overlays (`border-amber-`, `bg-amber-`,
  `text-amber-`, `border-gray-`, `bg-white`, `text-gray-`,
  `border-slate-`, `bg-slate-`, `text-slate-`).
