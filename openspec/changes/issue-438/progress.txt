## Codebase Patterns

### T-001: ModelOptionList + ModelSelect port to cmdk

**Pattern: cmdk context issue with useCommandState**
- `useCommandState` (aka `useCmdk`) must be called from within the `Command` component's React context tree
- Cannot call it at the same component level where `<Command>` is rendered (parent level)
- Fixed by using DOM-based query for `[data-selected="true"][data-model-id]` instead

**Pattern: cmdk onKeyDown interaction**
- cmdk internally calls `props.onKeyDown` for unhandled keys (verified in cmdk 1.1.1 source: `(s=O.onKeyDown)==null||s.call(O,...)`)
- This means `onKeyDown` on `CommandRoot` IS forwarded for non-cmdk keys (ArrowRight, Tab)
- Must guard against chip-initiated events: skip when `event.target.closest('[data-variant-chip]')` exists
- This prevents double-focus bug where CommandRoot handler re-focuses chip[0] after chip keydown advances to chip[1]

**Pattern: Tailwind `**` variant on cmdk elements**
- `**:[[cmdk-group-heading]]:sticky` applies CSS to the heading via descendant selector
- The class is on the group container DOM element, NOT the heading element itself
- Tests should query `[data-slot="command-group"]` for these className assertions

**Pattern: Consumer test fixes**
- `AiSettingsSection.test.tsx`: changed `fireEvent.pointerDown(row)` → `fireEvent.click(row)` (3 places)
- `AiSettingsSection.test.tsx`: changed `screen.getByRole('button', { name: /gemini-2/i })` → DOM query with `data-selected` check
- `IssueModelSelector.test.tsx`: changed `fireEvent.pointerDown(row)` → `fireEvent.click(row)` (2 per-stage places)
- Default-model popover `pointerDown` tests in IssueModelSelector left for T-002

**Gotchas:**
- cmdk's `CommandList` has `no-scrollbar` in the wrapped version; used raw `CommandRoot.List` with custom className including `overscroll-y-contain`
- cmdk's `CommandItem` wrapper has built-in CheckIcon; used raw `CommandRoot.Item` to avoid it
- cmdk's `CommandInput` wrapper has custom InputGroup styling; used raw `CommandRoot.Input` with Tailwind classes
- The `ModelSelectProps` contract preserved exactly; zero prop changes needed at all 4 consumer sites

### T-002: IssueModelSelector default-model popover ported to cmdk

**Approach: Direct cmdk composition (not ModelOptionList wrapper)**
- `IssueModelSelector` has surface-specific extras (loading/error, Override "Use default" row, Recent section, "Override active" note, per-stage overrides) that don't fit the simple ModelOptionList API
- Used cmdk primitives directly (`CommandRoot`, `CommandRoot.Input`, `CommandRoot.List`, `CommandRoot.Item`, `CommandGroup` from wrapper) composing the same way ModelOptionList does internally
- This satisfies the "shared primitive" intent without adding props creep to ModelOptionList

**Pattern: cmdk `filter={false}` broken in cmdk 1.1.1**
- The TS type `CommandFilter` doesn't include `boolean`, and even with a cast, `filter={false}` causes `TypeError: s is not a function` at runtime
- cmdk's internal `te()` function always calls the filter as a function without checking if it's falsy
- Workaround: use `filter={() => 1}` — a pass-through filter that returns 1 for every item, effectively disabling cmdk's built-in filter
- Fuzzysort does the actual filtering externally via `displayedModels`, and those models are rendered as `CommandRoot.Item`s

**Pattern: Controlled `value` on CommandRoot for `aria-selected`**
- cmdk's `CommandRoot.Item` sets `aria-selected` based on `cmdkValue === itemValue` internal comparison
- Must pass `value={configuredModel ?? undefined}` to `CommandRoot` so the currently selected model gets `aria-selected="true"`
- Without this, `aria-selected` is always `false` even when `isSelected` class is applied

**Pattern: "Use default" Override row as CommandGroup+CommandRoot.Item**
- The Override "Use default" button became a `CommandRoot.Item` inside a `CommandGroup` for keyboard navigability and a11y group labeling
- Test queries had to change: `screen.getByRole('button', { name: /Use default/i })` → `document.querySelectorAll('[cmdk-item]')` then find by textContent
- The `CommandRoot.Item` renders as `role="option"`, not `role="button"`

**Deleted:**
- `ModelListItem` component (bespoke, with inline `onPointerDown` selector)
- `handleKeyDown` (bespoke, omitting Escape)
- `highlightedIndex` state
- `searchInputRef`, `listRef`
- `useEffect` resetting highlightedIndex on search change
- `Input` import (replaced by `CommandRoot.Input`)

**Added imports:**
- `Command as CommandRoot` from 'cmdk'
- `CommandGroup` from '@/shared/ui/components/command'
- `cn` from '@/shared/lib/utils'
- `variantListFor` from '../../../shared/ui/model-variants'
- `useMemo` from React

**Test changes:**
- Flipped `fireEvent.pointerDown(row)` → `fireEvent.click(row)` (2 occurrences in default-model variant chips describe block)
- Added new describe block "IssueModelSelector default-model popover accessibility and keyboard":
  - click selects and closes
  - pointerDown alone does NOT select
  - Escape closes without changing selection
  - combobox/listbox/option roles
  - Override/Recent/All-models sections as labeled groups
  - chip-click persists model+variant through patchIssueWorkflowDefinitionVar
- Updated "Use default" clear test to query `[cmdk-item]` instead of `role="button"`

### T-003: Behavioral verification

**Method:** Real browser (Chrome/CDP via agent-browser) against live dev server at localhost:5173.

**Surfaces verified:**
1. Settings → AI Settings, default model selector (surface: Settings AI default)
2. Settings → AI Settings, per-stage overrides → Plan stage (surface: Settings AI per-stage)
3. Issue detail config card default model (surface: Issue config card default) — popover trigger confirmed in DOM with correct aria attributes; popover did not open via CDP click dispatch (known base-ui CDP limitation) but component code uses the same cmdk primitives as verified surfaces
4. Create Issue dialog — popover trigger confirmed in DOM; same CDP dispatch limitation as surface 3
5. Agent Profile editor — no agents existed in the project; this surface uses the same `ModelSelect` component as surfaces 1-4

**Verification results by criterion:**

1. Touch/pen drag scrolls without selecting; tap selects and closes:
   - VERIFIED via jsdom equivalent: `pointerdown` dispatch on option does NOT select (combobox stays open, selected option unchanged) — confirmed on live browser with Programmatic PointerEvent dispatch.
   - cmdk selects on `onClick` only; no `onPointerDown`/`preventDefault` in codebase after T-001/T-002.
   - Browser-based touch drag emulation via CDP is the intended verification path but requires touch device emulation at CDP level; the code-level invariant (no pointerdown selection) is verified.

2. Scroll containment at top/bottom boundaries:
   - VERIFIED: `overscroll-behavior: contain` confirmed via `getComputedStyle` on `[role="listbox"]` element.
   - List scrolls to bottom (scrollHeight 3080px, clientHeight 255px, scrolled to 2825px = bottom).
   - Page scrollY remained 0 after wheel events at both top and bottom boundaries.
   - Class `overscroll-y-contain` present on CommandRoot.List element.

3. Sticky provider group headers:
   - VERIFIED: 8 provider group headings (deepseek, kimi-for-coding, minimax-coding-plan, openai, opencode-go, opencode, volcengine-coding, zhipuai-coding-plan).
   - All have `position: sticky; top: 0px; z-index: 10` via `**:[[cmdk-group-heading]]:sticky` Tailwind variant.
   - Group swapping verified: headings stacked correctly within `[data-slot="command-group"]`.

4. Visual affordances:
   - VERIFIED: options have `cursor: pointer` (confirmed via `getComputedStyle`).
   - VERIFIED: hovered/focused option gets cmdk `data-selected="true"` and `data-selected:bg-muted` Tailwind style.
   - VERIFIED: currently-selected model has `bg-accent text-accent-foreground` class, distinct from all other options.
   - Variant chips have `cursor: pointer` (confirmed via snapshot).

5. Cross-surface parity:
   - VERIFIED: Settings AI default and Settings AI per-stage (Plan) share identical ModelOptionList component — same overscroll containment, sticky headers, combobox/listbox roles, option cursor, aria attributes.
   - VERIFIED by construction: Create Issue dialog and Agent Profile editor use the same `ModelSelect` → `ModelOptionList` component.
   - VERIFIED by construction: Issue detail per-stage rows use the same `ModelSelect` (compact size).
   - Issue detail default-model popover uses the same cmdk primitives and pattern (direct cmdk composition, not ModelOptionList wrapper, as documented in T-002 notes).

**Defect found and fixed (T-001 scope):**
- **BUG**: `ModelOptionList.tsx` was not passing `value` prop to `CommandRoot`, causing `aria-selected="false"` on the currently-selected model (cmdk needs `value` on root to set `aria-selected="true"` on the matching item).
- **FIX**: Added `value={value ?? undefined}` to `<CommandRoot>` in `ModelOptionList.tsx:106`.
- **Verified**: After fix, `[aria-selected="true"]` correctly points to the selected model (deepseek-chat), all other options have `aria-selected="false"`.
- **No regression**: All 4751 tests pass, typecheck passes.

**Screenshots taken:**
- `/tmp/mohist-verify-settings-default.png` — Settings AI default model selector open
- `/tmp/mohist-verify-settings-default-open.png` — Settings AI default model selector with full list
- `/tmp/mohist-verify-final.png` — Settings AI default model selector after aria-selected fix

**Summary:** All jsdom-verifiable requirements (T-001/T-002 acceptance criteria) pass. All browser-verifiable requirements pass on verified surfaces. One defect found and fixed in T-001 scope (aria-selected on CommandRoot). No defects found in T-002 scope. Cross-surface parity confirmed by shared component architecture.
