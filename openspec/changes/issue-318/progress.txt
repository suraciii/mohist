## Codebase Patterns

### Workflow status writes follow scattered-guard pattern (per D2)

The WorkflowRun aggregate has no central `CanTransition(from, to)` matrix
or private setter; instead each command handler carries its own
ad-hoc guard. This is the deliberate, audit-table approach (D2 in
design.md) — the new state machine just changed which targets the
guards point to. A writer can therefore still mis-write `run.Status =`,
and an audit-pass (grep `_run!.Status = WorkflowRunStatus.`) is part
of the contract.

### `WaitingForDispatchStatus(run)` and `ActiveOrWaitingForDispatchStatus(run)` helpers

These are the two dispatch-status selectors added in T-001 and live in
`Shared.cs` next to the other small aggregate helpers. They are the
"Ready if assigned, Pending otherwise" (and "in-flight overrides
that") helpers that the new scattered-guard pattern points to. New
transition sites that need "fall back to a dispatch state after
clearing failure / adding work / etc." should call one of these two
helpers rather than hard-coding `Ready` or `Running`.

### `ReconcileReadyStatusWithInFlightWork()` is the activation-time shim

The grain calls this on every `OnActivateAsync` to catch the
edge case where a `Ready` row was persisted with in-flight work
(e.g. the historical migration missed a task or a
sql-side repair left the row in `Ready` with a Running task).
The shim is the runtime backstop for the imperfect migration in
T-004; without it a Ready row that actually has in-flight work
would be blocked by `PollWorkAsync`'s status guard.

### `BuildPendingWork` guard is intentionally a T-005 concern

`WorkflowStatusMapper.BuildPendingWork` still only returns work for
`Status == Running`. The fix (also surface pending work for `Ready`
and `Pending`) is part of T-005 (UI surface). T-001 does not touch
it, so domain/grain tests that exercise the status mapper directly
must not assert on `PendingWork` for non-`Running` rows.

### Controllable workflow status check in `IssueGrain.CancelAsync`

The "controllable workflow" status set used by
`CancelAsync`'s cannot-close-while-running guard was extended to
include `created` / `pending` / `ready` (the new non-terminal
states from D1). The set is now {created, pending, ready, running,
paused, awaiting-approval}. This was not in the original
WorkflowRunStatus enumeration's spec but is a downstream consumer
of the new vocabulary.

### ResetCheck now calls Advance()

`WorkflowRun.ResetCheck` (the "pending" check-result path) used to
emit only `CheckPending`. Under the new state machine this left the
run in `Running` with no in-flight work and no `ChecksWorkId` —
`PollWorkAsync` would then refuse to redispatch. Adding the
`run.Advance()` call re-evaluates the run state and lands the run on
`Ready` (assigned, dispatchable work = the re-queued check), so
the next poll picks the work up.

### Runner-side `WorkflowRunStatusName` mirrors the server enum contract

`packages/runner/src/runtime/workflow-terminal-status.ts` exports a
TS union whose values must stay in sync with the server's
`WorkflowRunStatus` enum (D1). Listed in canonical D1 order:
`Created | Pending | Ready | Running | AwaitingApproval | Paused |
Stopped | Completed | Failed`. The cleanup safety logic only looks
at `TERMINAL_WORKFLOW_STATUSES`, which is intentionally fixed at the
three terminal values — adding a new server enum value requires
updating the union here so the `isTerminalWorkflowStatus` type
guard narrows correctly for any string the server sends, but no
behavioral change is needed. New enum values are added in their D1
position; future readers can map 1:1 to the server enum.

## Decisions and gotchas

### T-001 inherits partial state from a prior attempt

The branch `mohist/run-wr_48e6ff1bf9a54ab99d7cac4fda095812` was
already carrying ~9 of the ~19 write-site changes when I picked it up
(enum order, IsTerminal extension, Shared.cs helpers, Start/Pause/
Stop/Resume transitions, the Advance() default, Retry/Rerun paths).
The remaining work was:
  - the write sites in WorkflowRun.Task.cs / .Check.cs / .Failure.cs /
    .Work.cs that needed `WaitingForDispatchStatus(run)` (not
    `Running`),
  - the AssignRunner → `Ready` write in WorkflowAssignment.cs,
  - the `Status = WorkflowRunStatus.Running` write in
    WorkflowGrain.MarkChecksRunning (was missing — checks would
    dispatch but the run would not flip to `Running`),
  - the PollWorkAsync guard admit `Ready`,
  - the activation-time `ReconcileReadyStatusWithInFlightWork()` shim
    and the grain's `IsTerminal()` delegation,
  - the WorkflowRunQuerier in-memory Pending filter (will be replaced
    by the STORED-column query in T-002, but the in-memory fallback
    must still match the new vocabulary),
  - the WorkflowRunStatusTransitionSpecs.cs test (15 facts covering
    every transition point in the spec).

The state of the partial fixes matched the design D2 audit table,
so I did not have to re-litigate any transition.

### Existing test vocabulary updates

Splitting `Running` into `Ready` / `Pending` / `Running` invalidated
~9 existing tests that asserted the old conflated `Running` state.
The updates were:
  - StatusSpecs: new `WorkflowStatusBeforeRunnerAssignmentIsPending`
    + an inline comment on `WorkflowDoesNotStoreIssueOrWorkspaceContext`
    explaining the pre-runner state is now `pending`.
  - PausingWorkSpecs.StoppedWorkflow_HasTerminalStatus: the
    pre-stop state is `Pending` (no runner ever claimed it).
  - BacklogSpecs.NoRunner_WorkflowWaitsInBacklog: same.
  - BoundarySpecs.CheckReportsPending_CheckRunsAgain: this is the
    test that surfaced the ResetCheck-Advance() gap.
  - DispatchAndLoadingSpecs.RuntimeTaskWithInvalidateChecks_ReopensStageChecks
    + the two WorkflowRetrySpecs: status after a non-failure
    command that resets dispatchable work lands on `Ready`, not
    `Running`.
  - WorkflowRetrySessionHealthGuardSpecs.TaskFails_SessionContextInWarnBand_RetrySucceedsAndStatusClears
    same pattern.
  - AgentJobOwnerKindSpecs: the runner-side `report.WorkflowStatus`
    now reports `Ready` after task completion (check still pending).
  - IssueFeedbackApiSpecs.CreateFeedback_AtAwaitingApprovalStage_ResumesStageAndPersistsFeedback:
    the post-`RequestChanges` state is `Ready` (feedback task
    queued, runner still assigned, no in-flight work yet).
  - IssueWorkflowLifecycleSpecs.RerunAsync_WhenFailureReasonIsUnknownLegacyValue_RerunsExistingWorkflow:
    the post-`RerunAsync` state is `Pending` (the prior
    `StopAsync` left no assignment, so the new state machine lands
    on Pending).

### DeliveryTimeMetrics are pre-existing flaky tests

Four `IssueMetricsApiSpecs.DeliveryTimeMetrics_*` tests fail in
parallel runs of the full integration suite because of an apparent
shared-state issue with the fake time provider. The same tests pass
in isolation and the same failures reproduce on the master branch
state (verified by stashing my changes and re-running). Not related
to T-001; left as-is per "no unrelated fixes" policy.

### `WorkflowGrain.AssignRunnerAsync` admit-set

`AssignRunnerAsync` was already updated to admit `Pending` (was
implicitly `Running` in the old machine). The check at line 269
(`_run.Status.IsTerminal() || _run.Status is Created or Paused or
AwaitingApproval`) is the rejection set, and line 277 (the explicit
`Pending` + dispatchable-work check) is the success condition. This
is the right shape — `Created` rejects because there's no work yet,
`Paused` and `AwaitingApproval` reject because the runner must
resume the run rather than claim it fresh.

## T-002 status

### Work was already merged into T-001 commit

The T-002 feature slice (D3 computed column, D4 query rewrites, poll-loop
simplification, the `CountRunningAssignedToAsync` count query) was already
implemented as part of the T-001 commit (`ed6bc4ec`) because T-001's audit
table required the domain to land on the new statuses, which in turn
required the persistence/querier to actually filter on those statuses —
otherwise the domain transitions would be inconsistent with what the
scheduler surfaces. The relevant diffs are:

- `MohistDbContext.cs` — STORED `Status` computed column +
  `IX_WorkflowRuns_Status` index (D3).
- `WorkflowRunRow.cs` — `Status` projected scalar.
- `WorkflowRunQuerier.cs` — `FindAssignedToAsync` = `status == Ready && runner`,
  `FindAssignableAsync` = `status == Pending && unassigned` (in-memory
  deserialize-and-`NextWork()` loop deleted), plus the new
  `CountRunningAssignedToAsync` count query (D4).
- `RunnerGrain.cs` — `ActiveWorkflowCountAsync` now uses
  `CountRunningAssignedToAsync`; `PollAssignedOrAssignableWorkflowAsync`
  no longer calls `GetCurrentWorkIdAsync` (D4 / D7).
- `GrainTestConfig.cs`, `InboxProjectionTestSupport.cs`,
  `MohistIntegrationFixture.cs`, `MohistServiceRegistration.cs` — the
  test-only DDL that materializes the STORED column / index / trigger in
  test databases (T-004's migration will own the durable form) and the
  `PendingModelChangesWarning` suppression needed while T-002 ships
  ahead of T-004.

### Spec files added under T-002

- `packages/server/tests/.../Specs/Workflow/Querier/WorkflowRunQuerierSchedulingSpecs.cs`
  — 9 facts covering DB-layer filtering on `Pending` / `Ready && runner`,
  no-deserialize of non-matching `State` rows, the `CountRunningAssignedToAsync`
  count, and the schema (`Status` column + `IX_WorkflowRuns_Status`).
- `packages/server/tests/.../Specs/Runner/Grain/RunnerPollSchedulingSpecs.cs`
  — 5 facts covering direct `PollWorkAsync` pickup of `Ready` rows,
  `ActiveWorkflowCountAsync` slot-budget behavior, the count query, and
  structural checks that the poll loop and the count gate no longer
  call `GetCurrentWorkIdAsync`.

### Test fixes applied in T-002

The two structural specs in `RunnerPollSchedulingSpecs.cs` were written
against the new T-001 code but the file-paths / comment-stripping
ergonomics needed tightening:

- `GetProjectRoot()` was climbing 4 directories up from
  `bin/<config>/<tfm>/` and landing on `packages/server/tests/`
  instead of `packages/server/`. The source-composition produced
  `packages/server/tests/src/...` (missing) — the structural tests
  threw `DirectoryNotFoundException`. Fixed to climb 5 directories.
- `ExtractMethodBody` used a hard-coded 2-space marker; the actual
  source uses 4-space indentation. Replaced with a token search that
  accepts any leading whitespace.
- The `GetCurrentWorkIdAsync` `DoesNotContain` assertion was
  false-positive tripping on the documentation comment inside the
  method body that explicitly mentions the dropped symbol. Added a
  small `StripCSharpComments` helper (line + block comments + string
  literals blanked out) so the structural check only sees the
  executable body.

### Pre-existing parallel-run flakes

The full integration suite (parallel by collection) has 147
pre-existing failures in master (`git stash` verified) that are
unrelated to issue-318: 4 `DeliveryTimeMetrics_*` flakes noted in
T-001's progress plus ~143 inbox/epic/database-init specs that share
SQLite fixtures and break under parallel load. Focused
`Specs.Workflow` (541/541) and `Specs.Runner` (218/218) suites are
clean.

### Net effect of T-002

The T-002 commit lands with no domain change and no behavioral
regression. The 2 previously-failing structural tests in
`RunnerPollSchedulingSpecs` go green (file-path / indentation /
comment-stripping fixes). All 9 querier specs and all 5
poll-scheduling specs pass.

## T-003 status

### Cleanup-guard vocabulary widening only

Per design D7, the cleanup-safety logic in
`packages/runner/src/runtime/workflow-terminal-status.ts` was
already correct by construction (everything not in
`TERMINAL_WORKFLOW_STATUSES` blocks removal) — T-003 only needed
to widen the `WorkflowRunStatusName` union type to add `"Created"`
and `"Ready"`, matching the new server enum contract from D1.

The final shape:

- `WorkflowRunStatusName = "Created" | "Pending" | "Ready" | "Running" | "AwaitingApproval" | "Paused" | "Stopped" | "Completed" | "Failed"`
- `TERMINAL_WORKFLOW_STATUSES` remains exactly `{ "Completed", "Stopped", "Failed" }`

Union entries are listed in the canonical D1 enum order so a future
reader maps 1:1 to the server enum.

### New convergence-guard tests pinning the cleanup contract

The task acceptance criterion calls out that a workspace owned by a
workflow run in `created`, `pending`, or `ready` state must never be
removed by automatic cleanup. The runner's convergence backstop is the
authoritative enforcement point for that contract (active → eligible
transitions happen only on terminal reports), so three new
explicit-per-status tests were added to
`packages/runner/tests/cleanup-convergence.spec.ts`:

- `RunOnce_OnCreatedStatus_LeavesEntryActive (D1 created — built not started)`
- `RunOnce_OnPendingStatus_LeavesEntryActive (D1 pending — waiting for claim)`
- `RunOnce_OnReadyStatus_LeavesEntryActive (D1 ready — assigned, waiting for pickup)`

Each asserts `{ queried: 1, transitioned: 0, dropped: 0 }` and verifies
the registry entry remains in phase `active` with `terminalAt === null`.
The existing
`RunOnce_OnNonTerminalStatus_LeavesEntryActive` /
`RunOnce_OnRunningStatus_LeavesEntryActive` /
`RunOnce_OnPausedStatus_LeavesEntryActive` tests cover the other
non-terminal values.

### Terminal-set is still tested as exactly three values

`workflow-terminal-status.spec.ts`'s
`TerminalStatusesSetExactlyMatchesTheThreeTerminalStates` already
pins `TERMINAL_WORKFLOW_STATUSES = ["Completed", "Failed", "Stopped"]`
after sort — T-003 did not need to touch it. Two new
negative-case tests (`ReturnsFalseForCreated`, `ReturnsFalseForReady`)
were added to `isTerminalWorkflowStatus`'s describe block so a
regression in either new value would surface immediately.

### SignalR push handler expands its non-terminal vocabulary

`runner-signalr-workflow-status.spec.ts::OnNonTerminalPush_LeavesEntryActive`
previously iterated `["Running", "Paused", "AwaitingApproval", "Pending"]`;
that loop now includes `["Created", …, "Ready", …]` for the full D1
vocabulary. The handler body in
`runner-signalr.ts::handleWorkflowRunStatus` was unchanged — the
guard delegates to `isTerminalWorkflowStatus`, which is correct by
construction.

### Net effect of T-003

No behavioral code change beyond the union widening. The cleanup
safety logic was already correct by construction; adding the new
enum values to the type union keeps the TS-side wire-shape contract
in sync with the server's `WorkflowRunStatus` enum from D1.

All 758 runner tests pass (`npm test -w packages/runner` → 55 files,
55 passed). `tsc -p packages/runner --noEmit` is clean.
