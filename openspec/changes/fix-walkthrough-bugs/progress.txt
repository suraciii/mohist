# Ralph Progress Log
Started: 2026-04-22T11:15:01.388Z

---


[2026-04-22T00:00:00Z] T-001: Fixed readTasks() to normalize parsed tasks with default values (attempts→0, passes→false, order→999999, error→null). Used spread + nullish coalescing to preserve explicit values. Build and typecheck pass. Next: T-002 (skip logic passes:false).

[2026-04-22T00:00:00Z] T-002: Changed skip branch (line 508) from passes:true to passes:false. Updated error message to include actual attemptsUsed value. The condition `attemptsUsed === nextTask.attempts` catches the case where the for-loop never executed (e.g. NaN arithmetic from missing attempts field). Build passes. Next: T-003 (workflow_log issueId format fix).

[2026-04-22T11:46:00Z] T-003: Fixed two roots of FK failure. (1) workflow-controller.ts: changed `const issueId = String(issue.number)` to `const issueId = issue.id` (UUID). (2) ralph-executor.ts: separated `sseIssueId = String(context.issueNumber ?? '')` for eventBus and `logIssueId = context.issueId || ''` for workflow_log. Also fixed pre-existing infinite loop bug where paused tasks without onAskUser get re-picked forever — added auto-skip branch. Updated test expectations in build-pipeline-observability.test.ts and ralph-executor-logging.test.ts. Build + typecheck + all 32 tests pass. Next: T-004 (approval state display).

## 2026-04-22 T-004 Add approval state display to mo issue show
- **What was done**: Added conditional rendering of `approvalState` in `issue.ts` show action, after Stage/Status lines. Displays status (awaiting/approved/rejected) with color coding, stage, and self-review notes from the `output` field when present.
- **Learnings**: The `output` field on ApprovalState holds the self-review notes. It can be a string or object, handled both cases.
- **Next steps**: All 4 tasks complete. Change ready for final verification.
