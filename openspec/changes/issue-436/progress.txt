## Codebase Patterns

- Executor tests use `verifyOnlyWorkspaceManager` + `nonGitRunner` pattern (from executor-recovery.spec.ts, executor-task-log.spec.ts). The nonGit runner returns `{ success: false, stderr: "not a git repository" }`, which makes `readWorktreeSnapshot` treat the directory as clean-by-default and `checkBranchStability` return `{ kind: "ok" }`.
- Actions capture context in tests by registering a `registry.register("test/capture-context", async (ctx) => { capturedContext = ctx; return { status: "success" } })` handler.
- `baseContext()` in executor.ts returns `Omit<ActionContext, "with" | "workDir">`, so `with` and `workDir` (and now `rawWith`) are supplied inline at the call site.

## T-001: Expose server-expanded with to actions as ActionContext.rawWith

- Added `rawWith?: JsonObject | null` to `ActionContext` in `packages/runner/src/core/types.ts` with JSDoc naming the rule: actions propagating a sub-tree to follow-up tasks MUST read from `rawWith`, never from `with`.
- In `executeOne` (`packages/runner/src/runtime/executor.ts`), added `rawWith: work.with` to the context passed to the action handler. `executeChecks` is unchanged.
- Created `packages/runner/tests/executor-raw-with.spec.ts` with one test: constructs a `RenderedWorkItem` with `with.task.with.options = "${{ vars.agent }}"` and `variables.vars.agent = { type: "opencode", model: "openai/gpt-5.4" }`, captures the context in a stub action, and asserts:
  - `context.rawWith.task.with.options` equals the literal `"${{ vars.agent }}"` string
  - `context.with.task.with.options` deep-equals the resolved agent object
- Typecheck (`npm run typecheck -w packages/runner`) passes.
- Full test suite (`npx vitest run`): 101 files, 1185 tests pass, including the new test and all existing executor/openspec-tasks tests.
