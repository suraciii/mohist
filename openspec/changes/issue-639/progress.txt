## Plan self-review dispositions

- MF-1 addressed in `design.md` and `tasks.json`: the plan now requires persisted `SourceKind == workflow` classification before the pure-activity gate. Unattributed non-activity and mixed batches are rejected before append even when no matching Workflow turn exists for the runtime; pure current-binding activity remains allowed.
- MF-2 addressed in `design.md`, `tasks.json`, and the convergence spec: cleanup delivery now has an explicit receipt-array contract. New cleanup operations return one validated `session.cleanup` receipt; idempotent cleanup replays return 2xx `[]` after identity checks. The Server route, connection method, and delivery adapter are assigned to preserve that empty array so the two-empty settlement rule is reachable for cleanup records. The cleanup caller must not enqueue follow-on input after an already-consumed outcome.
- MF-3 addressed in `design.md` and `tasks.json`: threshold is fixed at exactly three deterministic refusals. The terminal classifier is an explicit `(status, code)` allowlist including the observed `(409, conflict)` response and named runtime/session binding validation codes; unknown 4xx, auth/authorization, 404, 408, and 429 remain retryable. The open question was removed.

Validation: `git diff --check` passed. `tasks.json` should be parsed with Node or another JSON parser when available; the first Python validation command could not run because `python` is not installed. `self-review.md` was not edited.

## T-001 implementation notes

- The session-scoped `/api/runner/{runnerId}/agent-sessions/{sessionId}/runtime-events` route already permits absent AgentSession/turn identity and delegates to `AgentSessionGrain.AppendRuntimeEventsAsync`; the generic project/session route delegates to the same grain. The Workflow-labeled `/sessions/{projectId}/{workflowRunId}/{sessionName}/runtime-events` route has its own complete-binding validation and must remain unchanged.
- Existing grain behavior only rejected unattributed events when a matching Workflow turn existed for the reported runtime. The durable `SourceKind == "workflow"` metadata label must be checked first so unattributed Workflow-session batches are restricted to non-empty pure `session.activity`, including when no Workflow turn exists.
- `AppendEventsAsync` already applies the current physical runtime-session fence before mutating state and returns an empty successful result for stale/missing bindings. Keeping the fence there preserves stale activity no-op behavior and prevents transcript/activity mutation.
- Baseline focused Server spec run passed: `dotnet test packages/server/tests/Mohist.Server.SpecTests/Mohist.Server.SpecTests.csproj --filter 'FullyQualifiedName~WorkflowAgentSessionExecutionBoundaryApiSpecs|FullyQualifiedName~AgentSessionGrainInputBoundarySpecs|FullyQualifiedName~AgentSessionRuntimeEventSpecs' --no-restore -p:SkipWebBuild=true` (2997 passed). The same command without `-p:SkipWebBuild=true` was blocked by missing `node_modules/.bin/tsc` during the web build.
- Implemented the persisted SourceKind gate in `AgentSessionGrain.AppendRuntimeEventsAsync`: unattributed Workflow-introduced sessions accept only non-empty pure activity batches; workflow execution-bound requests and Session follow-up turn requests retain their existing validation. The current runtime-session fence remains in `AppendEventsAsync`.
- Added grain and API regression coverage for active/idle activity-only acceptance, stale/missing binding no-op behavior, pure-batch rejection, no turn creation, and no Workflow observations. The focused new classes pass: API boundary 7/7 and grain boundary 8/8.

## T-002 implementation notes

- Runtime-event HTTP failures now use `RuntimeEventDeliveryError`, preserving HTTP status and the top-level Server `ApiResponse.code` as typed metadata. Runtime-event success parsing and return shapes remain unchanged.
- Binding-reconcile terminal classification is an explicit `(status, code)` allowlist: the five named 409 codes and five named 400 validation/runtime-identity codes. Unknown 4xx, auth/authorization, 404, 408, 429, 5xx, malformed, timeout, and transport outcomes remain retryable.
- Refusal counts are process-local and keyed by `runtimeEventDeliveryKey`; exactly three consecutive allowlisted refusals remove every currently pending record for that key in one existing version-1 snapshot write. The actionable terminal error is logged only after that write succeeds. Persistence failure restores records, resets the refusal window, and leaves the outbox retryable/unhealthy.
- Focused Runner verification passed with the available transaction-local toolchain: `node node_modules/typescript/lib/tsc.js -p packages/runner/tsconfig.json --noEmit`, `node node_modules/typescript/lib/tsc.js -p packages/runner/tsconfig.tests.json --noEmit`, and the focused Vitest files (`runtime-event-outbox.spec.ts`, `runtime-event-delivery.spec.ts`, `runtime-event-delivery-cleanup.spec.ts`, `server-connection-generic-session.spec.ts`) passed 53/53. The repository-local `npm run typecheck`/`npm run test` commands initially lacked installed binaries; the temporary untracked `node_modules` symlink was removed before commit.

## T-003 implementation notes

- Cleanup admission now exposes an `AlreadyRecorded` grain result. The Server cleanup route returns a one-element JSON receipt array for a new operation and `[]` for a fully identity-validated replay; Runner connection and delivery preserve the array unchanged.
- Matching-receipt input and cleanup records track an in-memory consecutive empty-response count. The first valid empty response remains durable; the second removes and persists the record before rejecting its waiter with `AlreadyConsumedRuntimeEventError` (`classification: already-consumed`, `recordId`). Positive receipt identity validation remains unchanged.
- Reporter callers mark already-consumed input/cleanup as rejected without setting `agentTurnId`; cleanup therefore does not enqueue the follow-on runtime `session.input` or attributed events. Retryable failures remain ordinary errors.
- Verification used the available neighboring repository toolchain because the runner-local `node_modules` contains only Vite cache directories: Runner typechecks (`tsc` production and tests) passed; focused Vitest coverage passed 66/66 across outbox, cleanup delivery, Server connection, and Workflow reporter specs. Server build passed, and the focused `WorkflowAgentSessionCleanupSpecs` passed 1/1. `git diff --check` passed.

## T-004 implementation notes

- Retention observability now tracks a process-local `retentionOverCap` edge state. The existing streaming-delta eviction and version-1 snapshot format are unchanged; non-delta over-cap queues warn once per interval, clear when removals bring the retained count back to the cap, and warn again on a later crossing. Recovery initializes the state from the loaded snapshot without persisting a schema change.
- Retention state is refreshed after load/enforcement, enqueue rollback, and every durable settlement/removal path so persistence failures restore both records and the current over-cap interval consistently.
- Added fake-timer coverage for sustained over-cap warning suppression, later warning crossings, recovered over-cap snapshots/version 1, and mixed historical binding-refusal/already-consumed groups progressing alongside a live Workflow receipt. Existing round-robin fairness coverage remains green.
- Focused Runner typechecks and five outbox suites passed with the available transaction-local toolchain: 51 tests passed across `runtime-event-outbox.spec.ts`, `runtime-event-outbox-batching.spec.ts`, `runtime-event-outbox-cleanup.spec.ts`, `runtime-event-outbox-fairness.spec.ts`, and `runtime-event-outbox-liveness.spec.ts`. `git diff --check` passed.
