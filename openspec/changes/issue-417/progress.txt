# issue-417 progress

## Codebase Patterns

- 既有 `design/architecture.md` 的「聚合与事务」节已经把跨聚合推进固化为「本聚合提交
  → durable handler → 目标聚合幂等命令」。T-001 在该节下追加 `### 持久化应用协调者`
  子节，明确协调者 grain 是该模式的窄化特例而不是新权威。
- 既有上下文映射表（`design/domain-analysis.md`）使用 DDD 关系代号（`C/S`、`C`、
  `OHS+PL`、`SK`、`PL`），并对每个 leaf domain 给出唯一约束。T-001 在表尾追加两行
  `Conformist` 关系（#14、#15），并在「Dependency invariants」加一条说明协调者不是
  独立限界上下文、且不能被参与者反向引用。
- 设计 spec 用中文写正文，保留英文术语（aggregate、handler、participant interface 等），
  协调者 grain 的命名沿用设计 D2 的 `IssueRepositoryCoordinator` / `IIssueRepositoryCoordinatorGrain`。
- 设计文档与 OpenSpec `design.md`/`tasks.json` 同步指代：两者都引用同一个
  `IssueRepositoryCoordinatorGrain` 名称，未来 T-005 的实现与命名要继续保持一致。

## Notes

- 协调者 grain 在 architecture 文档里被定位为「Project-scoped 应用层 process manager」，
  不加入「Subdomains」表（它不是业务问题域），只在「Bounded contexts and relationships」
  与「Dependency invariants」两处出现；这样读文档的人能从模型层面理解它不是新领域。
- 适用范围一节明确写「当前唯一」是 issue 417 的绑定协调者，避免之后被泛化滥用；「不
  适用范围」一节把已有交互（参与者内部校验、UI 推送、session 绑定）排除出去。
- T-001 没有改任何生产代码、没有跑测试（设计阶段任务）。后续 T-002 起的迁移 / 编码
  任务如果要把协调者 grain 真正落出来，需要遵守本节五条约束，并由 T-005 的 ArchTest
  兜底「仅协调者依赖窄 participant 接口」。

## T-002 notes (AddIssueRepositoryProjection migration)

- 走的还是 `AddAgentLaunchLabelComputedColumns` 既有的 SQLite stored
  generated column 套路：`AddColumn` 先把列加出来（plain nullable
  string），再 `AlterColumn` 把同一列改成 STORED computed 定义，触发
  EF 的自动 `CREATE TABLE ... AS (...) STORED` table rebuild。
  比 `AddIssueIsArchivedComputedColumn` 多走两步是因为后者本来就在建表
  时直接生成。
- ProjectRow 新加的 `RepositoryRevision` (long) 和
  `LastRepositoryCommandJson` (string?) 是普通列，迁移里给前者显式
  `defaultValue: 0L`，协调者真正落地前不会被任何业务读写；后者 null
  即可。`Design.md` D2 把这两个字段定位为「协调者进程的 receipt 与
  revision 载体」，T-005 实现时才把读写连起来。
- EF Core 11 的 `HasDefaultValue(0L)` 并不会让 INSERT 省略列——
  测过两遍都是同一结果：EF 仍然把列加进 INSERT 语句。所以测试侧
  想在「老 schema + 新 model」组合下不踩到缺失列，唯一干净的做法是
  把 schema 升级到当前 model（`EnsureCreated()`），而不是把列
  排除在 INSERT 之外。这条规律对后续 T-005 写新 spec 时同样适用。
- `MigratedSqliteTemplate.CopyTo(connection, "...")` 把 schema 钉
  在某个历史 migration 上，本任务里只对「专门验证该 migration 行为」
  的测试安全；任何走「用当前 model 写一行」的测试都得切到完整模板
  或 `EnsureCreated`，否则会撞上新加列——`InboxSubscriptionsMigrationSpecs
  .Up_StoresAndReadsSubscriptionRow` 因此改成用 `EnsureCreated()`。
- 跨任务命名统一：`IssueRow.RepositoryName`（camelCase JSON
  path `$.repositoryRef`）+ `IX_Issues_ProjectId_RepositoryName_Status`
  索引名沿用 D3 的规范；T-004 的 blocker 查询可以直接消费。
- `dotnet test` 全跑过：911 CLI + 1372 unit + 27 arch + 2789 spec
  + 1016 runner，`dotnet build Mohist.sln -p:SkipWebBuild=true`
  在 warnings-as-errors 下 0 warning 0 error。