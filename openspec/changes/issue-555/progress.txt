# Progress: issue-555 self-review disposition

Task: dispose of every must-fix finding in self-review.md by fixing the plan
artifacts. Observations require no action. self-review.md itself is NOT edited
(regenerated by the next review).

## Findings inventory

- MF-1 (must-fix): stop mapping not caller-bound in design.md Decision B —
  contradicts external-write-idempotency spec ("Durable keyed mappings are
  scoped per command", stop caller-binding MUST + "Stop keys are caller-bound"
  scenario) and design/agent-api.md. DISPOSITION: FIXED.
- Observations 1-7: no action required. Obs 1 (design.md Decision E says
  "all 20 keys"; the allowlist has 22) was fixed in passing while editing
  design.md, per the reviewer's own note ("fix the prose when touching the
  file"). All other observations left as-is.

## MF-1 fix applied

Mechanism chosen (per reviewer fix direction, first option): stop scope_key
embeds callerKeyId → `turnId|callerKeyId|key`, so the single unique index
(command, scope_key) gives caller-scoped stop uniqueness. The filtered
per-turn pending index stays as the cross-caller/cross-key 409
stop_outcome_unknown block. Cross-caller outcome now stated explicitly:
while A's stop is pending, B (any key, incl. A's key string) → 409
stop_outcome_unknown, nothing persisted; once no unresolved stop remains,
B's key creates B's own mapping and classification evaluates it (terminal →
no-op, queued → local cancel, running → new fenced stop). B never sees A's
row/outcome/frozen target.

Files changed:
- design.md Decision B: scope_key row (stop now turnId|callerKeyId|key),
  caller_key_id row, unique-index paragraph (caller-scoping rationale),
  fingerprint step (caller deliberately excluded; separation comes from
  scope), write-path step 3 (constraint discrimination by index name), new
  cross-caller stop outcome paragraph after step 5.
- design.md Decision D: "blocks other keys" → "any other stop request for the
  Turn (other keys and other callers)".
- design.md Decision E: "all 20 keys" → "all 22 keys" (Obs 1, in passing).
- tasks.json T-006: description now names the caller-scoped stop scope_key +
  cross-caller block; caller-binding acceptance criterion rewritten as the two
  concrete observable outcomes (pending → 409, resolved → own mapping).
- tasks.json T-004: parenthetical noting the stop scope_key embeds
  callerKeyId, since T-004 creates the table/indexes (would otherwise risk
  recreating MF-1 at schema time).

## Status

- [x] Read self-review, plan artifacts, spec, target contract
- [x] progress.txt created (commit f27fb3748)
- [x] design.md fixed (MF-1 + Obs 1 prose) (commit 4afbf4ead)
- [x] tasks.json fixed (T-006 description + criterion, T-004 note); JSON
      validated (commit after 4afbf4ead)
- [x] Final sweep: no remaining caller-unbound stop scope references in the
      plan artifacts; self-review.md untouched; working tree clean

Disposition summary: MF-1 FIXED; observations 1-7 no action required
(observation 1's prose count fixed in passing per the reviewer's note);
no won't-fix dispositions — no must-fix finding was left unfixed.

# T-001: Direct API authentication boundary and route group

Status: COMPLETE. Auth pipeline extension (carrier kind +
ExternalAgentCaller in HttpContext.Items), ExternalAgentApiMiddleware,
/api/v1 route group with RouteScopeRequirement metadata, public error
envelope with Bearer challenge, and 9 pipeline-order SpecTests all
shipped and green.

## Codebase Patterns

- Middleware composition is pinned in MohistHostFactory:
  UseMohistAuth() -> UseExternalAgentApi() -> MapMohistApi(). The direct
  boundary middleware is factory-based (IMiddleware + IScopedService),
  same as AuthResolutionMiddleware. Routing runs before both (implicit
  UseRouting), so RouteValues/endpoint metadata are readable there.
- AuthResolutionMiddleware now AUTHENTICATES ONLY on /api/v1 paths
  (IsDirectApiPath): it skips its own scope/runner-impersonation gates
  there and delegates authorization to ExternalAgentApiMiddleware so
  401/403 use the direct error envelope, not the control-plane one.
- Carrier + caller facts travel in HttpContext.Items:
  "mohist.credentialCarrier" (CredentialCarrier enum) and
  "mohist.externalAgentCaller" (ExternalAgentCaller). The caller is
  recorded for CredentialKind.Pat only — caller presence in Items is
  exactly "the bearer credential is a PAT".
- 401 vs 403 split on /api/v1 (spec-mandated): non-Bearer carrier OR
  non-PAT bearer credential (runner/integration/service file cred) ->
  401 non-classifying; PAT without grant / wrong scope / out-of-grant
  project -> 403. operator_all is honored ONLY as the persisted grant
  kind, never inferred from operator scope.
- DirectApiAuthResponses is the single 401/403 writer shared by the auth
  layer (unresolvable token on /api/v1) and the boundary middleware, so
  the non-classifying body cannot drift between the two rejection points.
- New production comments must NOT reference issue numbers, T-00x task
  IDs, design/*.md paths, or openspec/ — CommentReferenceRules
  (ArchTests) ratchets against comment-reference-baseline.json, which is
  EMPTY by convention. Keep comments self-contained; do not grow the
  baseline.
- Route group: DirectApiRoutes.MapDirectApiRoutes() is the ONE
  registration (called first inside MohistApiRegistration.MapMohistApi);
  PathPrefix constant "/api/v1" + projectId route name are shared with
  the middleware. Seven templates = design/agent-api.md route table
  exactly. Placeholder delegates answer 501 not_implemented via
  DirectApiResults; T-003..T-007 replace delegates in place without
  touching the pipeline.
- SpecTests for the direct API live in
  packages/server/tests/Mohist.Server.SpecTests/Specs/DirectApi/,
  collection "IntegrationMisc". PATs for tests are minted through the
  real control-plane route POST /api/auth/tokens
  (body: name/scope/projectIds/allProjects; response data.token).
  FakeTimeProvider is pinned at 2026-06-30 — seed credential rows with
  ExpiresAt in 2027 and RevokedAt before "now" as needed.

## Gotchas

- MTP (Microsoft.Testing.Platform) ignores VSTest --filter syntax on
  SpecTests; use `dotnet run --project ... -- -class "<FQN>"` for a
  focused run, or the plain `dotnet test` runs the whole suite (~2.5min).
- npm install is needed before npm run test:fast (concurrently isn't
  installed by default in this workspace).
- ArchTests embed server sources as resources and re-run on every build;
  after editing src comments re-run test:fast:arch (or full arch suite)
  — the comment-reference rule only fails on NEW offenders.

## Decisions

- Non-PAT bearer credentials on /api/v1 answer 401 (not 403): the spec's
  "request without a usable Bearer PAT -> 401 unauthenticated" clause
  covers runner/integration/file credentials; 403 stays reserved for a
  genuine PAT that lacks grant/scope/project authorization. Pinned by
  NonPatBearerCredential_CannotSubstituteForAPat_AndAnswersUnauthenticated
  and the extended non-classifying battery.
- Unmatched /api/v1 paths still pass through the boundary middleware
  (routing leaves endpoint null but the pipeline runs), so "any /api/v1
  path" rejection semantics hold even for unknown routes.

# T-002: Public execution projection engine and PublicExecutionRead snapshot

Status: COMPLETE. Three public projection table families (one additive
migration), PublicExecutionProjector hosted background service as their
only writer, checkpointed atomic batches, PublicExecutionRead DTO with
the 22-key allowlist and five-state precedence, terminal fences,
generation switching, and 21 new tests (6 unit contract + 14 projection
Specs + 1 hosted-projector Spec). Full SpecTests suite (3944) green;
npm run test:fast green.

## Codebase Patterns

- Namespace split matters for the arch rule "InfrastructureData must not
  depend on application layer (Agent.Grains)": the projection ENGINE
  lives in Infrastructure/PublicApi/ (may read AgentJobState etc.); the
  ROWS stay in Infrastructure/Data/PublicApi/ (pure persistence).
- Migrations are hand-authored (no dotnet-ef CLI): new migration =
  <date>_Name.cs + Designer.cs (calls
  MohistDbContextModelSnapshot.BuildModelCore) + hand-edited snapshot.
  The snapshot diff test (ProjectEventsModelDebug) catches drift; watch
  .IsRequired() on non-nullable string properties — SQLite/EF differ
  flags them as AlterColumn diffs when the snapshot omits IsRequired.
- JsonSerializerOptions must come from the shared JSON facade (ArchTests
  rule): added JSON.PublicApi (explicit nulls, RFC3339 UTC converter,
  JsonPropertyName-pinned names) for public payloads.
- Spec/unit ledger rule: SpecTests classes in Specs/ must be
  integration-flavored (SpecUnitMigrationLedger rule flags
  "static-light" candidates for migration to UnitTests). The contract
  tests are pure serialization tests -> they live in
  Mohist.Server.UnitTests/DirectApi/. Helper classes in Specs/ must end
  with an allowed suffix — PublicProjectionTestSupport.
- AgentSessionRow computed columns LabelProjectId/LabelAgentId
  (json_extract from State) are the public project/agent identity source
  for the projection.
- AgentJobRow.Status column is NOT reliably maintained by the grain; the
  engine deserializes State JSON (AgentJobState.Status) instead.
- Canonical output string is JSON; ExtractPublicOutput only surfaces
  {"text": "..."} when a non-empty string text key exists.

## Gotchas

- MTP focused run: dotnet run --project ... -- -class "<FQN>". Full
  SpecTests dotnet test takes ~4min.
- FakeTimeProvider in the integration fixture is pinned at 2026-06-30;
  use fixture.TimeProvider.GetUtcNow() for canonical timestamps.
- TestWait.ForAsync advance callbacks must cross a real async boundary
  without banned Task.Delay/DateTimeOffset.UtcNow — an HTTP GET through
  the same host works.
- EF transactions: engine reads checkpoints as TRACKED rows (not
  AsNoTracking) so re-saving an already-projected session updates rows
  instead of colliding on the composite PK.
- The hosted projector runs in EVERY MohistIntegrationFixture host; it
  tolerates sessions without a public project label by consuming and
  checkpointing without publishing.

## Decisions

- Follow-up projection waits for the canonical Session Input/Turn fact
  (engine derives journal transitions from session ledger state only).
- Durable input rejection: the canonical input record is live
  (inputId present, turnId null, outcome=rejected). A rejection that
  created no records leaves the Job anchor at preparing until a durable
  fact lands — driven by durable facts, no invention.
- outcome_pending = AgentTurnStatus.Executing + an active
  AgentSessionStopClaim on the Session (aggregate running, admission
  blocked, reason stop_outcome_unknown).
- session.context_reset is derived from durable
  com.mohist.agent-session.runtime-bound journal rows: the FIRST bind is
  the initial bind; every later one is a context boundary (RebindRuntime
  Session is the only rebinding transition and only reset/runtime-change
  /missing-recovery reach it).
- Rebuild re-derives all transitions into generation+1 (fences do not
  apply to the rebuild's own re-emission), flips active_generation
  atomically in the same transaction, and preserves next_sequence (the
  global allocator) so sequences continue past the old generation's
  head. The rebuild entrypoint itself is control-plane and lands later.
- Unknown reasonCode: unresolved consumed facts carry
  reasonCode=stop_outcome_unknown when a stop is involved; session-wide
  unknown activity also maps to it (stable safe public code, no cause
  detail).

# T-003: Job, Input, and Turn public read routes with projection lag

Status: COMPLETE. Added the three projection-only GET routes, canonical
membership checks with resource-specific 404s, shared public-read outcome
mapping, and the 503 projection_lag freshness gate with Retry-After. Added
canonical Input/Turn discovery so an owned record without a committed
snapshot returns lag rather than a false 404; deleted or foreign canonical
records remain indistinguishable from missing resources. Added exact
allowlist/lag route specs, a deterministic pre-snapshot lag recovery spec,
and the DirectApi internal-read isolation architecture rule.

## Codebase Patterns

- PublicExecutionReadQuerier lives in Infrastructure/PublicApi and reads
  canonical rows only to establish Project membership and source watermarks;
  response bodies are always the stored SnapshotJson from
  PublicExecutionSnapshotRow.
- Job reads compare AgentJob ledger revision and AgentJob journal MAX(id),
  plus the joined Session feeds. Input/Turn reads locate the canonical
  record in Session state, then compare the Session ledger digest, Session
  journal head, and joined Job ledger/journal feeds.
- DirectApiResults.PublicRead is the shared transport mapper: Found serves
  the persisted JSON verbatim, NotFound maps to the caller-supplied resource
  code, and ProjectionLag emits only the error envelope with Retry-After.

## Gotchas

- The architecture test embeds production sources, so its internal-type
  regex must use actual word-boundaries; a backspace character in a verbatim
  string silently disables the intended check.
- The projection engine must checkpoint joined AgentJob journal heads when it
  consumes a Session target, otherwise a correct read freshness check would
  remain permanently behind for jobs with canonical journal rows.

## Verification

- npm run test:fast: typecheck, unit, architecture, workflow, CLI, web, and
  runner lanes all passed.
- Focused SpecTests passed for PublicExecutionProjectionSpecs (14),
  PublicExecutionProjectorHostingSpecs (1), PublicExecutionReadRouteSpecs (7),
  and PublicExecutionReadQuerierSpecs (1).
- Final rerun: npm run test:fast passed all seven lanes (typecheck, unit,
  architecture, workflow, CLI, web, and runner).

# T-004: Idempotency engine and launch command

Status: IMPLEMENTED. Added the direct API idempotency mapping table and
indexes, strict launch body/key validation, versioned server fingerprinting,
direct-only derived coordinator identity, durable rejection observations, and
the launch route on the existing canonical launcher/projection.

## Codebase Patterns

- The mapping table lives in `Infrastructure/Data/DirectApi/` with a partial
  EF model configuration in `MohistDbContext.DirectApiModels.cs`; mappings use
  the composite `(Command, ScopeKey)` key plus the explicit unique index and
  the nullable `TurnId` column needed by the filtered pending-stop index.
- `DirectApiIdempotencyService.GetOrCreateAsync` commits the pending row in a
  separate SaveChanges call before invoking canonical launch. A raced insert
  reloads the unique-index winner; completion is a second durable update.
- Mapping `Outcome` is internal JSON. A pending launch stores the deterministic
  direct coordinator key there; completed rows add canonical Job/Session/Input/
  Turn IDs. The route never returns this JSON or the fingerprint.
- The direct route uses `AgentLaunchCoordinatorCodec.StableToken` over a
  direct-only namespace string. Its coordinator request sets
  `ExactPromptFingerprint=true` and `Origin=direct-api`, so text is kept
  byte-significant while existing Web launch normalization remains unchanged.
- Pending retries resume the coordinator through `ResumeIdempotentAsync` first,
  then fall back to `LaunchIdempotentAsync` only when no coordinator plan
  exists. This avoids re-reading mutable Agent readiness after a crash.
- Whitespace-only direct text is accepted by the launch path and Session/Job
  admission checks because Web callers still pass the already-trimmed prompt.
  Empty text remains invalid at the direct JSON boundary.

## Verification

- Existing T-003 regression: `PublicExecutionReadRouteSpecs` passed (7).
- Model/snapshot diff check: `ProjectEventsModelDebug` passed (1).
- T-004 focused SpecTests passed (7): key/body validation, archived Agent,
  exact-text conflict, whitespace preservation, replay uniqueness, pending
  mapping recovery, and durable rejection replay.

## T-004 follow-up verification

- The direct exact-text path also required the canonical `AgentSessionGrain.EnsureInitialLaunchAsync` validation to use `IsNullOrEmpty`; Web launch callers remain normalized before reaching that boundary, while direct whitespace text is persisted byte-significantly.
- Direct launch responses can briefly be `503 launch_pending` while the coordinator converges; repeated same-key requests complete the durable mapping without changing the canonical IDs. The whitespace spec accepts that documented window and asserts the mapping reaches `completed` with the exact fingerprint.
- Re-ran T-004 three times, T-003 public-read routes, public projection specs, and `npm run test:fast`; all passed. The server/spec build was run with `-p:SkipWebBuild=true` for focused .NET verification because the server project’s default web-asset assertion expects a generated `packages/web/dist/index.html`.

# T-005: Follow-up input command with session-derived targeting

Status: IMPLEMENTED. The direct follow-up route now validates the shared
write contract, resolves Project and Agent only from the canonical Session,
creates a durable `(sessionId|Idempotency-Key)` mapping, pre-mints stable
Input/Turn identities, admits through `IAgentSessionGrain`, and serves the
projection-backed Input observation. Capacity admission failures become
stable terminal rejection observations with null live IDs; transient runtime,
recovery, stop, and unresolved activity conditions leave the mapping pending
and return a retryable safe error.

## Codebase Patterns

- `CanonicalFollowupTarget` carries the canonical Session Project and Agent
  labels alongside the existing delivery target; the direct route never accepts
  derived identity in the body or fingerprint.
- Follow-up fingerprints are fixed-order JSON containing only version,
  `followup`, canonical `sessionId`, and the accepted body. Input identity is
  `StableToken(sessionId|key|followup-input)`; Turn identity adds a `|turn`
  discriminator while remaining deterministic for the mapping.
- The existing grain idempotency key remains the canonical Session fence. The
  direct relational mapping is completed before dispatch; a retry after a
  response-loss window re-enters the grain with the same pre-minted IDs and
  `AcceptFollowupAsync` returns the original pair without another Input.
- `AgentSessionGrain` and the domain follow-up transition treat any non-empty
  text, including whitespace-only text, as text. Existing Web routes reject
  whitespace-only input at their own request boundary, while the direct API
  preserves it byte-for-byte.
- An accepted mapping whose Input snapshot has not appeared yet maps a
  temporary projection miss to `503 projection_lag`; it cannot expose a
  misleading `input_not_found` after the Session grain has durably accepted
  the request.

## Verification

- Existing T-004 idempotency specs passed (7), auth pipeline specs passed (9),
  and public execution read route specs passed (7) after the follow-up changes.
- New `DirectApiFollowupSpecs` passed (5): Session membership 404 and no
  mapping, Session-derived target plus deterministic replay, stable payload
  conflict, durable queue rejection, and readonly-scope fencing.
- `npm run test:fast` passed all seven lanes: typecheck, unit, architecture,
  workflow, CLI, web, and runner.

# T-007: Public session event stream route and HMAC cursors

Status: IMPLEMENTED. Added the projection-only per-Session event route with
exclusive-after paging, a default/max page size of 100, deterministic event
cursors, cursor validity/expiry errors, closed-stream tombstones, and public
payload allowlists. The HMAC-SHA256 key is stored through the encrypted server
secret store under a dedicated deployment-wide server-secret address and is
created during host startup; loading the key per request makes persisted key
rotation immediately invalidate old cursors.

## Codebase Patterns

- `PublicSessionEventStreamQuerier` validates the signed cursor binding and
  active generation before querying `public_session_events`. It reads only the
  active generation's persisted rows, orders by sequence, and uses the shared
  `PublicExecutionReadQuerier.IsSessionProjectionBehindAsync` gate before
  serving an active stream.
- `PublicSessionEventCursorCodec` signs a canonical JSON payload containing
  projectId, sessionId, generation, afterPosition, and version, then appends
  the 32-byte HMAC signature before unpadded base64url encoding. Malformed,
  tampered, cross-bound, rotated-key, and wrong-generation tokens all return
  the same `cursor_invalid` result without a fallback read.
- Public event envelopes are built from the stored `PayloadJson`: execution
  events expose the exact `PublicExecutionRead` object and
  `session.context_reset` exposes only `PublicSessionEventPayload`. Null
  sibling payload properties are omitted so the two allowlists cannot bleed
  into one another.
- `PublicStreamStateRow` remains the authority for active generation,
  earliest/latest bounds, and closed tombstone behavior. No event-bus,
  SignalR, Runner, transcript, or UI data participates in ordering or cursor
  construction.

## Gotchas

- The encrypted secret-store table uses SQLite owner/kind check constraints;
  adding a deployment-wide cursor key required a migration that rebuilds the
  existing `StoredSecrets` table while copying all prior rows and preserving
  its owner index.
- Empty pages use the active stream's high-water sequence for `nextCursor`;
  non-empty pages use the last returned sequence. Tombstone expiry returns
  `earliestSequence=null` and the last safe latest bound, while a purged
  stream state makes even a correctly signed old cursor invalid.
- Synthetic event route specs use a canonical Session row plus public rows
  and a matching checkpoint in one database write. This avoids competing
  with the hosted projector while still exercising the real persistence-only
  route and its lag comparison.

## Verification

- Existing T-003 `PublicExecutionReadRouteSpecs` passed after the shared lag
  helper change.
- New `PublicSessionEventCursorCodecTests` passed (3) and
  `PublicSessionEventStreamRouteSpecs` passed (5), covering paging, cap,
  empty positioning, payload allowlists, tamper/cross-binding/generation,
  expiry, tombstones, purge, rotation, and projection lag.
- Migration/model consistency, database initialization, direct auth pipeline,
  and direct follow-up regression specs passed.
- `npm run test:fast` passed all seven lanes after replacing the cursor JSON
  options with the shared `JSON.PublicApi` facade required by ArchTests.

# T-008: Move agent-api and auth docs to shipped status

Status: IMPLEMENTED. Updated design/agent-api.md and design/auth.md to
`status: shipped` with shipped-behavior status sections. Replaced the user-doc
implementation-gap sections with direct API usage guidance covering all seven
routes, Bearer PAT and persisted Project grants, strict idempotency, the
PublicExecutionRead allowlist and five-state aggregate, projection_lag, and
opaque cursor resume with cursor_invalid/cursor_expired handling. Moved the API
out of the WIP docs index and marked it available in the repository README.
The operator-triggered projection rebuild entrypoint is not described as a
shipped user command.

Verification so far: `npm run docs:check` passed (92 Markdown files).
The final file-size ratchet also required splitting existing changed helpers
into `MohistDbContext.Helpers.cs`, `PublicApiProjectionEngine.Checkpoints.cs`,
and `CanonicalFollowupTarget.cs`; this is behavior-preserving and keeps the
full gate within its structural limits.

Final verification: `npm run verify` passed. This covered docs, file sizes,
format, solution and workspace builds, 3,969 Server SpecTests, 2,671 Server
unit tests, 1,848 CLI tests, 4,724 Web tests, 1,639 Runner tests, and 70 Slack
tests.

## T-002 verification rerun

- Focused `PublicExecutionProjectionSpecs` (14) and
  `PublicExecutionProjectorHostingSpecs` (1) passed on the current tree.
- The full `npm run test:fast` gate passed again: typecheck, server unit and
  architecture, workflow, CLI, web, and runner lanes all green.
- The T-002 implementation remains contained in commit `85ff6bec2`; this
  rerun found no additional projection or migration changes necessary.

## T-003 verification rerun (current workflow)

- The existing T-003 implementation in commit `462449568` was audited against
  the public-execution-read and public-execution-projection requirements. The
  three GET routes use the persisted snapshot body, canonical membership is
  checked before the snapshot/watermark path, and the shared `PublicRead`
  mapper emits resource-specific 404s or the `projection_lag` envelope with
  `Retry-After` and no stale body.
- Focused `PublicExecutionReadRouteSpecs` (7),
  `PublicExecutionReadQuerierSpecs` (1), the public DTO contract tests, and the
  DirectApi internal-read architecture rule passed. The first parallel unit
  invocation hit shared .NET `obj/bin` file contention only; its serial rerun
  passed all 2,671 unit tests.
- `npm run test:fast` passed all seven lanes: typecheck, unit, architecture,
  workflow, CLI, web, and runner. No additional implementation changes were
  needed on the current workflow branch.

## T-004 verification rerun

- Launch command responses now map a completed durable mapping with a missing
  public Job anchor to `projection_lag`, matching the shared projection-only
  response behavior used by follow-up and reads; a successful canonical
  operation is never translated into `job_not_found` while its projection is
  catching up.
- `DirectApiWriteValidation` owns the launch/follow-up scope builders and the
  stop scope/fingerprint builders. Normal launch/follow-up scope strings are
  unchanged; stop scope embeds `callerKeyId` and stop fingerprints contain
  the version, stop discriminator, canonical Turn ID, and empty body for T-006.
- Added unit coverage for printable ASCII key boundaries, strict JSON
  rejection (including duplicate/unknown properties and trailing commas),
  exact whitespace-preserving text, and command scope/fingerprint inputs.
- Focused Direct API suites passed: launch (7), follow-up (5), and database
  initialization (7). The full unit assembly passed 2,675 tests and the
  server build passed with `SkipWebBuild=true`.
- `npm run test:fast` passed all seven lanes: typecheck, unit, architecture,
  workflow, CLI, web, and runner.

## T-005 verification rerun

- Audited the existing follow-up route against canonical Session targeting,
  deterministic Input/Turn pre-minting, durable mapping, projection-backed
  replay, conflict, rejection, and readonly-scope requirements.
- Added a route-level SpecTest proving caller-supplied `projectId` and `agentId`
  fields are rejected as `invalid_request` before Session resolution, mapping,
  or canonical Input/Turn admission.
- The updated focused follow-up suite passed all 6 tests.
- `npm run test:fast` passed all seven lanes: typechecks, 2,675 Server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests.

## T-006: Fenced direct stop command

Status: IMPLEMENTED. The direct stop route now enforces an empty body,
caller-bound stop scopes, filtered pending-stop supersession blocking, and
projection-only public Turn responses. It resolves canonical Turn membership
without accepting Session or binding data from the caller, freezes the Turn
revision, Session binding epoch/context generation, complete delivery binding,
deadline, and canonical stop operation in `FrozenTarget` before the first
Runner delivery, and reconstructs pending retries from that frozen target.

- Terminal Turns are durable no-ops; queued follow-up Turns cancel locally and
  launch Turns continue through the canonical Job cancellation path. Existing
  `AgentSessionStopOperations`, claim/apply fencing, and
  `ISessionStopDelivery` were not modified.
- A filtered-index insert conflict returns `409 stop_outcome_unknown` without
  persisting the losing caller's mapping. Caller A and B have separate scope
  rows after the Turn is resolved; a matching pending retry observes the
  existing canonical claim and does not redeliver from the HTTP route.
- Unknown and unavailable delivery outcomes leave the direct mapping pending.
  Settled canonical terminal facts or stop dispositions reconcile that mapping
  before a later caller can create its own operation. Responses are served only
  from the public Turn projection and expose no frozen target or operation ID.
- Added `DirectApiStopSpecs` covering empty-body validation, terminal no-op,
  queued local cancellation, unknown pending blocking, caller binding, frozen
  operation retry, and response privacy. Focused stop (5), launch (7),
  follow-up (6), and canonical stop (18) suites pass.
- The first full fast-gate run exposed the expected Direct API architecture
  isolation check for `TurnControlResult`; completion classification was moved
  to the root `Api` layer. Architecture then passed (69 tests), and the
  remaining fast lanes passed: 2,675 unit, 178 workflow, 1,848 CLI, 4,724 Web,
  and 1,639 Runner tests.
- The file-size ratchet required moving the canonical Turn resolver and
  `CanonicalTurnStopTarget` into `AgentSessionQuerier.StopTargets.cs`; the
  behavior and public API surface are unchanged. `npm run check:filesizes`,
  the server SpecTests build, and the focused T-006 suite pass after the
  split.

## T-007 verification rerun (current workflow)

- The existing T-007 implementation was audited against
  `public-session-event-stream/spec.md`: the route reads only
  `PublicSessionEventRow` records after cursor binding, generation, and
  projection-lag checks; it does not consult the event bus, SignalR, Runner,
  transcript, or UI timeline.
- The focused `PublicSessionEventStreamRouteSpecs` suite passed all 5 tests,
  covering exclusive paging, limit capping, allowlisted execution and
  context-reset payloads, cursor tamper/cross-binding/generation rejection,
  expiry bounds, tombstones, physical purge, and projection lag. The
  `PublicSessionEventCursorCodecTests` coverage ran with the full 2,675-test
  unit assembly and passed. `npm run test:fast:arch` passed all 69 tests.
- The HMAC key remains persisted through the encrypted server-secret store at
  the deployment-wide server address; the hosted initializer creates it on
  startup and each request reloads it, so persisted rotation invalidates old
  cursors immediately.
- An initial repository-wide `npm run test:fast` rerun was interrupted by 10
  unrelated 5-second timeouts in
  `packages/runner/tests/runner-host-cleanup-config.spec.ts`; `concurrently`
  then terminated the other lanes. No Runner files were changed. The isolated
  Runner lane subsequently passed all 1,639 tests, and the final full
  `npm run test:fast` rerun passed all seven lanes: typecheck, 2,675 unit,
  69 architecture, 178 workflow, 1,848 CLI, 4,724 Web, and 1,639 Runner
  tests.

## Verification diagnosis (current run)

- The initial `git status` and `git diff` were clean. No failed verification
  log was present in the repository; the only recorded interruption was the
  earlier `test:fast` run with transient Runner cleanup timeouts, followed by
  a passing isolated Runner lane and passing full fast gate.
- Reproduced `npm run verify` on the current commit. Docs, file-size, format,
  solution/workspace build, and the complete test suite all passed: 3,975
  Server SpecTests, 2,675 Server unit tests, 1,848 CLI tests, 4,724 Web
  tests, 1,639 Runner tests, and 70 Slack tests.
- Focused Direct API suites also passed: auth (9), launch idempotency (7),
  follow-up (6), stop (5), projection (14), read routes (7), projector
  hosting (1), and event stream (5). Verification created only ignored build
  outputs, and no implementation, test, or build-configuration defect was
  reproducible. No source fix was necessary.

## T-001 verification rerun (current workflow)

- Audited the existing direct boundary against
  `external-agent-caller-auth/spec.md`: AuthResolutionMiddleware records the
  carrier and PAT-derived ExternalAgentCaller once; ExternalAgentApiMiddleware
  enforces bearer-only authentication, PAT grant presence, route scope, and
  Project grant before endpoint delegates.
- The focused direct auth pipeline suite passed all 9 SpecTests, and the
  focused auth/unit regression command passed all 2,675 unit tests. The
  seven-route registration remains centralized in DirectApiRoutes with the
  expected RouteScopeRequirement metadata and the shared `{ error: { code,
  message } }` envelope.
- No source correction was necessary because the T-001 implementation is
  already present and clean on the workflow branch; this entry records the
  current-run verification before the required fast gate and commit.

## T-002 verification rerun (current task)

- Audited the existing T-002 implementation against
  `public-execution-projection/spec.md`: the additive migration, isolated
  public projection rows, single hosted projector writer, checkpointed EF
  batches, strict DTO, five-state aggregator, fences, crash recovery, and
  generation switching are present and unchanged.
- Focused verification passed: `PublicExecutionProjectionSpecs` (14),
  `PublicExecutionProjectorHostingSpecs` (1),
  `PublicExecutionReadContractTests` (6), and
  `ProjectEventsModelDebug` (1).
- `npm run test:fast` passed all seven lanes: typecheck, 2,675 server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests. No source correction was necessary;
  this run records the required T-002 verification on the clean workflow
  branch.

## T-003 verification rerun (current task)

- Audited the existing T-003 implementation against
  `public-execution-read/spec.md`: the Job, Input, and Turn routes use the
  persisted public snapshot JSON, perform canonical Project membership checks
  after the direct grant middleware, and share the `PublicRead` response
  mapper and `projection_lag` Retry-After result.
- Focused verification passed: `PublicExecutionReadRouteSpecs` (7),
  `PublicExecutionReadQuerierSpecs` (1), the complete server unit assembly
  (2,675), and the architecture suite (69), including the DirectApi
  internal-read isolation rule.
- `npm run test:fast` passed all seven lanes: typecheck, 2,675 server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests. No source correction was necessary;
  the T-003 implementation and coverage were already present and clean on
  the workflow branch.

## T-004 verification rerun (current task)

- Re-read the external-write-idempotency requirement and audited the existing
  implementation: the additive mapping migration contains the composite
  command/scope uniqueness and filtered pending-stop index; strict key/body
  validation happens before mapping admission; fingerprints are versioned,
  server-built SHA-256 values over canonical route IDs and the accepted text;
  and direct launches use the namespaced StableToken coordinator key.
- The launch SpecTests passed (7), including missing/invalid key and body
  validation, exact-text conflict and whitespace preservation, pending-row
  recovery, archived-Agent lookup, durable rejection replay, and one-mapping
  replay uniqueness. The server unit assembly passed all 2,675 tests, and the
  full `npm run test:fast` gate passed all seven lanes.
- No source correction was necessary on this run: T-004 is already complete
  on the workflow branch in the existing implementation commits, and the
  required mapping, route, launcher, migration, and test coverage are present
  and clean.

## T-005 verification (current task)

- Re-read the follow-up idempotency requirement and audited the existing route:
  Project membership is checked before mapping admission, Project and Agent
  come only from canonical Session labels, and caller-supplied derived fields
  are rejected by the strict body reader.
- The follow-up mapping uses `sessionId|Idempotency-Key`, stores the canonical
  outcome, and passes deterministic pre-minted Input/Turn IDs into the Session
  grain. Matching retries re-enter the grain idempotently or read the public
  projection; payload conflicts never admit a second input.
- Capacity and concurrency admission failures are completed as durable public
  rejections with null live IDs. Runtime/recovery/stop uncertainty remains
  pending and returns a retryable safe error, so it cannot become a silent
  acceptance on replay.
- Focused `DirectApiFollowupSpecs` passed all 6 tests. `npm run test:fast`
  passed all seven lanes: typecheck, 2,675 Server unit tests, 69 architecture
  tests, 178 workflow tests, 1,848 CLI tests, 4,724 Web tests, and 1,639
  Runner tests. No source correction was necessary on the current clean tree.

## T-006 verification (current task)

- Re-read the unresolved-stop requirement and audited the direct stop adapter:
  empty-body validation precedes canonical lookup and mapping creation;
  terminal turns are durable no-ops; queued follow-up turns cancel locally;
  launch turns continue through the canonical Job cancellation branch; and
  running turns freeze revision, context generation, binding, deadline, and
  operation identity before the first delivery effect.
- The caller-scoped stop scope embeds `callerKeyId`; the filtered pending-turn
  index rejects both another key and another caller with
  `stop_outcome_unknown` without persisting the losing mapping. After the first
  lifecycle resolves, a caller replaying the first key creates a separate row
  and is classified against the current Turn rather than receiving the first
  caller's mapping or frozen target.
- Matching pending retries observe the existing canonical claim without
  redispatching. Unknown and unavailable delivery outcomes remain pending,
  and canonical recovery or a later terminal fact is reconciled before the
  next caller can create a stop mapping. Responses are projection-only public
  Turn observations and contain no frozen target or operation identity.
- Added a caller-binding assertion to `DirectApiStopSpecs` and ran the focused
  suite successfully (5 tests). The full `npm run test:fast` gate remains the
  final verification for this task.

## T-007 completion (current task)

- Audited the public Session event route against
  `public-session-event-stream/spec.md`: cursor verification and route binding
  happen before projection reads; the stream state supplies generation,
  retention bounds, tombstone status, and high-water positioning; and event
  rows are read only from the persisted active-generation public journal.
- Hardened response serialization so persisted event payloads are
  deserialized into `PublicExecutionRead` or `PublicSessionEventPayload` and
  reserialized through `JSON.PublicApi`. Unknown event types are rejected and
  private fields accidentally present in a projection row cannot cross the
  direct API boundary. Added a SpecTest that injects `prompt` and `runtime`
  fields into persisted rows and verifies the response remains allowlisted.
- Focused verification passed: `PublicSessionEventStreamRouteSpecs` (5),
  `PublicSessionEventCursorCodecTests` (3), and the Server build. The complete
  `npm run test:fast` gate passed all seven lanes: typecheck, 2,675 Server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests.

## T-008 final verification (current workflow)

- The existing T-008 documentation commit was rechecked against the selected
  acceptance criteria: both design documents are `status: shipped`; the user
  API document contains the seven routes, Bearer PAT and Project grant rules,
  idempotency, the PublicExecutionRead allowlist and five-state aggregate,
  projection lag, and cursor resume/error behavior; the auth guide documents
  direct `/api/v1` PAT use and grant-before-lookup ordering; and README lists
  the External Agent API as available.
- `npm run docs:check` passed for 92 Markdown files.
- `npm run verify` passed: docs, file-size, format, solution/workspace build,
  3,975 Server SpecTests, 2,675 Server unit tests, 1,848 CLI tests, 4,724 Web
  tests, 1,639 Runner tests, and 70 Slack tests. No source changes were needed
  beyond this progress record.

## Verification diagnosis (current task)

- The initial worktree inspection was clean; no failed verification output was
  checked into the repository. Ignored build products were the only existing
  verification artifacts.
- Reproducing `npm run verify` exposed one real failure: 3,974 of 3,975
  Server SpecTests passed. The generated MTP log identified
  `PublicExecutionReadRouteSpecs.SessionCheckpointLag_GatesInputAndTurnReadsUntilCaughtUp`
  at `PublicExecutionReadRouteSpecs.cs:290`, with expected `503 ServiceUnavailable`
  and actual `200 OK`.
- The failure was a test-harness race, not a stale-read implementation defect.
  The test commits a deliberately rewound Session projection checkpoint, but
  the shared integration host's five-second `PublicExecutionProjector` timer
  can repair that checkpoint before the HTTP assertion. The route then
  correctly serves `200` because the projection is current again.
- Fixed the harness in `MohistIntegrationFixture` by using a one-hour test-only
  sweep interval. Checkpoint-rewind specs now use their explicit projector
  nudge for recovery, leaving the production five-second timer unchanged and
  preventing it from racing the intended lag observation.
- Focused read-route (7) and event-stream (5) suites passed after the fix.
  The repaired `npm run verify` passed docs, file-size, format, build, 3,975
  Server SpecTests, 2,675 Server unit tests, 1,848 CLI tests, 4,724 Web tests,
  1,639 Runner tests, and 70 Slack tests. Verification produced only ignored
  build outputs; no Git-visible side effects remained.

## T-001 verification (current task)

- Re-read `external-agent-caller-auth/spec.md` and audited the direct boundary:
  AuthResolutionMiddleware records the Bearer/cookie carrier and PAT-derived
  ExternalAgentCaller once; ExternalAgentApiMiddleware enforces PAT-only
  carrier, persisted grant, route scope, and Project grant before delegates;
  and all seven routes remain centralized under the shared `/api/v1` group.
- Added unit coverage proving a PAT records `callerKeyId`, principal, scopes,
  and its persisted Project grant in HttpContext.Items, plus cookie carrier
  recording without a caller. Added a pipeline SpecTest proving an authenticated
  cookie is rejected on an unmatched `/api/v1` path as well as every registered
  route. The auth path now aliases the middleware's single prefix constant.
- Focused verification passed: AuthResolutionMiddlewareTests (23) and
  DirectApiAuthPipelineSpecs (10). Existing implementation and pipeline tests
  cover the required non-classifying 401, grant-less 403, readonly write fence,
  out-of-grant 403-before-lookup, operator_all persistence, error envelope, and
  zero-effect rejection behavior.

## T-001 completion (current workflow)

- The auth regression check passed serially before finalizing this task:
  2,676 server unit tests, the 10 DirectApiAuthPipelineSpecs, and all 69
  architecture tests. The direct boundary remains centralized and no endpoint
  or unrelated control-plane behavior changed.
- The final source delta aliases AuthResolutionMiddleware's direct path
  constant to ExternalAgentApiMiddleware.PathPrefix, preventing auth and
  boundary routing from drifting. The added tests pin caller facts in
  HttpContext.Items and cookie rejection on unmatched /api/v1 paths.

## T-002 completion (current workflow)

- Audited the projection implementation against the binding projection and
  read requirements. The additive public projection migration, isolated rows,
  single hosted writer, checkpointed transactions, strict DTO, five-state
  mapping, terminal fences, crash recovery, and generation switching remain
  present and model-consistent.
- Corrected terminal fence bookkeeping so `TerminalSequence` is populated
  from the normalized terminal journal transition for every affected
  Job/Input/Turn anchor. Later fenced passes preserve that value with the
  terminal output, error, and public sequence; added a regression assertion
  to the terminal-fence SpecTest.
- Focused projection (14), hosted projector (1), DTO contract (6), and EF
  model-diff (1) tests passed. `npm run test:fast` passed all lanes: 2,676
  server unit, 69 architecture, 178 workflow, 1,848 CLI, 4,724 Web, and
  1,639 Runner tests, plus typechecks.

## T-003 completion (current workflow)

- Re-read the public-execution-read requirement and confirmed the existing
  implementation serves Job, Input, and Turn responses from persisted public
  snapshot JSON, with canonical Project membership and exact Input/Turn
  anchoring performed only to establish 404 ownership and source watermarks.
- The shared `PublicRead` mapper emits the resource-specific 404 codes or a
  bodyless `503 projection_lag` response with `Retry-After`; it is reused by
  command response paths. The public DTO contract and production architecture
  rule keep internal read types and unlisted execution fields out of the
  `Api/DirectApi` boundary.
- Focused verification passed: `PublicExecutionReadRouteSpecs` (7),
  `PublicExecutionReadQuerierSpecs` (1), `PublicExecutionReadContractTests`
  (6), and `ProductionContractRules` (3). `npm run test:fast` passed all
  seven lanes: typecheck, 2,676 server unit, 69 architecture, 178 workflow,
  1,848 CLI, 4,724 Web, and 1,639 Runner tests. No source correction was
  necessary on this current workflow branch.

## T-004 completion (current workflow)

- Re-read `external-write-idempotency/spec.md` and verified the existing
  implementation satisfies the selected launch slice: strict header/body
  validation precedes lookup and mapping admission; the additive mapping
  migration has the composite uniqueness and filtered pending-stop index;
  fingerprints are versioned full SHA-256 values over canonical route IDs
  and exact accepted text; and the direct coordinator key is deterministic,
  persisted in the internal mapping outcome, and namespace-separated from
  the Web key input.
- Focused `DirectApiIdempotencySpecs` passed all 7 tests, the direct write
  validation unit coverage passed within the 2,676-test server unit assembly,
  and `ProjectEventsModelDebug` passed the EF model/migration consistency
  check.
- `npm run test:fast` passed all seven lanes: typecheck, 2,676 server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests. No source correction was necessary;
  T-004 implementation and coverage were already present and clean on the
  workflow branch.

## T-005 completion (current workflow)

- Audited the existing follow-up route against the binding idempotency and
  public-read requirements. Canonical Session membership is resolved after
  direct authorization and before mapping admission; Project and Agent come
  only from immutable Session labels; fingerprints contain only the version,
  follow-up discriminator, canonical Session ID, and accepted body; and the
  pre-minted Input/Turn IDs are stable for `sessionId|key`.
- Corrected a transient admission classification gap: an unaccepted existing
  follow-up lease now throws `FollowupOperationInProgressException` from the
  Session grain. The direct route already maps that typed condition to `503
  followup_pending`, so it no longer catches it as a definitive durable
  rejection. Existing control-plane callers retain the same base exception
  contract.
- Added `InProgressAdmission_RemainsPendingAndRetryable` to
  `DirectApiFollowupSpecs`. It verifies two retries return `503
  followup_pending`, retain a pending mapping with the deterministic IDs, and
  create no Input or Turn.
- Pre-change T-004 regression passed (7 tests). Focused T-005 follow-up
  coverage passed (7 tests), the Session grain follow-up suite passed (37),
  and `npm run test:fast` passed all seven lanes: typecheck, 2,676 Server
  unit tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests,
  4,724 Web tests, and 1,639 Runner tests. The first fast run encountered
  the known concurrent Runner cleanup timing race; the isolated Runner lane
  and full rerun both passed. Format and file-size checks are clean.

## T-006 completion (current workflow)

- Audited the existing fenced direct stop implementation against the binding
  unresolved-stop requirement. Empty-body validation runs before canonical
  Turn lookup and mapping admission; terminal Turns are durable no-ops;
  queued follow-up Turns cancel locally; and queued launch Turns continue to
  the canonical AgentJob cancellation branch without Runner delivery.
- Verified that running stops persist the caller-bound mapping and its frozen
  Turn revision, binding epoch/context generation, delivery binding, deadline,
  and operation ID before the first Runner delivery. Matching pending retries
  use the frozen target and canonical stop claim; unknown or unavailable
  delivery remains pending, while the filtered pending-Turn index blocks other
  keys and callers with `stop_outcome_unknown` without persisting the loser.
- Confirmed response privacy: direct stop responses are projection-backed
  public Turn observations only and do not expose the frozen target, binding,
  deadline, owner, lease, fence, or operation ID. Caller B creates a separate
  mapping after caller A's pending stop resolves and is classified against the
  Turn's current state.
- Focused `DirectApiStopSpecs` passed all 5 tests. `npm run test:fast` passed
  all seven lanes: typecheck, 2,676 Server unit tests, 69 architecture tests,
  178 workflow tests, 1,848 CLI tests, 4,724 Web tests, and 1,639 Runner
  tests. No source correction was necessary on this clean workflow branch.



## T-007 completion audit (current workflow)

- Re-read `public-session-event-stream/spec.md` and audited the existing
  implementation: `GET /api/v1/projects/{projectId}/agent-sessions/{sessionId}/events`
  reads only the active-generation persisted public journal after validating
  the HMAC cursor binding and the shared Session projection checkpoint. It
  does not use event-bus, SignalR, Runner, transcript, or UI data for cursor,
  ordering, generation, or payload behavior.
- Confirmed default and capped limits, exclusive-after paging, ascending
  sequences, deterministic duplicate page content, high-water empty-page
  cursors, wrong-generation/cross-binding/tampered cursor rejection, safe
  expiry bounds, closed-stream tombstones, and physical-purge invalidation.
  Persisted event payloads are deserialized into the strict public DTOs and
  reserialized through `JSON.PublicApi`, preventing private projection fields
  from crossing the route boundary.
- Confirmed the deployment-wide 32-byte HMAC key is created at startup and
  persisted through the encrypted server secret store; each request reloads
  it, so replacing the persisted key invalidates existing cursors.
- Focused `PublicSessionEventStreamRouteSpecs` passed all 5 tests and the
  cursor codec coverage passed within the complete 2,676-test unit assembly.
  `npm run test:fast` passed all seven lanes: typecheck, 2,676 Server unit
  tests, 69 architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724
  Web tests, and 1,639 Runner tests. No source correction was necessary on
  this clean workflow branch.

## T-008 final verification (current workflow)

- Confirmed the shipped documentation slice is present on the required run
  branch: both design documents use `status: shipped`, the user API guide
  documents all seven routes and the direct authentication, idempotency,
  projection, five-state, lag, and cursor contracts, the auth guide documents
  persisted Project grants and grant-before-lookup ordering, and README lists
  the External Agent API as available.
- `npm run docs:check` passed for 92 active Markdown files.
- `npm run verify` passed: docs, file-size and format checks, solution and
  workspace builds, 3,977 Server SpecTests, 2,676 Server unit tests, 1,848
  CLI tests, 4,724 Web tests, 1,639 Runner tests, and 70 Slack tests.

## Verification diagnosis and repair (current task)

- The initial worktree was clean. The first reproduced `npm run verify` failure
  was `DirectApiStopSpecs.MatchingRetryUsesTheFrozenOperationAndBinding`:
  the first unresolved stop expected `503` but received `200` when the public
  projector had already caught up.
- The direct stop adapter left an unavailable/unknown delivery mapping pending
  but fell through to `ReadTurnObservationAsync`; a caught-up projection could
  therefore make an unresolved command look successful. The route now returns
  `503 stop_pending` with `Retry-After` until the fenced lifecycle settles, and
  the focused stop spec asserts that public error code.
- A subsequent full-gate run exposed two independent read-spec timing races:
  a joined Job read could observe the legitimate intermediate prepared snapshot,
  and sequential Input/Turn lag reads could straddle a pending checkpoint
  repair. The test helper now waits for the required terminal state, and the
  lag assertions read both anchors concurrently while retaining `503
  projection_lag` assertions for both.
- Focused Direct API stop and public-read suites passed (5 and 7 tests).
  The final `npm run verify` passed all checks: 3,977 Server SpecTests, 2,676
  Server unit tests, 1,848 CLI tests, 4,724 Web tests, 1,639 Runner tests,
  70 Slack tests, builds, docs, format, and file-size checks. Verification
  created only ignored build/test outputs; no Git-visible side effects remain.

## Review must-fix disposition work

- MF-1 implementation committed as `a02d82d21`: `AgentSessionStore` now
  appends normalized lifecycle history rows transactionally with Session state;
  the public projector consumes those durable rows and reconstructs historical
  execution payloads, preserving compressed queued/running/terminal transitions
  and distinct unknown episodes.
- MF-2 implementation is in the same commit: Session deletion closes the
  existing public stream and preserves its safe bounds in the same transaction;
  `IAgentSessionStreamRetention.PurgeDeletedAsync` is the explicit physical
  purge path and removes the tombstone only after closure.
- Focused projection (16) and event-stream (6) suites pass, as does the
  server build and EF model consistency spec. Full repository verification is
  running next.
