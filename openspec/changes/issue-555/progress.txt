# Progress: issue-555 self-review disposition

Task: dispose of every must-fix finding in self-review.md by fixing the plan
artifacts. Observations require no action. self-review.md itself is NOT edited
(regenerated by the next review).

## Findings inventory

- MF-1 (must-fix): stop mapping not caller-bound in design.md Decision B —
  contradicts external-write-idempotency spec ("Durable keyed mappings are
  scoped per command", stop caller-binding MUST + "Stop keys are caller-bound"
  scenario) and design/agent-api.md. DISPOSITION: FIXED.
- Observations 1-7: no action required. Obs 1 (design.md Decision E says
  "all 20 keys"; the allowlist has 22) was fixed in passing while editing
  design.md, per the reviewer's own note ("fix the prose when touching the
  file"). All other observations left as-is.

## MF-1 fix applied

Mechanism chosen (per reviewer fix direction, first option): stop scope_key
embeds callerKeyId → `turnId|callerKeyId|key`, so the single unique index
(command, scope_key) gives caller-scoped stop uniqueness. The filtered
per-turn pending index stays as the cross-caller/cross-key 409
stop_outcome_unknown block. Cross-caller outcome now stated explicitly:
while A's stop is pending, B (any key, incl. A's key string) → 409
stop_outcome_unknown, nothing persisted; once no unresolved stop remains,
B's key creates B's own mapping and classification evaluates it (terminal →
no-op, queued → local cancel, running → new fenced stop). B never sees A's
row/outcome/frozen target.

Files changed:
- design.md Decision B: scope_key row (stop now turnId|callerKeyId|key),
  caller_key_id row, unique-index paragraph (caller-scoping rationale),
  fingerprint step (caller deliberately excluded; separation comes from
  scope), write-path step 3 (constraint discrimination by index name), new
  cross-caller stop outcome paragraph after step 5.
- design.md Decision D: "blocks other keys" → "any other stop request for the
  Turn (other keys and other callers)".
- design.md Decision E: "all 20 keys" → "all 22 keys" (Obs 1, in passing).
- tasks.json T-006: description now names the caller-scoped stop scope_key +
  cross-caller block; caller-binding acceptance criterion rewritten as the two
  concrete observable outcomes (pending → 409, resolved → own mapping).
- tasks.json T-004: parenthetical noting the stop scope_key embeds
  callerKeyId, since T-004 creates the table/indexes (would otherwise risk
  recreating MF-1 at schema time).

## Status

- [x] Read self-review, plan artifacts, spec, target contract
- [x] progress.txt created (commit f27fb3748)
- [x] design.md fixed (MF-1 + Obs 1 prose) (commit 4afbf4ead)
- [x] tasks.json fixed (T-006 description + criterion, T-004 note); JSON
      validated (commit after 4afbf4ead)
- [x] Final sweep: no remaining caller-unbound stop scope references in the
      plan artifacts; self-review.md untouched; working tree clean

Disposition summary: MF-1 FIXED; observations 1-7 no action required
(observation 1's prose count fixed in passing per the reviewer's note);
no won't-fix dispositions — no must-fix finding was left unfixed.
