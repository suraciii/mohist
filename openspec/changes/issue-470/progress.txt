Issue 470 current plan repair

- [completed] F2: design names `SuppressInstrumentationScope` and its exact HTTP/background lifetime, composed with existing inbound/exporter filters.
- [completed] F3: `MohistHostRunner`, `IMohistHostFactory`, `IMohistHost`, and `IOtelBindFailureClassifier` define the deterministic fallback test boundary.
- [completed] F4: process/storage failure atomically invalidates cached fields and growth/CPU baselines; recovery starts fresh warm-up.
- [completed] F1: merged status mapping, readiness, sampler/startup, normal collector publication, and complete off/healthy/degraded behavior into one usable T-003; downstream tasks were renumbered and dependencies updated.
- [completed] Revalidated proposal/spec/design/task consistency: 8-task DAG and anchors pass, 16 requirements/52 scenarios remain covered, targeted F1-F4 audit returned PASS, diff checks pass, and `self-review.md` remains unchanged at `c5c9a70267cb2eac97da8da838bb2fc755ac1c22bf87fb39fd1318407b497c1a`.
- [completed] Current review F1: metadata readability no longer establishes ingestion health; `storage_unverified` remains owned by `storage_write` until a real production write transaction commits.
- [completed] Current review F2: one content-type-aware response writer now fixes JSON/protobuf full success, partial success, malformed-body and retryable-failure wire contracts.
- [completed] Current review F3: fake lifecycle tests own runtime host/probe facts; non-starting production composition owns only immutable-plan and service-registration facts.
- [completed] Current review F4: request duration brackets the awaited endpoint with injected TimeProvider timestamps and is deterministically covered for success, exception and cancellation.
- [completed] Final verification: tasks.json parses; all 8 task dependencies are acyclic and all spec anchors resolve; 17 requirements/58 scenarios remain represented; diff checks pass; no repair commit includes `self-review.md`, whose pre-existing worktree modification remains untouched.
- [completed] Latest review F1: a rolled-back accepted subset now takes batch-level precedence over provisional rejection/drop, returns retryable 503, records only parsed received attempts plus storage-write degradation, and has explicit mixed JSON/protobuf wire and counter coverage in T-002.
- [completed] Latest review F2: the runner now requires IMohistDatabaseInitializer, invokes it with each host's Services before every primary or alternate start, and has explicit failure/order tests; DatabaseInitializationSpecs and DatabaseInitializerRepositoryUpgradeSpecs own the concrete migration/data-upgrade chain, removing the untested production-adapter delegation gap.
- [completed] Latest review F3: T-007 fake tests are restricted to lifecycle, probes and RuntimeObservability projection; T-003's in-memory HTTP API spec remains the sole owner of degraded-OTel versus /api/health behavior.
- [completed] Latest final verification: independent targeted review reports F1-F3 PASS with no new inconsistency; tasks.json parses, the 8-task DAG is acyclic, all anchors resolve, and 17 requirements/60 scenarios are present. Diff checks pass and repair commits exclude self-review.md. The openspec executable is unavailable in this workspace, so openspec validate could not run.
- [completed] Current self-review F1: fallback guarantees collector_bind_failed only for the alternate's initial latest reason; later process/storage/protection events replace it by the common sequence rule while the collector source remains active.
- [completed] Current self-review F4: bind-failure classification now consumes the collector listener BindHost/Port intent only, with an explicit test where the outbound exporter Endpoint differs.
- [completed] Current self-review F2: transaction-time request cancellation has its own cancelled write result, one received-only outcome, unchanged storage-write source, no protection activation/refresh, ordinary protection expiry, unchanged exception propagation, and a signal-driven deterministic test.
- [completed] Current self-review F3: EventDispatcherPoke and all three calling stores now join the scope-suppressing launcher boundary; tests cover the detached grain invocation and its unchanged originating request counts.
- [completed] Current final verification: independent review reports F1-F4 PASS with no new finding; tasks.json parses, the 8-task DAG is acyclic, all anchors resolve, and 17 requirements/63 scenarios are present. Diff checks pass, repair commits exclude self-review.md, and the openspec executable remains unavailable.
- [completed] T-001 runtime publication core: added reusable RuntimeEpoch, singleton-ready RuntimeObservability, fixed Mohist.Server.Runtime catalog, source-owned collector/process/storage/ingest publication, normalized request and agent-path facts, immutable snapshots, and typed ingest outcome derivation.
- [completed] T-001 verification: added MeterListener, source-isolation, normalization, protection-expiry, reason-bounding, epoch-reuse, and ingest precedence tests; focused telemetry tests pass (51), server build passes, and no metrics exporter targets the built-in receiver.
- [completed] T-001 contract tightening: IngestOutcomeBuilder now requires classified totals plus exactly one typed write result at construction, and successful process samples reject non-positive processor counts.
- [completed] T-002 telemetry integration: TraceIngester now prepares an immutable-shaped classified batch before opening SQLite, applies an injectable accept-all protection decision by default, publishes exactly one derived outcome for commit, rollback, cancellation, and non-attempted batches, and preserves existing synchronous ingest compatibility.
- [completed] T-002 response contract: OtlpTraceResponseWriter selects JSON/protobuf from request Content-Type, emits empty full-success messages, canonical partial-success protobuf-JSON/protobuf fields, and google.rpc.Status code 3/14 errors; route integration now maps malformed input and retryable writes without publishing parse failures.
- [completed] T-002 focused verification: rejection, no-write accounting, cancellation-after-begin, response encoding, and malformed status tests pass; full Server unit/spec/architecture/CLI suites pass. Repository npm test first reached an unrelated flaky runner concurrency failure in `runner-host-task-log.spec.ts`; its focused rerun passed. A later full npm test reached one unrelated AgentJob grain timing failure; its focused rerun passed.
- [completed] T-003 status slice: replaced count-based status assembly with the bounded snake-case DTO over RuntimeObservability, removed TraceQuerier status SQL, added readiness-only SQLite opening and metadata probe, added process/storage sampler with startup gating and isolated failure publication, wired normal collector-online publication, and updated CLI rendering.
- [completed] T-003 focused verification: telemetry specs (130), telemetry unit tests (55), OTel CLI tests (18), readiness/probe tests, and raw-null status DTO coverage pass.
- [completed] T-004 request accounting: added lock-protected RequestWorkScope with linearizable close, TimeProvider-driven RuntimeRequestMetricsMiddleware, enabled/off-state agent scope handling, OTel endpoint exclusion, and exception-safe status normalization.
- [completed] T-004 adapters/wiring: added MohistDbContext command interception with otel.db exclusion, caller-side Orleans counting plus incoming scope isolation, physical HttpClient send counting after the existing handler/filter chain, and EF/filter registration in MohistServiceRegistration, MohistIntegrationFixture, and GrainTestConfig.
- [completed] T-004 verification: deterministic scope, fake-time duration, exception propagation, off-state agent, OTel exclusion, and physical HTTP tests pass; repository npm test passes with all .NET, Web, Runner, and CLI suites green.
- [completed] T-005 detached execution isolation: added IBackgroundTaskLauncher with RequestWorkScope suppression, migrated system updates, Hermes notifications, and all three post-commit EventDispatcher pokes; queued callbacks own grain lookup, await, and best-effort logging.
- [completed] T-005 OTel feedback suppression: added awaited endpoint middleware for /otel/v1 and /otel/api, scoped sampler storage probes and registered maintenance callbacks, and kept existing ASP.NET/exporter filters as defense in depth.
- [completed] T-005 focused verification: production launcher scope capture, terminal snapshot immutability, endpoint failure disposal, normal-route preservation, dispatcher/system-update regressions, and server build pass.
- [completed] T-006 route diagnostics: added a 301-bucket, one-second half-open ring with 256 named routes plus overflow, route observation capture from enabled request completion, fake-time expiry, bounded merge/ranking, restart reset, and off-state exclusion.
- [completed] T-006 status/CLI: existing status mapping now publishes ranked route rows and `mo otel status` renders all six route fields; focused runtime, status, CLI, server unit/spec, and CLI suites pass.
- [completed] T-007 host runner orchestration: added `MohistHostRunner` accepting `IMohistHostFactory`, `IOtelBindFailureClassifier`, and `IMohistDatabaseInitializer`; runner drives primary StartAsync, publishes collector online, runs the alternate only after a classified bind failure, and uses one `MohistHostFactory.Build(plan)` composition path shared by both attempts.
- [completed] T-007 lifecycle interfaces/adapters: added `IMohistHost` (Services, StartAsync, StopAsync, DisposeAsync, WaitForShutdownAsync) plus `IMohistHostFactory.CreatePrimary/CreateAlternate`, with a `WebApplication` adapter and TCS-controlled fake hosts; `MohistHostPlan` carries the shared `RuntimeEpoch`, enabled intent, listener `OtelListenerIntent`, and initial `CollectorResult`; `MohistHostPlan.Alternate` mirrors only the listener and seeds `collector_bind_failed`.
- [completed] T-007 database initialization: introduced `IMohistDatabaseInitializer` and `MohistDatabaseInitializer` (production); runner calls it once per host (primary or alternate) before `StartAsync`; `DatabaseInitializationSpecs` and `DatabaseInitializerRepositoryUpgradeSpecs` now exercise the concrete `MohistDatabaseInitializer` over in-memory SQLite to prove EF migration → repository data upgrade.
- [completed] T-007 classifier purity: `OtelBindFailureClassifier` consumes only the collector listener intent and delegates the bind pattern to `OtelBindFailureDetector`; explicitly does not see the outbound exporter `Endpoint`.
- [completed] T-007 host tests: 17 unit tests covering alternate-plan derivation, ordered-seed latest degradation on a process failure, primary init/single/dual failure ordering, alternate build only after clean primary shutdown, classifier intent-only matching with differing BindHost/Port/Endpoint values, and a non-starting composition test that confirms one sampler, shared `RuntimeEpoch`/`RuntimeObservability`/`OtelDb` registrations per plan.
- [completed] T-007 partial verification: focused host-lifecycle and database-initialization suites pass; full unit suite passes (1328 passed / 0 failed). Full `Mohist.Server.SpecTests` and `npm test` not yet run in this turn.
