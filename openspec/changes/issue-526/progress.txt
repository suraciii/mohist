## Codebase Patterns

- The `IAgentConnectionProviderCleanup` interface is the established seam for cascading provider rows on Connection delete. Each child store implements `DeleteForConnectionAsync` and is forward-registered in `MohistServiceRegistration.cs:106-111` so the cascade loop in `AgentConnectionStore.DeleteProviderRecordsAsync` resolves the same scoped instance. New child stores MUST be added there (now 7 forward-registrations).
- The channel ingress (`HandleChannelIngressAsync`) uses a single equality check that is now consolidated into `SlackConnectionAccessDecider.EvaluateAsync`. The decider is read once per ingress and reused at all five owner-check sites via a local `decision` variable.
- `AgentConnectionRow` has both a `LastHeartbeatAt` style nullable `string?` and a `string` (with default) AccessPolicy. The migration uses the `HasDefaultValue(AccessPolicyKind.OwnerOnly)` pattern, and the snapshot needs `HasDefaultValue("owner_only")` to match the model exactly (string form, not constant name).
- `MohistDbContextModelSnapshot` must mirror the runtime model exactly or `IMigrationsModelDiffer` returns `AlterColumnOperation` (default-value differences) and `AddCheckConstraintOperation` (missing check constraints) that abort Migrate().
- `SlackConnectionAccessDecider` takes `IDbContextFactory<MohistDbContext>` for future use (T-002 needs it for live users.info) but the T-001 owner_only + DM branches do not use it. Keep the parameter for the upcoming T-002 wiring.
- Test classes in `Mohist.Server.SpecTests/Specs/Slack/` MUST end with `Specs` or `Collection` (arch test enforced). I initially named a unit-style test `SlackConnectionAccessDeciderTests` and the arch test caught it.
- Spec test classes that don't need IAsyncLifetime (and own their resources with a constructor + `IDisposable`) work fine; some specs in the codebase use `IAsyncLifetime` but it's not required.
- The `SlackOwnerClaimService` constructor now takes `SlackConnectionAccessDecider` (5th positional parameter). All test sites that instantiate it directly (e.g. `SlackOwnerClaimSpecs`, `SlackOwnerTransferSpecs`) must be updated.
- Arch test rule `ServerSourceComments_DoNotIntroduceNewIssueSpecDesignPathReferences_BeyondBaseline` blocks issue/openspec references in production code comments. Pattern: `issue-\d+|T-\d{3}|design/[^*\s]+\.md|openspec/`. Do not put such references in `.cs` comments.
- For `IAsyncLifetime`-based test classes, the initialize runs before each test method. The constructor-based approach is more reliable for tests that need a single fresh fixture.

## T-002 — Allowlist/Anyone live validation

- `SlackConnectionAccessDecider` now takes `ISlackApiClient` + `ISecretStore` (4-arg constructor). The owner-only + DM paths short-circuit before either dependency, so the `owner_only` default still costs zero Slack API calls. Allowlist/Anyone load the Connection's Bot token via `ISecretStore` (the same `SecretStoreAddress` shape `SlackOwnerClaimService.TryClaimAsync` already uses) and call `users.info` per non-Owner invocation.
- `SlackOwnerClaimService.IsEligibleMember` is reused unchanged for "current regular workspace member" semantics — bot, deleted, guest, restricted, ultra-restricted, other-team members all reject identically across claim, access, and diagnostic paths.
- Safe-deny direction: `users.info` returning not-OK, throwing, or yielding no user → deny (`VerificationFailedReason`). `conversations.info` returning not-OK, throwing, or `Channel.IsMember == false` → deny (`ChannelNotVisibleReason` for the false case, `VerificationFailedReason` for unverifiable). The Owner check needs no Slack API, so the Owner is always invokable.
- `RecordingSlackApiClient` (SpecTests) grew per-user `UsersInfoByUser` + resolver, queue-backed / resolver-backed `ConversationsInfo`, and call-history lists. `UsersInfo` is kept as a property alias for `DefaultUsersInfo` so the existing T-001 setup shape still compiles.
- The new integration-level T-002 specs (`SlackAccessPolicyT002Specs`) live in a dedicated `[CollectionDefinition("SlackAccessPolicyT002", DisableParallelization = true)]` so the shared `RecordingSlackApiClient` fixture doesn't race with the parallel `MohistIntegration` collection's per-user / default UsersInfo mutations. Constructor resets `UsersInfoByUser`, `ConversationsInfoResponses`, `UsersInfoCalls`, `ConversationsInfoCalls`, and the resolver delegates so each spec starts from a clean mock state.
- The unit-style `SlackConnectionAccessDeciderSpecs` stays in the parallel default collection because it owns a fresh `RecordingSlackApiClient` per test class instance. Use `UsersInfoResolver` rather than mutating `DefaultUsersInfo` when the same user id must answer with different roles across tests.
- T-003 baseline: CLI test assembly passed; Server Slack policy specs passed with `-p:SkipWebBuild=true`; Web connection widget tests and typecheck passed after `npm ci`.
- T-003 route design: keep Manage access on the project-resolved management group without OperatorCredential; validation must finish before a single DbContext transaction updates the parent and child rows. Existing child store methods use separate contexts, so replacement needs a transaction-aware operation.
- T-003 read-model decision: expose stable Slack IDs in `allowMembers`, with display information supplied by a separate member-search contract; synthesize the Owner in the effective read model rather than persisting an Owner child row. Keep `anyoneDisclosure` as a static server contract string, not persisted state.
- T-003 CLI decision: access flags branch to POST `/manage-access`; presentation flags remain a separate PATCH. Render the canonical disclosure before an `anyone` POST and gate non-interactive calls with `--yes`.
- T-003 Web decision: mount an access-policy editor in the Connection diagnostic page, use a project/connection-scoped Slack member search endpoint, submit only stable IDs, and require an explicit confirmation checkbox before an `anyone` mutation.
- T-003 server done: `SlackConnectionAccessManager.ReplaceAsync` validates every member via `ISlackApiClient.UsersInfoAsync` + `SlackOwnerClaimService.IsEligibleMember`, opens one `MohistDbContext` transaction that deletes existing `SlackConnectionAllowedMembers` rows, updates `AgentConnection.AccessPolicy`, and inserts the new non-Owner set; the Owner id is filtered out before insertion and is never stored. Validation runs before any DB write; `--allow-member` with `owner_only` or `anyone` is rejected with code `allow_members_not_allowed`, unknown policy yields `invalid_access_policy`, invalid members yield `invalid_allow_member`. The new `GET /{id}/members?q=...` route delegates to `SlackMemberSearchService.SearchAsync`, which iterates `users.list` pages, applies `IsEligibleMember`, deduplicates, and caps results at 50. The manage-access response includes `connection`, `accessPolicy`, `allowMembers`, and the static `anyoneDisclosure` contract string from `SlackConnectionAccessContract.AnyoneDisclosure`. `ApiResults.Fail` now always writes a `code` (defaults to `bad_request`) so the spec error envelope is queryable. `SlackUserInfo` now carries optional `DisplayName`, `RealName`, `Email`, and `AvatarUrl` for member search presentation; `SlackConnectionAllowedMemberStore` gained `ListAsync`. Server `SlackAccessPolicyT003Specs` (10 facts) pass; full server suite (3664) green.
- T-003 not done (interrupted): CLI `--access-policy` / `--allow-member` flags, the Anyone disclosure confirmation gate, the `delete --yes` confirmation, the Web access-policy section, the member-search widget, CLI tests, Web tests, and the final integration verification across the three layers.

## T-004 — Channel turn control

- Channel control is classified after a message is attributed to exactly one Connection or exactly one bound thread mapping, and before the normal access-policy gate or launch/follow-up dispatch. This lets a session initiator control their own Turn while still rejecting an allowed non-initiator.
- Initiator authority comes from `GetInitialLaunchAsync().Input.Provenance.MemberId`; no new storage is needed. Owner and initiator comparisons use the Slack stable user id with ordinal equality.
- Channel control accepts an inbox route for active, no-active, and already-ended cases. On redelivery, the existing stored route and Turn id are reused instead of resolving the current Turn again; this prevents a stale gesture from controlling a later Turn in the same session.
- Unauthorized control returns the actionable rejection before inbox acceptance. Normal follow-up still goes through the access decider and session follow-up dispatcher.
- `SlackChannelTurnControlSpecs` covers Owner stop, initiator stop, allowed non-initiator rejection without Runner interruption, stale control redelivery, no-active/already-ended replies, and allowed non-initiator follow-up. The stale redelivery assertion checks the later Turn remains executing; the original outbox reply is intentionally unchanged by dispatch-ref deduplication.

## Verification follow-up

- The first `npm run verify` reproduction exceeded the command timeout after ArchTests started and before SpecTests reported completion; it produced no test failure output and left no Git-visible changes.
- A standalone `npm test` then passed all Server, Web, Runner, and Slack workspace checks, and a second complete `npm run verify` passed the build plus the same test suites. The initial timeout was a transient verification-run hang rather than an implementation or test defect; no code workaround was needed.
- The focused `SlackChannelTurnControlSpecs` run also passed all 8 cases; the repository remains clean apart from this progress record.
