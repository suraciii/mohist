# Ralph Progress Log
Started: 2026-04-09T05:28:25.664Z

---

## T-003: Create ChangeArtifactsManager (2026-04-09T05:35:00Z)

**What was done:**
- The ChangeArtifactsManager already existed at `packages/cli/src/artifacts/change-artifacts-manager.ts` with full implementation
- Fixed TypeScript errors: duplicate function implementations, unused variables, wrong variable types
- Renamed private methods `readArtifact`/`writeArtifact` (issueNumber version) to `readArtifactByIssue`/`writeArtifactByIssue` to avoid naming conflict with public methods
- Fixed `listChanges()` method - was using `number.length` incorrectly (number is not a string)
- Removed unused `DEFAULT_CONFIG` constant and `config` property
- Fixed `restoreChange()` unused `archivePath` variable
- TypeScript compilation now passes

**Learnings:**
- TypeScript doesn't support function overloading - duplicate function names with different signatures cause errors even if they have different parameter types
- When renaming variables that shadow built-ins (like `number`), ensure the correct type is used

**Next steps:**
- T-003 is complete (passes: true)
- T-004 (Planner Agent) depends on T-002 and T-003, both now complete

---

## T-004: Implement Planner Agent (2026-04-09T06:20:00Z)

**What was done:**
- Created PlannerAgent class at `packages/cli/src/agents/planner-agent.ts`
- Implemented `plan()` method that:
  - Explores codebase using LLM with read_file/glob/grep tools
  - Generates design artifacts (proposal.md, design.md, specs/, prd.json)
  - Self-reviews artifacts against Prompt-defined criteria
  - Self-corrects issues up to max_iterations (default 3)
- Added `PLANNER_DEFAULT_PROMPT` constant with default prompt template
- Created `packages/cli/src/agents/prompts/planner-default.yaml` for YAML prompt template storage
- Exported `PlannerAgent`, `createPlannerAgent`, `PLANNER_DEFAULT_PROMPT`, `PlannerAgentOptions`, `CodebaseInfo` from `agents/index.ts`
- Fixed TypeScript errors: removed unused imports, fixed implicit `any` type on filter callback

**Learnings:**
- AI SDK v6 `streamText` returns result with `.text` property directly (not via async iteration)
- `fs.readdirSync().filter()` needs explicit type annotation when using module-level require
- The PlannerAgent is designed to work with WorkflowController which already has the interfaces defined

**Next steps:**
- T-004 complete (passes: true)
- T-005 (Build phase execution) depends on T-002, T-003 - both complete
- T-006 (Reviewer Agent) depends on T-002 - complete

---

## T-005: Implement Build phase execution (2026-04-09T07:15:00Z)

**What was done:**
- Added `executeCoderTask` function to `packages/cli/src/tools/spawn-coder.ts` - exports a simplified API for executing single coding tasks
- Added `CoderTaskResult` interface to `spawn-coder.ts` for structured return values
- Implemented `executeBuildStage()` method in `WorkflowController` that:
  - Reads prd.json tasks using `artifactManager.readPrd()`
  - Sequentially executes each task using `executeCoderTask` (which calls opencode acp)
  - Retries failed tasks up to 3 times
  - Escalates to user (`requiresApproval: true`) when all retries fail
  - Returns structured output with task results on success
- Added `buildTaskPrompt()` helper to construct detailed task prompts for the Coder Agent
- Added `readPrd` method to `ChangeArtifactsManager` interface
- Updated `ChangeArtifactsManager` interface in workflow-controller.ts to include `readPrd`

**Learnings:**
- The spawn_coder tool internally uses runAcpOneshot which is not exported - created a wrapper function `executeCoderTask` that provides a clean API
- Task prompts should include issue context, task description, acceptance criteria, and spec references
- The Build phase should not auto-transition to Review - user should approve after seeing Build results

**Next steps:**
- T-005 complete (passes: true)
- T-006 (Reviewer Agent) depends on T-002 - complete
- T-007 (user approval interface) depends on T-004, T-006
- T-008 (integrate with issue service) depends on T-005, T-007

---

## T-006: Implement Reviewer Agent (2026-04-09T08:30:00Z)

**What was done:**
- Created ReviewerAgent class at `packages/cli/src/agents/reviewer-agent.ts`
- Implemented `review()` method that:
  - Identifies changed files using git diff
  - Reviews each file according to Prompt-defined dimensions (correctness, complexity, test_coverage, security)
  - Runs tests/build as part of review
  - Provides structured ReviewResult with pass/fail and issues
- Added `REVIEWER_DEFAULT_PROMPT` constant with default prompt template covering all 4 dimensions
- Created types: `ReviewDimension`, `ReviewIssue`, `DimensionResult`, `ReviewResult`
- Exported all types and the agent from `agents/index.ts`
- Fixed TypeScript errors: removed unused `issue` variable and unused `checksMatches` variable

**Learnings:**
- The ReviewerAgent interface was already defined in workflow-controller.ts, so the implementation just needed to match that interface
- Dimensions are extracted from the prompt using regex patterns, allowing full customization via custom prompts
- The review process runs each dimension separately and aggregates results at the end
- TypeScript unused variable errors must be fixed - prefixing with underscore or removing the variable

**Next steps:**
- T-006 complete (passes: true)
- T-007 (user approval interface) depends on T-004, T-006 - both now complete
- T-008 (integrate with issue service) depends on T-005, T-007

