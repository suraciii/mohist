Issue 467 current build

- [completed] T-001 history-bounded active agent-status selection: STORED Activity projection + composite index migration; purpose-specific ListStatusCandidatesAsync on AgentSessionQuery (two-branch single SQL: direct Sessions with LabelSourceKind=agent-launch and Activity=active, non-direct Sessions whose LabelSourceId is in the running-Workflow set for the requested project); project-scoped WorkflowActivityQuerier.ListActiveAgentsResultAsync switched to the new path; one Workflow status read per distinct run retained; amplification, response shape, ordering, and #470 counters preserved.
- [completed] T-001 test seams: internal AgentSessionQuery.OnRowsMaterializedCallback for materialized-row counts (not part of public API); virtual WorkflowQuerier.GetStatusAsync so a CountingWorkflowQuerier can derive per-run call counts and deterministic WorkflowStatusView snapshots; AgentStatusHistoryBoundedFixture hosts its own xUnit collection so the counting querier never leaks into IntegrationSessions.
- [completed] T-001 spec coverage: AgentSessionStatusActivityProjectionMigrationSpecs asserts the migration adds the STORED Activity column and IX_AgentSessions_StatusProject_SourceKind_Activity_CreatedAt; AgentStatusHistoryBoundedSelectionSpecs covers active-direct selection, inactive-direct exclusion, non-running Workflow exclusion, terminalized-Workflow exclusion via current-status check, multiple-candidate Workflow read de-dup, cross-project Workflow reference exclusion, thousands-of-historical-rows response/candidate/materialized-row/db-call/downstream-call stability, zero-active-work explicit candidates/processed = 0, and the materialized-row seam counts.
- [completed] T-001 spec regression: existing AgentPathAmplificationSpecs.Status_reports_filtered_candidates_with_truthful_off_state_counts expected candidates=21 after 20 inactive inserts; updated to assert candidates=1 to reflect the new history-bounded contract.
- [completed] T-001 ArchTests: spec-file-size-baseline.json adds an entry for the new specs file (27000-byte allowance).
- [completed] T-001 verification: focused Sessions / AgentPathAmplification / AgentSessionActivityVisibility / AgentSessionQuery / AgentLaunchLabelComputedColumnsMigration / AgentSessionStatusActivityProjectionMigration / AgentStatusHistoryBoundedSelection suites pass; full Mohist.Server.UnitTests 1455 / Mohist.Server.SpecTests 3142 / Mohist.Server.ArchTests 32 / Mohist.Cli.Tests 1375 / Mohist.Workflow.Definition.Tests 175 all green.
- [completed] T-001 code patterns: STORED Activity column is LOWER(json_extract(State, '$.status.activity')) with PascalCase fallback, mirroring WorkflowRuns.Status convention; JSON path is $.status.activity because AgentSessionStatusSnapshot serializes Activity as a string enum via JSON.Options' JsonStringEnumConverter(CamelCase); candidate predicate composes the indexed LabelProjectId, LabelSourceKind, Activity, CreatedAt for the direct branch and the indexed WorkflowRuns.Status, MetadataProjectId for the joined branch so the SQLite planner uses index scans, never table scans, regardless of historical rows.
- [completed] Review fixes: ListStatusCandidatesAsync now unions a direct candidate branch with an AgentSessions-to-WorkflowRuns database-side join in one ordered materialization, rather than materializing running Workflow IDs in memory first. The Workflow branch requires a nonblank LabelWorkId so records that cannot match pending work are neither deserialized nor counted; AgentStatusHistoryBoundedSelectionSpecs covers that exclusion and confirms no Workflow status read occurs.
- [completed] Review follow-up fixes: the Workflow candidate branch includes legacy null source kinds, matching the prior status assembly semantics. The bounded-status specs now cover that legacy visibility, terminalization after selection through a derived query test seam, and complete small-versus-large active-agent JSON, candidate, materialization, and amplification stability.
- [completed] Review follow-up verification: `npm test` passes with Server UnitTests 1455, Server SpecTests 3142, Server ArchTests 32, CLI 1375, Workflow Definition 175, Web 5133 passed / 1 skipped, and Runner 1426.
