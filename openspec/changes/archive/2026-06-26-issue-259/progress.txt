# Issue 259 Progress

## T-001: Mount PulseZone into the dashboard Pulse zone slot

### Completed
- Extended `DashboardPage.tsx` zone selector to mount `<PulseZone />` inside the `pulse` `<DashboardZone>` branch, mirroring the existing `digest` → `<DashboardDigestWidget>` pattern. `productivity` remains the only bare placeholder.
- Added `vi.mock('@/widgets/coder-session/model/activity-cards', ...)` in `DashboardPage.test.tsx` returning an empty `useActivityCards` view-model so `PulseZone` renders its empty state without timers/refetch.
- Flipped pulse slot assertions from placeholder (`childElementCount === 0`) to mounted (`childElementCount > 0`, contains `pulse-empty-state` / `pulse-zone`) and added negative leakage assertions for digest/productivity.

### Verification
- `npm run typecheck -w packages/web`: passed.
- `npm run test:run -w packages/web`: passed (165 files, 2395 tests passed, 1 skipped).
- Focused run of `DashboardPage.test.tsx`, `PulseZone.test.tsx`, `CompactSessionCard.test.tsx`: all 34 tests passed.

### Gotchas / Patterns
- `DashboardZone` applies `border-dashed` unconditionally, so a mounted zone (digest or pulse) still has the dashed border class. Do not use `border-dashed` as a placeholder-vs-mounted discriminator; use `childElementCount` and widget testids instead.
- Mock at the `useActivityCards` derived view-model boundary, not the widget module, so the dashboard test exercises the real `PulseZone` mount point while staying deterministic.
- First full `test:run` showed a flaky failure in `EpicDetailPage.test.tsx` (graph tab `aria-selected`), but it passed in isolation and on the next full run. It is unrelated to this task.
- No changes to `PulseZone`, `CompactSessionCard`, backend, API, or domain state.
