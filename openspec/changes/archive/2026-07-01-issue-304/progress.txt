progress for issue-304

## Codebase Patterns

- T-001 (display-vs-functional-entry convention): conventions.md style is rule+example over prose; uses ## top-level sections, tables, and code-fence callouts. Prior sections already use this pattern; new section follows it. Only conventions.md is touched; architecture.md is explicitly out of scope per design.md Decision 4.
- design.md Decision 4 specifies the exact three-tier wording; T-001 implementation just transcribes that into conventions.md as a standing reference (not epic-specific commentary).
- Acceptance check uses `grep -i 'web.*only|display' design/conventions.md` — pre-existing matches exist for unrelated "display number / display key" prose; the new tier-1 row and section heading satisfy the grep criterion.
- The "tier 2 but not tier 3" justification for keeping operations in the dispatcher (Decision 1) is recorded inline so future readers see both rules together.

- T-002 (epic skill autopilot-first rewrite): source truth is `docs/epics.md` lifecycle section (already current — verified, no re-authoring). Skill edits: removed "does **not** participate in workflow execution" sentence from Epic shape intro; replaced single-section `### Lifecycle: done vs close` with `### Lifecycle: prefer autopilot (start / pause / resume)` + `### Lifecycle: terminal (done / close)` so autopilot is the recommended default and done/close remain the tail; added explicit idempotency subsection (Start on running = no-op) and running-but-idle subsection (NOT a sixth state, inspect via `progress.nextIssueReason`); front-matter description now mentions autopilot and lists start/pause/resume + done/close; end-to-end checklist now includes autopilot posture.
- The stale framing check `grep -i 'does not participate in workflow execution\|non-executing organizer' …SKILL.md` returns zero matches after the rewrite (the original used bold markup `**not**` but the substring `'does not participate in workflow execution'` still matched case-insensitively — case-insensitive grep is what the spec check uses).
- Lifecycle wording mirrors `docs/epics.md` exactly: five states `idle`/`running`/`paused`/`done`/`closed`; same idempotency definition ("重复执行当前状态对应的操作不报错"); same running-but-idle clarification ("不是第六个状态"); same terminal tail semantics (`done` and `closed` are terminal). This keeps skill and user doc from drifting.
- Pre-existing failing tests (AgentCostRollupApiSpecs.GetCost_TodayBucketBoundaryMatchesTimeseriesCurrentDay, AgentCostRollupApiSpecs.GetCost_TodayCostBucketExcludesPriorDays, AgentUsageTimeseriesApiSpecs.GetUsage_RangeToBoundaryIsExclusive) fail on the current date boundary (2026-07-01 vs 2026-07-02) — confirmed pre-existing by running with my change stashed; unrelated to skill-text edits. Skills test suite (62 tests) passes clean.

- T-003 (dispatcher rewrite with complete issue/epic lifecycle command surface): the partial cheat-sheet (`show|list|start|approve|close`) and the "Promote to a scenario skill once a stable practice emerges" caveat were both removed and replaced with a structured surface: a complete issue-lifecycle table (start/approve/reject/retry/rerun/rerun-from-stage/stop/force-stop/resume/rebase/close/reopen) with stop-vs-force-stop, retry-vs-rerun-vs-rerun-from-stage, and reject-vs-stop distinctions called out explicitly; an epic-lifecycle table covering the autopilot trio (start/pause/resume) plus terminal tail (done/close), link/unlink, and show with `progress.nextIssueReason` for running-but-idle; and a small read-only/aux block (show/events/logs/diff/commits/sessions, session transcript/followup, comment/prereq/workflow status/timeline) so the agent does not have to guess about inspecting state.
- Decision 1 is embodied in the dispatcher's prose and frontmatter: frontmatter now states "issue 与 epic 的完整生命周期命令面也由本 skill 承担（不另开专题 skill）" and a paragraph above the sibling-skills table explicitly says these commands are "reference-level operations fully answerable by `mo <cmd> --help`" and live in the dispatcher as the single source of truth — so a future reader sees the rule (tier 2 but not tier 3) inline, not just in design.md.
- Every advertised `mo` command was cross-checked against the live CLI (`mo <cmd> --help` succeeds). Verified commands include the new additions: `issue rerun-from-stage --stage`, `issue reopen`, `issue session transcript <n> <name>`, `issue session followup <n> <name> --text`, `issue comment add <n> --body`, `issue prereq add <n> <prereq>`, `issue workflow status/timeline`, `epic link/unlink/list/update`, `workflow list`, `label list`. The `mo issue comment add` body is a flag (`--body <text>` / `--body-file <path>`), not a positional arg — easy to get wrong from a casual reading of `mo issue comment --help` which only shows `<number>` as a positional.
- Acceptance checks (`grep -c "mo issue <cmd>" SKILL.md`) all return >=1 for the 10 required lifecycle commands (start/approve/reject/retry/rerun/stop/force-stop/resume/rebase/close) and for the 3 required epic autopilot commands (start/pause/resume). The "Promote to a scenario skill once a stable practice emerges" caveat and the old `show|list|start|approve|close` partial cheat-sheet both return zero matches.
- manifest.json was deliberately not modified (no `mohist-operate` entry) — Decision 1 says the dispatcher is the single source; adding the entry would contradict the decision this task is embodying. T-005 will refresh managed cache from source.

- T-004 (CLI reference complete + humble scope): opening rewritten from "`mo` 是 Mohist 的命令行入口。和 Web UI 功能等价 …本文是完整命令参考" to a humble scope statement (entry point for scripting/automation; documents the command groups; full tree via `mo --help`; CLI/Web 各有分工 not claiming parity). Four new sections added in the existing Issue/Epic style: ## Agent 管理 (create/list/ls, show, update, delete + session list/show/transcript/launch/followup/cancel with the same `--text`/`--text-file`/`--text-stdin` followup source options used in Issue session), ## Label 管理 (list/ls, add, update, remove/rm + `--description`/`--supported-values`), ## Workflow 管理 (list/ls, with an explicit "不同于 `mo project workflow template`/`mo project workflow config`" cross-reference back to 项目管理), ## OTel 管理 (`query <sql>` no-server-required with `-d/--db <path>` defaulting to `~/.mohist/otel.db` + `status` server-required + two example queries). Each new section ends with the standard "所有子命令支持 `-o table|json` 和 `--project`/`--project-id`. 完整 flag 见 `mo <cmd> --help`." note matching the existing 项目管理/WORKFLOW 模板 sections.
- Cross-checked every documented subcommand against the live CLI (`mo agent --help`, `mo agent session --help`, `mo label --help`, `mo workflow --help`, `mo otel --help`, plus `mo agent create/update --help` and `mo label add/update --help` for the flag tables). No invented commands. `mo workflow list` lives in its own top-level section and points to 项目管理 for the distinct `mo project workflow template/config` group, so the two cannot be conflated. The Agent session surface (`list/show/transcript/launch/followup/cancel`) is documented separately from the issue-session surface in `## Issue 管理` (kept distinct because the underlying issue-129 generic AgentSession resource is separate from the issue-scoped sessions).
- Acceptance grep `grep -i 'web ui 功能等价\|功能等价\|完整命令参考\|完整参考' docs/cli-reference.md` returns zero matches after the rewrite (exit=1). Per design.md Decision 3 the opening deliberately does NOT re-assert completeness — a standing absolute would re-drift the next time a command group lands; the humble statement names `mo --help` as the full-tree source instead.