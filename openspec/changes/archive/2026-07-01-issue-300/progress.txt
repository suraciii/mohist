## Codebase Patterns

### Runner capacity source of truth

The authoritative runner capacity projection lives in
`packages/server/src/Mohist.Server/Runner/Services/RunnerStatusService.cs`:

- `ProjectRunnerAsync` builds `RunnerCapacityView(UsedSlots, TotalSlots)`:
  - `UsedSlots` = count of `runtime.ActiveWorks` where `OwnerKind == Workflow`
    **distinct by `OwnerId`** (matches `WorkflowGrainSpecs.AssignActiveWorkForTestAsync` setup pattern).
  - `TotalSlots` = `await runnerGrain.GetSlotsAsync()` (persisted on the grain).
- A view with `runtime is null` or `slots is null` returns `Capacity = null`.
- `GetRunnersAsync` returns every runner in the global registry regardless of
  online status; offline runners typically come back with `Capacity = null`.

### Runner online filter

`GetOnlineRunnersAsync` (new in T-001) reuses
`IRunnerRegistryGrain.ListEligibleRunnersAsync` and additionally requires
`runnerGrain.GetRuntimeStateAsync().Status == RunnerStatus.Online`. The status
read is wrapped in try/catch so a missing/deactivated grain is filtered out
without failing the whole call (matches the original
`AgentRoutes.ListAvailableRunnersAsync` semantics). A runner is online as
soon as `RunnerGrain.RegisterAsync` runs (it sets `_status = Online`
unconditionally).

### Aggregate accessor

`GetCapacityAsync` walks `GetOnlineRunnersAsync` views and sums
`(UsedSlots, TotalSlots)` of any view with non-null `Capacity`. This is the
single aggregate accessor that `/agent/status` (and T-002's
`/agent/activity`) consume. No separate capacity service or DTO is
introduced.

### `/agent/status` route rewiring

`Api/AgentRoutes.cs` `GET /agent/status` now:
- Calls `RunnerStatusService.GetOnlineRunnersAsync(project.Id)` once.
- Reads `activeAgents` from `WorkflowActivityQuerier` only for the visibility
  list and the `Running` flag — never for slot counts.
- `AgentStatusResponse.Create` takes the views + a precomputed `RunnerCapacityView`
  from `SumCapacity(runners)`; both the per-runner `Runners[].Active/Max` and
  the top-level `Capacity` derive from the same views, so they are
  guaranteed to agree.
- `LoadPersistedSlotsByRunnerAsync` and `activeSlotsByRunner` are gone.

### Test infrastructure gotchas

- The integration collection (`MohistIntegrationFixture`) shares the global
  runner registry across tests; any test asserting total `Capacity.Active/Max`
  must drain `IRunnerRegistryGrain.ListRunnerIdsAsync()` first to stay
  deterministic — see `AgentStatus_WhenRunnerRegisteredWithoutActiveWork_ReportsIdleRuntime`
  in `RuntimeEntrySpecs.cs` and the divergence spec below.
- `RunnerStatusProjectionSpecs` extends `WorkflowGrainSpecs` and already
  exposes `ClearGlobalRunnerRegistryAsync`, `AssignActiveWorkForTestAsync`,
  `RegisterRunnerForProjectAsync`, `CreateService`, and `TimeAt(...)`
  helpers. Use them when adding capacity tests rather than re-wiring helpers.
- `WorkflowStartInput(Metadata: new WorkflowRunMetadata(...))` works with
  only `projectId` in `Annotations`; `WorkflowGrain.StartAsync` resolves the
  template via `WorkflowProfileManager.LoadStructureAsync`, falling back to
  `IssueWorkflowProfiles.LocalId` if no project/issue profile is set. The
  `SeedRuntimeDivergenceTemplateAsync` helper inserts both
  `ProjectWorkflowTemplates` and `ProjectWorkflowProfiles` rows to make
  startup deterministic.

## T-001 Specific Findings

- The new divergence spec in `RuntimeEntrySpecs.cs` proves
  `capacity.active == 2` while `activeAgents` is empty and `Running == false`
  — i.e. capacity reflects runner active works even when no AgentSession is
  visible.
- The pre-existing `AgentStatus_WhenRunnerRegisteredWithoutActiveWork_ReportsIdleRuntime`
  was changed from `Capacity.Max >= 2` to `Capacity.Max == 2` because we now
  control the registry via a drain at the start of the test; the tightened
  assertion is meaningful and stable.
- `AgentCostRollupApiSpecs.GetCost_*` and
  `AgentUsageTimeseriesApiSpecs.GetUsage_*` failures are pre-existing on
  `master` (date-boundary tests anchored at 2026-07-01 — today's date is
  2026-07-01, so they fail under the default fixture time). Not caused by
  this change.
## T-001 Verification

- `dotnet build` (TreatWarningsAsErrors): clean (0 warnings, 0 errors).
- `RunnerStatusProjectionSpecs` filter: 34/34 pass — includes the four
  new capacity tests (`GetCapacityAsync_AcrossOnlineRunners_SumsUsedAndTotalSlots`,
  `GetCapacityAsync_ExcludesRunnersNotRegisteredThroughRunnerGrain`,
  `GetOnlineRunnersAsync_OnlyReturnsRegisteredGrainsWithOnlineStatus`,
  `GetCapacityAsync_RunnerActiveWorksExceedVisibleSessions_CapacityFollowsRunner`).
- `RuntimeEntrySpecs` filter: 10/10 pass — includes the new divergence spec
  `AgentStatus_WhenRunnerActiveWorksExceedVisibleSessions_CapacityReflectsRunner`
  (asserts `capacity.active == 2`, `ActiveAgents` empty, `Running == false`).
- `ApiContractSpecs` filter: 27/27 pass — `AgentStatus_ReportsRegisteredRunnerWorkflowSlots`
  already drains the registry and asserts exact 4/0 values.
- `Runner` filter (broad): 343/346 pass — 3 skipped (pre-existing,
  architecture + session lifecycle skips), 0 failures.
- Full server test run (`npm test`): 3287 pass, 13 skipped, 3 fail. The 3
  failures (`AgentCostRollupApiSpecs.GetCost_*` × 2,
  `AgentUsageTimeseriesApiSpecs.GetUsage_RangeToBoundaryIsExclusive`) are the
  pre-existing date-boundary failures already documented above.

Acceptance criterion crosscheck:
1. Single aggregate accessor (`RunnerStatusService.GetCapacityAsync`) — done.
2. `/agent/status` route + per-runner `Runners[].Active/Max` derive from the
   same runner-status views — done.
3. `AgentStatusResponse.Create` signature change (drops `persistedSlotsByRunner`,
   no longer computes `activeSlotsByRunner`) — done; the direct call site
   `RuntimeEntrySpecs.cs:195` now passes `RunnerCapacityView(0, 0)` and
   compiles.
4. `activeAgents` only feeds `ActiveAgents[]` and `Running` — verified by
   grep: only `Running: activeAgents.Count > 0` remains.
5. `ApiContractSpecs` / `RuntimeEntrySpecs` capacity assertions rewritten to
   the unified source — done; assertions that tied capacity to active
   AgentSession count are removed.
6. New divergence spec proving runner active works > visible AgentSessions
   yields `capacity.active == runner active-works count` — done
   (`AgentStatus_WhenRunnerActiveWorksExceedVisibleSessions_CapacityReflectsRunner`).
7. `RunnerStatusProjectionSpecs.GetRunnersAsync_CapacityUsesPersistedSlots`
   and the rest of the authoritative-source suite still pass (34/34).
8. `npm test` passes (3 pre-existing date-boundary failures unrelated to
   this change).

## T-002 Specific Findings

### `/agent/activity` route rewiring

`Api/AgentRoutes.cs` `GET /agent/activity` now:
- Drops the local `ListAvailableRunnersAsync` lookup that produced the
  `runnerIds` list (it only existed to feed the `+1` heuristic and is no
  longer called by any route — left in place as dead code because T-001
  kept it and removing it is out of scope for T-002).
- Injects `RunnerStatusService` and calls `GetCapacityAsync(project.Id)`
  once; passes the resulting `RunnerCapacityView` into the querier as
  `capacity:`. `AgentSessionQuerier.GetActivityAsync` accepts
  `RunnerCapacityView? capacity` (nullable so the querier stays usable
  in unit tests without the runner service).
- Still calls `BuildWaitingCardsAsync` and threads the result through
  `waiting:` so the waiting count is preserved.

### `AgentSessionQuerier.GetActivityAsync` signature change

- Dropped `IReadOnlyList<string>? runnerIds` (it fed only `runnerIds.Count
  + 1`).
- Added `RunnerCapacityView? capacity` (defaults to `0/0` when null so
  tests that do not exercise the unified source still get a sane
  `ActivitySlotUsageDto`).
- The querier is **session-pure**: it does no capacity math of its own.
  `summary.Slots` is now `new ActivitySlotUsageDto(capacity?.UsedSlots
  ?? 0, capacity?.TotalSlots ?? 0)`.
- `summary.Active` continues to reflect the visible active session card
  count — it is still the visibility signal, just no longer the capacity
  source.
- `RunnerStatusService` is NOT injected into the querier (D4).

### Existing test updates

- No existing `AgentSessionSpecs` test pinned `summary.slots` to active
  session card count or `runnerCount+1`. The only existing assertions on
  `summary` were `Assert.Equal(0, activity.Summary.Active)` (skipped
  `RunnerReport_WhenAgentWorkFailsBeforeTelemetry_ClosesCreatedSession`)
  and `Assert.Equal(1, activity.Summary.Failed)` — neither depends on
  the slot derivation, so neither needed rewording.
- The wire shape `{ active, max }` of `ActivitySlotUsageDto` is preserved;
  only the value derivation changes (proposal D3).

### New divergence spec

`AgentSessionSpecs.AgentActivity_WhenRunnerActiveWorksExceedVisibleSessions_SlotsReflectRunner`
mirrors the T-001 divergence spec for `/agent/status`:
- Drains `IRunnerRegistryGrain.ListRunnerIdsAsync()` so the assertion
  reflects only this test's runner.
- Registers a runner with `slots = 4`.
- Starts two workflow runs, assigns both to the runner, polls twice so
  the runner carries two distinct workflow owners in `ActiveWorks`.
- Calls `/agent/activity` and asserts:
  - `summary.slots.active == 2` (runner active-works count, NOT the
    zero visible AgentSession count).
  - `summary.slots.max == 4` (persisted runner slots).
  - `summary.active == 0` (visible AgentSession count, intentionally
    divergent from capacity to prove the two surfaces are independent).
- Uses `SeedActivityDivergenceTemplateAsync` to seed both
  `ProjectWorkflowTemplates` and `ProjectWorkflowProfiles` rows for
  the synthetic workflow project id (same pattern as T-001's
  `SeedRuntimeDivergenceTemplateAsync`).

## T-002 Verification

- `dotnet build` (server + tests, TreatWarningsAsErrors): clean (0
  warnings, 0 errors).
- Targeted `dotnet test` filter covering `AgentSessionSpecs`,
  `AgentSessionActivityVisibilitySpecs`, `RunnerStatusProjectionSpecs`,
  `RuntimeEntrySpecs`, `ApiContractSpecs`: 101/101 pass, 2 skipped
  (pre-existing skips).
- The new divergence spec `AgentActivity_WhenRunnerActiveWorksExceed
  VisibleSessions_SlotsReflectRunner` passes (asserts
  `summary.slots.active == 2`, `summary.slots.max == 4`,
  `summary.active == 0`).
- Full server test run (`npm test`): 3288 pass, 13 skipped, 3 fail.
  The 3 failures (`AgentCostRollupApiSpecs.GetCost_TodayBucket*
  × 2`, `AgentUsageTimeseriesApiSpecs.GetUsage_RangeToBoundaryIsExclusive`)
  are the same pre-existing date-boundary failures already documented
  in the T-001 verification section. They use `DateTime.UtcNow.Date`
  rather than `_fixture.TimeProvider.GetUtcNow().Date` so they break
  whenever the test runs at the day boundary. Not caused by this
  change.

Acceptance criterion crosscheck:
1. `/agent/activity.summary.slots.active/max` are sourced from
   `RunnerStatusService.GetCapacityAsync` — done; identical
   underlying source to `/runners[].capacity` (both walk
   `GetOnlineRunnersAsync`).
2. `AgentSessionQuerier.GetActivityAsync` no longer accepts
   `runnerIds` and no longer computes `ActivitySlotUsageDto` from
   `cards.Count(active)` or `runnerCount+1` — done.
3. `AgentSessionQuerier` remains session-pure (`RunnerStatusService`
   not injected) — done; `RunnerStatusView`/`RunnerCapacityView` are
   the only runner-service DTO the querier references, and only as a
   method parameter.
4. No existing `AgentSessionSpecs` assertions tying `summary.slots`
   to active-session-card count or `runnerCount+1` remain (there
   were none in the file before this change).
5. Divergence assertion added
   (`AgentActivity_WhenRunnerActiveWorksExceedVisibleSessions
   _SlotsReflectRunner`).
6. `npm test` (server) passes (3 pre-existing date-boundary failures
   unrelated to this change).

## T-003 Specific Findings

### `IssueDetailPage.tsx` capacity gating fix (D5)

The one client-side capacity math spot at `IssueDetailPage.tsx:410` is
now `isCapacityFull = !!capacity && capacity.max > 0 && capacity.active
>= capacity.max` (matches `derive-runtime-decision.ts:377` canonical
gating exactly). The previous `maxConcurrent = agentStatus?.capacity?.max
?? Infinity; isCapacityFull = activeAgents.length >= maxConcurrent` is
gone. `activeAgents` is unchanged and still drives `isAgentRunningOnThis`
(line 243) and the `N agents running on other issues` indicator (line
1283) — only the capacity gating reads from `capacity.active` /
`capacity.max`.

The `capacity.max > 0` guard mirrors `derive-runtime-decision.ts:377` so
that a placeholder `capacity: { active: 0, max: 0 }` (no runner
registered) does not falsely disable the Start button (Start stays
enabled so the user can attempt start and the server can reject with
its authoritative state). When `capacity` is missing entirely (e.g.
`tests/IssueDetailPage.test.tsx` mock fixture, which sets
`mocks.agentStatus = { activeAgents: [], maxConcurrentAgents: 3 }` with
no `capacity` field), the new gating yields `isCapacityFull = false` —
same as the old behavior when `maxConcurrent` defaulted to `Infinity`.

### New capacity-gating test file

Added
`packages/web/src/pages/issue-detail/ui/IssueDetailPage.capacity-gating.test.tsx`
with 6 tests pinning the new derivation:

1. **Capacity full from server** — `capacity: { active: 2, max: 2 }` with
   `activeAgents: []` disables Start and shows "Capacity full...". Proves
   capacity gating no longer depends on `activeAgents.length`.
2. **Capacity not full enables Start** — `capacity: { active: 0, max: 2 }`
   with `activeAgents: []` enables Start with `^Start$` label.
3. **Long activeAgents does not gate** — `activeAgents.length === 3`
   with `capacity: { active: 1, max: 4 }` keeps Start enabled. Proves
   `activeAgents.length >= maxConcurrent` is no longer the gating rule.
4. **Runner works full, sessions empty** — `activeAgents: []` with
   `capacity: { active: 4, max: 4 }` disables Start. Proves the runner-
   works divergence (activeAgents empty but capacity full from runner
   grain) flows through.
5. **Zero-max placeholder stays enabled** — `capacity: { active: 0,
   max: 0 }` does not disable Start, matching `derive-runtime-decision.ts`
   `capacity.max > 0` guard.
6. **Other-issues indicator preserved** — `activeAgents` with a
   different `issueNumber` still renders the `N agent running on other
   issues` indicator at line 1283 (visibility, not gating).

### Existing test files — no edits needed

- `IssueDetailPage.test.tsx`, `IssueDetailPage.archived.test.tsx`,
  `IssueDetailPage.readiness.test.tsx` all mock
  `capacity: { max: 1 }` (no `active` field). With the new gating this
  yields `isCapacityFull = !!{ max: 1 } && 1 > 0 && undefined >= 1 =
  false` (the `&&` short-circuits before `capacity.active` is touched),
  so Start stays enabled in backlog-Issue contexts, which matches the
  existing test expectations. All 12+30+16 = 58 existing IssueDetailPage
  tests still pass with no fixture change required.
- `tests/IssueDetailPage.test.tsx` mock `agentStatus` has no `capacity`
  field at all. Old gating: `maxConcurrent = Infinity`,
  `isCapacityFull = false`. New gating: `capacity = undefined`,
  `isCapacityFull = false`. Same outcome — all 45 tests pass.

## T-003 Verification

- `npm run typecheck -w packages/web`: clean.
- `npm run test:run -w packages/web`: 3191 pass, 1 skipped (pre-existing),
  0 failures. Includes the 6 new
  `IssueDetailPage.capacity-gating.test.tsx` tests and the 58 pre-
  existing `IssueDetailPage*.test.tsx` tests across 4 files.

Acceptance criterion crosscheck:
1. `IssueDetailPage.tsx` computes `isCapacityFull` from
   `capacity.active >= capacity.max` (matching
   `derive-runtime-decision.ts:377`) — done; matches `derive-runtime-
   decision.ts` canonical gating exactly.
2. `activeAgents` still used for `isAgentRunningOnThis` (line 243) and
   the other-issues running indicator (line 1283) — verified by
   `IssueDetailPage.capacity-gating.test.tsx` test 6.
3. Web unit tests updated to assert the new derivation — done; new
   `IssueDetailPage.capacity-gating.test.tsx` has 6 tests, all passing.
4. `npm run typecheck -w packages/web` and `npm run test:run -w
   packages/web` pass — done.
