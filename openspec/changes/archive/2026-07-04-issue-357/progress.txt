# Issue 357 — stale system-update lock recovery

## Codebase Patterns

- The system-update subsystem slice (`packages/server/src/Mohist.Server/SystemInfo/`) keeps `ISystemUpdateStore` + `FileSystemSystemUpdateStore` co-declared in `FileSystemSystemUpdateStore.cs`. Convenience helpers (`ReleaseLockFile`, `TryCreateLockFile`, `ReadLatestUnlockedAsync`) are private to that file.
- `FileSystemSystemUpdateStore` uses a `SemaphoreSlim _gate` to serialize all disk-touching operations; lock state is held in-memory (`_locked` + `_lockOwnerJobId`) on top of the on-disk `<state>.lock` file, but `_lockOwnerJobId` is process-local and is the reason a plain `ReleaseLockAsync` no-ops after a restart.
- `ReleaseLockFile(jobId)` already does content-matched deletion (read owner from file, delete only if matches `jobId`); `IOIOException` is swallowed there because the on-disk state still serves correctness via the gate/lock file.
- The in-memory store fake (`InMemoryUpdateStore`) is a private nested class in `SystemUpdateServiceSpecs.cs` and gates both record-keeping and waiter state behind a single `lock (_gate)`. `WaitForUnlockAsync` listens on a `TaskCompletionSource` list completed by both `ReleaseLockAsync` and the (new) `ReleaseStaleLockAsync` when they flip `_locked = false`.
- The `OrderTrackingStore` fake only collects call events in a `List<string> Events`; mirrored method just records `"ReleaseStaleLock"` so any order-sensitive specs that observe lock-release can spot the new path being taken.
- Existing `SourceAudit_ReleaseLockAsyncOnlyInSharedHelpersAndRunUpdateFinally` only pins `await _store.ReleaseLockAsync(...)` to be inside `PersistTransitionAsync` / `RunUpdateAsync`. Adding a NEW store method (`ReleaseStaleLockAsync`) does not show up in that regex, so the audit stays green without edits. The reconciler (T-002) will use the new method on `ISystemUpdateStore`, NOT `_store.ReleaseLockAsync` inside `SystemUpdateService`.
- Tests on `FileSystemSystemUpdateStore` consistently use a per-test temp file `Path.GetTempPath() + "mohist-system-update-{Guid:N}.json"` plus a try/finally that deletes both `.json` and `.json.lock`.
- All test fakes for `ISystemUpdateStore` need to implement every interface member — adding the interface method forces an update to: production class, `InMemoryUpdateStore`, `OrderTrackingStore`. (No other fakes were found, but worth grepping again before commit.)

## T-001 — `ReleaseStaleLockAsync` (COMPLETED)

### Decisions confirmed
- **Add a new method, don't loosen `ReleaseLockAsync`.** `ReleaseStaleLockAsync` calls `ReleaseLockFile(jobId)` directly, bypassing the `_lockOwnerJobId == jobId` check. Content match inside `ReleaseLockFile` is the safety: a fresh process holding a different owner is preserved.
- Gate still held inside `ReleaseStaleLockAsync` to serialize against `TryAcquireLockAsync` re-acquiring concurrently; the actual deletion is content-matched, so two stale-release calls can't trample each other.
- Async shape: kept `Task`-returning to match interface signature; the body is sync (CPU + short filesystem read+delete). `_gate.Wait` (sync) inside is fine; the file I/O is bounded.
- Fake mirror: `InMemoryUpdateStore.ReleaseStaleLockAsync` mirrors the production semantics (release when held by `jobId`, otherwise idempotent), so the existing `WaitForUnlockAsync` test signal works for the new path. `OrderTrackingStore.ReleaseStaleLockAsync` records `"ReleaseStaleLock"` in `Events`.

### Test coverage
Five new specs in `SystemUpdateServiceSpecs.cs`:
- `FileSystemStore_ReleaseStaleLockAsync_DeletesStaleLockFileAndAllowsReacquisitionOnFreshInstance` — primary acceptance: stale lock file is gone after release, fresh instance `TryAcquireLockAsync("new-job")` succeeds.
- `FileSystemStore_ReleaseStaleLockAsync_IsIdempotentWhenLockFileAbsent` — idempotent no-op when no .lock file.
- `FileSystemStore_ReleaseStaleLockAsync_LeavesLockHeldByDifferentOwner` — content match guard: if the file says `"real-owner"`, calling release with `"someone-else"` leaves it; a subsequent acquire still fails.
- `FileSystemStore_ReleaseLockAsync_StillNoOpsAfterRestart` — regression guard for the existing `ReleaseLockAsync` semantics: on a fresh instance `_lockOwnerJobId` is null and `ReleaseLockAsync(staleJobId)` must NOT delete the on-disk lock file. This pins the additive-only contract.
- `InMemoryUpdateStore_ReleaseStaleLockAsync_ReleasesHeldLockWithoutProcessLocalMatch` + `InMemoryUpdateStore_ReleaseStaleLockAsync_IsIdempotentWhenLockNotHeld` — fake mirror behavior.

### Build/test outcome
- `dotnet build Mohist.sln -p:SkipWebBuild=true` clean (0 warnings, 0 errors) — `TreatWarningsAsErrors` acts as lint.
- `npm test` (server .NET): 3780 passed / 13 skipped (pre-existing) / 0 failed.
- `vitest` failures on web/runner are env-only (tsc/vitest not installed in this sandbox), unrelated to .NET changes.
- All `SystemUpdateServiceSpecs` (53 tests, including the 5 new ones) pass.
- `SourceAudit_ReleaseLockAsyncOnlyInSharedHelpersAndRunUpdateFinally` continues to pass — the audit regex only matches the existing `ReleaseLockAsync` site; the new `ReleaseStaleLockAsync` lives outside `SystemUpdateService`.

### Gotchas for T-002
- The reconciler (T-002) calls `ISystemUpdateStore.ReleaseStaleLockAsync(jobId)`, NOT `_store.ReleaseLockAsync` from `SystemUpdateService` — so it doesn't violate the SourceAudit rule even if it were audited in `SystemUpdateService` (it won't be, because the reconciler is a new file and the audit only scans `SystemUpdateService`).
- The fake's `_lockOwnerJobId` does not survive across store constructions, mirroring the post-restart real situation in semantics (the fake never persists to disk). So the `InMemoryUpdateStore_ReleaseStaleLockAsync_ReleasesHeldLockWithoutProcessLocalMatch` test name is slightly misleading — in the in-memory fake the in-process flag still matches because we never construct a second instance there. The TEMP-FILE tests on `FileSystemSystemUpdateStore` are the ones that truly exercise the "fresh process" case (new `CreateFileSystemStore` instance, file still on disk).
- No `DateTimeOffset.UtcNow` / `Environment.TickCount` reads inside reconciler; both time sides come through `TimeProvider` (already wired in #356) and the new `IProcessStartTimeProvider`.

## T-002 — startup recovery service (COMPLETED)

### Decisions confirmed
- Direct `IHostedService` (not `BackgroundService`). The hosted host awaits every `IHostedService.StartAsync` before serving requests — this is the seam that closes the early-request race. Reused the existing `EpicReconciliationService` / `AttachmentCleanupService` / `StagePopulationSnapshotService` registration shape (`AddHostedService<T>()`) without copying their `BackgroundService` base class.
- `IProcessStartTimeProvider` exposed a single-purpose seam (`DateTimeOffset GetStartTime()`), not a `TimeProvider` subclass and not a captured-at-DI-build `DateTimeOffset` parameter. Default `ProcessStartTimeProvider` reads `Process.GetCurrentProcess().StartTime.ToUniversalTime()` via an internal `Func<Process>` ctor (lets tests swap a fake but defaults to the real process). No .NET 11 alternative API exists (`Environment.ProcessStartTime`-style), so the design's fallback is the implementation.
- Reconciler shape mirrors `SystemUpdateService.CreateFailedTransition` but is inlined (D5): `latest with { Status = "failed", Stage = "Failed", Reason = InterruptedByProcessRestartReason, CompletedAt = now, UpdatedAt = now, Logs = … }`, then `SaveAsync` + `ReleaseStaleLockAsync`. Reason is exposed as `public const string InterruptedByProcessRestartReason = "interrupted by process restart"` so specs can assert without stringly-typed drift.
- Reconciler does NOT depend on `SystemUpdateService` (D5). The `SourceAudit_ReconcilerHasNoWallClockOrProcessInfoReads` test pins this — it strips XML doc comments (to ignore the explanatory `<see cref="SystemUpdateService"/>` reference in the summary) and asserts no `DateTimeOffset.UtcNow` / `DateTime.UtcNow` / `Environment.TickCount` / `GetCurrentProcess` / `Process.StartTime` / bare `SystemUpdateService` token appears in code.

### Registration order
- `MohistServiceRegistration.ConfigureMohistServices` registers `IProcessStartTimeProvider` via `TryAddSingleton<IProcessStartTimeProvider, ProcessStartTimeProvider>()` BEFORE `AddHostedService<SystemUpdateRecoveryService>()`. `ISystemUpdateStore` and `TimeProvider` are already registered earlier in the same method (lines 96 / 91 respectively), so the reconciler's DI graph resolves cleanly.
- `SystemUpdateRecoveryService` deliberately does NOT implement `ISingletonService` / `IScopedService` so the `AddMohistConventionalServices` scanner leaves it alone; only the explicit `AddHostedService<SystemUpdateRecoveryService>()` registers it.

### Test coverage
New `Specs/SystemSpecs/SystemUpdateRecoverySpecs.cs` (15.6 KB, under the 24 KB budget) drives all spec scenarios with fake time + injected process start time:
- `StartAsync_StaleRunningJob_IsMarkedFailedAndLockReleased` — primary acceptance for active `running` stale job.
- `StartAsync_StaleWaitingForReconnectJob_IsMarkedFailedAndLockReleased` — active `waiting-for-reconnect` stale job.
- `StartAsync_NoPersistedJob_IsNoOp` — empty-store case.
- `StartAsync_FreshActiveJob_IsLeftInPlace` / `StartAsync_FreshWaitingForReconnectJob_IsLeftInPlace` — fresh active jobs left alone (one with `UpdatedAt > process start`, one with `UpdatedAt == process start` to pin the strict-`<` rule).
- `StartAsync_TerminalJob_IsNeverModifiedRegardlessOfUpdatedAt(status)` — `Theory` covering all five terminal statuses (`succeeded`, `failed`, `recovered`, `superseded`, `cancelled`).
- `StartAsync_AfterRecovery_FreshStartAsyncAcquiresLockWithoutUpdateInProgress` — wires a real `SystemUpdateService` against the recovered in-memory store, asserts `Started = true` and `Code = null`.
- `StartAsync_AfterFileSystemStoreRecovery_FreshStartAsyncAcquiresLock` — temp-file pattern: confirms the on-disk `.lock` is actually removed and a follow-up `TryAcquireLockAsync` on the same instance succeeds (mirrors `SystemUpdateServiceSpecs.FileSystemStore_ReleaseStaleLockAsync_*`).
- `ProcessStartTimeProvider_DefaultReadsActualProcess` — sanity check that the production default returns a UTC offset-zero timestamp `<= UtcNow`.
- `SourceAudit_ReconcilerHasNoWallClockOrProcessInfoReads` — pins the design rules: no wall-clock reads, no process-info reads, no `SystemUpdateService` dependency in code.

### Build/test outcome
- `dotnet build Mohist.sln -p:SkipWebBuild=true`: 0 warnings, 0 errors. `TreatWarningsAsErrors` acts as lint.
- `dotnet test --filter "FullyQualifiedName~SystemUpdateRecoverySpecs"`: 14 passed / 0 failed.
- `dotnet test --filter "FullyQualifiedName~SystemUpdate"`: 68 passed / 0 failed (54 existing `SystemUpdateServiceSpecs` + 14 new `SystemUpdateRecoverySpecs`). Regression confirmed: `SourceAudit_ReleaseLockAsyncOnlyInSharedHelpersAndRunUpdateFinally` and every existing test still pass.
- `dotnet test --filter "FullyQualifiedName~ArchitectureRules"`: 24 passed / 3 skipped (pre-existing) / 0 failed. `SpecFiles_MustStayBellowSizeBudget`, `SpecNamespaces_MustBeUnderSpecs`, `SpecClasses_MustBePublic`, `SpecFiles_MustHaveSpecOrCollectionSuffix` all green.
- Full `npm test` server .NET suite: 3794 passed / 13 skipped / 0 failed.

### Gotchas for downstream
- xUnit2017 (analyzer) flags `Assert.True(list.Contains(x))` — must use `Assert.Contains(list, item => item == x)` for the released-stale-job list assertions.
- The doc-comment `<see cref="SystemUpdateService"/>` in the reconciler summary survives the source audit because the test strips all `<see cref="..."/>` references before grepping for `SystemUpdateService`. Without that strip, the audit would always fail for a service that mentions the class it intentionally avoids depending on.
- `ArchitectureRules.FeatureDirectories_ShouldOnlyContainDomainGrainsAndServices` whitelists `Agent` / `Epic` / `Issue` / `Project` / `Runner` / `Sessions` / `Workflow` as feature roots — `SystemInfo` is NOT in that list, so adding files to `packages/server/src/Mohist.Server/SystemInfo/` is fine (matches the placement pattern used by `SystemUpdateService`, `FileSystemSystemUpdateStore`, etc.).
