{
  "project": "mohist",
  "description": "Document the expected local post-update smoke validation path in rendered context.",
  "tasks": [
    {
      "id": "T-001",
      "title": "Add smoke validation note",
      "spec": "specs/change-artifacts/spec.md#requirement-rendered-context-documents-post-update-smoke-validation",
      "description": "Add a concise rendered-context note to the issue-3 change artifacts that records the expected local post-update smoke validation path. Keep the note documentation-only and explicitly include running `mo update`, checking `GET /api/health`, and opening `/issues`.",
      "acceptanceCriteria": [
        "A rendered-context note exists in the issue-3 change artifacts.",
        "The note is short and describes the local post-update smoke validation path.",
        "The note explicitly mentions `mo update`, `GET /api/health`, and `/issues`.",
        "No runtime code, API behavior, dependencies, storage, runner behavior, or UI behavior are changed."
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Updated issue-3 change artifact documentation containing the rendered-context smoke validation note.",
      "dependsOn": [],
      "passes": false,
      "notes": "Follow design.md decisions: keep the note short, checklist-oriented, and use the exact required tokens."
    },
    {
      "id": "T-002",
      "title": "Validate documentation-only scope",
      "spec": "specs/change-artifacts/spec.md#scenario-post-update-smoke-validation-note-is-present",
      "description": "Review the completed artifact change to verify it satisfies the spec scenario and remains within the documentation-only scope. Confirm the dependency graph is acyclic and that this validation consumes the note produced by T-001.",
      "acceptanceCriteria": [
        "The rendered-context note produced by T-001 is present during review.",
        "The note mentions running `mo update`, checking `GET /api/health`, and opening `/issues` exactly enough for straightforward review.",
        "The change set contains no server, web UI, runner, storage, dependency, or command implementation changes.",
        "All task dependencies reference existing lower-priority tasks and form a DAG."
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "REVIEW",
      "output": "Validation result confirming the note satisfies the spec and the change remains documentation-only.",
      "dependsOn": ["T-001"],
      "passes": true,
      "notes": "Validated: rendered-context.md exists and mentions all three required items (mo update, GET /api/health, /issues). Change set is documentation-only (only openspec/changes/issue-3/rendered-context.md modified). Dependency graph is acyclic (T-001 prio 1, T-002 prio 2)."
    }
  ]
}
