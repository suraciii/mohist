{
  "project": "mohist-local",
  "description": "拆分 runner SignalR 传输层：删死代码、抽纯 helper（git 解析器/workspace 路径/session-target/liveness）、测试先行补齐 git handler 缺口、再分别抽 git/推送 handler 簇为 register*Handlers 自由函数，runner-signalr.ts 收敛为组装+委托。所有 SignalR 线上契约字节级不变。",
  "tasks": [
    {
      "id": "T-001",
      "title": "删除 normalizeMaterializePayload 死代码",
      "spec": "openspec/changes/issue-313/proposal.md#What Changes",
      "description": "删除 runner-signalr.ts 中无任何 src 调用方的 normalizeMaterializePayload 及其 6 个 helper（parseSetVars/parseOutputs/parseJsonObject/readString/readNullableString/readNullableNumber，约 75 行）。纯删除，无行为变化，先清场为后续抽取做准备。",
      "acceptanceCriteria": [
        "runner-signalr.ts 中 normalizeMaterializePayload、parseSetVars、parseOutputs、parseJsonObject、readString、readNullableString、readNullableNumber 全部移除",
        "rg 在 packages/runner/src 下对 normalizeMaterializePayload 零命中",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿（现有 runner-signalr.spec.ts / runner-signalr-workflow-status.spec.ts / workspace-registry-integration.spec.ts 不变通过）"
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/runner-signalr.ts 删除死代码后的精简版本",
      "dependsOn": [],
      "passes": false,
      "notes": "零风险纯删除。design.md P0。"
    },
    {
      "id": "T-002",
      "title": "提取 git 输出解析器到 git-parsers.ts 并补单元测试",
      "spec": "specs/workspace-git-queries/spec.md#Numstat parsers tolerate binary entries and malformed lines",
      "description": "把 parseDiffFiles/splitDiffByFile/parseCommits/parseAheadBehind/parseNumstatTotal 五个纯函数提取到 packages/runner/src/server/git-parsers.ts 并 export，runner-signalr.ts 改为从新模块导入。同步新增 git-parsers.spec.ts 直接覆盖解析器（含 binary 行、malformed 行、ahead/behind 容错、commits 短行丢弃等全部 spec 场景）。",
      "acceptanceCriteria": [
        "git-parsers.ts 导出 parseDiffFiles/splitDiffByFile/parseCommits/parseAheadBehind/parseNumstatTotal 五个函数",
        "runner-signalr.ts 从 git-parsers.ts 导入这些函数，函数体不再内联于 runner-signalr.ts",
        "新增 git-parsers.spec.ts 覆盖：binary 行（-\\t-\\t）得 additions=0/deletions=0/isBinary=true、malformed/空行被跳过、parseNumstatTotal 聚合、parseAheadBehind 正常/畸形返回 [0,0]、parseCommits 丢短行/空输入返回空数组",
        "splitDiffByFile 按 b/ 路径键组装每文件 patch 的行为不变",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/git-parsers.ts + packages/runner/tests/git-parsers.spec.ts",
      "dependsOn": ["T-001"],
      "passes": false,
      "notes": "design.md P1 / D2。纯函数零行为变化，为 P5/P6 测试与迁移铺路。"
    },
    {
      "id": "T-003",
      "title": "提取 workspace 路径解析到 runtime/workspace-query.ts 并消解循环/跨层 import",
      "spec": "specs/workspace-git-queries/spec.md#Workspace query resolution requires explicit head and base refs",
      "description": "把 resolveWorkspaceQuery/isUnderRunnerRoot/WorkspaceQuery 类型提取到 packages/runner/src/runtime/workspace-query.ts（放 runtime 使 cleanup-loop 同层导入、消解 runtime→server 反向 import）。更新 runtime/cleanup-loop.ts 改从 workspace-query.ts 导入 isUnderRunnerRoot；删除 runtime/workspace-registry.ts 中 isPathUnder 的循环 import workaround 重复实现，改调权威实现。runner-signalr.ts 改从新模块导入。",
      "acceptanceCriteria": [
        "runtime/workspace-query.ts 导出 resolveWorkspaceQuery、isUnderRunnerRoot、WorkspaceQuery 类型",
        "resolveWorkspaceQuery 行为不变：workspacePath/baseBranch/branch 任一缺失返回 null，绝不回退 mo/issue-{N}",
        "runtime/cleanup-loop.ts 从 runtime/workspace-query.ts 导入 isUnderRunnerRoot（不再跨层 import server/）",
        "runtime/workspace-registry.ts 删除 isPathUnder 重复实现与循环 import workaround 注释，改调 isUnderRunnerRoot",
        "全量 typecheck + test 无模块加载期 circular dependency 报错",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿（workspace-registry-integration.spec.ts / cleanup-loop.spec.ts 断言不变通过）"
      ],
      "priority": 3,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/runtime/workspace-query.ts 及 cleanup-loop.ts / workspace-registry.ts / runner-signalr.ts 导入更新",
      "dependsOn": ["T-002"],
      "passes": false,
      "notes": "design.md P2 / D2。放 runtime 而非 server 的理由见 D2。"
    },
    {
      "id": "T-004",
      "title": "提取 session-target 解析到 session-target.ts",
      "spec": "specs/runner-signalr-push-handlers/spec.md#Session target resolution discriminates on target.kind with legacy fallback",
      "description": "把 resolveSessionTarget 及相关 ReceiveFollowup/CancelAgentSession 输入/目标类型提取到 packages/runner/src/server/session-target.ts。runner-signalr.ts 改从新模块导入。resolveSessionTarget 行为不变：target.kind 判别（generic 需 sessionId+非空 projectId、workflow 需 workflowRunId+sessionName+非空 projectId）、无 target 时回退顶层 workflowRunId/sessionName 得 projectId:\"\" 的 workflow 目标、其余返回 null。",
      "acceptanceCriteria": [
        "server/session-target.ts 导出 resolveSessionTarget 及 followup/cancel 相关 wire/target 类型",
        "spec 全部场景由现有 runner-signalr.spec.ts 覆盖且通过：generic 缺 sessionId→null、workflow 缺 sessionName→null、未知 kind→null、legacy 顶层字段→{kind:workflow,projectId:\"\"}、无 target 无 legacy→null",
        "runner-signalr.ts 从 session-target.ts 导入，函数体不再内联",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿"
      ],
      "priority": 4,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/session-target.ts 及 runner-signalr.ts 导入更新",
      "dependsOn": ["T-003"],
      "passes": false,
      "notes": "design.md P3 / D2。纯函数；为 followup/cancel handler 抽取铺路。"
    },
    {
      "id": "T-005",
      "title": "提取连接存活/重连 helper 到 liveness-probe.ts",
      "spec": "specs/runner-connection-liveness/spec.md#probeLiveness returns false unless a Ping resolves before timeout or abort",
      "description": "把 probeLiveness/forceReconnect/notifyReconnected 提取到 packages/runner/src/server/liveness-probe.ts。runner-signalr.ts 改为委托调用。hub URL 构造、withAutomaticReconnect([0,2000,5000,10000,30000]) 序列、probeLiveness 的 settle-once 幂等/abort-at-call-time/非 Connected 不调 Ping、forceReconnect 的 stop→start 顺序/stop 异常吞咽/Disconnected 直 start/stop 后 abort 短路、onReconnected 回调（connectionId 缺失回落 connection.connectionId）全部字节级不变。补 liveness-probe.spec.ts 直接单测。",
      "acceptanceCriteria": [
        "server/liveness-probe.ts 导出 probeLiveness、forceReconnect、notifyReconnected",
        "probeLiveness：非 Connected 返回 false 不调 Ping、Ping resolve 返回 true、reject/timeout/abort 返回 false、已 aborted 的 signal 立即返回 false、settle-once 幂等",
        "forceReconnect：Connected 走 stop→start 且 stop 抛错仍 start、Disconnected 直 start 不调 stop、stop 后 abort 短路 start、成功 start 后通知 onReconnected",
        "notifyReconnected：回调参数为非空字符串用它、否则回落 connection.connectionId、两者皆空不调回调",
        "withAutomaticReconnect([0,2000,5000,10000,30000]) 序列与 hub URL 构造（buildGitHash null 时省略该参数、去尾斜杠）不变",
        "新增 liveness-probe.spec.ts 直接覆盖以上分支",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿"
      ],
      "priority": 5,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/liveness-probe.ts + packages/runner/tests/liveness-probe.spec.ts",
      "dependsOn": ["T-004"],
      "passes": false,
      "notes": "design.md P4 / D2 / D5。已有间接覆盖，补直接单测。"
    },
    {
      "id": "T-006",
      "title": "测试先行：补齐 GetDiff/GetCommits/GetCommitDiff/GetFileContent handler 直接测试",
      "spec": "specs/workspace-git-queries/spec.md#GetDiff returns the per-file diff with binary markers and totals",
      "description": "在迁移任何 git handler 代码之前，先在 runner-signalr.spec.ts 内经现有 findHandler + setRunnerSignalRGitRunnerForTest 接缝，为 GetDiff/GetCommits/GetCommitDiff/GetFileContent 四个 handler 补齐全部 spec 场景的直接测试（当前零覆盖）。覆盖：head ref 缺失返回 null 不发 diff、merge-base 失败回退 baseBranch、per-file diff 按 b/ 路径键、GetCommits 解析 tab 分隔 log、GetCommitDiff 成功/非零返回 null、GetFileContent base/head 独立解析（base 缺/head 在、皆缺返回 {base:null,head:null}）、unresolvable workspace 各 handler 返回对应 not-found sentinel、git worktree 探测失败短路。",
      "acceptanceCriteria": [
        "runner-signalr.spec.ts 新增覆盖 GetDiff/GetCommits/GetCommitDiff/GetFileContent 全部 spec 场景的用例",
        "断言 GetDiff 返回形状 {base,head,mergeBase,ahead,behind,commitCount,totalAdditions,totalDeletions,files}（files 含 {file,additions,deletions,diff,isBinary}）",
        "断言 unresolvable workspace：GetDiff/GetCommits/GetCommitDiff 返回 null、GetFileContent 返回 {base:null,head:null}，且不发起 git 调用",
        "断言 head ref 缺失 GetDiff 返回 null 不发 diff/merge-base、merge-base 失败 mergeBase 回退 baseBranch、commitCount log 失败回退 0",
        "断言 GetFileContent base/head 独立解析（base 缺 head 在、皆缺）",
        "本任务只加测试，不改 runner-signalr.ts 任何 handler 实现",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿（含新增用例）"
      ],
      "priority": 6,
      "mode": "AFK",
      "type": "TEST",
      "output": "packages/runner/tests/runner-signalr.spec.ts 新增 git handler 直接测试用例",
      "dependsOn": ["T-005"],
      "passes": false,
      "notes": "design.md P5。acceptance criteria 硬约束『测试先行』，必须在 T-007 迁移代码前完成以钉死当前行为。"
    },
    {
      "id": "T-007",
      "title": "抽 git handler 簇到 workspace-git-handlers.ts 为 registerWorkspaceGitHandlers",
      "spec": "specs/workspace-git-queries/spec.md#Git worktree presence is probed before any git query",
      "description": "把 GetDiff/GetCommits/GetCommitDiff/GetWorkspaceStatus/GetFileContent 五个 handler 连同 isGitWorkTree 探测、git() helper、setRunnerSignalRGitRunnerForTest/setRunnerSignalRExistsCheckerForTest 可变绑定提取到 packages/runner/src/server/workspace-git-handlers.ts，导出 registerWorkspaceGitHandlers(conn, deps) 自由函数（deps 按簇最小化：resolveQuery/runCommand/pathExists 注入点）。RunnerSignalRClient 构造时组装 deps 并调用注册。runner-signalr.ts 再导出两个 setter 及 resolveWorkspaceQuery/isUnderRunnerRoot 使现有测试 from runner-signalr.js 导入零改动。T-006 的测试迁移后继续全绿。",
      "acceptanceCriteria": [
        "server/workspace-git-handlers.ts 导出 registerWorkspaceGitHandlers、WorkspaceGitHandlerDeps、isGitWorkTree、git() helper 及两个 test setter 权威定义",
        "五个 SignalR 方法名 GetDiff/GetCommits/GetCommitDiff/GetWorkspaceStatus/GetFileContent 字节级不变",
        "各 handler 返回形状、not-found sentinel（null / {exists:false} / {base:null,head:null}）、worktree 探测顺序、mergeBase/commitCount 回退、numstat/ahead-behind 容错全部不变",
        "git worktree 探测（path 存在 + rev-parse --is-inside-work-tree 输出 true）行为不变",
        "RunnerSignalRClient 构造时调用 registerWorkspaceGitHandlers 注册全部五个 handler",
        "runner-signalr.ts 再导出 setRunnerSignalRGitRunnerForTest/setRunnerSignalRExistsCheckerForTest/resolveWorkspaceQuery/isUnderRunnerRoot，现有测试 from runner-signalr.js 导入零改动",
        "T-006 新增的 git handler 直接测试迁移后继续全绿（契约钉死）",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿"
      ],
      "priority": 7,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/workspace-git-handlers.ts 及 runner-signalr.ts 委托改造",
      "dependsOn": ["T-002", "T-003", "T-006"],
      "passes": false,
      "notes": "design.md P6 / D3 / D4 / D6。hot path 中风险，依赖 T-006 测试钉死。各 handler 内部 new AbortController() 局部模式保留以最小化行为差异。"
    },
    {
      "id": "T-008",
      "title": "抽推送类 handler 簇为各自 register*Handlers 自由函数",
      "spec": "specs/runner-signalr-push-handlers/spec.md#ReceiveFollowup is fire-and-forget and never blocks the prompt",
      "description": "把 RemoveWorkspace/ReceiveFollowup/CancelAgentSession/ReceiveWorkflowRunStatus 四个 handler 分别提取到 workspace-removal-handler.ts/followup-handler.ts/cancel-handler.ts/workflow-run-status-handler.ts，各自导出 register*Handlers(conn, deps) 自由函数，deps 按簇最小化（followup/cancel 共用 followupTargetResolver + serverConnection；remove-workspace 用 isUnderRunnerRoot + registry；workflow-status 用 registry）。RunnerSignalRClient 构造时逐个调用注册。runner-signalr.ts 收敛为 URL/reconnect 构造 + register* 调用 + liveness 委托。所有推送契约字节级不变。",
      "acceptanceCriteria": [
        "四个新模块各自导出 register*Handlers 自由函数：registerWorkspaceRemovalHandler/registerFollowupHandler/registerCancelHandler/registerWorkflowRunStatusHandler",
        "SignalR 方法名 RemoveWorkspace/ReceiveFollowup/CancelAgentSession/ReceiveWorkflowRunStatus 字节级不变",
        "ReceiveFollowup fire-and-forget 不变：不 await connection.prompt、runtime event emit 失败不阻塞 prompt、resolver 抛错/返回 null 静默丢弃、不向 transport 抛；workflow 走 workflowAgentSessionRuntimeEvents、generic 走 agentSessionRuntimeEvents；session.input 事件 payload 标 kind=followup/role=user/source=followup",
        "CancelAgentSession 回复 {state:\"cancelled\"|\"not-cancellable\"}（镜像进 HTTP）不变；resolver 命中+connection.cancel resolve 才 cancelled，其余一律 not-cancellable 不抛；cancel send 失败/resolver throw 记日志",
        "ReceiveWorkflowRunStatus 仅 Completed/Stopped 将 active→eligible 盖 terminalAt（幂等不重盖/不重写文件），Failed/非终态保持 active/terminalAt:null，未知 runId/null payload/缺 workflowRunId/registry 失败不抛",
        "RemoveWorkspace runner-root 包含检查、越界拒绝 {removed:false,status:failed,reason:workspace_cleanup_refused}、目录缺失仍删 registry 条目、删除成功 {removed:true,status:removed,reason:null}、删除失败 workspace_cleanup_failed",
        "legacy 顶层 workflowRunId/sessionName 回退（projectId:\"\"）不变",
        "runner-signalr.ts 收敛为组装+委托（URL/reconnect 构造、register* 调用、liveness 委托），不再内联 handler 实现",
        "runner-signalr.spec.ts / runner-signalr-workflow-status.spec.ts / workspace-registry-integration.spec.ts 全部通过（断言不变）",
        "npm run typecheck -w packages/runner 全绿",
        "npm test -w packages/runner 全绿"
      ],
      "priority": 8,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/runner/src/server/{workspace-removal-handler,followup-handler,cancel-handler,workflow-run-status-handler}.ts 及 runner-signalr.ts 收敛",
      "dependsOn": ["T-003", "T-004", "T-007"],
      "passes": false,
      "notes": "design.md P7 / D2 / D3 / D5。已有覆盖，行为不变。与 T-007 都改 runner-signalr.ts，故串行依赖 T-007。"
    }
  ]
}
