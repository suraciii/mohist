# Review

The current change satisfies the issue acceptance criteria. All 19 supported built-in Actions have contract coverage with manifest-aligned inputs, outputs, and business error codes; the examples use declared inputs and identified bindings; the overview enumerates the supported Actions without the remaining-coverage note; and its grouped links resolve to the rendered contract sections. The tombstoned `mohist/acp-agent` remains undocumented, and the Runner manifest/runtime files are unchanged.

<promise>PASS</promise>
