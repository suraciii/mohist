## T-001 progress

- Added four stored computed columns to `IssueRow`: `Title`, `Priority`, `IsDraft`, `PrerequisiteNumbersJson`.
- Registered them in `MohistDbContext.OnModelCreating` with `HasComputedColumnSql(..., stored: true)` using `COALESCE(json_extract(State, '$.camel'), json_extract(State, '$.Pascal'))`.
- Generated EF Core migration `20260626200645_AddEpicListDerivedColumns` (timestamped after `20260625052135_AddIssueIsArchivedComputedColumn`); `MohistDbContextModelSnapshot.cs` regenerated automatically.
- Added integration tests in `Specs/Issue/Data/IssueDerivedColumnsSpecs.cs` covering derived-column tracking after State update and null-safety for missing/legacy keys.
- `dotnet build Mohist.sln` and `dotnet test Mohist.sln -p:SkipWebBuild=true` pass (2830 passed, 14 skipped).

## Codebase Patterns

- Computed columns are declared as nullable properties on the row class and configured in `MohistDbContext.OnModelCreating`.
- Use `COALESCE(camelCase, PascalCase)` to tolerate both casings in `State` JSON.
- For SQLite, use `stored: true` for columns that will be joined/selected heavily; virtual columns are acceptable for indexed status columns but not for join columns.
- Migrations are generated with `~/.dotnet/tools/dotnet-ef migrations add <Name>` from `packages/server` using the server project as both `--project` and `--startup-project`.
- In-memory SQLite integration tests use `Microsoft.Data.Sqlite.SqliteConnection("Data Source=:memory:")` + `DbContextOptionsBuilder< MohistDbContext>().UseSqlite(connection)` + `db.Database.Migrate()`.

## T-002 progress

- Rewrote `EpicQuerier.ListAsync` to issue one parameterized raw SQL query joining `Epics` ⋈ `EpicIssues` ⋈ `Issues`, selecting only the columns needed for progress.
- Added private `EpicIssueListItem` DTO for `SqlQueryRaw<T>` mapping. Initially named `EpicIssueListRow`, but the ArchUnit rule `RowModels_AreInInfrastructureData` rejects `*Row` classes outside `Infrastructure.Data`, so renamed to `EpicIssueListItem`.
- Implemented in-memory single-pass grouping by epic and `BuildLinkedIssuesFromRows` to construct `LinkedIssueDto` directly from joined columns.
- Reconstruct `CanStart`/`StartBlocker` from `IsDraft` + parsed `PrerequisiteNumbersJson` + prerequisite statuses resolved from the same result set. Undelivered prerequisites are those whose canonical status is not `done`/`completed`.
- Approximate `Health` as `active` for `in_progress` issues; all other statuses keep their canonical status as Health. This makes `blockedIssues` empty on the list path while preserving exact progress/nextIssue/CanStart.
- Filter archived linked issues on the list path so all-archived epics report zero counts.
- Canonicalize the raw `IssueStatus` column value (`inProgress` from JSON enum serialization) to `in_progress` via `MohistDefaultWorkflowProjection.IssueStatusName` so it matches what `EpicProgress.Build` expects.
- Detail path (`GetAsync`/`GetByNumberAsync`/`ToDetailAsync`/`GetLinkedIssuesAsync`) left untouched.
- Added integration tests in `Specs/Epic/Domain/EpicQuerierListAsyncSpecs.cs` covering: no `IssueQuerier` invocation, single SELECT regardless of epic count, progress counting, empty/archived epics, next-issue ordering/CanStart, draft/prerequisite blocking, Health approximation, and epic ordering.
- Verified with `dotnet build Mohist.sln` and `npm test` (2841 passed, 14 skipped).
