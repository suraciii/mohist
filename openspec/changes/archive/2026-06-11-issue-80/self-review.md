# Self Review Report

## Result: PASS

## Repaired Items

- [ID: item-1]
  Severity: info
  Scope: consistency
  Evidence: `specs/web-ui/spec.md` referenced the post-rename symbol's old name `filterClosedFromDone` in 3 places (Requirement text line 47 and two Scenario `**WHEN**` clauses at lines 50, 55), even though `design.md` D1 and `tasks.json` T-001 explicitly rename the function to `filterCancelledFromColumns`. After T-001 lands, the spec scenarios would be invoking a function that no longer exists, which makes the spec unenforceable.
  Verification: Updated all three references in `specs/web-ui/spec.md` from `filterClosedFromDone` to `filterCancelledFromColumns`. Re-grep confirms only the new name remains in the spec; the other OpenSpec artifacts (proposal, design, tasks) consistently use the new name for the post-rename identity. No other artifacts needed editing — the design.md and proposal.md references to the old name are all in the "current behavior" / "evidence" sections, which is correct.
  Status: resolved

## Blocking Items

(none)

## Follow-up Items

- [ID: item-2]
  Severity: follow-up
  Scope: consistency
  Evidence: `design.md` Risk "Test refactor is brittle if the column `data-testid` is changed" and D7 / T-005 / T-008 all anchor on `data-testid="stage-column-cancelled"`. The id is generated by `` `stage-column-${status}` `` in `StageColumn.tsx:75` and the value `Cancelled` comes from `IssueStatus.Cancelled`. If `IssueStatus.Cancelled` is ever renamed in the future, the spec scenarios and test anchors would silently fail. Worth a follow-up issue to either (a) define a constant for the test id stem, or (b) test against a semantic role/data attribute. Out of scope for this change.
  SuggestedAction: Future hardening: introduce a shared `KANBAN_TEST_IDS.stageColumn(status)` helper and reference it from both `StageColumn.tsx` and the test files. Tracked separately.
  Status: follow-up

- [ID: item-3]
  Severity: follow-up
  Scope: feasibility
  Evidence: The design notes the Cancelled column's "No issues" empty state (`StageColumn.tsx:120-124`) and the toggle's "hide" empty state would both render similar text. The spec Scenario "Rendering decides visibility" says the in-column render path "may show a clear 'no issues shown' affordance" — note the word "may", which leaves the wording unspecified. T-005's acceptance criteria do not pin down the exact empty-state copy, so two reasonable implementations differ.
  SuggestedAction: During T-005 implementation, decide on a specific empty-state string (the design's example "Cancelled issues hidden — show?" is reasonable) and add it to the spec's Scenario "Rendering decides visibility" as a concrete SHALL.
  Status: follow-up

## Verification Summary

- **Alignment:** All 6 proposal "What Changes" entries map to issue evidence (terminology drift, off-column toggle, mobile count, status pill, reverse toggle, no-new-feature). All 5 issue acceptance criteria are covered by spec requirements.
- **Completeness:** 5 spec Requirements ↔ 9 tasks. Every Requirement has at least one task. Every non-trivial task has acceptance criteria.
- **Consistency:** Spec ↔ Design ↔ Tasks are aligned after item-1 repair. The only cross-document terminology drift was the spec referring to the pre-rename symbol; repaired.
- **Feasibility:** T-001 (no deps) → T-002 (rename first) → T-003/T-004 (parallel candidates, sequential by dependsOn) → T-005/T-006 (parallel, both depend on T-004) → T-007 (independent IssueCard) → T-008 (waits on T-005/T-006/T-007) → T-009 (final gate). T-007 having no deps is intentional (IssueCard is decoupled from KanbanBoard/StageColumn).
- **Dependency completeness:** All non-first tasks have `dependsOn`. All `dependsOn` IDs (T-001..T-008) exist and have strictly lower priority. No cycles. No dangling references.

<promise>PASS</promise>
