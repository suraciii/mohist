# Issue 317 progress

Working memory for the partial-split refactor of `MohistCliCommands.Issue.cs`.
Each task appends its findings; the next task reads the section it cares about.

## Codebase Patterns

### `partial class` + private-access cross-partial rule (from C# language)

- `internal static partial class` merges all partials into one class at compile
  time, so `private static` members declared in the core partial are visible
  from every cluster partial with no visibility widening. This is what makes
  the carve viable — no `[InternalsVisibleTo]` or `internal` bump needed.

### C# tuple-destructuring shadowing gotcha

- Both `ValidateOutput` and `ResolveProjectId` helpers return
  `(T, int Exit)`. Many handlers call **both** helpers in the same scope, so
  destructuring both as `var (..., exit) = ...` causes CS0128
  (`exit` redeclared).
- Convention used here: the **first** call uses the helper name's own
  semantics (`var (mode, exit) = ValidateOutput(...)`) and the **second** call
  uses a helper-specific alias for the exit (`var (resolvedProjectId,
  resolveExit) = await ResolveProjectId(...)`). This keeps each call site
  readable while letting both helpers live in the same scope.
- Same rule of thumb applies to any future cross-cutting helper that returns
  `(T, int)`: name the exit after the helper (e.g. `validateExit`,
  `lookupExit`) to avoid collisions.

### Helper bodies are verbatim from `EpicCommands.ValidateOutput` / pure delegation

- `ValidateOutput` byte-for-byte matches `EpicCommands.ValidateOutput`
  (MohistCliCommands.Epic.cs:38). Do not "improve" it during carve steps —
  the match is the contract that keeps behavior preservation reviewable.
- `ResolveProjectId` is a pure-delegation wrapper. It adds no I/O and writes
  no extra diagnostics; `api.ResolveProjectIdAsync` already owns all error
  output and returns `null` on failure. The wrapper just turns `null` into
  exit code `1` symmetrically with `ValidateOutput`. If future carve steps
  touch the helper, this no-I/O invariant must hold.

### Two `internal` → `private` flips (D5)

- `ParseLabelsFromIssue` (line ~502) and `PrintCreateGuidance` (line ~680)
  flipped from `internal` to `private` once T-001 landed. They are called
  only from `BuildUpdate` / `LoadCurrentLabelsAsync` and `BuildCreate`
  respectively, both in the CRUD cluster. Repo-wide grep confirmed zero
  external callers — compile gate is the safe check. Subsequent carve steps
  move them to `Issue.Crud.cs` and they stay `private`.

### Idiom-collapse replacement shape

- 24× output-mode blocks and 31× project-id blocks all share a uniform 7-line
  / 3-line shape across the file. 22 output-mode sites were byte-identical
  and replaced via `edit.replaceAll`. 2 outliers used `invalidOutput` as the
  pattern variable name and were handled individually. The standard pattern
  never matched the helper body itself, so `replaceAll` was safe.
- Two sites had an inner `var mode = ((...)validation).Mode;` redeclaration
  inside a nested block (`BuildArchive` inside `if (allCompleted)`,
  `BuildSessionFollowup` at top level after `BodyInputResolver.ResolveAsync`).
  These inner redeclarations were dropped — outer `mode` from the helper is
  reused.

### `IssueTemplatesPath` placement

- Stays in the core partial even though only the Template cluster calls it —
  the cli-module-structure spec enumerates it as a required core shared
  helper alongside `NumberArg`/`ProjectIssuesPath`/`IsOptionProvided`.
  Carve steps preserve this placement.

## Behavioral invariants to preserve in later carve steps

- Command names / aliases (`list`/`ls`, `force-stop`, `unarchive`, `logs`/
  `events`/`diff`/`commits`, `coder-sessions`, `prerequisites`,
  `prereq add`/`prereq remove`, `feedback` create/list/show,
  `comment add`, etc.) — verbatim.
- Flag / option names and shapes (`--project`/`--project-id`,
  `--all`/`--archived`, `--all-completed`, `--stage-models` / `--stage-model-variants`
  with `@file` expansion, `--body`/`--body-file`/`--body-stdin`, etc.) —
  verbatim.
- HTTP path shapes under `/api/projects/{id}/issues/...`,
  `/api/issue-templates?projectId={id}` — verbatim.
- `mo issue update` PATCH field-omission semantics — verbatim.
- Output formats (`table`, `json`) and exit codes (`0`, `1`) — verbatim.

## Test status at T-001 close

- `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj` — 0 Warning(s),
  0 Error(s) (Directory.Build.props sets `TreatWarningsAsErrors=true`).
- `dotnet test packages/cli/tests/Mohist.Cli.Tests/... --filter
  'FullyQualifiedName~CliIssue'` — 231 passed, 0 failed.
- Full CLI test project — 559 passed, 0 failed (covers non-Issue specs too).
- Server tests via `dotnet test Mohist.sln` — 3525 passed, 13 skipped
  (pre-existing skips, not introduced by this task).
- `vitest` for web/runner — `vitest` not installed in this environment;
  unrelated to this task. Recorded as an environment note, not a regression.

## File metrics after T-001

- `MohistCliCommands.Issue.cs` shrank from 2268 → 2137 lines
  (Δ −131L). The 24 + 31 inline idiom lines collapsed into single-line
  helper calls plus the check.
- scc Complexity still to be re-measured after all 8 carve tasks complete;
  this task is required to land first because partial visibility / shared
  helper accessibility only works after `static partial class` + `ValidateOutput`
  + `ResolveProjectId` exist.

## Carve notes for T-002 (and later)

### CRUD cluster top/bottom is non-contiguous

- The CRUD cluster spans **three non-adjacent** regions in the core file:
  1. `BuildList` + `BuildCreate` + `ApplyFrontmatter` (top, before the
     shared helpers);
  2. `BuildShow` + `BuildUpdate` + `LoadCurrentLabelsAsync` +
     `ParseLabelsFromIssue` (middle, sandwiching the shared helpers);
  3. `PrintCreateGuidance` (bottom, after `BuildStop`).
- The middle two regions sit **between** the three shared helpers
  (`IsOptionProvided`, `ValidateOutput`, `ResolveProjectId`) and the
  Lifecycle cluster, so a verbatim move cannot be one contiguous drop.
- Lesson for later carve steps that also intermix with shared helpers:
  do separate edits for each region, leaving the shared helpers exactly
  where they are. `Edit` (find/replace) on the whole CRUD dump at once
  is impractical for these clusters — split into top-group /
  middle-group / bottom-group edits.

### `PrintCreateGuidance` placement

- Lives in `Issue.Crud.cs` (not `Issue.Lifecycle.cs` where it physically
  sits in the core file at line 713-747). It is called **only** from
  `BuildCreate` (line 255) and is a post-create user guidance routine
  specific to the create result payload — design D1 cluster-ownership
  rule wins over physical locality.

### Partial file using-directives

- Each partial needs its own `using` block. The `using System.CommandLine.Parsing;`
  import required by `IsOptionProvided` (signature `ParseResult`, `Option`)
  stays only in the core partial because `IsOptionProvided` is the only
  cluster-crossing signature using `ParseResult` / `Option` outside their
  `Command` constructor paths. CRUD partial only needs `System.CommandLine`,
  `System.Net.Http` (for `HttpRequestException` in `LoadCurrentLabelsAsync`),
  and `System.Text.Json.Nodes` (for `JsonNode?` in `PrintCreateGuidance` /
  `ParseLabelsFromIssue`).

## File metrics after T-002

- `MohistCliCommands.Issue.cs` shrank from 2137 → 1618 lines (Δ −519L).
  The 527-line `MohistCliCommands.Issue.Crud.cs` partial offsets by 527
  gross, 519 net (after shared helper region is counted once across
  both files: helpers stay in core; CRUD body moves verbatim with minor
  `using` import trim).
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: full CLI test project — 559 passed, 0 failed (matches
  T-001 close). CRUD-filter subset — 50 passed, 0 failed.

## File metrics after T-003

- `MohistCliCommands.Issue.cs` shrank from 1618 → 1351 lines (Δ −267L).
  The 272-line `MohistCliCommands.Issue.Lifecycle.cs` partial offsets by
  272 gross, 267 net (lifecycle body moves verbatim with `using
  System.CommandLine;` trimmed down to just that one import — no
  `System.Net.Http` / `System.Text.Json.Nodes` / `System.CommandLine.Parsing`
  needed because no lifecycle method touches HttpRequestException,
  JsonNode, or ParseResult signatures).
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: `dotnet test packages/cli --filter
  'FullyQualifiedName~CliIssueRerunFromStageSpecs|FullyQualifiedName~CliIssueRejectAndStopSpecs'`
  → 11 passed, 0 failed (rerun-from-stage, reject, force-stop behavior
  green).
- Spec gate: full CLI issue suite — 231 passed, 0 failed (matches
  T-001/T-002 baseline, no regression in neighboring clusters).
- Spec gate: full CLI test project — 559 passed, 0 failed.

## Carve notes for T-004 (Session)

### Lifecycle cluster was a clean single contiguous region

- Unlike the CRUD cluster which had to be split into three non-adjacent
  regions (T-002 notes), the lifecycle cluster sits as one contiguous
  block between `ResolveProjectId` (line 74) and `BuildSessions` (line
  350) — exactly the ideal pattern: helpers above, one cluster below,
  one edit.
- All 7 lifecycle methods (lines 83-348, ~266 lines) drop out via a
  single `edit` find/replace anchored on the
  `BuildAction` → `BuildSessions` boundary. The seam line count drops by
  the full 266, no helper-related bookkeeping.

### Verb factories stay as one logical unit each

- `BuildAction` (8 verbs) and `BuildGetSub` (4 verbs) are kept as single
  contiguous methods inside the partial — design D1 rejected one-file-
  per-verb because the factory + its verb-registration loop at
  `Build()` is the natural unit. Verbatim move preserves this shape
  byte-for-byte.

### Lifecycle partial `using` block is minimal

- Only `using System.CommandLine;` is needed — no `System.Net.Http`
  (no `HttpRequestException` catches), no `System.Text.Json.Nodes`
  (no `JsonNode?` shapes), no `System.CommandLine.Parsing` (no
  `ParseResult` / `Option` signatures beyond their `Command` constructor
  paths). `Uri.EscapeDataString` (used by `BuildArchive` line 317) lives
  in `System` and is auto-imported.
- The same minimal-import rule applied to `Issue.Crud.cs` (per T-002
  notes), confirming partial files only need the imports their own
  members touch — shared helpers (`NumberArg`, `ProjectIssuesPath`,
  `ValidateOutput`, `ResolveProjectId`, `IsOptionProvided`,
  `IssueTemplatesPath`) stay in the core partial and are not re-imported.

## Carve notes for T-004 (Session)

### Session cluster is one contiguous region (like Lifecycle, unlike CRUD)

- The Session cluster sat as a single contiguous block from line 83
  (`BuildSessions`) to line 356 (`BuildSessionFollowup` close brace) in
  the post-T-003 core file. The seam anchor at the boundary
  `BuildSessionFollowup` → `BuildWorkflow` was a clean single `edit`
  find/replace with no shared-helper bookkeeping (T-002's three-region
  CRUD problem does not recur here). The session cluster does not
  interleave with the shared helpers in any way — it sits entirely
  between the helpers and the Workflow cluster.
- 8 methods (lines 83-356, ~274 lines) dropped out via one `edit`. The
  result: 281-line `MohistCliCommands.Issue.Session.cs` partial, 1076-line
  core. Net Δ −275L.

### `BuildSessionFollowup`'s cross-helper ordering is preserved verbatim

- `BuildSessionFollowup` is the only Session method that calls
  `ValidateOutput` BEFORE `ResolveProjectId` (the others call project-id
  first, then output-mode). This ordering is intentional: it short-
  circuits the `BodyInputResolver` / file I/O step on bad `--output`
  values. The verbatim move preserves this byte-for-byte; do not
  reorder during a future carve.

### Session partial `using` block is minimal

- Only `using System.CommandLine;` is needed — no `System.Net.Http`
  (no `HttpRequestException` catches), no `System.Text.Json.Nodes`
  (no `JsonNode?` shapes), no `System.CommandLine.Parsing` (no
  `ParseResult` / `Option` signatures beyond their `Command`
  constructor paths). `BodyInputResolver` lives in the same namespace
  (`Mohist.Cli`) so no import is needed. The minimal-import rule
  applied to Lifecycle (T-003) and Crud (T-002) holds for Session too.

### `SessionNameArg` lives with its callers

- `SessionNameArg` is called only from the four sub-subcommand builders
  (`BuildSessionShow` / `BuildSessionTranscript` / `BuildSessionCompact` /
  `BuildSessionReset` / `BuildSessionFollowup`), all of which now live
  in the Session partial. `SessionNameArg` therefore moves with them
  per D1 cluster-ownership rule — not to the core partial even though
  it looks like a small shared helper, because nothing outside the
  Session cluster ever calls it.

## File metrics after T-004

- `MohistCliCommands.Issue.cs` shrank from 1351 → 1076 lines
  (Δ −275L). The 281-line `MohistCliCommands.Issue.Session.cs` partial
  offsets by 281 gross, 275 net (no shared-helper region — Session
  methods sit in one block, no scattered sub-region).
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: `dotnet test packages/cli/tests/Mohist.Cli.Tests/... --filter
  'FullyQualifiedName~CliIssueSessionSpecs'` → 44 passed, 0 failed
  (session list/show/transcript/compact/reset/followup output and
  exit codes bit-for-bit green).
- Spec gate: full CLI issue suite — 231 passed, 0 failed
  (matches T-001/T-002/T-003 baseline).
- Spec gate: full CLI test project — 559 passed, 0 failed
  (matches T-001/T-002/T-003 baseline, no regression in
  neighboring clusters).

## Carve notes for T-005 (Workflow)

### Workflow cluster sat as one contiguous region (after T-004)

- The Workflow cluster after T-004 sat as a single contiguous block from
  line 83 (`BuildWorkflow`) to line 646 (close brace of
  `BuildWorkflowConfigClear`) in the 1076-line core file. No interleaving
  with shared helpers — the seam between `ResolveProjectId` (line 81
  close) and `BuildWorkflow` (line 83 start) is exactly the ideal pattern:
  helpers above, one cluster below, one Python-script line-range drop.
- 7 of the 8 methods (~564 lines) dropped out cleanly via `python3` line
  slicing on `lines[82:] + lines[646:]` (preserving the blank line that
  separated the helpers from the Feedback cluster starting at the
  former line 648). The blank blank line was then collapsed to a single
  blank via a 3-line `edit`.

### `BuildWorkflowConfigSet` carve into its own partial

- Per design D2, the 228-line `BuildWorkflowConfigSet` was placed in its
  own `MohistCliCommands.Issue.WorkflowConfigSet.cs` file because keeping
  it inside `Issue.Workflow.cs` would have re-created a ~600L outlier
  violating the complexity-ranking acceptance criterion. Final line
  counts land almost exactly on the design's targets: Workflow.cs 350L
  (~370L estimate), WorkflowConfigSet.cs 228L (~230L estimate).

### Workflow partial `using` block minimal

- `using System.CommandLine;` — `Command` / `Option` / `Argument` shape.
- `using System.Text.Json.Nodes;` — `JsonObject` shape used by
  `BuildWorkflowConfigClear`'s prompt-deletion body.
- No `System.Net.Http` (no `HttpRequestException` catches), no
  `System.CommandLine.Parsing` (no direct `ParseResult` / `Option`
  signatures outside their `Command` constructor paths).

### WorkflowConfigSet partial `using` block is single-import

- Only `using System.CommandLine;` — `BuildWorkflowConfigSet` has no
  `JsonObject` shapes (its PATCH body is `Dictionary<string, object?>`)
  and does not touch `HttpRequestException` or `ParseResult` / `Option`
  signatures beyond their `Command` constructor paths.

## File metrics after T-005

- `MohistCliCommands.Issue.cs` shrank from 1076 → 511 lines
  (Δ −565L, matching the carved cluster size exactly).
- `MohistCliCommands.Issue.Workflow.cs` is 350L gross.
- `MohistCliCommands.Issue.WorkflowConfigSet.cs` is 228L gross.
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: `dotnet test packages/cli/tests/Mohist.Cli.Tests/...
  --filter 'FullyQualifiedName~CliIssueWorkflowConfigSpecs|
  FullyQualifiedName~CliIssueWorkflowProfileSpecs|
  FullyQualifiedName~CliIssueExecutionConfigFlagsSpecs'`
  → 86 passed, 0 failed (workflow config get/preview/clear/set,
  `--stage-models` / `--stage-model-variants` `@file` expansion,
  PATCH semantics, execution config flags all bit-for-bit green).
- Spec gate: full CLI issue suite — 231 passed, 0 failed (matches
  T-001/T-002/T-003/T-004 baseline).
- Spec gate: full CLI test project — 559 passed, 0 failed (matches
  T-004 baseline, no regression in neighboring clusters).

## Carve notes for T-006 (Feedback)

### Feedback cluster sat as one contiguous region (after T-005)

- The Feedback cluster after T-005 sat as a single contiguous block from
  line 83 (`BuildFeedback`) to line 279 (close brace of `ExtractLatestId`)
  in the 511-line core file. The seam anchor at the boundary
  `ExtractLatestId` → `BuildPrereq` was a clean single `edit`
  find/replace with no shared-helper bookkeeping (T-002's three-region
  CRUD problem does not recur here — Feedback sat entirely between
  `ResolveProjectId` and `BuildPrereq`).
- 5 methods (~200 lines) dropped out via one `edit` covering the whole
  cluster verbatim. The result: 204-line
  `MohistCliCommands.Issue.Feedback.cs` partial, 311-line core.
  Net Δ −200L.

### `ExtractLatestId` moved per D1 call-graph ownership

- `ExtractLatestId` was physically sitting near `BuildArchive` /
  `BuildStop` in the Lifecycle region of the pre-T-003 file, but its
  sole caller is `BuildFeedbackShow` (line 245 in the pre-T-006 core
  file). Design D1 cluster-ownership rule says it goes with its caller
  cluster, so it lands in `Issue.Feedback.cs` not `Issue.Lifecycle.cs`.
- This is the first D1-physical-vs-call-graph-ownership carve in the
  series; previous tasks (T-002/T-003/T-004/T-005) had methods that
  physically sat within their caller's region. Pattern confirmed: any
  helper called by only one cluster goes with that cluster even if it
  physically sits elsewhere.

### Core-file using-directive trim

- After the Feedback carve the core file's body uses neither
  `System.Net.Http` (no `HttpRequestException` catches) nor
  `System.Text.Json.Nodes` (no `JsonNode?` shapes). Both imports were
  dropped from the core `using` block, mirroring the trim rules
  established in T-002/T-003/T-004/T-005 carve notes.
- `System.CommandLine.Parsing` import (used by `IsOptionProvided`'s
  `ParseResult` / `Option` signatures) stays because `IsOptionProvided`
  is a shared helper that lives in the core partial.

### Feedback partial `using` block is single-import

- Only `using System.CommandLine;` is needed — no `System.Net.Http`
  (no `HttpRequestException` catches in the cluster), no
  `System.Text.Json.Nodes` (the cluster's only `JsonNode?` shape,
  `ExtractLatestId`, uses fully-qualified
  `System.Text.Json.Nodes.JsonNode?` / `JsonArray` / `JsonObject` so no
  import is needed), no `System.CommandLine.Parsing` (no direct
  `ParseResult` / `Option` signatures beyond their `Command`
  constructor paths). `BodyInputResolver` and `MohistCliApi` live in
  the same namespace (`Mohist.Cli`) so no extra import is needed.

## File metrics after T-006

- `MohistCliCommands.Issue.cs` shrank from 511 → 311 lines
  (Δ −200L). The 204-line `MohistCliCommands.Issue.Feedback.cs`
  partial offsets by 204 gross, 200 net (no shared-helper region —
  Feedback methods sit in one block).
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: `dotnet test packages/cli/tests/Mohist.Cli.Tests/...
  --filter 'FullyQualifiedName~CliIssueCommentAndFeedbackSpecs'`
  → 13 passed, 0 failed (feedback create/list/show + comment add output
  and exit codes bit-for-bit green).
- Spec gate: full CLI issue suite — 231 passed, 0 failed
  (matches T-001/T-002/T-003/T-004/T-005 baseline).
- Spec gate: full CLI test project — 559 passed, 0 failed
  (matches T-005 baseline, no regression in neighboring clusters).

## Carve notes for T-007 (Prereq + Comment + Template)

### Three small clusters grouped because each is too trivial for a separate task

- Per the design (`D1` cluster-ownership, plus the task's grouping note),
  the three remaining small clusters were carved together because each is
  only 60–100 lines — splitting into 3 tasks would have been over-splitting
  for verbatim moves that follow the same shape.
- All three sat as one contiguous block between `ResolveProjectId` (line 72)
  and `IssueTemplatesPath` (line 304) in the post-T-006 core file. The seam
  anchor at the boundary `ResolveProjectId` → `BuildPrereq` and at
  `BuildTemplateGet` → `IssueTemplatesPath` was a clean two-edit find/
  replace with no shared-helper bookkeeping (the only D1-physical-vs-call-
  graph wrinkle, `ExtractLatestId` already shipped in T-006, does not
  recur here — all three clusters sit exactly where their call-graph owner
  physically sits).

### `IssueTemplatesPath` placement moved into the core helpers region

- After T-006, `IssueTemplatesPath` was sitting at the **bottom** of the
  core file (lines 304–310) — only because that was where it physically
  landed in the pre-refactor monolithic file. Per the cli-module-structure
  spec it is a required core shared helper alongside `NumberArg`,
  `ProjectIssuesPath`, `IsOptionProvided`, `ValidateOutput`,
  `ResolveProjectId`. The carve moved it up to sit alongside the other
  five helpers (now at line 81 of the 87-line core), so the core partial
  reads as: `Build()` → 6 shared helpers → close. This is the
  "core partial only Build + helpers" scenario from the spec.
- Only the Template cluster uses `IssueTemplatesPath` today (the only
  call sites are `BuildTemplateList` and `BuildTemplateGet`), but keeping
  it in the core enforces the spec invariant and prevents the next
  cluster-on-the-edge from accidentally re-introducing the physical-order
  discrepancy. `BuildTemplateList` and `BuildTemplateGet` reach it via
  partial-class `private` access — same rule as the other helpers.

### Prereq / Comment / Template partial `using` blocks are minimal

- All three need only `using System.CommandLine;` — no `System.Net.Http`
  (no `HttpRequestException` catches), no `System.Text.Json.Nodes` (no
  `JsonNode?` shapes), no `System.CommandLine.Parsing` (no direct
  `ParseResult` / `Option` signatures beyond their `Command` constructor
  paths).
- `MohistCliCommands.ProjectRefOption` / `OutputOption` / `Escape` /
  `NoActiveProjectMessage`, `BodyInputResolver`, `MohistCliApi` all live
  in the same namespace (`Mohist.Cli`) so no extra imports are needed.
  Same minimal-import rule that applied to Crud (T-002), Lifecycle
  (T-003), Session (T-004), Workflow (T-005), Feedback (T-006) holds for
  Prereq/Comment/Template as well.

### Cross-cluster shared helper access is unchanged

- All three clusters call `ResolveProjectId`, `ValidateOutput`,
  `NumberArg`, `ProjectIssuesPath` (Template also calls `IssueTemplatesPath`),
  exactly the same as the pre-T-007 in-file calls. The carve moves the
  methods, not the call sites of those helpers. Verbatim move confirmed
  by the 40-test filtered spec run below.

### Core partial final state (T-007 close)

- Core contains only `Build()` (public entry) + 6 private static helpers:
  `NumberArg`, `ProjectIssuesPath`, `IsOptionProvided`, `ValidateOutput`,
  `ResolveProjectId`, `IssueTemplatesPath`. No concrete subcommand build
  methods. Spec scenario "核心 partial 只保留 Build 与共享 helper" satisfied.
- Three `Subcommands.Add(...)` calls inside `Build()` (lines 38-40)
  reference `BuildPrereq`/`BuildComment`/`BuildTemplate` — those are
  forwarded to the partials; no method definitions remain in the core.

## File metrics after T-007

- `MohistCliCommands.Issue.cs` shrank from 311 → 87 lines (Δ −224L).
- `MohistCliCommands.Issue.Prereq.cs` is 96L gross (matches ~100L target).
- `MohistCliCommands.Issue.Comment.cs` is 60L gross (matches ~60L target).
- `MohistCliCommands.Issue.Template.cs` is 82L gross (close to ~90L target).
- Spec gate: `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj`
  → 0 Warning(s), 0 Error(s).
- Spec gate: `dotnet test packages/cli/tests/Mohist.Cli.Tests/...
  --filter 'FullyQualifiedName~CliIssuePrereqSpecs|
  FullyQualifiedName~CliIssueCommentAndFeedbackSpecs|
  FullyQualifiedName~CliIssueTemplateCommandSpecs'`
  → 40 passed, 0 failed (prereq add/remove, comment add, template list/get
  bit-for-bit green).
- Spec gate: full CLI issue suite — 231 passed, 0 failed (matches
  T-001/T-002/T-003/T-004/T-005/T-006 baseline).
- Spec gate: full CLI test project — 559 passed, 0 failed (matches
  T-006 baseline, no regression in neighboring clusters).

## T-008 audit (CRUD re-carve)

### Why the CRUD cluster had to be re-carved (not just verified)

- T-007 left `MohistCliCommands.Issue.Crud.cs` at 527L / scc Complexity 78.
  That put it at position 4 of the cli package's complexity ranking — inside
  the top 5 — which violates the spec scenario "各分文件脱离 cli 包复杂度前列"
  (`specs/cli-module-structure/spec.md#各分文件脱离 cli 包复杂度前列`)
  and T-008's "no `Issue.*` file in the top five" acceptance criterion.
- The other 10 Issue.* partials were already outside the top 5 at T-007
  close: Lifecycle 27, Session 25, Workflow 52, WorkflowConfigSet 40,
  Feedback 30, Prereq 8, Comment 5, Template 9, Issue 6.
- A T-008 REVIEW-only pass cannot "verify" a failing invariant; the review
  gate has to ship a corrective carve so the acceptance check holds.

### Re-carve shape — Read/Write split (Workflow precedent)

- Following the same precedent T-005 used to keep Workflow cluster complexity
  out of the cli top tier (Workflow.cs + WorkflowConfigSet.cs split), the
  CRUD cluster is split into:
  - `MohistCliCommands.Issue.CrudReads.cs` (~100L / Complexity 10) —
    `BuildList` + `BuildShow`. Both follow the same shape (resolve
    project-id → validate output → GET endpoint), so they read naturally as
    one cluster.
  - `MohistCliCommands.Issue.CrudWrites.cs` (~430L / Complexity 68) —
    `BuildCreate` + `BuildUpdate` + the four helpers
    (`ApplyFrontmatter`, `LoadCurrentLabelsAsync`, `ParseLabelsFromIssue`,
    `PrintCreateGuidance`). Helpers all live alongside their sole caller:
    `ApplyFrontmatter` + `PrintCreateGuidance` (BuildCreate),
    `LoadCurrentLabelsAsync` + `ParseLabelsFromIssue` (BuildUpdate).
- `MohistCliCommands.Issue.Crud.cs` is deleted; the cluster now occupies
  CrudReads + CrudWrites only. File rename is allowed because the spec
  invariant is "each cluster lives in independent partial file(s)" (not
  a specific filename) — see self-review.md item-2 follow-up.

### Read/Write split has real semantic justification (not pure size-split)

- Reads: project-id resolve → output-mode validate → GET endpoint → done.
  No payload assembly, no option-mutual-exclusion, no body-source
  resolution.
- Writes: project-id resolve → draft-flag mutual exclusion → label parse →
  body-source mutual exclusion + BodyInputResolver → frontmatter merge →
  `@file` expansion for `--stage-models`/`--stage-model-variants` →
  patch-style field-omission payload → POST/PATCH endpoint → optional
  post-create guidance.
- The 24× output-mode idiom and 31× project-id idiom collapses already
  removed the bulk of the shared shape; what's left in the writes are
  genuinely write-only concerns (field-omission payload assembly).

### Idiom-cleanliness re-check after the read/write split

- `rg -n 'ValidateOutputMode\|OutputModeResult' packages/cli/Mohist.Cli/MohistCliCommands.Issue.*.cs`
  → 0 matches in cluster partials (only the core partial holds the
  `ValidateOutput`/`ResolveProjectId` helpers). The grep positive hits in
  Lifecycle (`--message is required`) and CrudWrites
  (`ServerUnavailableMessage`) are unrelated diagnostic strings, not the
  output-mode unpack pattern.
- `rg -n 'ResolveProjectIdAsync' packages/cli/Mohist.Cli/MohistCliCommands.Issue.*.cs`
  → 0 matches outside the core partial. All 31 site-level idiom calls go
  through the `ResolveProjectId` helper.

## scc complexity ranking at T-008 close

`scc packages/cli/Mohist.Cli/ --by-file --sort complexity` top 10:

| Rank | File | Lines | Code | Complexity |
|------|------|------:|-----:|-----------:|
| 1 | MohistCliApi.cs | 1,117 | 976 | **112** |
| 2 | MohistCliCommands.ProjectWorkflow.cs | 792 | 728 | **98** |
| 3 | ScheduledTaskInstaller.cs | 858 | 768 | **88** |
| 4 | MohistCliCommands.Agent.cs | 899 | 798 | **75** |
| 5 | DeliveryFailureGuidance.cs | 440 | 394 | **75** |
| 6 | **MohistCliCommands.Issue.CrudWrites.cs** | 433 | 380 | **68** |
| 7 | Update/UpdateOperations.cs | 499 | 431 | **67** |
| 8 | SystemdServiceInstaller.cs | 395 | 337 | **60** |
| 9 | TableRenderer.Issues.cs | 420 | 377 | **57** |
| 10 | TableRenderer.IssueTemplates.cs | 282 | 252 | **55** |

**No `Issue.*` file in the top 5.** First Issue.* entry is CrudWrites at
position 6 / Complexity 68. Spec scenario satisfied.

All Issue.* partials ranked by complexity:
- MohistCliCommands.Issue.CrudWrites.cs — 68 (position 6)
- MohistCliCommands.Issue.Workflow.cs — 52 (position 11)
- MohistCliCommands.Issue.WorkflowConfigSet.cs — 40 (position 15)
- MohistCliCommands.Issue.Feedback.cs — 30
- MohistCliCommands.Issue.Lifecycle.cs — 27
- MohistCliCommands.Issue.Session.cs — 25
- MohistCliCommands.Issue.CrudReads.cs — 10
- MohistCliCommands.Issue.Template.cs — 9
- MohistCliCommands.Issue.Prereq.cs — 8
- MohistCliCommands.Issue.cs — 6
- MohistCliCommands.Issue.Comment.cs — 5

## File metrics at T-008 close

- Core `MohistCliCommands.Issue.cs` — 88 lines, Complexity 6
  (was 2268L / Complexity-223 pre-refactor → 87L / Complexity-6 today).
- All 11 Issue.* files sum to 2,198 lines, total per-file Complexity
  sum is 280 (was 2268L / Complexity 223 in one file). Each file sits
  well below the pre-refactor monolith line count and below the
  Complexity-78 threshold that put Crud into the top 5.
- Net file split: −1 file (Crud.cs deleted), +2 files (CrudReads.cs +
  CrudWrites.cs). File count went 10 → 11; the delete/rename is the only
  shape change since T-007.

## Acceptance check matrix at T-008 close

| Acceptance criterion | Result |
|----------------------|--------|
| Core partial holds only `Build()` + 6 shared helpers | PASS — verified via `rg` member audit (line 8 = Build, lines 45-87 = NumberArg / ProjectIssuesPath / IsOptionProvided / ValidateOutput / ResolveProjectId / IssueTemplatesPath). |
| No `Issue.*` file in cli package complexity top 5 | PASS — first Issue.* entry is CrudWrites at position 6 / Complexity 68. |
| No output-mode or project-id idiom inlined in any cluster partial | PASS — `ValidateOutputMode` / `OutputModeResult` / `ResolveProjectIdAsync` grep returns 0 cluster hits; all 24+31 sites delegate through `ValidateOutput` / `ResolveProjectId` helpers. |
| `dotnet build` with TreatWarningsAsErrors | PASS — 0 Warning(s), 0 Error(s). |
| Full CLI issue spec suite (`FullyQualifiedName~CliIssue`) | PASS — 231 passed, 0 failed (covers CliIssueCommandSpecs + CliIssueWorkflowConfigSpecs + CliIssueSessionSpecs + CliIssueLabelSpecs + CliIssueTemplateCommandSpecs + CliIssueUpdatePatchBodySpecs + CliIssuePrereqSpecs + CliIssueCommentAndFeedbackSpecs + CliIssueRerunFromStageSpecs + CliIssueRejectAndStopSpecs + CliIssueExecutionConfigFlagsSpecs + CliIssueWorkflowProfileSpecs). |
| `IssueCommands.Build()` public shape unchanged | PASS — only caller is `MohistCliCommands.Build` (root composer at MohistCliCommands.cs:27) and Program.cs uses that. No `IssueCommands.Build(api)` calls elsewhere in the repo. |

## Spec gate runs at T-008 close

- `dotnet build packages/cli/Mohist.Cli/Mohist.Cli.csproj` →
  0 Warning(s), 0 Error(s).
- `dotnet test packages/cli/tests/Mohist.Cli.Tests/...
   --filter 'FullyQualifiedName~CliIssue'` →
  231 passed, 0 failed (full issue suite).
- `dotnet test packages/cli/tests/Mohist.Cli.Tests/...` (full CLI suite) →
  559 passed, 0 failed (matches T-001/T-007 baseline, no regression).
- `scc packages/cli/Mohist.Cli/ --by-file --sort complexity` →
  no Issue.* file in top 5 (first Issue entry at position 6).
