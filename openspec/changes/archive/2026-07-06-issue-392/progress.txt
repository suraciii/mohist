progress.txt — issue-392 working memory

## Codebase Patterns

### Epic aggregate
- Domain transitions live in `packages/server/src/Mohist.Server/Epic/Domain/Epic.Transitions.cs` (sealed partial class).
- Each transition mutates `_status`, calls `Touch(now)`, records an `EpicStatusChanged` event with the string `EpicStatusName.*` constants.
- Exceptions live in `packages/server/src/Mohist.Server/Epic/Domain/EpicLifecycleExceptions.cs` — all `InvalidOperationException` subclasses with `EpicId` / status fields.
- Status literals: `EpicStatus` enum + `EpicStatusName.ToName` / `Parse` for the DB string form.
- `EpicEvent` is a `union` type; tests use a manual `EpicEventAssertions.OfType<T>` (pattern-match) helper because `Enumerable.OfType<T>()` triggers CA2021 (empty sequence at compile time).
- `Epic.LinkIssue` previously was status-agnostic (membership only); the T-001 change adds the `_status is EpicStatus.Closed → throw EpicClosedCannotLinkException` guard before the duplicate check.

### EpicGrain
- `EpicGrain.GrainKeyForTest` is the seam that lets unit tests instantiate the grain without Orleans (set the key, call methods).
- `LinkIssueAsync` previously had a binary `targetIsTerminal` gate (`if (!targetIsTerminal)` around active-membership insert); T-001 replaces it with a three-way branch keyed on live `row.Status`.
- `IsIssueOpenAsync(db, projectId, issueNumber)` is a new helper that uses `IssueRowMapper.ByNumber` (returns `Dictionary<int, Issue>`) and classifies via `issue.Status is not (Done or Cancelled)`.
- Cross-aggregate ownership uniqueness check `GetActiveMembershipOwnerAsync` is hoisted above the wake-up so the uniqueness invariant is enforced before a done+open wake consumes an active slot.
- After a successful wake commit, `LinkIssueAsync` tail-calls `TryStartNextAsync` so autopilot starts the just-linked open issue (mirrors `StartAsync` post-commit at L404-407).
- The `DbUpdateException` catch (originally gating on `!targetIsTerminal`) is now unconditional since a wake-up also performs an active-membership insert and could race.
- The `EpicClosedCannotLinkException` throw happens before any `db.EpicIssues.Add` so no rows are created on rejection.

### Specs
- Unit specs use `RecordingGrainFactory` (from `EpicProgressionSpecs.cs`) that records `IIssueGrain.StartWorkAsync` calls; it implements the full `IGrainFactory` interface (lots of `throw new NotSupportedException()` for unused paths).
- Spec setup uses `TestDatabase` (in-memory SQLite, `Data Source=:memory:`) with `TestDbContextFactory` and `CreateFactory(params IInterceptor[])` for tests that need interceptors.
- `SaveChangesInterceptor` lives in `Microsoft.EntityFrameworkCore.Diagnostics` (along with `DbContextEventData`, `InterceptionResult<T>`, `IInterceptor`).
- `FakeTimeProvider` from `Microsoft.Extensions.Time.Testing` is injected into `EpicGrain`.
- `SeedIssueAsync` writes JSON `IssueStore.Serialize(issue)` to the `IssueRow.State` column; `canStart` is controlled by `issue.IsDraft = !canStart`.
- `IssueStatus` enum: `Backlog`, `InProgress`, `Done`, `Cancelled`.

### Conventions to preserve
- Domain rules live in the aggregate (`Epic.cs` / `Epic.Transitions.cs`); grain orchestrates persistence + cross-aggregate calls.
- HTTP layer maps domain exceptions to 409 with distinct error codes (e.g. `EPIC_CLOSED_CANNOT_LINK`).
- Tests use the spec track (product behavior) for end-to-end through the grain + DB; unit track only for pure-parsers.

## T-001 decisions / gotchas

- **Order of guards in `LinkIssueAsync`**: `alreadyLinkedToThisEpic` runs BEFORE the `closed` guard so re-linking to a closed epic stays idempotent (the issue was linked before close, so the early-return short-circuits).
- **Ownership check hoist**: runs for `done` + open and non-terminal targets. The closed path throws before reaching the ownership check, so no behavior change there.
- **Wake-up adds active row only when status was Done AND the issue is open**: the `if (row.Status != EpicStatusName.Done || wakeUpEpic)` guard ensures we don't insert an active row for terminal-issue links to done epics.
- **Tail-call `TryStartNextAsync` reads `links` AFTER SaveChangesAsync**: the new row is in the change tracker, so the re-query includes it; `TryStartNextAsync` only cares about the issue numbers and statuses (it queries `db.Issues` separately).
- **Test updates required**: several existing tests in `EpicMembershipSpecs` / `EpicBatchMembershipSpecs` linked issues to `closed` epics expecting success; per the new design, those tests now (a) flip to `done` and (b) seed the issue as `IssueStatus.Done` so the wake-up branch doesn't fire. The closed-link rejection is exercised in the new `EpicWakeUpSpecs.LinkIssueAsync_ClosedEpic_ThrowsEpicClosedCannotLinkException_NoRowsCreated` scenario.
- **`LinkIssueAsync_DoneEpic_AlreadyLinked_IsIdempotent_NoStatusChange`** reuses the early-return for `alreadyLinkedToThisEpic` so the wake-up branch is not entered on re-link — this preserves the spec scenario "Idempotent re-link ... does not wake the epic".
- **Rollback test uses an EF save interceptor**: `FailOnEpicActiveIssueInsertInterceptor` throws when an `EpicActiveIssueRow` is staged for the target epic. The epic stays `done`, no rows are persisted, and a retry with the regular factory performs the full wake-up.
- **`WakeFromDone` uses `EpicAlreadyTerminalException`** for misuse (calling on non-done). Reusing the existing exception keeps the failure surface narrow and consistent with how `StartAsync`/`ResumeAsync` already raise the same exception on terminal inputs.
- **No `EpicWoken` event**: per design D1, the wake emits only `EpicStatusChanged(done → running)`; no new event type. Consumers branch on the status literal, not on a wake-specific event.

## Outstanding for next tasks

- T-003: pin invariant 2 (`MarkDone` / auto-done rejected while open linked issues exist) — pure test-add task.

## T-002 decisions / gotchas

- **`wasDoneAtEntry` snapshot for the autopilot tail-call**: tracked at entry (after the closed pre-check). The post-loop condition `wasDoneAtEntry && row.Status == running` is the symmetric close of the wake-once logic — if the batch woke the epic, `row.Status` is now `running` (live), and we invoke `TryStartNextAsync` exactly once to advance the new open work with no caller-issued start.
- **`willInsertActiveRow` derivation**: `wakeUpEpic || !targetIsTerminal`. Replaces the old binary `targetIsTerminal` gate as the single source of truth for both (a) when the cross-aggregate ownership check runs and (b) when the `EpicActiveIssues` row is staged. For a done + terminal-issue link: `wakeUpEpic == false` AND `targetIsTerminal == true` → no ownership check, no active row. For a done + open link: `wakeUpEpic == true` → ownership check runs BEFORE wake-up, active row is inserted in the same commit. For a non-terminal target: `!targetIsTerminal == true` → unchanged from prior behavior. For a closed target: rejected before the loop.
- **`DbUpdateException` catch-all gate**: still gated on `willInsertActiveRow`. A wake-up commits an active row, so the racing-claim catch applies. A done + terminal-issue link commits only the link row, so the catch is unreachable for that path — no spurious reclassification.
- **Per-item failure isolation preserved**: `existingLinks.Add(item.IssueId)` only happens after a successful `SaveChangesAsync`. A subsequent item's failure (or a conflict outcome) does not roll back the prior successful link(s) — the design risk note on batch partial wake is satisfied by relying on the existing per-item persistence semantics.
- **API test for closed-rejection** uses the public `/{id}/close` route to drive the epic into `closed` state — exercises the real `Close()` transition path and proves the batch route maps `EpicClosedCannotLinkException` to `409 EPIC_CLOSED_CANNOT_LINK` end-to-end. The error-code field on `ApiResponse` is `code` (not `errorCode`) — that was the API spec test's first attempt, fixed after the initial KeyNotFoundException surfaced it.
- **`LinkIssuesAsync_OnTerminalEpic_RecordsIssueLinkedEvent` (existing test)** keeps working because the issue is seeded as `IssueStatus.Done` → `wakeUpEpic == false` → wake branch doesn't fire → link row + event persist unchanged from the old terminal-target semantics.

## T-003 decisions / gotchas

- **No production code change** per design D5 — invariant 2 is already implemented in `Epic.MarkDone` (`Epic.Transitions.cs:111-122`), `TryAutoMarkDoneAsync` (`EpicGrain.cs:761-784`), and `ReconcileAfterTerminalInternalAsync` (`EpicGrain.cs:679-708`). Pure test-add task.
- **Tests added to existing `EpicAutoDoneSpecs.cs`** rather than a new file. The file already housed `AutoMarkDoneIfReadyAsync_*` scenarios, so the auto-done no-op lives next to its peers; the `SetStatusAsync` and `ReconcileAfterTerminalAsync` regression scenarios also belong here since they exercise the same invariant. Per `notes` field in `tasks.json`, this avoids file overlap with T-001's `EpicWakeUpSpecs.cs`.
- **Existing `SetStatusAsync_Done_StillThrowsOnUnreadyEpic_RegressionCheck`** used `Assert.ThrowsAnyAsync<Exception>` — too loose. Added a tighter sibling `SetStatusAsync_Done_EpicWithOpenLinkedIssue_ThrowsNotReadyToMarkDoneAndStatusUnchanged` that explicitly asserts `EpicNotReadyToMarkDoneException` (with `EpicId` and `OpenLinkedCount == 1`) AND verifies the persisted status stays `running`. Spec requirement "Manual mark done with an open linked issue is rejected" needs the exception type pinned for it to mean anything.
- **Three scenarios cover the three entry points**:
  1. Explicit `MarkDone` via `SetStatusAsync("done")` — raises `EpicNotReadyToMarkDoneException`, status unchanged. Maps to spec scenario "Manual mark done with an open linked issue is rejected".
  2. `AutoMarkDoneIfReadyAsync` with open linked issue — no-op, status unchanged. Maps to spec scenario "Auto-done with an open linked issue is a no-op".
  3. `ReconcileAfterTerminalAsync` (the reconcile-on-terminal-event path) with open linked issue — does NOT flip to `done`, status unchanged. Same spec scenario, second entry point. Seeded as `idle` + InProgress linked issue: `ReconcileAfterTerminalInternalAsync` skips done/closed/paused, sees `open.Count > 0`, does not enter the auto-mark-done branch, and because status is `idle` (not `running`) does not invoke `TryStartNextAsync`. So the no-op falls out without needing `RecordingGrainFactory`.
- **Pattern for `running` status in `MarkDone` regression**: seeded as `running` with an `InProgress` linked issue. The `MarkDone` precondition check throws before reaching the `EnsureNotTerminal(EpicStatus.Done)` ordering issue — `EpicNotReadyToMarkDoneException` propagates from `domain.MarkDone(open, ...)` straight up through `SetStatusAsync`. `idle` would also work (it's not terminal) but `running` more faithfully represents the realistic case where the user actively tries to mark done.
- **`Assert.ThrowsAsync<EpicNotReadyToMarkDoneException>` requires the exception to propagate from the grain entry point**. Verified by reading `SetStatusAsync` (`EpicGrain.cs:525-563`) — it does not catch the domain exception, so it bubbles up unchanged.
- **All three scenarios < 500ms** (cold first run includes JIT/discovery; warm runs are well under). Confirms spec track timing requirement.