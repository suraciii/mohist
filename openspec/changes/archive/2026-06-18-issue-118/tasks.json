{
  "project": "mohist-local",
  "description": "Coordinated accessibility + responsiveness pass over the 6 Settings tabs (settings-accessibility capability). Audit-first: build the a11y harness and capture a confirmed-defect baseline, then fix the real gaps (touch targets, keyboard/ARIA state, heading structure + labeling, contrast + live regions).",
  "tasks": [
    {
      "id": "T-001",
      "title": "Add a11y test harness and capture baseline audit",
      "spec": "specs/settings-accessibility/spec.md#settings-accessibility-regression-coverage",
      "description": "Add the accessibility regression foundation required by the rest of the change. Integrate axe-core via vitest-axe for the structural rules that run under jsdom (aria-*, heading-order, label, button-name, tabindex), and add a scoped Playwright + @axe-core/playwright suite that loads each /settings/<tab> route in a real browser for color-contrast verification (jsdom cannot compute contrast). Then run the baseline audit across all 6 tabs and record the confirmed-defect list BEFORE any fix, per the design's audit-first mandate. Do not modify application source in this task.",
      "acceptanceCriteria": [
        "vitest-axe runs structural axe rules against each of the 6 Settings tabs inside the existing vitest suite",
        "A Playwright + @axe-core/playwright suite loads /settings/<ai|agent|repositories|workflows|templates|system> and asserts color-contrast (and a full re-run) with zero unmet critical/serious violations as the target state",
        "A test:a11y script is added; the main vitest run (test/test:run) is unaffected and still passes",
        "A baseline audit artifact is recorded listing the confirmed real defects (e.g. h-7 buttons, missing aria-expanded on Stage Overrides disclosure, orphan labels, no page h1, TemplateEditor is inline-not-modal reclassification)",
        "No application source files under packages/web/src/pages/settings are modified by this task",
        "Existing SettingsPage.test.tsx and section unit tests still pass"
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web a11y test harness (vitest-axe + Playwright/@axe-core/playwright), test:a11y script, and a recorded baseline defect list under openspec/changes/issue-118/",
      "dependsOn": [],
      "passes": false,
      "notes": "Decision 2 (design.md): two-track coverage. Decision 1: the baseline audit list is the source of truth for what later tasks fix — do not fix anything not confirmed here. Several issue assumptions are already disproved (responsive grids fine; Git URL truncation fine; sonner toast already role=status; TemplateEditor is an inline CardSection, not a modal Dialog)."
    },
    {
      "id": "T-002",
      "title": "Bring Settings interactive controls up to touch-target size",
      "spec": "specs/settings-accessibility/spec.md#settings-interactive-elements-meet-minimum-touch-target-size",
      "description": "Fix WCAG 2.5.5 touch-target gaps. Remove sub-44px fixed heights (h-7/h-8) from Settings interactive controls and extend hit areas to >=44x44px via padding so dense rows (Repositories list) stay visually compact. Cover the narrow-viewport legibility of the Add Repository form so Name/Base Branch/Git URL labels and inputs do not crowd or truncate at 375px.",
      "acceptanceCriteria": [
        "grep of packages/web/src/pages/settings shows no button/link/tab/icon-button capped below 44px by h-7/h-8 or equivalent fixed height",
        "RepositoriesSection Set default and Remove buttons (and any icon-only buttons such as TemplateEditor close) have a >=44x44px hit area including padding",
        "At a 375px viewport the Add Repository form renders Name + Base Branch + Git URL with labels visible and no horizontal overflow or label truncation",
        "vitest-axe structural rules still pass; a Playwright touch-target/bounds assertion is added for the Repositories action buttons",
        "No backend API or persisted-data changes; existing Settings tests pass"
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Updated packages/web/src/pages/settings/ui/*Section.tsx and TemplateEditor.tsx with compliant hit areas, plus touch-target assertions",
      "dependsOn": ["T-001"],
      "passes": false,
      "notes": "Decision 3 (design.md): use a Settings-local min-h-11/min-w-11 hit-area convention via padding; do NOT edit the shared Button component. Real confirmed gap: RepositoriesSection.tsx:95,106 (h-7) and TemplateEditor.tsx:214 (size=icon-xs)."
    },
    {
      "id": "T-003",
      "title": "Fix keyboard navigation and dynamic ARIA state",
      "spec": "specs/settings-accessibility/spec.md#settings-keyboard-navigation-reaches-all-interactive-elements-in-dom-order",
      "description": "Make the 6 Settings tabs fully keyboard-operable and expose dynamic state to assistive tech. Add aria-expanded/aria-controls to the Stage Model Overrides disclosure in AiSettingsSection; verify Tab order, Enter/Space activation, Escape close, arrow-key Select/ModelSelect operation, focus-visible rings in both themes, and ModelSelect popover focus-on-open. Base UI Dialog/Popover already provide most of this natively — verify rather than re-implement.",
      "acceptanceCriteria": [
        "Stage Model Overrides disclosure button exposes aria-expanded reflecting its open state and aria-controls pointing at the collapsible region",
        "Tab/Shift+Tab reaches every interactive element in DOM order on each tab with no static-surface focus traps",
        "Enter and Space activate every Settings button (including the Stage Overrides disclosure); Escape closes any open dialog/popover; arrow keys operate Select/ModelSelect option lists",
        ":focus-visible ring is visible on all interactive elements in both light and dark themes",
        "ModelSelect popover moves focus to its search input when opened (verified)",
        "vitest-axe structural rules pass; a keyboard-traversal test is added for at least the ai and repositories tabs",
        "No shared Dialog/Popover/Select internal refactor; existing Settings tests pass"
      ],
      "priority": 3,
      "mode": "AFK",
      "type": "WRITE",
      "output": "AiSettingsSection.tsx disclosure ARIA attributes + keyboard/focus verification tests across settings sections",
      "dependsOn": ["T-001"],
      "passes": false,
      "notes": "Covers spec reqs keyboard-navigation, focus-visible-ring, and folding/dialog-state. Real confirmed gap: AiSettingsSection.tsx:88-98 disclosure has no aria-expanded. Decision 7: sonner/Base UI already accessible — verify via tests, do not modify shared components."
    },
    {
      "id": "T-004",
      "title": "Establish heading hierarchy and programmatic form labels",
      "spec": "specs/settings-accessibility/spec.md#settings-heading-hierarchy-is-monotone",
      "description": "Fix the screen-reader document-structure gaps. Add exactly one page-level <h1> to SettingsPage and make the heading hierarchy monotone (resolve whether SettingsSection must demote h3->h2 to satisfy axe heading-order, per the audit). Associate the orphan <label> elements in AiSettingsSection with their ModelSelect controls via aria-labelledby forwarded through ModelSelect.",
      "acceptanceCriteria": [
        "Each Settings tab renders exactly one page-level <h1> landmark",
        "Heading levels are monotone in document order and the surface passes the axe heading-order rule (if the audit requires SettingsSection h3->h2, record the decision and add the corresponding MODIFIED delta to settings-visual-consistency before archive)",
        "ModelSelect accepts and forwards an aria-labelledby prop; every AiSettingsSection ModelSelect label is programmatically associated",
        "Every form input across the 6 tabs has a label association (htmlFor/id or aria-labelledby); none rely on placeholder-only labeling",
        "vitest-axe structural rules (heading-order, label) pass; existing Settings tests pass",
        "No backend API or persisted-data changes"
      ],
      "priority": 4,
      "mode": "AFK",
      "type": "WRITE",
      "output": "SettingsPage.tsx page h1, resolved SettingsSection heading level, ModelSelect aria-labelledby forwarding, label associations, and heading/label tests",
      "dependsOn": ["T-001"],
      "passes": false,
      "notes": "Covers spec reqs heading-hierarchy and form-labels. RISKIEST task (Decision 4, design.md): changing the SettingsSection shell heading level affects all 6 tabs. Real confirmed gaps: no <h1> anywhere; AiSettingsSection.tsx:71,104 orphan labels. Open Question 1: the h2-vs-h3 verdict may reopen the proposal's 'Modified Capabilities: None' stance."
    },
    {
      "id": "T-005",
      "title": "Verify live-region mutation feedback and fix confirmed contrast gaps",
      "spec": "specs/settings-accessibility/spec.md#settings-surface-passes-wcag-aa-contrast-at-runtime",
      "description": "Close the contrast and live-region requirements. Verify (via test) that sonner mutation toasts already announce success/error through role=status/aria-live, applying only a minimal attribute patch if a real gap is found. Fix the confirmed contrast token issues — SettingsSection description (text-muted-foreground) on muted backgrounds and error-state text on tinted backgrounds — adjusting token usage per the audit and recording before/after ratios.",
      "acceptanceCriteria": [
        "Mutation success and failure feedback is announced via an aria-live region (sonner role=status verified by test; or a Section-level aria-live region if a real gap is found)",
        "Playwright @axe-core/playwright color-contrast scan reports zero critical and zero serious violations across all 6 Settings tabs",
        "SettingsSection description text meets >=4.5:1 contrast on muted/tinted backgrounds; error-state text meets >=4.5:1 on tinted backgrounds (before/after values recorded where tokens are adjusted)",
        "No refactor of the shared sonner/Dialog/Select internals beyond a minimal additive attribute patch",
        "Existing Settings tests pass; no backend API or persisted-data changes"
      ],
      "priority": 5,
      "mode": "AFK",
      "type": "WRITE",
      "output": "Contrast token adjustments in settings sections, live-region verification tests, and recorded before/after contrast ratios",
      "dependsOn": ["T-001"],
      "passes": false,
      "notes": "Covers spec reqs wcag-aa-contrast and mutation-feedback. Decision 7: sonner (^2.0.7) already renders role=status/aria-live — expect this to be verify-only. Run AFTER T-004 if both touch SettingsSection.tsx, to avoid token/structure merge friction (sequential by priority)."
    }
  ]
}
