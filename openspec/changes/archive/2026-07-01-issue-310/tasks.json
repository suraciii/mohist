{
  "project": "mohist-local",
  "description": "Refactor the web IssueDetailPage god-component (1375 lines, scc Complexity 211) into cohesive sub-components and hooks under pages/issue-detail/{model,ui}, preserving every data-testid, DOM/margin rhythm, TanStack Query key, navigation URL, and the issue-300 capacity-gating contract bit-for-bit.",
  "tasks": [
    {
      "id": "T-001",
      "title": "Extract presentational foundation (format helpers, pills, WorkflowYamlDialog)",
      "spec": "openspec/changes/issue-310/design.md#D6",
      "description": "Mechanically move the 9 inline presentational pieces out of IssueDetailPage.tsx into reusable modules: model/format.ts (formatRelativeTime, formatStageName, stageToIssueStatus, WORKFLOW_STAGE_LABELS, attachmentFromMetadata), ui/pills.tsx (PriorityChip, WorkflowStagePill, HealthPill, DraftPill, ArchivedPill as five exports), and ui/WorkflowYamlDialog.tsx. The page imports them from the new locations; no JSX body or behavior change.",
      "acceptanceCriteria": [
        "model/format.ts exists and exports the 5 helpers verbatim (no signature/return-value change).",
        "ui/pills.tsx exports exactly PriorityChip, WorkflowStagePill, HealthPill, DraftPill, ArchivedPill; ui/WorkflowYamlDialog.tsx exists.",
        "IssueDetailPage.tsx imports the moved pieces instead of defining them inline; no other page-body logic changes in this task.",
        "`rg 'data-testid' packages/web/src/pages/issue-detail/ui/IssueDetailPage.tsx` count is unchanged vs. pre-refactor (helpers carry no testids; moved testids travel with JSX).",
        "All 4 IssueDetailPage suites pass: `npm run test:run -w packages/web` is green for the issue-detail/ui tests; `npm run typecheck -w packages/web` passes."
      ],
      "priority": 1,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/model/format.ts, ui/pills.tsx, ui/WorkflowYamlDialog.tsx; slimmed imports in IssueDetailPage.tsx",
      "dependsOn": [],
      "passes": false,
      "notes": "One cohesive module of shared presentational primitives + helpers consumed by later tasks. Do not split into one-file-per-chip; pills.tsx holds all five (design.md D1 alternative)."
    },
    {
      "id": "T-002",
      "title": "Merge duplicated click-outside effects into useConfirmOutsideClick hook",
      "spec": "openspec/changes/issue-310/design.md#D4",
      "description": "Add model/useConfirmOutsideClick.ts that owns the union of the two near-duplicate effects (the forceStopConfirming block at ~L222-235 and the stopConfirming block at ~L319-332): a 5000ms setTimeout auto-dismiss plus a mousedown-outside listener, returning a RefObject<HTMLDivElement>. Swap both inline effects in the page for two hook calls; the forceStopPanelRef/stopPanelRef refs move with this change.",
      "acceptanceCriteria": [
        "model/useConfirmOutsideClick.ts exists with signature useConfirmOutsideClick({ confirming, setConfirming, timeoutMs? }): React.RefObject<HTMLDivElement>.",
        "model/useConfirmOutsideClick.test.ts covers: dismissing on outside mousedown, auto-dismiss after 5000ms (fake timers, no wall clock), no-op when confirming is false, resetting the timer correctly. Follows design/testing.md (no real time, <50ms unit).",
        "IssueDetailPage.tsx no longer contains either inline click-outside useEffect; both panels use the hook.",
        "Auto-dismiss timing remains exactly 5000ms; behavior of both confirm panels is unchanged.",
        "All 4 IssueDetailPage suites pass; `npm run typecheck -w packages/web` passes."
      ],
      "priority": 2,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/model/useConfirmOutsideClick.ts (+ .test.ts); page rewired to two hook calls",
      "dependsOn": [],
      "passes": false,
      "notes": "Independent cross-cutting UI-behavior concern; does not consume T-001 output. Ordered after T-001 only to keep the page-file edits sequential."
    },
    {
      "id": "T-003",
      "title": "Centralize the 13 mutations into useIssueDetailMutations hook",
      "spec": "openspec/changes/issue-310/design.md#D5",
      "description": "Add model/useIssueDetailMutations.ts that centralizes the 13 useMutation definitions (start, markReady, addPrerequisite, removePrerequisite, close, forceStop, stop, reopen, resume, retry, rerun, addComment, deleteComment) and their onSuccess query-invalidation only. Mutations that also reset local UI state (forceStop/stop reset confirming, addComment clears field, deleteComment clears deletingCommentId/deleteCommentError) accept an optional onSuccess/onError callback in the hook options so the owning component wires the local reset. Rewire the page to consume the hook.",
      "acceptanceCriteria": [
        "model/useIssueDetailMutations.ts exists and exposes all 13 mutations with identical call shapes and the exact same onSuccess query-invalidation (['issues'], ['issues', issueNumber], ['agent-status']) as before.",
        "Query keys are unchanged: no mutation switches its invalidateQueries target; `npm run typecheck -w packages/web` confirms no key drift.",
        "Local-state resets (confirm flags, comment field, deletingCommentId/deleteCommentError) behave identically, wired via the hook's callbacks by the owning component.",
        "IssueDetailPage.tsx no longer defines the 13 useMutation calls inline; it destructures them from the hook.",
        "All 4 IssueDetailPage suites pass; `npm run typecheck -w packages/web` passes."
      ],
      "priority": 3,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/model/useIssueDetailMutations.ts; page rewired to the hook",
      "dependsOn": [],
      "passes": false,
      "notes": "Data-layer concern; does not consume T-001/T-002 output. Placed in model/ for consistency with epic-detail (design.md D5 open question resolved to model/)."
    },
    {
      "id": "T-004",
      "title": "Extract Actions card as thin renderer over pure actionsState derivation",
      "spec": "openspec/changes/issue-310/design.md#D3",
      "description": "Add model/actionsState.ts as a pure computeActionsState({ issue, agentStatus, workflowTimeline, recovery }) function returning the description of the Actions card (showArchivedNote, startVariant, showForceStopPanel, forceStopContext, blockedActions{showRetry,showResume,showRerun,showStop,isInterrupted,showProjectedCheckRepair}, showStandaloneRerun, showError, errorMessages, showOtherAgents, otherAgentsCount). Build cards/IssueActionsCard.tsx as a thin renderer that consumes actionsState + useConfirmOutsideClick (force-stop/stop panels) + the relevant mutations from useIssueDetailMutations. The showCheckRepairActions=false constant and canStopWorkflow expression are preserved verbatim inside computeActionsState.",
      "acceptanceCriteria": [
        "model/actionsState.ts exports computeActionsState as a pure function with no React/DOM dependencies; model/actionsState.test.ts pins the cases the existing Actions tests cover (archived note, start variants, force-stop panel visibility, blocked retry/resume/rerun/stop, standalone rerun, isInterrupted).",
        "cards/IssueActionsCard.tsx is a thin renderer that preserves the exact JSX order/structure of the original ~300-line card, including the single IIFE at original L1170 rendered as sibling conditionals in the same order.",
        "forceStop/stop confirm panels use useConfirmOutsideClick (no inline effects); the refs are co-located with the card.",
        "The Actions state-machine branching no longer lives in IssueDetailPage.tsx; measured complexity of both card and page drops.",
        "All 4 IssueDetailPage suites (incl. the Actions state-machine assertions) pass; `npm run typecheck -w packages/web` passes."
      ],
      "priority": 4,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/model/actionsState.ts (+ .test.ts), ui/cards/IssueActionsCard.tsx",
      "dependsOn": ["T-001", "T-002", "T-003"],
      "passes": false,
      "notes": "Consumes format helpers/pills from T-001, the click-outside hook from T-002, and start/stop/forceStop/retry/resume/rerun mutations from T-003. This is the highest-value extraction (drives the 211 complexity score)."
    },
    {
      "id": "T-005",
      "title": "Extract remaining right-rail cards (Details/Drift/Configuration/Prerequisites/Readiness)",
      "spec": "openspec/changes/issue-310/design.md#D2",
      "description": "Build cards/IssueDetailsCard.tsx, IssueDriftCard.tsx, IssueConfigurationCard.tsx, IssuePrerequisitesCard.tsx, IssueReadinessCard.tsx, each rendering only inner content from props (issue, agentStatus, workflowTimeline, recovery, and the addPrerequisite/removePrerequisite/markReady mutations from useIssueDetailMutations). Per D2, the page keeps every structural wrapper the density test keys on (the space-y-6 right-rail column, each *-frame div carrying mb-8, structural data-testids); children render content only and do not re-introduce those wrappers.",
      "acceptanceCriteria": [
        "All 5 card files exist under ui/cards/ and render only inner content via props (no wrapper that owns mb-8 / space-y-6 / structural testid).",
        "The right-rail structural elements (column wrapper with space-y-6, frame wrappers with mb-8, right-rail data-testids) remain owned by IssueDetailPage.tsx exactly as before.",
        "Issue-300 capacity-gating isCapacityFull derivation from agentStatus.capacity and all navigation URLs in these cards are unchanged.",
        "All 4 IssueDetailPage suites pass (incl. density block asserting space-y-6 right rail and mb-8 frames); `npm run typecheck -w packages/web` passes."
      ],
      "priority": 5,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/ui/cards/Issue{Details,Drift,Configuration,Prerequisites,Readiness}Card.tsx",
      "dependsOn": ["T-001", "T-003"],
      "passes": false,
      "notes": "Sibling cards to the Actions card; does not depend on T-004. Verify the density test after each card extraction, not only at the end (design.md risk mitigation)."
    },
    {
      "id": "T-006",
      "title": "Extract main-column sections (Description/DiffFiles/Commits/Comments)",
      "spec": "openspec/changes/issue-310/design.md#D2",
      "description": "Build sections/IssueDescriptionSection.tsx, IssueDiffFilesSection.tsx, IssueCommitsSection.tsx, IssueCommentsSection.tsx, each rendering only inner content from props (issue, diffData, commitsData, resolveIssueAttachment, and the addComment/deleteComment mutations from useIssueDetailMutations). Per D2, the page keeps the space-y-8 main column and the grid > .lg:col-span-2 wrapper the density test asserts. resolveIssueAttachment stays a closure built in the page from issueNumber/issueProjectId/issue.attachments and is passed as a prop to the description and comments sections (unchanged behavior).",
      "acceptanceCriteria": [
        "All 4 section files exist under ui/sections/ and render only inner content via props (no wrapper that owns space-y-8 / the grid column class).",
        "The main-column structural elements (space-y-8 column, grid > .lg\\:col-span-2 wrapper, structural data-testids) remain owned by IssueDetailPage.tsx exactly as before.",
        "resolveIssueAttachment is still constructed in the page and passed down; comment add/delete behavior and Ask Agent / View files navigation URLs are unchanged.",
        "All 4 IssueDetailPage suites pass (incl. density block asserting space-y-8 main column and grid column resolution); `npm run typecheck -w packages/web` passes."
      ],
      "priority": 6,
      "mode": "AFK",
      "type": "WRITE",
      "output": "packages/web/src/pages/issue-detail/ui/sections/Issue{Description,DiffFiles,Commits,Comments}Section.tsx",
      "dependsOn": ["T-001", "T-003"],
      "passes": false,
      "notes": "Different layout column from T-005; genuinely independent functional area. Consumes format helpers (formatRelativeTime, attachmentFromMetadata) from T-001 and comment mutations from T-003."
    },
    {
      "id": "T-007",
      "title": "Consolidate IssueDetailPage to orchestration-only and verify structural goals",
      "spec": "openspec/changes/issue-310/design.md#goals",
      "description": "Final gate: confirm IssueDetailPage.tsx has collapsed to page-level orchestration (data loading, layout slots, dialog mount) with no inline sub-components, no Actions state machine, no click-outside effects, and no inline mutation definitions. Verify the aggregate structural goals of the refactor hold across all extractions.",
      "acceptanceCriteria": [
        "IssueDetailPage.tsx contains no inline definitions of the 9 presentational pieces, the 13 mutations, the Actions branching, or either click-outside effect; it only orchestrates data loading, layout slots, and dialog mount.",
        "The page-wide union of data-testid values is unchanged vs. pre-refactor (grep-driven inventory: before/after set diff is empty).",
        "IssueDetailPage.tsx line count and scc Complexity are materially reduced from 1375 / 211 (report the new numbers).",
        "All 4 IssueDetailPage suites pass green; `npm run typecheck -w packages/web` passes; web lint passes.",
        "The public route entry (IssueDetailPage export) is unchanged; no external consumer changes."
      ],
      "priority": 7,
      "mode": "AFK",
      "type": "REVIEW",
      "output": "Verification report: orchestration-only confirmation, data-testid inventory diff, new line/complexity numbers, green suites/typecheck/lint",
      "dependsOn": ["T-004", "T-005", "T-006"],
      "passes": false,
      "notes": "Cross-cutting acceptance that no single extraction task can verify (aggregate complexity/line reduction and full testid inventory). REVIEW-type gate over the three extraction tasks."
    }
  ]
}
