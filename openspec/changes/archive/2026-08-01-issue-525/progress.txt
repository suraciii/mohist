## Codebase Patterns

- Slack management routes use `AgentQuerier.GetByIdAsync(projectId, agentId)` for bound-Agent reads and `ApiResponse<T>` with camel-cased anonymous response objects.
- `AgentConnectionStore.CreateAsync` validates the active Agent and persists `BotName` verbatim; `WorkspaceTeamId`, `AppId`, and `BotUserId` stay empty until `SlackSetupVerifier` calls `BindSlackIdentityAsync` after verification.
- Pure Agent derivations are static `*Deriver` types under `Agent/Services`, with direct unit tests under `Mohist.Server.UnitTests/Agent/Services`.
- Slack's current manifest contract for `features.bot_user.display_name` is 1–80 characters using only lowercase `a-z`, `0-9`, `-`, `_`, and `.`; `display_information.description` allows up to 140 characters, but T-001 requires a non-blank Agent description to remain unchanged.

## T-001 Notes

- No prior `progress.txt` existed.
- Pre-change `SlackConnectionApiSpecs` passed when run directly with the xUnit v3 `-class` filter. `dotnet test --filter` is ignored by this repository's Microsoft.Testing.Platform runner and accidentally ran all 3,603 specs; one unrelated pre-existing AgentSession dispatch polling spec failed while all SlackConnectionApiSpecs passed.
- `SlackBotIdentityDeriver` treats an Agent name as directly valid only when it is non-empty, at most 80 characters, and entirely within Slack's lowercase display-name alphabet. Invalid names are lowercased/sanitized, capped, and receive an 8-character lowercase SHA-256 prefix derived from the Agent id; blank bases use `agent`.
- Explicit `BotName` keeps the pre-existing trim-and-persist behavior and remains the preview name. Omitted `BotName` persists the Agent-derived name. GET returns that persisted name (or derives one for a legacy blank value), derives the current Agent description, and still returns the connection with a generic preview if its Agent record is unexpectedly missing.
- Create and detail responses share the existing Slack App creation reference and expose `connection`, `botName`, `appDescription`, and `slackAppCreationReference`; no avatar field was added.
- Added pure unit coverage and HTTP specs for valid derivation, stable suffixing, blank-description fallback, Agent non-mutation, persistence, GET resumability, explicit BotName compatibility, and verification-only immutable binding fields.
- Final `npm test` passed: all .NET unit/spec/architecture/CLI tests and all Web, Runner, and mohist-slack workspace tests succeeded.

## T-002 Notes

- The masked credential input lives at `packages/web/src/shared/ui/components/masked-credential-input.tsx` and is re-exported from `packages/web/src/shared/ui/index.ts` (slice public API) per the FSD rule "新增导出时先改 slice 的 `index.ts`，而不是在调用方加深路径". Consumers can use either `@/shared/ui` or `@/shared/ui/components/masked-credential-input`.
- Implementation reuses the existing `Input` primitive and forces `type="password"` plus `autoComplete="off"`, `autoCorrect="off"`, `autoCapitalize="off"`, `spellCheck={false}`. The `type` prop is removed from the public prop type so no caller can put the field into a cleartext type — masking is enforced at the type boundary, not just by convention.
- No reveal/show toggle is added (and the prop type excludes `type`, so a future PR can't sneak one in through `type="text"`). The form (T-004) is expected to do clear-on-success and body-only transmission; this primitive deliberately does not own those concerns.
- Test placement and pattern: colocated `*.test.tsx` under `components/`, following `alert-dialog.test.tsx` / `button.test.tsx`. Uses `userEvent.type` for the controlled-input path and `screen.getByLabelText` for queries; `screen.getAllByRole('textbox')` does not match password inputs in jsdom, so a previous draft of the "no toggle" test threw — switched to direct label queries.
- Assertions cover the spec's "masked" / "no cleartext path" / "no storage writes" acceptance criteria: `input.type === 'password'`, no `<button>` rendered, body `textContent` does not contain the secret, `localStorage`/`sessionStorage`/`URL` unchanged after typing + unmount, and `Storage.prototype.setItem` spy never sees the secret. Console warn/error spies assert the same — the setup file's blanket mock would otherwise throw, but the local spies give a clearer signal if the component ever logs the secret.
- FSD: `shared/ui/components/masked-credential-input.tsx` imports `@/shared/ui/components/input` and `@/...` paths are resolved inside the same `shared` slice (no slice boundary crosses). `npm run check:fsd -w packages/web` and `npm run check:test-boundaries -w packages/web` both pass after the change.
- Test boundaries: `npm run test:run -w packages/web` passes 5,096 / 5,096 with the five new specs included. `npm run typecheck -w packages/web` is clean.

## T-003 Notes

- `entities/agent-connection` already exports only `getConnectionDiagnostic` (read-only); T-003 extends it with a project-scoped `listAgentConnections` (`GET /slack-connections`), `createAgentConnection` (`POST /slack-connections`), and a `useCreateAgentConnection` mutation that invalidates the project list on success. The public surface (model types, client, query/mutation options) is re-exported from `entities/agent-connection/index.ts` per the FSD rule "新增导出时先改 slice 的 `index.ts`，而不是在调用方加深路径".
- `AgentConnectionCreateRequest` / `AgentConnectionCreateResponse` types in `model/types.ts` mirror the existing Server shape (camelCased `connection`, `botName`, `appDescription`, `slackAppCreationReference`). The widget consumes `response.connection.id` for navigation, so the per-Agent Connection is created and persisted before the operator leaves the Agent detail page — closing the page does not lose the work.
- `widgets/agent-connections` mirrors `widgets/agent-subscriptions`: a card with header, "Add Slack" button, and a per-row list. `ConnectionsSection` exposes the same testability surface as `SubscriptionsSection`: a `connectionOperationsHook` injection plus a default hook that uses the real `useAgentConnections` / `useCreateAgentConnection`. The default signature is `(agentRef: string) => ConnectionOperations` for parity with `SubscriptionsSection`, even though the project-scoped list does not actually need `agentRef` (filtering by `connection.agentId === agent.id` happens after the list resolves).
- Per-row state derives from the list response's `setupProgress` / `connectionHealth` / `desiredState` (no per-row diagnostic fetch — that is the connection page's job, per Decision A). The data attributes `data-connection-state` (`amber` | `muted` | `emerald`) carry the surface state for test assertions.
- Add Slack is disabled on archived agents (parity with `SubscriptionsSection`), with a matching `agent-connections-archived-notice` block. The mutation is still callable via the operations hook, so the gate is the surface (button `disabled`), not the data layer.
- `AgentDetailPage.tsx` now takes a `ConnectionsSection` in its `components` prop (in addition to the existing `AgentProfileEditor` / `SubscriptionsSection`) and renders it in the right rail immediately after `SubscriptionsSection` (so Subscriptions still own their existing line and Connections are visually adjacent — same right-rail column, same boundary tests).
- Test file split: `AgentDetailPage.test.tsx` was already at the 500-line budget cap, so the new `ConnectionsSection` injection specs (active + archived) live in a separate colocated `AgentDetailPage.connections.test.tsx`. The main file's `components` stub is now `ConnectionsSection: () => null` so wiring still type-checks without exceeding the budget.
- Tests: `ConnectionsSection.test.tsx` exercises the widget through the injected `operationsHook` (no real network) for rendering, list filtering by `agentId`, all four connection states, row links, Add Slack mutation calls + navigation, and the archived gate. `ConnectionsSection.msw.test.tsx` exercises the default hook with MSW handlers (no real network) for empty list, agent-filtered list, and Add-Slack POST + navigation. `client.test.ts` adds MSW coverage for `listAgentConnections` and `createAgentConnection` (path, body, response shape).
- `npm run typecheck -w packages/web`, `npm run test:run -w packages/web` (5,115 / 5,115 across 388 files), `npm run check:fsd -w packages/web`, and `npm run check:test-boundaries -w packages/web` all pass.

## T-004 Notes

- `entities/agent-connection` extends the existing T-001/T-003 surface with three new client functions (`getAgentConnection` for `GET /slack-connections/{id}`, `configureAgentConnection` for `POST /{id}/configure`, `claimAgentConnectionOwner` for `POST /{id}/claim-owner`) and three new hooks (`useAgentConnection`, `useConfigureAgentConnection`, `useClaimAgentConnectionOwner`). Types `AgentConnectionDetailResponse`, `AgentConnectionConfigureRequest`, and `AgentConnectionClaimOwnerResponse` mirror the server shape and are re-exported from `entities/agent-connection/index.ts`. Query options for `connectionDiagnosticQueryOptions` and `agentConnectionDetailQueryOptions` now opt into `refetchInterval: 5000` + `refetchOnWindowFocus: true` to match the agent-detail status cadence (Decision D).
- The Setup surface lives at `packages/web/src/pages/connection-diagnostic/ui/` alongside the existing page. Four colocated components: `setup-step-list.tsx` (the ordered step view derived from `facts.setupProgress`, modeled on `entities/settings/ui/ProgressStages.tsx:17-48`), `identity-preview-step.tsx` (the first-step preview + Create in Slack entry, with the avatar note pointing to Slack App settings), `credential-form-step.tsx` (built on T-002's `MaskedCredentialInput`, clears state on submit so the typed values never linger, no reveal toggle, and POSTs to `/configure`), `claim-owner-code-step.tsx` (one-time code shown once with explicit "Regenerate invalidates the previous code server-side" copy).
- `ConnectionDiagnosticPage` now renders solely from server state (`primaryState`/`nextAction`/`facts`). The default `useDefaultData` hook keeps the existing TanStack-Query-driven diagnostic read; the new `useDefaultOperations` hook wires `useAgentConnection` + `useConfigureAgentConnection` + `useClaimAgentConnectionOwner`. Both hooks accept an injected replacement so the read-only test fixture and any future storybook-style mount can swap them without depending on the network. `readOnlyOperations` + `useReadOnlyOperations` are exported so the original `ConnectionDiagnosticPage.test.tsx` (read-only path) keeps working without QueryClientProvider. The page also owns an unmount-only cleanup that calls `reset()` on both mutations so cached `data` (e.g. an undisplayed claim code) is discarded when the operator leaves the page.
- Render mapping: `setupProgress === 'create_app_credentials'` → identity preview + Create-in-Slack entry + credential form; `waiting_for_slack_service` → "Waiting for Slack service" message; `fix_slack_setup` → "Fix Slack setup" message; `claim_owner` → claim code generator. `setupProgress === 'complete'` hides the step list and lets the diagnostic summary drive the next action (e.g. `agent_needs_setup`, `service_offline`, `credentials_invalid` all surface without losing the four supporting facts).
- Tests: the MSW integration suite for the page was split into two colocated files to stay under the 500-line budget cap (`ConnectionDiagnosticPage.setup-msw.test.tsx` covers step rendering, resume on refetch, blocker states; `ConnectionDiagnosticPage.actions-msw.test.tsx` covers configure body-only + clear-on-success, claim-owner shown-once + regenerate-supersedes, and storage-side-effect assertions across the submit cycle). Colocated unit tests cover each sub-component (`setup-step-list.test.tsx`, `identity-preview-step.test.tsx`, `credential-form-step.test.tsx`, `claim-owner-code-step.test.tsx`). `client.test.ts` adds MSW coverage for `getAgentConnection` / `configureAgentConnection` (asserting the URL query/path is free of token values) and `claimAgentConnectionOwner`.
- Refetch-on-focus + 5s interval is enough to satisfy "a step completed via the CLI is reflected in the Web on the next refetch" without coupling to the SignalR stream (Decision D). The MSW test mutates the diagnostic handler between renders and triggers `queryClient.invalidateQueries` to assert the transition lands on the next render.
- `npm run typecheck -w packages/web`, `npm run test:run -w packages/web` (5,147 / 5,147 across 394 files; +32 specs from T-004), `npm run check:fsd -w packages/web`, and `npm run check:test-boundaries -w packages/web` all pass.

## fix-ci Notes (AgentJob dispatch polling flake)

- `npm test` (full verify chain: `dotnet test Mohist.sln -p:SkipWebBuild=true` + `npm run test:ci --workspaces --if-present`) succeeded once with 3,606/3,606 SpecTests passing, then failed the next run with 3,604/3,606 and a third run with 3,605/3,606; the same retry confirmed again on 5 sequential full-suite runs (4 pass / 1 fail). The flake is in the AgentJob dispatch polling path, not in issue-525's Slack-Connection or Web Setup surface.
- Failing examples across runs (all in `packages/server/tests/Mohist.Server.SpecTests/Specs/Agent/Api/`, all calling `AgentSessionLaunchRoutesTestSupport.PollDispatchForSessionAsync` at `:51`): `AgentLaunchObservationRoutesSpecs.Observation_DuringTerminalState_ReportsJobResultAndTurnResult` (`:109`/`138`), `AgentLaunchObservationRoutesSpecs.Observation_AfterReportTimeout_ReportsJobUnknownWithoutCreatingNewInputsOrTurns` (`:161`/`192`), `AgentLaunchObservationRoutesSpecs.Observation_AfterCompletedJob_LeavesAgentSessionUsable` (`:331`/`358`), `AgentSessionLaunchRoutesSpecs.Launch_WithoutRuntimeOverride_UsesAgentConfigRuntime` (`:252`/`259`). Error shape: `Runner '{runnerId}' did not return AgentJob '{agentJobId}' for AgentSessionId '{sessionId}'. Runner registry: {runnerId} status=Online works=[none]` — i.e. the runner is online but the second `/api/runner/{id}/poll` returns no dispatch for the freshly-admitted AgentJob.
- `PollDispatchForSessionAsync` (`:41-58`) only re-polls twice after `AgentJobDispatches.WaitForAssignmentPreparedAsync(agentJobId)` fires, so a transient DB-read race against the freshly committed ledger row (assigned, `Status=pending`, `ReadySince=now`) is enough to fail the test. The sibling `PollAgentJobDispatchAsync` (`:60-71`, fixed in commit `e55c4a3751a9f3256964bec2d920f56b386ff043` for issue-513) already uses `TestWait.ForAsync` with a 5s timeout / 25ms step for the same retry budget — applying that same pattern here is a test-infrastructure reliability fix that strengthens, not weakens, the existing assertion (`Assert.Equal(agentJobId, dispatch.AgentJobId)` is unchanged). The race itself pre-dates issue-525 (no path in `git diff master..HEAD --name-only` touches `AgentJobGrain.cs`, `RunnerGrain.cs`, `DispatchService.cs`, or the affected test classes), so the issue-525 scope (`SlackConnectionRoutes.cs`, `Agent/Services/SlackBotIdentity.cs`, Web `entities/agent-connection` / `widgets/agent-connections` / `pages/connection-diagnostic` / `shared/ui/components/masked-credential-input` / `pages/agent-detail`) does not author or aggravate it.
- Fix: `packages/server/tests/Mohist.Server.SpecTests/Specs/Agent/Api/AgentSessionLaunchRoutesTestSupport.cs` — `PollDispatchForSessionAsync` now uses `TestWait.ForAsync` (5s / 25ms) mirroring `PollAgentJobDispatchAsync`. Removed the now-unused `using Xunit.Sdk;` import (no manual `XunitException` throw remains). The error message stays descriptive: `"Runner '{runnerId}' to return AgentJob '{agentJobId}' for AgentSessionId '{expectedSessionId}'"`.
- Verification: 5 sequential full-suite `dotnet test packages/server/tests/Mohist.Server.SpecTests` runs all pass (3,606/3,606 each). Full `npm test` chain now passes end-to-end: Workflow.Definition.Tests 175/175, Cli.Tests 1487/1487, UnitTests 1723/1723, ArchTests 51/51, SpecTests 3606/3606, Web 5147/5147 (394 files), Runner 1510/1510 (134 files), mohist-slack 8/8 (2 files). `npm run archtest` passes 7/7.
