# Progress — Issue 226 (Scrutor DI migration)

## Codebase Patterns

- **Service registration lives in one place.** `MohistServiceRegistration.ConfigureMohistServices` is the single registration entry point, called by both production (`Program.cs` → `AddMohistServerCore`) and tests (`MohistDbFixture` etc.). Anything that should be available in both must live there.
- **MS DI "last registration wins".** Adding the same `ServiceDescriptor` twice does NOT throw — `GetService` returns the implementation registered most recently. This is the lever that makes scanning safe to coexist with hand-written registrations: scan first, then hand-write overrides.
- **Central package management.** `Directory.Packages.props` at the repo root holds `<PackageVersion>` entries; `.csproj` files only declare `<PackageReference Include="..." />` without a version. Add a new package in both places.
- **`.csproj` convention.** New `PackageReference` entries are alphabetised within the `<ItemGroup>` block (Scrutor sits between EnvironmentAbstractions/CloudNative.CloudEvents items).
- **Marker interfaces live in `Mohist.Server.Infrastructure.Hosting`.** They sit next to `MohistServiceRegistration` so the registration contract is discoverable in one folder.
- **Scrutor 7.0.0** is the latest stable (targets net8.0+; compatible with our net11.0 TFM).
- **Scrutor `Scan` filter for open generics.** `AddClasses(...).AssignableTo<IOpenGeneric>()` matches closed generics, not the unbound definition — known limitation, not relevant to T-001.
- **Scrutor `AddClasses()` defaults to `publicOnly: true`.** It silently drops `internal` types. Test fixtures that live in the production assembly but need to be picked up by the scanner must be `public`. We use a dedicated `Mohist.Server.Infrastructure.Hosting.TestTypes` sub-namespace and never reference them from production code.
- **Test fixtures for Specs that need the production service graph.** Use `[Collection("MohistDb")]` + constructor injection of `MohistDbFixture`; do NOT instantiate the fixture inside the test (xunit linter forbids blocking `.GetAwaiter().GetResult()` on fixture init/dispose).

## Gotchas

- `services.Scan` from Scrutor with `AsSelf()` is idempotent in the sense that re-running it appends registrations; that's the design — combined with the "last wins" MS DI behaviour, hand-written `AddXxx` calls later in the pipeline override the scan.
- `nullable enable` is on at the solution level (`Directory.Build.props`) and `TreatWarningsAsErrors=true`, so every new file must respect nullable annotations and avoid unreferenced private members.
- `MohistDbFixture` overrides several services after `ConfigureMohistServices` via `RemoveAll<...>` + `AddSingleton` — this pattern continues to work unchanged because scanning sits underneath those overrides.
- `AddMohistServerCore(EmptyConfig())` is safe to call from a unit test even though it runs the full pipeline: `BuildServiceProvider()` is lazy, so no DB connection / hosted service / `IHttpClientFactory` is opened unless the test actually resolves a service that needs them. Use this in the "production entry" test to exercise the real `ConfigureMohistServices` path.
- `ServiceCollection.Last(d => d.ServiceType == T)` is the right way to assert which lifetime "wins" when an explicit hand-written registration follows the scan; `Single` would throw because both descriptors remain in the collection.
- **MohistDbFixture.Services does not expose IServiceCollection.** Once `BuildServiceProvider()` is called inside `InitializeAsync`, the original collection is gone — the fixture only exposes `IServiceProvider`. For descriptor-level assertions use a fresh `new ServiceCollection()` + `AddMohistServerCore(...)`; for resolution-level assertions use `_fixture.Services.CreateScope().GetService<T>()`.
- **A migrated service may depend transitively on services the fixture doesn't host.** e.g. `AgentSessionResolver` needs `IGrainFactory` (Orleans silo — fixture has none) and several Scoped services transitively need `ConfigService` which needs `IConfiguration`. The fixture adds `IConfiguration`/`ILogging`/`IEnvironmentVariableProvider` itself, so resolution works there, but a bare `AddMohistServerCore(EmptyConfig())` test provider does NOT include those — only inspect descriptors, don't resolve, unless the test sets up the same services the fixture sets up.

## Decisions made in T-001

- `AsSelf()` only (no `AsMatchingInterface`); the candidate set is all concrete self-registrations and this matches current behaviour exactly.
- A type implementing both `IScopedService` and `ISingletonService` is allowed at T-001; the scan will register it twice (once Scoped, once Singleton) and the LAST `AddMohistConventionalServices` segment wins. We will revisit this in T-002/T-003/T-004 if any real domain type ever lands in that state; for now it is "fail-loud at review", not "fail-fast at scan". This matches the task's `notes` field.
- Scan call sits at the very top of `ConfigureMohistServices`, before `ResolveSqliteConnectionString` / `AddDbContextFactory<…>` and before every other `services.AddXxx` line.

## T-001 outcome

- `Directory.Packages.props` + `Mohist.Server.csproj` now reference `Scrutor 7.0.0`.
- `IScopedService` / `ISingletonService` empty marker interfaces added under `Mohist.Server.Infrastructure.Hosting`.
- `ServiceCollectionExtensions.AddMohistConventionalServices` is the new scan entry point, scanning `typeof(MohistServiceRegistration).Assembly` only.
- `ConfigureMohistServices` calls `AddMohistConventionalServices` as its first line, before every other registration.
- 5 new tests in `Specs/Foundation/ConventionalRegistrationSpecs.cs` (all passing) — three of them map 1:1 onto the acceptance criteria's bullet points; the other two (singleton lifetime + Scoped-vs-Scoped cross-scope identity) are extra defence for the lifetime mapping.
- `npm test` (server): 2024 passed, 10 skipped, 0 failed. `npm run build` (Mohist.sln): succeeded, 0 warnings, 0 errors.

## Decisions made in T-002

- Migration domain: split per the task description. T-002 = core Issue/Agent/Epic/Project/Label/Sessions; T-003 = Workflow; T-004 = Runner/System/Artifact. Serialise across `MohistServiceRegistration.cs` so each task's diff is conflict-free.
- Marker goes on the concrete class declaration: `public class X : IScopedService` (or `ISingletonService`). One marker per service — no service in T-002 implements both, so the "fail-loud at review" rule from T-001 stays informal and we don't add a runtime check.
- The fixture's resolution semantics must remain identical to the pre-migration state. Because the fixture is the same `ConfigureMohistServices` entry point that production uses (per `Codebase Patterns`), adding the marker + removing the hand-written line is by construction equivalent — same descriptor, same lifetime, same implementation type.

## T-002 outcome

- 14 core domain services migrated:
  - Singleton: `ProjectQuerier`, `ProjectRefResolver`, `IssueRepositoryResolver`, `SystemLabelDefinitions` — all marked `ISingletonService`.
  - Scoped: `IssueIdentityResolver`, `IssueQuerier`, `AgentQuerier`, `EpicQuerier`, `LabelCatalogService`, `IssueWorkflowProfileRegistry`, `IssueTemplateRegistry`, `AgentSessionQuery`, `AgentSessionQuerier`, `AgentSessionResolver` — all marked `IScopedService`.
- 14 corresponding `services.AddScoped/AddSingleton<T>()` lines removed from `MohistServiceRegistration.ConfigureMohistServices`. Interface mappings (e.g. `IStateStore<Issue>→IssueStore`, `IAgentSessionStore→AgentSessionStore`) and factory registrations left untouched as required.
- Unused `using` directives in `MohistServiceRegistration.cs` cleaned up (`Mohist.Server.Epic.Grains`, `Mohist.Server.Project.Services`, `Mohist.Server.Agent.Services`, `Mohist.Server.Issue.Services.WorkflowProfiles`, `Mohist.Server.Issue.Services.IssueTemplates`, `Mohist.Server.Sessions.Services`, `Mohist.Server.Workflow.Services.Sessions`, `Mohist.Server.Label.Services`, `Mohist.Server.Workflow.Domain.Run`, `Mohist.Server.Infrastructure.Data.Workflow.Prompts`, etc.). The `using Mohist.Server.Infrastructure.Data;` was re-added because `IStateStore<>` lives there and the interface mappings still need it.
- 1 new test file `Specs/Foundation/MigratedDomainServicesRegistrationSpecs.cs` with 41 cases driven by `Theory`/`MemberData` over the 14 services:
  - **Production-entry descriptor assertion**: for each service, asserts the only descriptor is `ServiceType == ImplementationType == T` with the expected lifetime, and that the last descriptor agrees. Catches "forgot the marker" / "added marker but kept the hand-written line and the lifetime flipped".
  - **Fixture-resolution + cross-scope identity**: resolves each service from `MohistDbFixture` via `CreateScope().GetService<T>()` and asserts Singleton-vs-Scoped cross-scope behaviour (`Assert.Same` for Singleton, `Assert.NotSame` for Scoped). `AgentSessionResolver` is excluded because it needs `IGrainFactory` (no silo in this fixture — use `WorkflowGrainFixture`).
  - **Fixture-vs-production agreement**: for each service, asserts the descriptor-level expectation AND resolves from the fixture when possible to confirm type identity. Covers the acceptance criterion "MohistDbFixture 与生产对已迁移服务的解析结果一致".
- `dotnet build Mohist.sln`: succeeded, 0 warnings, 0 errors.
- `npm test` (server): 2065 passed, 10 skipped, 0 failed (T-001 baseline 2024 → 2065 = +41 new cases).

## Decisions made in T-003

- Same pattern as T-002: marker goes on the concrete class declaration. None of the 8 Workflow services implement both markers, so the "fail-loud at review" rule from T-001 remains informal.
- `WorkflowDispatchBuilder` lives in `Mohist.Server.Workflow.Grains` namespace (next to the `IWorkflowGrain` / `WorkflowGrain` types it builds dispatches for), unlike its six siblings in `Mohist.Server.Workflow.Services` — that asymmetry pre-dated T-003 and is preserved. The marker is added in its existing namespace.
- `WorkflowSessionHealthService` is a `sealed` class with a **primary constructor** (C# 12). The marker goes on the primary-constructor declaration: `public sealed class WorkflowSessionHealthService(...) : IScopedService`. The `WorkflowDispatchBuilder` primary constructor is treated the same way.
- `WorkflowSessionHealthService` depends on `IGrainFactory` (Orleans) at construction time, so `MohistDbFixture` (no silo) cannot resolve it — same situation as `AgentSessionResolver` from T-002. Skip it in the `ResolvableThroughDbFixture` data set; cover it with descriptor-only assertions + production-vs-fixture agreement (descriptor level only, not resolution).
- Workflow domain interface mappings (`IWorkflowRunStore→WorkflowRunStore`, `IPromptLoader→FilePromptLoader`, `IEventStore→EventStore`, `IWorkflowBacklogDirectory→InMemoryWorkflowBacklogDirectory`) stay hand-written and unchanged — the task explicitly excludes them from this batch.

## T-003 outcome

- 8 Workflow domain services migrated:
  - Singleton: `PromptTemplateEngine` — marked `ISingletonService`.
  - Scoped: `WorkflowSessionHealthService`, `WorkflowActivityQuerier`, `WorkflowQuerier`, `WorkflowProfileManager`, `WorkflowDispatchBuilder`, `ProjectWorkflowProfileManager`, `IssueWorkflowProfileManager` — all marked `IScopedService`.
- 8 corresponding `services.AddScoped/AddSingleton<T>()` lines removed from `MohistServiceRegistration.ConfigureMohistServices` (one `AddSingleton<PromptTemplateEngine>` + seven `AddScoped<...>`). Interface mappings (`IWorkflowRunStore`, `IPromptLoader`, `IEventStore`, `IWorkflowBacklogDirectory`) and factory registrations left untouched as required.
- Unused `using Mohist.Server.Workflow.Services;` removed from `MohistServiceRegistration.cs` (all 7 services that needed it are now marker-discovered). `Mohist.Server.Workflow.Grains`, `Mohist.Server.Workflow.Services.Prompts`, `Mohist.Server.Workflow.Services.Artifacts` are still imported because of `IssueWorkflowReconciliationService` / `InMemoryWorkflowBacklogDirectory` / `IPromptLoader` / `FilePromptLoader` / `WorkflowArtifactBindService` / `WorkflowArtifactQuerier`.
- 1 new test file `Specs/Foundation/MigratedWorkflowServicesRegistrationSpecs.cs` with 23 cases driven by `Theory`/`MemberData` over the 8 services:
  - **Production-entry descriptor assertion** (8 cases): identical shape to T-002's first test method. Each service has exactly one descriptor where `ServiceType == ImplementationType == T`, lifetime matches, and the last descriptor agrees.
  - **Fixture-resolution + cross-scope identity** (7 cases — `WorkflowSessionHealthService` excluded because it needs `IGrainFactory`): same shape as T-002's second test method.
  - **Fixture-vs-production agreement** (8 cases): same shape as T-002's third test method. `WorkflowSessionHealthService` is short-circuited at the descriptor level (production-vs-fixture agreement on the registration shape) without a fixture resolution.
- `dotnet build Mohist.sln`: succeeded, 0 warnings, 0 errors.
- `npm test` (server): 2088 passed, 10 skipped, 0 failed (T-002 baseline 2065 → 2088 = +23 new cases; the 8 services × 3 methods = 24, minus 1 because `WorkflowSessionHealthService` is excluded from the resolution test data set). 467 Workflow SUT tests pass independently. T-001 (5) + T-002 (41) + T-003 (23) = 69 conventional-registration cases all green.


## Decisions made in T-004

- Same marker-on-class pattern as T-002/T-003. None of the 11 services in this batch implement both `IScopedService` and `ISingletonService`, so the "fail-loud at review" rule from T-001 continues to be informal.
- `RuntimeBuildInfo` is registered as itself (via `ISingletonService` marker) AND has a hand-written `services.AddSingleton<IRuntimeBuildInfo>(sp => sp.GetRequiredService<RuntimeBuildInfo>())` factory forwarding to it. This stays — it's the explicit "interface→implementation" mapping the design reserves for hand-writing, just like `IStateStore<Issue>→IssueStore` from T-002. The factory's `GetRequiredService<RuntimeBuildInfo>()` resolves the scanner-registered Singleton, so the same instance is returned for both keys — no duplicate instance, no drift.
- `WorkflowArtifactUploadService` has TWO public constructors:
  1. `(IDbContextFactory, IWorkflowArtifactStorage, IGrainFactory, ILogger)` — chains to the second ctor with a fresh `WorkflowGrainWorkContextResolver(grains)`.
  2. `(IDbContextFactory, IWorkflowArtifactStorage, IWorkflowArtifactUploadWorkContextResolver, ILogger, TimeProvider, TimeSpan)` — the "real" one.
  `IWorkflowArtifactUploadWorkContextResolver` is **not** registered in `ConfigureMohistServices` (only `WorkflowGrainWorkContextResolver` is constructed inline by ctor 1). MS DI therefore picks ctor 1, which transitively needs `IGrainFactory`. That makes `WorkflowArtifactUploadService` unresolvable through `MohistDbFixture` (no silo). The same applies to `AgentJobArtifactUploadService` (its only ctor takes `IGrainFactory` directly) and `RunnerStatusService` (takes `IGrainFactory` directly). All three are grain-blocked from the fixture resolution test data set, the same way `AgentSessionResolver` (T-002) and `WorkflowSessionHealthService` (T-003) are.
- `SystemUpdateService` is interesting: it has two ctors as well. The "default" one (`(SystemInfoService, ISystemUpdateStore, ISystemUpdateCommandRunner, ISystemReadinessProbe, IConfiguration, IEnvironmentVariableProvider, ILogger)`) is fully DI-resolvable through the fixture — every interface it depends on is registered in production (and the fixture adds `IConfiguration` / `ILogging` / `IEnvironmentVariableProvider` itself). The internal ctor `(Func<...>, ISystemUpdateStore, ISystemUpdateCommandRunner, ISystemReadinessProbe, IConfiguration, IEnvironmentVariableProvider, ILogger)` takes a `Func<>` MS DI cannot resolve, so MS DI falls through to ctor 1. Verified by the resolution test that `SystemUpdateService` resolves from `MohistDbFixture`.
- Cleaned `MohistServiceRegistration.cs` usings: removed `Mohist.Server.Infrastructure.Config` (was for `ConfigService`), `Mohist.Server.Runner.Services` (was for `RunnerStatusService`). Kept `Mohist.Server.Runner.Services.SignalR` because `RunnerWorkspaceClient` (interface mapping) still references it; kept `Mohist.Server.Issue.Services` for `IssueWorkflowReconciliationService`, `Mohist.Server.Issue.Services.Attachments` for `AttachmentCleanupService`, `Mohist.Server.Workflow.Services.Artifacts` for `WorkflowArtifactBindService` / `WorkflowArtifactQuerier`. `Mohist.Server.SystemInfo` stays because `RuntimeBuildInfo` is still referenced in the `IRuntimeBuildInfo` factory. Other usings unchanged from T-003.

## T-004 outcome

- 11 Runner/System/Artifact services migrated:
  - Singleton (7): `ConnectionSubscriptionRegistry`, `ConfigService`, `RuntimeBuildInfo`, `SystemdInstallDetector`, `SystemInfoService`, `SystemUpdateService`, `RunnerConnectionTracker` — all marked `ISingletonService`.
  - Scoped (4): `AttachmentService`, `WorkflowArtifactUploadService`, `AgentJobArtifactUploadService`, `RunnerStatusService` — all marked `IScopedService`.
- 11 corresponding `services.AddScoped/AddSingleton<T>()` lines removed from `MohistServiceRegistration.ConfigureMohistServices`. Preserved: `IRuntimeBuildInfo` factory forwarding, `IFileSystem` / `IEnvironmentVariableProvider` / `IGitSourceInspector` / `IServiceStatusChecker` / `ISystemUpdateStore` / `ISystemUpdateCommandRunner` / `IAttachmentStorage` / `IWorkflowArtifactStorage` / `IRunnerWorkspaceClient` / `IWorkflowArtifactBindService` / `IWorkflowArtifactQuerier` / `IWorkflowBacklogDirectory` / `IPromptLoader` / `IEventStore` / `IWorkflowRunStore` / `IAgentSessionStore` / `IAgentSessionTranscriptStore` / `IStateStore<…>` / `IUserNotificationDispatcher` / `ITranscriptEventPublisher` interface mappings (from T-002/T-003 too), `AddHttpClient<ISystemReadinessProbe, HttpSystemReadinessProbe>` configuration, all `Configure<…Options>` bindings (`WorkflowArtifactStorageOptions`, `AttachmentStorageOptions`, `AgentJobOptions`), `AddHostedService<IssueWorkflowReconciliationService>` / `AddHostedService<AttachmentCleanupService>`, `AddCloudEventBus` / `AddCloudEventHandlersFromAssembly` / `AddSignalR`, `IGitService` factory with `runnerRoot`, `AddDbContextFactory<MohistDbContext>`. `ConfigureMohistServices` body is now ~50 lines (was ~80 in T-003 baseline) and contains zero "scannable concrete self-registration" lines.
- Unused using directives in `MohistServiceRegistration.cs` cleaned up: `Mohist.Server.Infrastructure.Config`, `Mohist.Server.Runner.Services`.
- 1 new test file `Specs/Foundation/MigratedRunnerSystemArtifactServicesRegistrationSpecs.cs` with 30 cases driven by `Theory`/`MemberData` over the 11 services:
  - **Production-entry descriptor assertion** (11 cases): same shape as T-002/T-003. Each service has exactly one descriptor where `ServiceType == ImplementationType == T`, lifetime matches, and the last descriptor agrees.
  - **Fixture-resolution + cross-scope identity** (8 cases — `WorkflowArtifactUploadService` / `AgentJobArtifactUploadService` / `RunnerStatusService` excluded because they all transitively need `IGrainFactory`): same shape as T-002/T-003.
  - **Fixture-vs-production agreement** (11 cases): same shape as T-002/T-003. The 3 grain-blocked types short-circuit at the descriptor level (production-vs-fixture agreement on the registration shape) without a fixture resolution.
- `dotnet build Mohist.sln`: succeeded, 0 warnings, 0 errors.
- `npm test` (server): 2118 passed, 10 skipped, 0 failed (T-003 baseline 2088 → 2118 = +30 new cases; the 11 services × 3 methods = 33, minus 3 because the grain-blocked types are excluded from the resolution test data set). T-001 (5) + T-002 (41) + T-003 (23) + T-004 (30) = 99 conventional-registration cases all green. All 4 spec acceptance criteria pass.
- All scannable concrete self-registrations are now migrated. The remaining `ConfigureMohistServices` lines are exclusively:
  - Interface→implementation mappings (`IStateStore<Issue>→IssueStore`, `IWorkflowRunStore→WorkflowRunStore`, etc., ~10 lines).
  - Factory delegates / forwarding (`IRuntimeBuildInfo`, `IGitService`, `IStateStore<AgentSession>`, ~4 lines).
  - Configuration bindings (`WorkflowArtifactStorageOptions`, `AttachmentStorageOptions`, `AgentJobOptions`, `IStateStore<…>`, ~4 lines).
  - `AddHttpClient<ISystemReadinessProbe, …>`, `AddHostedService<…>`, `AddCloudEventBus`, `AddSignalR`, `AddDbContextFactory<MohistDbContext>`, ~5 lines.
- Net line count for `ConfigureMohistServices` body: 80 → 50 (~37% reduction since T-001). The "concrete self-registration" pattern that motivated the issue no longer exists in this entry point.

## Verify check (in-progress)

After T-004, ran the verify commands:

- `npm run build` (server dotnet build Mohist.sln) — **PASS** (0 warnings, 0 errors).
- `npm test` (server dotnet test) — **PASS** (2118 passed, 10 skipped, 0 failed).
- `npm run typecheck -w packages/web` — **PASS**.
- `npm run test:run -w packages/web` — **FAIL**: 6 failing tests.
- `npm run typecheck -w packages/runner` and `npm test -w packages/runner` — **not yet run**.

### Failing web tests (6 total)

1. **`src/shared/ui/markdown-reader/MarkdownReader.test.tsx`** (4 failures)
   - `does not produce page-level horizontal overflow for a long code line`
   - `collapses long content in collapsible mode, expands, and collapses again`
   - `does not render an Expand control in collapsible mode for short content`
   - `never renders a Reader-level collapse control in full mode`
   - All four fail with `TypeError: Cannot delete property 'ResizeObserver' of #<Object>` thrown from `teardownResizeObserver` (test file line 56): `delete (globalThis as …).ResizeObserver`. The test installs `ResizeObserver` via `setupResizeObserver` (`globalThis.ResizeObserver = spy`) and tears it down with `delete`. In strict mode (vitest `globals: true` runs files as ES modules by default in this setup) `delete` of a non-configurable own property throws `TypeError`. Earlier in the suite `tests/setup.ts` installs `ResizeObserver` via `Object.defineProperty(window, 'ResizeObserver', { writable: true, value: ResizeObserverPolyfill })` without `configurable: true`, but the test installs a new value via plain assignment which should make it configurable — however the actual failure reproduces only on certain code paths. Next session must investigate why `delete` throws (likely because setup.ts property descriptor lacks `configurable: true` and survives across tests via the `jsdom` environment). Fix options: (a) install the polyfill with `configurable: true` in `tests/setup.ts`; (b) change the test setup to not `delete` but reassign to a no-op / use `beforeEach`/`afterEach` to restore the prior descriptor via `Object.defineProperty`. Prefer (a) at the root cause because the failing tests already assume the property is configurable.

2. **`tests/AgentSettingsSection.test.tsx`** (1 failure)
   - `renders field-level validation errors inline and does not toast them`
   - Error: `expected 'text-xs text-red-600' to contain 'text-red-700'`. Test expects the inline validation error to use Tailwind class `text-red-700` but the component currently renders `text-red-600`. Next session: inspect the component's error rendering and update either the component (use `text-red-700`) or the test (accept `text-red-600`) — prefer fixing the component if the design calls for stronger contrast.

3. **`src/entities/issue/lib/completion-snapshot.test.ts`** (1 failure)
   - `returns the {completed, failed, new} shape derived from useIssues()` — exact assertion mismatch not yet captured; need to inspect test source and `completion-snapshot` lib in next session.

### Next steps

1. Fix `tests/setup.ts` `ResizeObserver` polyfill: add `configurable: true` to the `Object.defineProperty` descriptor (and probably `enumerable: false` for hygiene). Re-run `MarkdownReader.test.tsx` to confirm all 4 pass.
2. Inspect `AgentSettingsSection` component → adjust error class to `text-red-700` (or update test if `text-red-600` is the correct design value).
3. Inspect `src/entities/issue/lib/completion-snapshot.test.ts` and `completion-snapshot.ts` → reconcile `{completed, failed, new}` shape mismatch.
4. Re-run `npm run test:run -w packages/web` to confirm green.
5. Run `npm run typecheck -w packages/runner` and `npm test -w packages/runner` to confirm runner suites are clean.
6. Update `progress.txt` with final tally and bump `openspec/changes/issue-226/tasks.json` if any T-004 test counts change as a side effect.
