# issue-443 progress

## Task T-001: Implement structured Workflow Action output end to end

## Codebase Patterns

- Runner `ActionResult.output` was `string | null`; converted to/from JSON at every consumer.
- `WorkItemResult.output` was `string | null` and was JSON.stringified over the runner report HTTP wire.
- Server `WorkResult.Output: string?`, `TaskReport.Output: string?`, `RunnerReportRequest.Output: string?`.
- `WorkflowWorkLifecycle.ParseOutputToJsonElement` wrapped unparseable strings as JSON strings; now removed.
- `WorkflowStatusMapper.MapTasks` re-serialized `t.Output` to a string via `JSON.Serialize(t.Output.Value)`; now passes the element directly.
- `WorkflowItemTranslator.TranslateTaskResultAsync` passed `result.Output` (string) to `TaskReport.Output`; now validates object-or-null and canonicalizes JSON null.
- `WorkflowItemTranslator.TranslateChecksResult` used `WorkflowDispatchHelpers.ParseCheckResults(result.Output)` which expected a JSON-string of an array; now consumes `JsonElement?`.
- Server `WorkResult` carries `Output: JsonElement?`.
- `AgentJobExecutor.projectTurnToWorkItemResult` JSON.stringified an object; now returns the object directly.
- `core/process` now returns `{ stdout: trimmed, exitCode: number }`.
- `TaskRun.Output` was already `JsonElement?`. We now assign directly without `ParseOutputToJsonElement`.
- Web `WorkflowTimelineTask.output` is to become `Record<string, unknown> | null`.
- `parseTimelineTaskOutput` and `parseTaskOutput` are to be removed.
- `pr-delivery.ts: parseJson` accepts strings or objects — kept for backward test compat with object-valued outputs.
- `recovery.ts: safeParseObject` reads `result.output` as string — now consumes structured output directly.

## Runner changes done

- Core types: `ActionResult.output: JsonObject | null`. `WorkItemResult.output: JsonValue | null`.
- `action-result.ts`: `succeed(output: JsonObject | null)`. New `validateActionOutputShape(output)` rejects strings, arrays, scalars, cyclic objects, non-finite numbers, undefined/function/bigint with the existing `unexpected-error` code and an actionable message.
- `executor.ts`: validates action output at the boundary, produces `failAction(...)` with `unexpected-error` before completion/recovery/setVars when the shape is invalid. `projectTaskOutput` preserves structured output; `mohist/opencode` projects a `{ promise }` object. Surfaces `result.output` directly.
- `check-execution.ts`: emits the structured result-row array (`JsonValue`), with passing rows carrying object-or-null `output`. Validates each row's Action output shape and reports `unexpected-error` per invalid check.
- `set-vars.ts`: traverses the `ActionResult.output` object directly; missing paths and null output fail the task atomically (no partial patch).
- `recovery.ts`: `failureContext.output` is now the structured value (object-or-null), no `safeParseObject`.
- `artifact-capture.ts`: `actionProducedArtifacts` reads the structured output object directly.
- `output-capture.ts`: traverses the structured output object.
- All built-in Action producers return objects directly: `core/process` → `{ stdout, exitCode }`; `core/script`, `core/artifact-exists`, `core/marker`, `merge-ready`, `core/process`, `mohist/push`, `mohist/rebase`, `mohist/rebase-status`, `mohist/openspec-*`, `mohist/archive-change`, `mohist/create-github-pr`, `mohist/mark-github-pr-ready`, `mohist/merge-github-pr`, `mohist/github-pr-status`, `mohist/opencode`, `mohist/workspace-prepare` — no more `JSON.stringify` of public success output.
- `AgentJobExecutor.projectTurnToWorkItemResult` returns the object directly. AgentJob keeps the non-Action domain shape but its `terminalOutput` is now serialized via `GetRawText()` at the AgentJob boundary so its existing terminal-output contract is preserved.

## Server changes done

- `RunnerReportRequest.Output: JsonElement?`.
- `WorkResult.Output: JsonElement?`.
- `TaskReport.Output: JsonElement?`.
- `TaskStatusView.Output: JsonElement?`.
- `WorkflowItemTranslator.TranslateTaskResultAsync`: validates object-or-null via `TryCanonicalizeTaskOutput`, canonicalizes JSON null → nullable null, and on an invalid shape creates a durable failed TaskReport (`unexpected-error`) with a clear message so the runner can retire the work.
- `WorkflowItemTranslator.TranslateChecksResult`: passes `result.Output` (JsonElement?) directly to `ParseCheckResults`.
- `WorkflowDispatchHelpers.ParseCheckResults(JsonElement?)`: reads the structured array directly (no JSON parse of a string).
- `WorkflowWorkLifecycle`: removes `ParseOutputToJsonElement` and assigns `report.Output` directly to `currentTask.Output`.
- `WorkflowStatusMapper.MapTasks` returns the JsonElement directly via `MapTaskOutput`, mapping historical non-object values to `null`.
- `WorkflowRun.Approval.ResolveFeedback` adds `ResolveFeedbackSummary(feedbackTasks, reportOutput)` which only interprets `core/process` task's `output.stdout` as text. Other Actions and any non-object output produce null summary; the feedback still resolves normally.
- `AgentJobGrain.FailureCategoryFrom(JsonElement?)` reads the failureCategory property from a JsonElement root, no string parsing.
- AgentJobTerminalResult.Output stays string; the Agent adapter serializes inbound object/array JsonElement via `GetRawText()` so the AgentJob domain boundary continues working.

## Tests updated

- `runner/tests/*`: 84+ tests updated from `JSON.parse(result.output ?? "{}")` to `result.output as Record<string, unknown>`. Action mocks now return `{ done: true }`, `{ ok: true }`, etc. instead of strings. `set-vars.test.ts` adds new tests for null output and structured core/process projection. `executor-cleanup.spec.ts`, `executor-recovery.spec.ts`, `executor-task-log.spec.ts`, `executor-completion.spec.ts`, `executor-artifacts.spec.ts`, `check-verdict.test.ts`, `executor-workspace-boundary.spec.ts`, `worktree-cleanup-delivery.spec.ts`, `agent-job-executor.spec.ts`, `merge-ready.spec.ts`, `push.spec.ts`, `github-pr-status.spec.ts`, `merge-github-pr.spec.ts`, `workspace-prepare-workflow.spec.ts`, `workspace-prepare.spec.ts`, `openspec-archive-change.spec.ts`, `artifact-capture.spec.ts` updated to use structured output objects.
- `server/tests/*`: bulk replace of `Output: "..."` (string) with `Output: JSON.DeserializeElement("...")` for spec helpers. `WorkflowGrainSpecs.ReportChecksAsync` and `FeedbackDispatchSpecs` convert to JsonElement; the latter now drives feedback via structured object + expects null summary (default mohist/opencode feedback task gets no text adapter summary).
- `server/UnitTests/Workflow/Domain/ApprovalFeedbackTests`: ResolveFeedback now takes `JsonElement?`; tests pass JSON-serialized strings for the legacy `reportOutput` path.

## Verification status

- Initial `verify.1` failed with two runner regressions, eight server specs, and the C# test-size ratchet.
- Root causes were a stale structured-output assertion, four GitHub PR Action builders dropping their established `output` field, check-report fixtures still sending serialized arrays, invalid scalar JSON fixtures, an AgentJob HTTP fixture still sending encoded JSON text, and verbose test migrations exceeding existing size allowances.
- Restored the four Action-owned `output` fields and added direct object assertions for create/status/ready/merge outputs.
- Migrated Web timeline/task output to `Record<string, unknown> | null`, removed task-output and PR-delivery JSON parsing fallbacks, and added structured process/PR/null display coverage in WorkflowView and TaskProgressPanel.
- Removed the obsolete string-output implementation gap from `design/workflow/actions.md`.
- Focused runner verification passes: 81/81 tests across executor artifacts and all four GitHub PR Actions.
- Focused server verification passes: the C# size ratchet and all eight originally failing specs.
- Focused Web verification passes: typecheck and 70/70 tests across WorkflowView, TaskProgressPanel, PrDeliveryIndicator, and PR metadata extraction.
- `git diff --check` passes after focused verification.

## Pending work before this slice can be marked complete

- None.

## Decisions / gotchas

- The ActionResult type stays narrower (`JsonObject | null`) than the shared `WorkItemResult.output` (`JsonValue | null`) per the proposal — checks emit arrays, AgentJobs emit their own non-Action shapes.
- `ActionResult` validation runs immediately after `stripRunnerPrivateFacts` and before `evaluateCompletion`/`tryRecovery`/`captureAndUploadArtifactsForWork`/`applySetVarsForWork`. The runner keeps `output` on the failed result as `null` so recovery context doesn't accidentally match on an invalidated output.
- `AgentJobTerminalResult.Output` remains `string?` (domain contract). The AgentJob adapter converts inbound JsonElement object/array to raw text at the server-side AgentJob boundary so its terminal output keeps the existing shape without crossing the Workflow Action contract.
- Feedback summary extraction is one Action-specific adapter: only `core/process` task's `output.stdout` becomes the resolution summary. Every other Action yields null summary (still resolves the feedback).
- `capturedOutputs` is intentionally not wired into the server (Runner proposal explicitly says not); we keep the runner-local field to keep the Web StageStateRead path unchanged.

## Review fixes in progress

- The Action validator now requires a plain object or null at the root and rejects non-JSON object instances recursively.
- Invalid check output is removed from the public check-result envelope before report serialization, so cyclic values produce an acknowledged `unexpected-error` instead of a runner retry.
- Server task translation validates Action output before artifact binding. Malformed check envelopes become failed results for every dispatched check rather than throwing during property access.
- Focused server translator specs and runner validation tests pass after removing the internal invalid-output marker from the transport row.
- Structured-output UI assertions now live in focused `TaskProgressPanel.output.test.tsx` and `WorkflowView.output.test.tsx`; the original test files are below their size budgets and the focused Web suite passes.
- The server translator output regressions are split into `WorkflowItemTranslatorOutputSpecs.cs` so the existing translator spec stays below the C# 24 KB test-file ratchet.
- Final verification passes: `npm test` (including all server/CLI tests, 4,929 Web tests, and 1,161 runner tests), `npm run typecheck -w packages/runner`, `npm test -w packages/runner`, `npm run typecheck -w packages/web`, and `npm run test:run -w packages/web`.
