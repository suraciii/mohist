## Codebase Patterns

- The web project uses **Vitest + Testing Library + jsdom** with `// @vitest-environment jsdom` directive at the top of test files.
- For the agent module, the pattern is: tests live next to the file (`*.test.tsx`), mock the entity module with `vi.hoisted` + `vi.mock`, render inside `QueryClientProvider` + `ProjectProvider` + `MemoryRouter`/`Routes`.
- Widget-level components (e.g. `AgentProfileEditor`) are stubbed via `vi.mock('../../../widgets/.../AgentProfileEditor', () => ({ AgentProfileEditor: ({ open }) => open ? <div data-testid="agent-profile-editor" /> : null }))`. The stub mirrors the real testid so list/detail specs can assert open/close wiring without dragging in the real editor's query deps.
- `tsconfig.json` enables `noUnusedLocals: true`, so any local variable / hook call whose return is never used must be removed (not just imports). `useNavigate`/`useProjectPath` calls in a function body that no longer use their return trip this check — even though the import line is still consumed by sibling components in the same file.
- `AgentProfileEditor` is the universal "agent create + edit" Dialog widget. `agent={null}` is create mode (`isEditing=false`, `useCreateAgent`, navigates to detail page on success). `agent={...}` is edit mode. The create branch already invalidates `['agents']` (`queries.ts:78`) and `navigate`s on success — callers should NOT also navigate.

## T-001 (Wire /agents create entry points to open AgentProfileEditor dialog)

- File touched: `packages/web/src/pages/agent-list/ui/AgentListPage.tsx`
  - Added `useState` to React import, added `AgentProfileEditor` import.
  - Added `const [editorOpen, setEditorOpen] = useState(false)` to the `AgentListPage` body.
  - Removed the now-dead `const navigate = useNavigate()` / `const toProjectPath = useProjectPath()` calls in `AgentListPage` (they were only there to build `/agents/new`; the new path no longer needs them). `AgentRow` (same file) still uses both hooks, so the imports stay.
  - Repointed the header "New Agent" button (`data-testid="agent-list-create"`) and the empty-state "Create Agent" button (`data-testid="agents-empty-create"`) to `setEditorOpen(true)`.
  - Conditionally mount the editor: `{editorOpen && <AgentProfileEditor agent={null} open={editorOpen} onClose={() => setEditorOpen(false)} />}`. No `onSaved` — the editor's create-mode success contract is the sole driver of the post-create `navigate` and `['agents']` invalidation.
- File touched: `packages/web/src/pages/agent-list/ui/AgentListPage.test.tsx`
  - Added `fireEvent` to the `@testing-library/react` import.
  - Added a `vi.mock` for the editor widget with the standard lightweight stub exposing testid `agent-profile-editor`.
  - Added a `describe('create entry points', ...)` block with 3 specs:
    - editor is NOT in the DOM before any entry point is clicked,
    - header "New Agent" click surfaces the editor stub,
    - empty-state "Create Agent" click surfaces the editor stub.
  - No spec asserts a route change. The `MemoryRouter initialEntries={['/agents']}` setup means a stray `navigate('/agents/new')` would surface as a missing-route error in the test, but we don't have to assert that explicitly — the absence of any "navigated to" assertion is the regression guard, mirroring the `AgentDetailPage` spec pattern.

## Verification

- `npm run typecheck -w packages/web` passes (no dead imports / unused locals).
- `npm run test:run -w packages/web` passes: 259 files, 4080 tests, 1 skipped. The agent-list file goes from 6 → 9 tests.
- All 9 agent-list specs pass, including the 3 new create-entry-point specs.
