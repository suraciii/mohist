{
  "project": "mohist",
  "description": "为 Project 模型新增 baseBranch 字段，使 worktree 创建基于确定性的远程主干分支起点",
  "tasks": [
    {
      "id": "T-001",
      "title": "数据库 migration: 新增 base_branch 列",
      "description": "在 migrations.ts 中新增 migrateToVersion8，为 projects 表添加 base_branch TEXT DEFAULT 'main' 列。对已有项目遍历检测 baseBranch 并回填。SCHEMA_VERSION 升至 8。",
      "acceptanceCriteria": [
        "projects 表新增 base_branch 列",
        "已有项目的 base_branch 被自动检测并回填",
        "检测失败时回退为 'main'",
        "SCHEMA_VERSION 更新为 8",
        "npm run build 通过"
      ],
      "priority": 1,
      "passes": false,
      "notes": "检测逻辑: git symbolic-ref refs/remotes/origin/HEAD，解析出分支名"
    },
    {
      "id": "T-002",
      "title": "Project 模型和 Repo 层支持 baseBranch",
      "description": "更新 Project 接口新增 baseBranch 字段。更新 ProjectRow 映射。更新 ProjectRepo 的 create、findById、findAll 等方法支持 base_branch 列。ProjectRepo.update() 支持更新 baseBranch。",
      "acceptanceCriteria": [
        "Project 接口包含 baseBranch: string",
        "ProjectRepo.create() 写入 base_branch",
        "ProjectRepo.findById/findByName/findAll 返回 baseBranch",
        "ProjectRepo.update() 支持更新 baseBranch",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "T-003",
      "title": "提取 baseBranch 自动检测为独立函数",
      "description": "新建 src/git/detect-base-branch.ts，实现 detectBaseBranch(projectPath: string): Promise<string>。逻辑: git symbolic-ref refs/remotes/origin/HEAD → 解析分支名，失败返回 'main'。ProjectService.create 和 migration v8 共用此函数。",
      "acceptanceCriteria": [
        "detectBaseBranch 函数可独立调用",
        "有 origin 时正确解析默认分支",
        "无 origin 时返回 'main'",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "T-004",
      "title": "ProjectService 和 API 支持 baseBranch",
      "description": "更新 CreateProjectData 新增可选 baseBranch 字段。ProjectService.create() 中未指定时调用 detectBaseBranch 自动检测。API POST /api/projects 接受可选 baseBranch 参数。API GET /api/projects/:id 返回 baseBranch。API PATCH /api/projects/:id 支持更新 baseBranch。",
      "acceptanceCriteria": [
        "POST /api/projects 不指定 baseBranch 时自动检测",
        "POST /api/projects 指定 baseBranch 时使用指定值",
        "GET /api/projects/:id 返回 baseBranch 字段",
        "PATCH /api/projects/:id 可更新 baseBranch",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "T-005",
      "title": "CLI 新增 --base-branch 选项",
      "description": "mo project create 命令新增 --base-branch <branch> 可选参数。传递给 API 的 baseBranch 字段。mo project list 输出中显示 baseBranch。",
      "acceptanceCriteria": [
        "mo project create --base-branch develop 正确传递到 API",
        "mo project create 不指定 --base-branch 时由服务端自动检测",
        "mo project list 显示 baseBranch 列",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "T-006",
      "title": "WorktreeManager 基于 baseBranch 创建 worktree",
      "description": "修改 WorktreeManager.create() 签名新增 baseBranch 参数。创建前执行 git fetch origin。使用 git worktree add -b mo/issue-N origin/<baseBranch> 创建。无 origin 时回退到本地 baseBranch 分支。",
      "acceptanceCriteria": [
        "WorktreeManager.create() 接受 baseBranch 参数",
        "创建前执行 git fetch origin",
        "基于 origin/<baseBranch> 创建分支",
        "无 origin 时回退到本地 baseBranch",
        "本地 baseBranch 不存在时返回错误",
        "npm run build 通过"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "T-007",
      "title": "API 调用 WorktreeManager 时传入 baseBranch",
      "description": "修改 issues.ts 中 start issue 路由，从 project 对象读取 baseBranch 传入 WorktreeManager.create()。同样更新 resume、retry 等路由中涉及 worktreePath 的调用。",
      "acceptanceCriteria": [
        "POST /api/issues/:number/start 使用 project.baseBranch",
        "已有的 resume/retry 路径也使用 project.baseBranch",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    }
  ]
}
