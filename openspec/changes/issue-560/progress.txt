# issue-560 plan fixes (self-review dispositions)

Working through `self-review.md` must-fix findings. `self-review.md` itself is
NOT edited (regenerated by the next review).

## MF-2 — replay fingerprint cannot cover model/variant hints → FIX (extend, not narrow)

Verified against code before editing:

- `AgentLaunchCoordinatorRequest` (AgentLaunchCoordinatorTypes.cs, Id 0–14)
  has no model/variant fields; `AgentLaunchCoordinatorCodec.Fingerprint`
  folds Prompt/AgentRef/Runtime/WorkspaceName+Path/Issue/Epic/Repo/Title/
  Origin/TargetId/attachments/connection-origin only.
- `AgentLaunchCoordinatorGrain` stores `RequestFingerprint` on the plan at
  creation and recomputes + ordinal-compares on resume → a naive append to
  the canonical string would false-conflict every in-flight plan across the
  deploy. Fix therefore requires: hint block folds only when a model/variant
  hint is present, so no-hint requests (all definition-first launches) hash
  byte-identically to today.

Edits: design.md D2 (folding mechanism + why-narrowing-rejected), D11 (replay
conflict matrix + codec compatibility tests), agent-task-launch spec
conflicting-replay scenario (enumerate runtime/model/variant, add/change/
remove), tasks.json T-002 fingerprint criterion.

## MF-1 — AC3 (model recommendations + full-options entry) not covered → FIX (cover, not de-scope)

Verified against code before editing:

- Web: catalog-backed `ModelSelect` (shared/ui) + `useAvailableModelIds` /
  `useModelVariants` hooks already power the definition editor
  (AgentProfileEditor.tsx:77–80,235).
- CLI: `mo agent model list` exists (MohistCliCommands.AgentModel.cs).
- Catalog carries model ids + variants only (no per-purpose metadata) → the
  defensible bounded recommendation is the labeled Project default (the
  owner's statement of what tasks in this Project run on), with the catalog
  as the full-options entry on every path. Stated explicitly in proposal.

Edits: proposal.md (What Changes bullet + capability description), web spec
inline-execution requirement (catalog-backed, labeled recommendation, adjust
affordance; requirement anchor preserved, one scenario renamed), cli spec
exit-behavior (+ `mo agent model list` entry), design.md D9 + D10 + resolved
open question, tasks.json T-004 (criteria/notes) + T-005 (guidance).

## Status

- MF-2: FIXED (commit "fold model/variant hints into the replay fingerprint").
- MF-1: FIXED (this slice): cover AC3 via catalog-backed selection + labeled
  Project-default recommendation + full-options entry (web), `mo agent
  model list` guidance (CLI). Chose coverage over de-scope because every
  piece already exists in the codebase.
- Post-edit validation: tasks.json JSON valid; task graph acyclic; all 6
  spec anchors referenced by tasks resolve; 21 requirements each with
  scenarios; no stale references to renamed scenario slugs.
- Observations O-1..O-6 require no action per the task instructions.
- self-review.md untouched.
