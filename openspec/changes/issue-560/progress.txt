# issue-560 plan fixes (self-review dispositions)

Working through `self-review.md` must-fix findings. `self-review.md` itself is
NOT edited (regenerated by the next review).

## MF-2 — replay fingerprint cannot cover model/variant hints → FIX (extend, not narrow)

Verified against code before editing:

- `AgentLaunchCoordinatorRequest` (AgentLaunchCoordinatorTypes.cs, Id 0–14)
  has no model/variant fields; `AgentLaunchCoordinatorCodec.Fingerprint`
  folds Prompt/AgentRef/Runtime/WorkspaceName+Path/Issue/Epic/Repo/Title/
  Origin/TargetId/attachments/connection-origin only.
- `AgentLaunchCoordinatorGrain` stores `RequestFingerprint` on the plan at
  creation and recomputes + ordinal-compares on resume → a naive append to
  the canonical string would false-conflict every in-flight plan across the
  deploy. Fix therefore requires: hint block folds only when a model/variant
  hint is present, so no-hint requests (all definition-first launches) hash
  byte-identically to today.

Edits: design.md D2 (folding mechanism + why-narrowing-rejected), D11 (replay
conflict matrix + codec compatibility tests), agent-task-launch spec
conflicting-replay scenario (enumerate runtime/model/variant, add/change/
remove), tasks.json T-002 fingerprint criterion.

## MF-1 — AC3 (model recommendations + full-options entry) not covered → FIX (cover, not de-scope)

Verified against code before editing:

- Web: catalog-backed `ModelSelect` (shared/ui) + `useAvailableModelIds` /
  `useModelVariants` hooks already power the definition editor
  (AgentProfileEditor.tsx:77–80,235).
- CLI: `mo agent model list` exists (MohistCliCommands.AgentModel.cs).
- Catalog carries model ids + variants only (no per-purpose metadata) → the
  defensible bounded recommendation is the labeled Project default (the
  owner's statement of what tasks in this Project run on), with the catalog
  as the full-options entry on every path. Stated explicitly in proposal.

Edits: proposal.md (What Changes bullet + capability description), web spec
inline-execution requirement (catalog-backed, labeled recommendation, adjust
affordance; requirement anchor preserved, one scenario renamed), cli spec
exit-behavior (+ `mo agent model list` entry), design.md D9 + D10 + resolved
open question, tasks.json T-004 (criteria/notes) + T-005 (guidance).

## Status

- MF-2: FIXED (commit "fold model/variant hints into the replay fingerprint").
- MF-1: FIXED (this slice): cover AC3 via catalog-backed selection + labeled
  Project-default recommendation + full-options entry (web), `mo agent
  model list` guidance (CLI). Chose coverage over de-scope because every
  piece already exists in the codebase.
- Post-edit validation: tasks.json JSON valid; task graph acyclic; all 6
  spec anchors referenced by tasks resolve; 21 requirements each with
  scenarios; no stale references to renamed scenario slugs.
- Observations O-1..O-6 require no action per the task instructions.
- self-review.md untouched.

## T-001 progress notes

### Codebase patterns (verified)
- Domain arch rules (Mohist.Server.ArchTests/ArchitectureRules.cs): Agent
  domain may NOT reference Project domain (only Agent→Runner/Sessions allowed).
  Shared execution-config types therefore live in `Mohist.Server.Infrastructure`
  (ExecutionConfigHint/ResolvedExecutionConfig/ExecutionConfigResolver/ExecutionConfigJson)
  — same shared-kernel precedent as AgentConfigSchema/AgentExecutionSnapshot.
- The scoped default reader lives in `Infrastructure/Data/Project/`
  (DB-backed, no IProjectGrain call from Agent domain); DI via IScopedService
  convention scan, no manual registration needed.
- EF migrations: repo does NOT use raw dotnet-ef output. Recent migrations use
  file-scoped namespace, doc comment, and a Designer that delegates to
  `MohistDbContextModelSnapshot.BuildModelCore(modelBuilder)`. dotnet-ef
  (11 preview, only runtime installed) rewrites the whole snapshot — restore
  the snapshot from git and hand-add the column, hand-write migration+Designer.
  Migration timestamps are hand-assigned future dates; latest was
  20260907000000, so T-001's is 20260908000000_AddProjectDefaultExecutionConfig.
  Run dotnet-ef with `SkipWebBuild=true` env var (MSBuild property via env).
- CliFieldContractTests pins CLI ResourceOutputCatalog fields == server DTO
  serialization fields: adding a serialized property to ProjectInfo requires
  adding it to the ProjectList/Project field catalog in
  packages/cli/Mohist.Cli/ResourceOutput.cs ("defaultExecutionConfig" added).
- AuthResolutionTestSupport.cs has a hand-written raw Projects DDL that must
  gain the new column manually.
- SpecTests: `AgentSessionLaunchRoutesTestSupport` (CreateProjectAsync/
  CreateAgentAsync/LaunchAsync/RegisterRunnerAndAwaitOnlineAsync/PollDispatch*)
  is the base for launch specs; collection "MohistIntegration".

### Decisions
- MatchesCurrentDefinition compares resolved tuples on both sides under the
  same current default when the launch-time AgentConfig snapshot exists.
  Legacy AgentJobInput records without that snapshot use the persisted
  dispatch values only for fields explicitly supplied by the current Agent
  definition; default-resolved fields remain free so a Project-default change
  cannot flip a completed conclusion. New launches still snapshot
  AgentConfigJson, and config-less agents launched via a default keep their
  null snapshot semantics. Unit-test History helper snapshots AgentConfig for
  new-launch cases and covers the legacy fallback.
- Readiness StructuralGaps non-object AgentConfig now yields model-missing
  when no default (previously a corrupt string config produced no gap) —
  unreachable via write surfaces, acceptable tightening.
- AgentLauncher.ResolveDefinitionAsync folds the default ONLY when one is
  configured (zero behavior change otherwise) and extracts the definition
  hint from the launch's own `agent.AgentConfig` (raw runtime, variant kept
  without model) — NOT from the snapshot resolver's opencode-fallback output,
  so a default runtime can fill an absent definition runtime.
- LaunchConnectionAsync (Slack) untouched: its dispatch gate is
  AgentReadinessDeriver, a separate static helper unaffected by this change.
- Route write surface: PUT and PATCH /default-execution-config both map to
  replace-on-set (same closed field set); no clear operation in v1.
- Final refinement: `ProjectInfo.DefaultExecutionConfig` is explicitly
  serialized as `defaultExecutionConfig: null` when unset, matching the
  shared GET read contract instead of relying on the global null omission
  policy. Readiness uses resolved-tuple matching for snapshot-backed jobs and
  the documented legacy dispatch fallback for pre-snapshot jobs.
- Verification so far: focused server unit tests, the server build, and the
  server architecture/integration suites passed on the .NET 11 preview SDK.
- Compatibility follow-up: the first rebuilt full-spec run exposed that
  manually seeded/pre-feature AgentJobInput values may lack AgentConfig. The
  readiness matcher now uses the exact resolved tuple for new snapshots and a
  definition-field-only dispatch fallback for legacy snapshots. This keeps
  existing completed Agents Ready while Project-default-only fields remain
  unaffected by default changes. Full server specs pass 3938/3938 after the
  rebuild.

### Final verification

- `npm run test:fast` passed: typecheck, 2,693 server unit tests, 68
  architecture tests, 178 workflow tests, 1,848 CLI tests, 4,724 Web tests,
  and 1,639 Runner tests.
- The focused T-001 server suites and the full 3,938-test server spec
  assembly also passed. The implementation is committed on the workflow
  branch; the pre-existing `SpecUnitMigrationLedger.json` worktree change
  remains intentionally unstaged.

## T-004 progress notes

- The Web Project model consumes the server Project read/list `defaultExecutionConfig` field directly through `ProjectProvider.currentProject`; no second Project endpoint is needed because the server read surface already projects the nullable default.
- `startAgentTask` is an additive Agent entity client/mutation posting to `/agent-tasks` with `X-Mohist-Launch-Origin: web`; the composer owns one caller-generated idempotency key and keeps it across rejected/pending retries, clearing it only after accepted success.
- The composer keeps definition-first launches unchanged when an Agent is selected. The new create-new path sends no execution hints when using the Project default, sends Runtime/Model/optional Variant only after opening Adjust, and uses the shared `ModelSelect` with `useAvailableModelIds`/`useModelVariants` for catalog-backed choices.
- The existing profile editor displayed Agent descriptions but did not edit or persist them. T-004 adds the description field so the created Agent is fully refinable from the session/detail path; edits remain ordinary definition edits for later Jobs.
- Session refinement is exposed by a direct Agent link in the session header and by the attachment-results launch interstitial. The Agents empty state routes its primary action to the project-scoped `agent-sessions/new` route while retaining the editor as a secondary action.
- Focused verification after the slice: Web typecheck, FSD boundary check, test-boundary check, and 145 relevant tests passed.

## T-002 progress notes

- The task-first route is registered beside the Agent definition route and
  keeps the definition-first launcher as the only execution pipeline. It
  pre-mints Agent, Session, Input, and Turn identities from the Project/key
  ownership identity and adopts an existing active Agent at that deterministic
  id after a process crash.
- `AgentTaskDefinitionFactory.Build` is the pure derivation boundary. It
  materializes runtime/model/variant, includes reserved built-in names and all
  active/archived Agent names in case-insensitive disambiguation, and uses
  Unicode-rune truncation for prompt-derived names. `CreateAsync` supports
  excluding the deterministic adopted Agent from its occupied-name probe.
- Task replay must return response metadata from the first accepted attempt,
  not revalidate mutable attachments or recompute workspace defaults. The
  coordinator plan/result and `AgentLaunchResult` therefore carry append-only
  workspace/origin/target and full attachment verdict fields. No-hint
  coordinator fingerprints remain byte-compatible because the model/variant
  block is appended only when a hint is non-null.
- Implicit Web/CLI workspace materialization is intentionally after all
  determinable task validation and Agent definition adoption. The route still
  resolves the candidate workspace name before replay so the existing
  fingerprint contract is preserved, but a rejected task does not create a
  workspace as a side effect.
- Focused verification: full Server UnitTests passed (2,700/2,700); the new
  `AgentTaskLaunchRoutesSpecs` passed 6/6; existing Agent API specs passed
  138/138, including the 8 launch idempotency cases. Server and SpecTests
  projects build cleanly. The architecture migration ledger was refreshed
  for the current Server test-source tree (859 files, digest
  `e0c4b9ac003ca8b91111d7c7197aff3962912da972fabf9130833cb57b9131e0`) and
  the stale FailIfMarker closure evidence; the architecture suite then passed
  68/68.

## T-005 progress notes

- `mo agent start` is registered beside definition-first `launch` and sends
  `POST /api/projects/{project}/agent-tasks` with only task-route fields. It
  preserves omitted execution hints so the Server can apply caller hint ->
  definition -> Project default precedence, and always sends
  `X-Mohist-Launch-Origin: cli`.
- Startup uses the same BodyInputResolver and attachment uploader as launch.
  Attachment-only tasks bypass the resolver's prompt-required branch and send
  an empty prompt with the accepted attachment ids; prompt and prompt-file
  conflicts, invalid runtime values, and malformed provider/model values fail
  locally before project resolution or HTTP.
- Table mode prints a generated idempotency key before the task request and
  renders `TableShape.AgentSessionLaunch`. Bare `--json` is task-first's raw
  Server-envelope mode; field subsets are rejected because this slice keeps
  JSON raw-only. Server rejection codes retain nonzero exits, with explicit
  guidance for unresolved execution configuration, model catalog discovery,
  conflicts, and pending retry using the same key.
- CLI coverage now includes help/flag matrix, context and hint forwarding,
  CLI-origin headers, attachment-only input, generated-key ordering, raw JSON,
  replay-key reuse, table identities, and actionable rejection guidance.
- Verification before the full gate: `npm run test:fast:cli` passed 1,859 tests;
  `npm run docs:check` passed. `npm run format` mechanically formatted 13
  already-committed T-004 Web files so the repository format gate is clean.
- Final `npm run verify` passed: docs and file-size checks, formatting, solution
  and workspace builds, 178 Workflow Definition tests, 2,700 Server unit tests,
  3,944 Server spec tests, 68 architecture tests, 1,859 CLI tests, 4,734 Web
  tests, 1,639 Runner tests, and 70 Slack tests.

## T-002 follow-up verification

- Re-ran the Server unit project after inspecting the existing T-002 slice:
  2,701 tests passed, including the new Unicode sentence-boundary case. The
  parallel full spec run had one unrelated dispatch-poll timing failure in
  `AgentSessionLaunchRuntimeResolutionSpecs`; its exact test rerun passed, and
  the prior T-002 baseline remains 3,944/3,944 specs. Japanese full stops now
  derive only the first sentence, preserving deterministic task naming for
  non-ASCII prompts.

## T-003 progress notes

- The coordinator plan uses append-only Orleans field `DefinitionCreatedByLaunch`
  at Id 52; the task-first command envelope carries the corresponding marker at
  Id 39. Missing fields deserialize false, so definition-first and older plans
  never archive their Agent.
- The task-first launcher passes the marker only after the definition is created.
  Abort convergence archives `IAgentGrain` before setting the terminal
  `RejectionReason`/`Completed` state. The pending abort payload retains the
  reason while archive recovery is incomplete, and repeated `ArchiveAsync` is
  idempotent across reminder replay.
- `launch_setup_pending` remains the non-terminal path: no abort completion or
  archive is invoked, so the active task-first Agent can recover with the same
  key. Terminal rejection and replay map to `launch_rejected`; definition-first
  terminal rejections use the same response mapping but leave their Agent active.
- Focused T-003 coverage passes: task terminal rejection archive + recorded
  replay, archived-name conflict and derived-name disambiguation, injected
  archive failure repaired by `ReceiveReminder`, 503 non-archival, and
  definition-first non-archival. The task route class passes 10/10, launch
  idempotency 8/8, spawn coordinator 3/3, and runtime-resolution regression
  10/10. The initial full regression had one unrelated timing poll failure;
  the exact runtime-resolution class passed on rerun.
- Final T-003 verification: `npm run test:fast` passed (typecheck, 2,701
  Server unit tests, 68 architecture tests, 178 workflow tests, 1,859 CLI
  tests, 4,734 Web tests, and 1,639 Runner tests); the complete Server spec
  assembly passed 3,948/3,948 and `npm run format:check` passed.

## T-004 follow-up verification

- Replaced the composer’s direct `crypto.randomUUID()` call with the shared
  fallback-safe `createIdempotencyKey()` helper; task-only retry coverage now
  proves the same caller key is retained after a failed response.
- Web typecheck, FSD boundaries, and test-boundary checks pass. Focused T-004
  coverage passes 153 tests; the full Web suite passes 4,735 tests.

## T-005 follow-up verification

- Added `start` to the progressive Agent command presentation so the task-first
  entry point remains visible in `mo agent --help`.
- Raw JSON startup output now writes accepted and rejected Server responses
  byte-for-byte without appending a CLI newline; focused tests assert exact
  response content and preserve actionable rejection guidance on stderr.
- `npm run test:fast:cli` passed with 1,860 tests and `npm run docs:check`
  passed before the full repository gate.

## T-002 follow-up repair

- The task route now declares its attachment rollback before the all-rejected
  `input_unusable` return and invokes it explicitly, keeping the determinable
  rejection path orphan-free even if attachment binding behavior changes.
- Added route coverage for per-file `NotFound` verdict projection and the
  no-Agent invariant on an all-rejected attachment-only task.
- `npm run test:fast` passed with 2,701 Server unit, 68 architecture, 178
  workflow, 1,860 CLI, 4,735 Web, and 1,639 Runner tests plus typechecks.
  The parallel 3,949-test spec assembly had two unrelated timing failures;
  the task route class passed 11/11 and both flaky tests passed in exact
  single-test reruns.

## T-003 verification follow-up

- Re-audited the existing T-003 slice: task-first envelopes alone set the
  append-only `DefinitionCreatedByLaunch` marker; terminal abort archives the
  Agent before persisting the recorded rejection, while setup-pending recovery
  leaves it active.
- The focused `AgentTaskLaunchRoutesSpecs` class passed 11/11, including
  terminal rejection replay, archived-name conflict/disambiguation, reminder
  recovery after injected archive acknowledgement failure, 503 non-archival,
  and definition-first non-archival.
- `npm run test:fast` passed: 2,701 Server unit, 68 architecture, 178
  workflow, 1,860 CLI, 4,735 Web, and 1,639 Runner tests plus typechecks.
  The full 3,949-test server assembly had one timing-sensitive polling failure
  in the unrelated runtime-resolution class; its exact rerun passed 10/10.

## T-004 conflict recovery follow-up

- Added a conflict-only `Start with a new launch key` action to the Web
  composer. It resets the caller key and mutation error while preserving the
  prompt, attachments, context references, Agent selection, and execution
  choices; pending and ordinary rejection retries continue using the same key.
- Composer coverage now proves a conflict can be recovered with a new key
  without losing the composed task. Focused composer and feedback tests pass
  40/40; Web typecheck, FSD boundaries, and test-boundary checks pass.

## T-005 final verification

- Audited the existing task-first CLI slice against the CLI spec: `start` has
  no Agent argument, forwards task/context/execution hints to `/agent-tasks`,
  sends the CLI origin header, prints the launch table shape, and keeps raw
  Server envelopes unchanged in JSON mode.
- `npm run test:fast:cli` passed 1,860 tests and `npm run docs:check` passed.
- The first full gate had one timing-sensitive dispatch poll failure in the
  unrelated runtime-resolution spec. The exact xUnit method passed in
  isolation, and the subsequent complete `npm run verify` passed: 3,949 Server
  specs, 2,701 Server unit tests, 68 architecture tests, 1,860 CLI tests,
  4,736 Web tests, 1,639 Runner tests, 70 Slack tests, and 178 workflow tests.

## T-001 audit follow-up

- `ProjectDefaultExecutionConfigReader` now caches by Project ID within its
  scoped lifetime, so a reused scope does not evict one Project's value when
  another Project is read. The reader spec covers first/second/first access
  and observes two SQL reads for two distinct Projects.
- The focused T-001 spec classes passed 19/19 after the cache change. The
  complete Server spec assembly passed 3,948/3,949; its only failure was the
  known timing-sensitive OpenCode dispatch poll, and the exact method passed
  in isolation. `npm run test:fast` passed with 2,701 Server unit tests, 68
  architecture tests, 178 workflow tests, 1,860 CLI tests, 4,736 Web tests,
  1,639 Runner tests, and all typechecks.

## T-002 completion verification

- Re-audited the task-first route, definition factory, coordinator fingerprint,
  deterministic Agent adoption, response replay projection, and the route/docs
  registration against the T-002 requirements. The implementation is already
  present on this workflow branch and no additional production change was
  required in this verification pass.
- The Server build passed with no warnings or errors. The focused factory/unit
  coverage passed as part of 2,701 Server unit tests.
- The complete Server spec assembly passed 3,949/3,950 tests; the one failure
  was the known timing-sensitive runtime dispatch poll outside T-002. The
  exact failing runtime-resolution test passed when rerun alone.
- `npm run test:fast` passed: typechecks, 2,701 Server unit tests, 68
  architecture tests, 178 workflow tests, 1,860 CLI tests, 4,736 Web tests,
  and 1,639 Runner tests.

## T-003 archive crash-window verification

- Corrected the coordinator's archive probe ordering so `IAgentGrain.ArchiveAsync`
  completes before the injected participant failure. Reminder replay now tests
  the real crash window after archival and before the terminal plan write;
  repeated archival remains idempotent and the recorded rejection is stable.
- The complete Server spec assembly passed 3,950/3,950 after the correction.
  The first parallel `npm run test:fast` attempt hit two unrelated Runner
  cleanup timing assertions; the isolated Runner suite passed 1,639/1,639,
  and the subsequent full `npm run test:fast` passed with all existing totals.

## T-004 verification audit

- Re-verified the committed task-first Web slice against every
  `web-agent-task-composer` requirement: task-only launch and stable retry key,
  selected-Agent definition-first regression, Project-default recommendation
  and adjusted catalog hints, no-default Runtime/Model/Variant catalog gating,
  actionable conflict/pending/execution-config feedback, empty-state routing,
  and Agent refinement links/edit surfaces.
- Focused Web verification passed 172 tests across the composer, Agent entity,
  empty state, Agent detail, session, and profile editor suites. Web typecheck,
  FSD boundaries, and test-boundary checks passed. The complete Web suite passed
  4,736 tests in 378 files.

## T-005 completion verification

- Re-ran `npm run test:fast:cli`: all 1,860 CLI tests passed, including the
  task-first flag matrix, generated-key ordering, raw JSON, launch identities,
  replay-key, and actionable rejection guidance cases.
- Re-ran `npm run docs:check`: all 92 active Markdown files passed.
- Re-ran the complete `npm run verify` gate: docs, file-size, format, solution
  and workspace builds, 178 Workflow Definition tests, 2,701 Server unit
  tests, 3,950 Server spec tests, 68 architecture tests, 1,860 CLI tests,
  4,736 Web tests, 1,639 Runner tests, and 70 Slack tests passed.

## Verification diagnosis

- Initial `git status` and `git diff` were clean; no intended task changes or
  verification side effects were present to classify.
- No failed issue-560 verification artifact was available in the repository or
  accessible current-run logs. A fresh `npm run verify` reproduction exited 0
  with the same complete suite green, so no implementation, test, or build
  defect was reproduced and no production code change was warranted.
- The fresh gate left no Git-visible changes; the completed issue work remains
  committed without a cleanup-only or assertion-weakening change.

## Verification repair

- A fresh `npm run verify` reproduced one Server Spec failure:
  `AgentSessionLaunchRuntimeResolutionSpecs.Launch_WithoutRuntimeOverrideOrConfig_DefaultsToOpenCode` failed at `PollDispatchForSessionAsync` because the helper performed one `/poll` request immediately after the durable assignment-prepared signal.
- The native xUnit method runner isolated the test and passed it, confirming a load-sensitive test-harness race rather than an implementation failure. The shared launch-spec helper now uses bounded `TestWait.ForAsync` polling for the existing dispatch, preserving all AgentJob, session, and dispatch assertions.
- Focused runtime-resolution coverage passed 10/10 and the full Server Spec assembly passed 3,950/3,950 after the repair. The final `npm run verify` gate also passed: 178 Workflow, 2,701 Server unit, 3,950 Server spec, 68 architecture, 1,860 CLI, 4,736 Web, 1,639 Runner, and 70 Slack tests, with docs, format, size, and builds clean.

## T-001 current-run audit

- The committed T-001 slice was re-audited against the selected task and the
  `agent-creation-defaults` requirements. The additive Project migration,
  replace-on-set grain method, closed PUT/PATCH routes, explicit nullable GET
  projection, scoped per-project DB reader, resolver precedence matrix,
  readiness matrix, definition-first launch resolution, and Configure an Agent
  documentation are all present; no production repair was required.
- Regression verification in this run passed: the focused Server UnitTests
  filter passed 2,701/2,701, the focused Project/default/readiness/launcher
  SpecTests filter passed 3,950/3,950, and the Server project build passed.
  The required `npm run test:fast` gate also passed: typecheck, 2,701 Server
  unit, 68 architecture, 178 workflow, 1,860 CLI, 4,736 Web, and 1,639 Runner
  tests.

## T-002 current-run audit

- The committed task-first route, deterministic definition factory, coordinator
  fingerprint extensions, deterministic Agent adoption, response replay
  projection, and task-first documentation were re-audited against
  `agent-task-launch` and the task-first creation requirements. No production
  repair was required in this run.
- The Server build passed. The available Microsoft Testing Platform runner
  executed all 2,701 Server unit tests and all 3,950 Server specification tests
  successfully; the task-first route, factory, replay, rollback, and
  coordinator-equivalence coverage is included in those totals. `git diff
  --check` was clean.
