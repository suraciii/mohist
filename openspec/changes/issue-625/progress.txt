# Progress: Issue 625 plan review fixes

- Reviewed the first self-review and mapped must-fix findings M-1, M-2, and M-3 to the plan artifacts.
- M-1 fixed: T-002 now explicitly forbids adding, restoring, configuring, or referencing resource-containment mechanisms and related failure codes while preserving only the existing timeout, report, and Runner-slot behavior.
- M-3 remains in progress; the next edits will pin down the per-lane `fix-ci` recovery contract and distinguish recovery scheduling reports from passing lane reports.
- M-2 fixed: lane gating and ordered dispatch are scoped to the complete lane sequence in each run's immutable bound definition; legacy aggregate runs remain on their existing path, including during mixed-version rollout, with no synthesized lanes, state migration, or rerun.
