## Findings

1. **[High] Most persistence waits still create their checkpoint after the operation under test.** The new [WaitForPersistenceAsync](/home/szf/.mohist/projects/workspaces/wr_3b41825840334303a130f5818fce9ce2/packages/server/tests/Mohist.Server.SpecTests/Support/AgentSessionPersistenceTestProbe.cs:130) is only a compatibility wrapper: it calls `PersistenceCheckpoint` at wait time. The remaining callers, for example [AgentSessionLifecycleDedupSpecs.cs](/home/szf/.mohist/projects/workspaces/wr_3b41825840334303a130f5818fce9ce2/packages/server/tests/Mohist.Server.SpecTests/Specs/Events/AgentSessionLifecycleDedupSpecs.cs:67), invoke it after the command has made persistence pending. If `PersistCallback` has already called `StartCycle` but has not yet reported when that wrapper runs, the new checkpoint has the same cycle id as the in-flight write. [WaitForNextAsync](/home/szf/.mohist/projects/workspaces/wr_3b41825840334303a130f5818fce9ce2/packages/server/tests/Mohist.Server.SpecTests/Support/AgentSessionPersistenceTestProbe.cs:72) discards that report as `<= checkpoint`, and the test waits for a later timer cycle which may never exist. Migrate every remaining `WaitForPersistenceAsync` and `WaitForTranscriptPartsAsync` caller to capture `PersistenceCheckpoint` before causing deferred data, then await that checkpoint afterward; remove the post-operation compatibility wrapper so future callers cannot reintroduce this race.

<promise>FAIL</promise>
