## Why

Test Issue 用于验证 openspec 工作流的端到端流程——从 proposal 创建到 spec、design、tasks 的完整 artifact 生成链路，确保 change 管理管道在最小变更场景下正常运作。

## What Changes

- 为 test issue 创建完整的 openspec artifact 包（proposal → specs → design → tasks）
- 验证 artifact 生成、目录结构和文件命名符合 openspec 规范

## Capabilities

### New Capabilities

_None_

### Modified Capabilities

_None_

## Impact

- `openspec/changes/1-test-issue/` — 新增 proposal.md 及后续 artifact 文件
